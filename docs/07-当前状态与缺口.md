# 当前状态与缺口

## 已完成底座

- 本地 dsh Host + 原生 ArkUI Client 定位已确定。
- `phone` / `tablet` / `2in1` 已写入模块设备类型，多窗口基础架构存在。
- `MobileTheme / Tokens / HarmonyTheme / NativePrimitives`（含 `Ds*` 基元族）形成 UI 设计链，其中 `MobileTheme` 为 v2 手机链唯一 token 源；`Breakpoints / LayoutController / NavigationController` 形成布局/导航决策链。
- P7 已覆盖目标操作、模型重试、审批、队列竞态、失败恢复、`@`/`/` token 替换、斜杠命令等。
- SDK 口径为 `6.1.1(24)`；ABI 为 `arm64-v8a` + `x86_64`。

## 2026-09-20 全量对等收口（`spec/dsh-full-parity`，Phase 1–12）

对等矩阵重述：**52 行 = DONE 41 · PARTIAL 5 · BOUNDARY 4 · TODO 2**（门禁实算一致；过程见 `parity-matrix.md` §5.2）。本轮新增/收口的主要能力与组件：

- **视觉令牌（US4）**：裸设计值清零（FR-016 / SC-002 达成，棘轮基线 0/0）；`HarmonyElevation` 补齐 shadow-1/2/3。
- **三栏交互（US5）**：`TrackResizer` 拖拽调宽 + 弹性动画（FR-019）+ 键盘方向键（FR-021）；`WorkspaceBrowser` 工作区分组折叠/展开（FR-020）；RAIL 新建会话入口（E362）。**（v2 已删除：`TrackResizer` 与 PC 原生三栏链随转向移除，见下节）**
- **子代理与任务（US6）**：`SubagentCard`（转录视图 FR-022 / 追问 FR-023 / 中断 FR-024 / `@` 引用 FR-025，`SessionHub.promptSubagent` / `interruptSubagent`）；`ConversationHeader` 任务详情参数/输出下钻（FR-026）。
- **工作区与文件（US7）**：`workspaceFileScopeId` 阻塞已解（FR-027，作用域 = 会话 id）；`FilePreviewPane` HTML/SVG 隔离预览（FR-028，Web 组件禁 JS/网络/文件访问）；工作区列表正确加载（FR-029）。
- **轨迹检查（US8）**：`TimelineOverview` 每步独立耗时（FR-030）；`TrajectoryInspector` 父子层级缩进/折叠（FR-031）。
- **手机远程双模式（US9）**：`RemoteShell` + `DrawerNav`（左抽屉）+ `LatticeDock`（队列/目标 dock，FR-033/035/036）；运行模式切换（FR-032）；`ConnectPane` LAN 自动发现（FR-034）；断连横幅 + 自动重试（FR-037）；`model/Subagent.ets` / `RemoteHost` / `DrawerTracks` 模型与 `LanDiscovery` 协议随之落地。
- **工具卡收口**：applied 对照（FR-012，`meta.diffs` 入投影）、图像缩略（FR-013）、技能工具行（FR-014）；交付物正文内文件引用可点（FR-015）。
- **其它接线收口**：popupSelect 命令选项列表（FR-005）、`@` 面包屑回跳（FR-006）、会话搜索（FR-003）、权限切换走 `/permission`（FR-011）、计划 chip 视觉（FR-010）；E384（随包 `sharp.impl` 桩件）已核实为真件并重打包。

## 2026-09-20 v2 官方 UI 复刻转向（`spec/dsh-full-parity` v2）

应用户「UI 差距较大，请完整复刻两个对标项目」的反馈，UI 路线从「原生 ArkTS 神似复刻」转向「官方产物直载 + 源码级对照复刻」：

- **PC / 平板 / 2in1 / 折叠展开（`DESKTOP_LIKE`）**：新增 `entry/src/main/ets/view/shell/WebShell.ets`——用 ArkWeb `Web` 组件**直载官方 Web UI**（本机 `http://127.0.0.1:3120`；远程模式加载远程主机地址），含 `WebShellFacade` 契约与 `HostReadyState` 状态机（BOOTING 等待页 / READY `loadUrl` / FAILED·TIMEOUT 错误页 + 重试）。与官方**同一前端产物**，60+ 官方 client-ui 组件全部生效。
- **手机 / 折叠闭合（`PHONE`）**：以克隆的 DSH Mobile（`.research/deepseek-harness-mobile`，Kotlin+Compose）源码为**唯一参照逐屏原生复刻**——新增 `appstate/src/main/ets/ui/MobileTheme.ets`（对照其 `ui/theme/*.kt` 逐值移植设计 token，明/暗两套色板）与 `NativePrimitives.ets` 的 **`Ds*` 基元族**（`DsButton` / `DsCard` / `DsPill` / `DsSegmented` / `DsIconButton` / `DsSheetHeader` / `DsEmptyHero` / `DsSectionHeader` / `DsStateDot` / `DsShimmerText` / `DsSkeletonBlock` / `DsDisclosureRow` / `DsToggleRow` / `DsContextMeter`）；回合流 / 顶栏 / 输入区 / 工具卡 / 抽屉 / dock / 轨迹台账 / Sheet 系列 / 连接设置屏逐屏对照重塑；新建 `view/SessionSearch.ets`（对照 `SessionSearch.kt`）。
- **架构收敛（删除）**：`view/shell/AppShell.ets`、`view/shell/MainHeaderShell.ets`、`view/shell/SidebarShell.ets`、`view/shell/TrackResizer.ets`、`view/ShortcutKeys.ets` 已删除（可达性终裁见 `spec/dsh-full-parity/deletion-manifest.md`）；`MainShell` / `RightbarShell` / `DetailPane` / `PendingPane` / `DiagnosticsPane` / `WorkspacePane` / `TabContentView` / `CorePane` 因**手机链与诊断页消费而保留**。
- **路由**：`pages/Index.ets` 按**形态 × 模式**矩阵渲染——`PHONE` → `RemoteShell`（本机/远程皆原生）；`DESKTOP_LIKE` → `WebShell`（本机/远程皆 Web 直载）。
- **对等口径不变**：`parity-matrix.md` 仍为 **52 行 = `DONE` 41 · `PARTIAL` 5 · `BOUNDARY` 4 · `TODO` 2**、§6 **11 个缺口**（门禁 `tools/check-parity.mjs` 实算一致，EXIT 0）。
- **延期项（不复刻）**：Terminal/PTY（`TerminalPanel.kt`）、Workflow runs、Dynamic Cordis UI、relay 配对（`PairScreen.kt`）。

> 说明：v1 交付的原生组件中，手机链继续依赖的（消息行 / 工具卡 / 会话列表 / 连接页 / 设置页等）保留并逐屏对照修正；仅服务 PC 三栏布局的组件删除。设计规范约束对象随之收敛为手机原生链（见 `docs/04`）。

## 2026-09-21 v3 核心升级 0.1.6-alpha.2（实现侧收口，T001-T018）

随包核心 0.1.5-rc.2/rc.3 → **0.1.6-alpha.2**（resfile 仅一个 zip，68.8 MB；历史勘误：旧"rc.3" zip 实为 rc.2 树，打包事故已纠正口径）。实现侧与本机可验层全部收口，端侧验证归 Phase 9。

**已验证（本机层）**：

- 打包全流程绿（六类补丁 + preset 逐锚点适配新源树，fail-loud 无静默跳过）；`check-core-loop` 22/22（boot 序列、会话闭环、文件变更流）。
- 协议面：端点 84 → **109**（+25 全为增量：`agentTeams`×3 / `officeToPdf`×2 / `permissionPresets`×1 / `pluginManager`×8 / `terminal`×10 / `workspace unarchiveSession`×1），既有端点 wire 参数零漂移；转发事件 19 → **23**；协议探测 18 项全预期形态；`compat-drift` 以刷新后基线跑零漂移。受支持矩阵已加入 0.1.6-alpha.2（`CompatIndex.ets`）。
- 0.1.6 适配要点：新增硬原生依赖 `node-addon-require-builtin`（pack 期垫片 createRequire 直通 + 宿主 argv 必带 `--expose-internals`，`RuntimePort.buildHostArgv` 已加）；`ondevice` preset 禁用五行（`tool-bash`/`tool-pwsh`/`tool-fs-search`/`workflow-ptc`/`tool-workflow`）；host 侧 boot 对 pending 改为**警告不失败**，端侧实测 pending 四条（`ptc-runtime`/`terminal-controller`/`ui-deliverables`/`workspace-changes`，根在 subprocess/sandbox 被禁）。细节见 `docs/50` §17。
- 工作区写入：0.1.6 的 `dsh-fs-local` `writeFileAtomic` 形态未变，`main.js` 的 link 降级垫片继续覆盖"新建 = `COPYFILE_EXCL`、覆盖 = `rename`"；workspace 链路（host-ready `workspace` 字段 → `workspaceId/cwd` 互斥回退 → `workspaceFiles` 列表）逐段核对零漂移。
- 设置存储：$DSH_HOME 逐包对比零不兼容（0.1.6 新增 `dshCachePath` 为纯增量）；Web UI 设置项核查清单就绪（`docs/14-设置项核查清单.md`，31 项）。

**新增缺口登记（v3）**：

| # | 缺口 | 事实 | 处置 |
|---|---|---|---|
| V3-1 | `permissionPresets/catalog` 在 ondevice profile 下 404 | 0.1.6 新端点（无参数），端侧 profile 未挂 permission 链（E116 上游硬约束）⇒ 探测 404 属预期形态 | 登记为缺口：权限预设选择器在端侧维持"无组合不显示"口径；上游放开 shell 服务依赖后再评估 |
| V3-2 | Web UI 交付物/变更面板端侧缺席 | `ui-deliverables` 行 0.1.6 新增依赖 `workspaceChanges`（根在 subprocess），端侧从未激活（boot pending 警告不失败）⇒ 桌面 Web UI 的"交付物/变更"面板在端侧缺数据源 | 上游硬约束，如实呈现；US2 的工作区文件面板不受影响（走 `workspaceFiles/*`，已激活） |
| V3-3 | 4 条新增转发事件仅登记不投影 | `permission-presets/catalog-changed`、`plugin-manager/changed`、`plugin-manager/install-log`、`plugin-manager/install-state`（全 emit）服务于官方插件管理页/权限预设页，端侧无对应界面 | `RemoteEvents.ets` 白名单登记（23 条），`SessionHub` 走"记录后忽略"安全降级；做这两类界面前先接线 |
| V3-4 | PTC 工作流端侧禁用 | 0.1.6 standard preset 新增 `workflow-ptc`/`tool-workflow`（依赖 ptcRuntime，根在 subprocess）；preset mount 全有或全无 ⇒ 端侧必须禁 | `ondevice` preset 随包禁用（pack-core `addOnDevicePreset`）；能力边界，不造假入口 |

**待端侧验证（Phase 9，如实列出）**：模型回合流式增量、图片内联、preset 实际会话工具面、真机四步文件链（新建→读取→覆盖→列目录）、设置项逐项走查、手机链一轮会话回归；v2 遗留验收（形态路由矩阵、删除收敛、WebShell 直载）一并回归。

## 当前最高优先级缺口

### P2 全量对等收口后的静态缺口（`parity-matrix.md` §6 为唯一清单）

- **`reference`**：`~` 主目录展开（FR-007）未接线——模型函数 `tildeExpand` 与快照字段 `hostHome` 已就位，`Composer` 缺消费点（接线量小，建议下一轮先做）。
- **`layout`**：手机横屏档位子句待真机（其浅层面板滑入动画/自身拖拽调宽若按双栏落成仍未做）；平板/PC 侧同项随 v2 Web 直载退役。
- **`skill`**：对话内 `@` 技能引用源。
- **`user-questions`**：`nav.minimize/maximize` 卡片收起/展开（纯观感）。
- **`settings-general`**：版本化欢迎通知本体。
- **`workflow-run` / `cordis`**：上游无协议端点，长期 TODO（不造假后端）。

### P0 返回与层级（P8-1 已收口，剩余项如下）

- **已完成**：`decideBack()` 的次序按“最上层可见内容优先”重排；“预览是不是整页层”由
  `previewIsLayer()` 判定（单栏 = 工作区第 2 层；多栏并排栏不算层）；关预览同时把下钻退回文件树
  （`drillAfterPreviewClosed()`），并**经中枢** `closeFilePreview()` 清（视图那份只是镜像，只清镜像 = 假关闭）。
- **已完成**：返回、页头返回箭头、文件树返回箭头、诊断/连接页返回箭头共用 `Index.consumeBack()`；
  浮层复位只保留 `closeSheet()` 一处；除 `STACK_TO_MAIN` 外新增 `BACK_TO_LIST`（手机单栏：会话是“盖在列表上的一层”，
  返回列表**保留会话**）——修掉“第一次白按、第二次退出应用”。
- **已完成**：组合 fixture 从“顺路几条”升级为**全组合矩阵**（2304 种，表驱动独立对账）。
- **剩余**：多栏下“预览是右栏面板”时是否该吃返回键（取决于该面板此刻是否显示）——需真机确认，见 `docs/device-validation.md` D40。
- **剩余**：Sheet 的关闭原因区分（完成 / 取消 / 返回 / 系统关闭 / 外部状态变化）目前只有“复位”，没有把原因回传给调用方；
  编辑态未保存内容的取消/保存策略仍按“直接丢弃草稿”处理。

### P0 敏感内容隔离（P8-5 已收口主要出口，剩余项如下）

- **已完成**：内部事件（系统提示词 / 未识别事件载荷）的正文不再进入**任何**用户可达出口：
  轨迹详情（时间线格子可点，此前**点一下就泄露整段系统提示词**）、复制、引用；
  判据 `Turns.mayExposeBodyToUser()`、取值 `Turns.userFacingBodyOf()`、脱敏 `Redact.scrubCredentials()`
  各只有一处，接线门禁把三者都钉成"必须有调用点"。
- **已完成**：Host 错误原文进界面前统一脱敏（`FailureText.hostDetail()` 一处收口，三处共用）。
- **剩余**：诊断报告的旁路日志未过脱敏（当前人读核对不含凭据，但无门禁保证）。
- **剩余（能力边界）**：`session/search` 的**内容命中来自 Host**，本端无法按 audience 分类；
  只有本地标题命中能过滤。上游若要保证这一点，应由 Host 侧过滤。

### P1 上下文契约（P8-3 已收口草稿这一半）

- **已完成**：输入草稿按**会话**保存与取回（官方 `views.d.ts`：草稿是会话视图状态的一部分）——
  修掉"在会话 A 写一半、切到 B 顺手发送 ⇒ 那段话发给了 B"。
- **已完成**：契约的后半截"**跨重载**"——草稿袋子落盘（`LocalPrefs` 的不透明字符串键 +
  模型层 `serializeDrafts`/`parseDrafts`），启动时读回进袋子、两个变更点各落一次盘，
  落盘失败给一次轻提示。矩阵该行已由 PARTIAL 升为 DONE，§6 登记划掉。
- **已完成**：进出页面保留**滚动位置**（P9-6）——离开又回到**同一个**会话时回到原处；
  换会话仍按既有决定"看最新"。**仍缺**：只记顶部**条目下标**（没记像素偏移，很高的行会回到该行顶部）；
  列表**选中项**（搜索命中/时间线聚焦）在页面往返后不复原，需真机看观感后定。

### P1 UI 完成度

- 首页/工作区视觉重心仍需收口。
- 会话页的信息层级、轨迹/工具/错误/重试结构需要统一。
- 设置页需要进一步按用户任务而不是实现模块分组。
- 异步状态和失败反馈需要逐页清理，消灭静默等待、伪成功、点击无反馈。
- 多形态下仍需实机检查间距、键盘、Sheet、滚动、焦点和系统返回。

### P1.5 多形态

- 折叠展开/闭合保持上下文需设备验证。
- 平板需要减少“手机式 Sheet”，扩大并排详情使用。
- 2in1 需要系统验证 hover/右键/快捷键/拖拽的完整一致性。

## 完成口径

现阶段不能把“功能代码存在”直接视为“UI 完成”。需要同时满足：功能、状态、返回、关闭、失败反馈、跨形态、无障碍、视觉收口。

详细审计见 `docs/08-UI与返回交互审计.md`。

## 工具链自身的修复（P8-5）

- **死代码门禁**（`tools/check-dead-code.mjs`）剥块注释时**不保留换行** ⇒ 剥壳后的行号与原文件行号
  错位一次，"跳过 import 块自身"的区间落到别的行上，计数被 import 行自己喂饱：
  **"零使用 import"这条规则长期假阴性**（一个真正没人用的 import 会被判合格）。
  两处修正：① 剥注释/字符串时**保留换行**，行号严格对齐；② import 使用计数改看"剥注释、
  **保留字符串**"的文本（`\`${SURFACE.apiChannel}\`` 这类模板串用法才算使用）。
  修正后仓库里 **44 个真正零使用的 import** 立刻现形，已全部删除（含 `MainShell`/`DetailPane`/
  `WorkspacePane`/`Fs`/`formatDuration` 等搬迁遗留）。
- 教训记一条：**门禁的行号与原文件不对齐，等于会随机说谎的门禁**。它是被 P8-5 一次无关的
  import 增删"碰巧"暴露出来的——所以每轮都要让门禁在已知坏版本上红一次（归真验证）。
