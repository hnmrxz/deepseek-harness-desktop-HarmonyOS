# 协议兼容事实基线（D2）

| 项 | 内容 |
|---|---|
| 文档编号 | `docs/10-协议兼容事实基线.md` |
| 版本 | v1.0 |
| 日期 | 2026-09-10 |
| 状态 | 待实测校验（标注「待实测」的条目必须由 `tools/protocol-suite` 补齐证据后才可实施） |
| 上位文档 | `docs/00-开发任务书.md`（D1）§附录A |
| 纪律 | **本文件只写事实，不写设计**。每条事实必须标注来源：`代码` / `官方文档` / `实测` / `待实测` |

---

## 0. 版本与基线

| 项 | 值 | 来源 |
|---|---|---|
| 本机 dsh 打包版本 | `deepseek-harness-pkg` 0.1.2-alpha.5 | 代码（`package.json`） |
| 核心依赖版本 | `@deepseek-ai/dsh` **0.1.2-rc.1** | 代码（`node_modules/@deepseek-ai/dsh/package.json`） |
| 本机 DSH 根目录 | `C:\Users\aotian\AppData\Roaming\io.github.hairyf.deepseek-harness-desktop\dependencies\dsh\` | 环境 |
| Node（本机） | v22.20.0 | 环境 |
| 目标端 SDK | HarmonyOS API 26（platformVersion 26.0.0，version 26.0.0.105） | 环境 |

> **注意**：`0.1.2-alpha.5` 是**打包包装包**的版本，不是 dsh 核心版本。任何「版本判断」必须读 `@deepseek-ai/dsh` 的版本，不要读外层 `package.json`。此坑在社区工程中出现过（§工程规划 18.6「命名误导」）。

---

## 1. 载体与端点

### 1.1 一元 RPC（HTTP）

| 项 | 事实 | 来源 |
|---|---|---|
| 方法/路径 | `POST /api/<endpoint>` | 代码 |
| endpoint 构成 | descriptor 的 `namespace` + `method`，以 `/` 连接 | 代码 |
| endpoint 段字符集 | `^[A-Za-z0-9_$.-]+$`，禁止空段、`.`、`..` | 代码（`ENDPOINT_SEGMENT_PATTERN`） |
| channel 约束 | channel 必须匹配 `^/[A-Za-z0-9._~-]+$` 且不得为保留值 `/api`（`/api` 由 Gateway 认领） | 代码 |
| 请求头 | `content-type: application/json` | 代码 |
| 请求体 | `{ "type": "client-request", "rpcId": "<uuid>", "method": "<endpoint>", "payload": { "args": { ... } } }` | 代码 |
| 响应体（成功） | `{ "type": "server-response", "rpcId": "<同请求>", "result": { "ok": true, "value": <T> } }` | 代码 |
| 响应体（业务失败） | `{ "type": "server-response", "rpcId": "<同请求>", "result": { "ok": false, "error": { "code": "<string>", "message": "<string>", "details": { ... } } } }` | 代码 |
| 传输失败 | HTTP 非 2xx ⇒ 客户端侧抛 `transport failure for <channel>/<endpoint>: HTTP <status>` | 代码 |
| 信封校验 | `type` 必须为 `server-response`；`rpcId` 必须为字符串且等于请求；`result` 必须为对象；`ok=true` 时须有 `value`；`ok=false` 时 `error.code`/`error.message` 为字符串、`error.details` 为对象 | 代码（`parseConnectionResponse`） |
| 取消 | 通过 `AbortSignal` 中止 HTTP 请求；Host 按 descriptor 的 signal 元数据支持取消 | 代码 + 官方文档 |

**待实测**：
- [ ] `details` 是否可能为 `null`／缺省（代码要求 `isRecord(details)`，但需以真实错误响应确认）
- [ ] 未知 endpoint 的响应（预期 404，需确认是否走信封）
- [ ] 超大 payload（图片）时的分块与 `maxRequestBodyBytes` 边界（默认 300 MiB）

### 1.2 逻辑流复用（WebSocket）

| 项 | 事实 | 来源 |
|---|---|---|
| 路径 | `/api/remote.mux`（精确 upgrade 路由，未命中直接关闭连接） | 代码（`REMOTE_STREAM_MUX_PATH`） |
| 客户端帧 · 开流 | `{ "type": "open", "streamId": "<uuid>", "endpoint": "<endpoint>", "payload": { "args": { ... } } }` | 代码 |
| 客户端帧 · 取消 | `{ "type": "cancel", "streamId": "<uuid>" }` | 代码 |
| 服务端帧 · 逐项 | 逐项产出，客户端以 `item` 帧接收 `value` | 代码 |
| 服务端帧 · 失败 | `error` 帧，携带 `{ code, message, details }`，客户端抛 `RemoteError` | 代码 |
| 正常终止 | 流结束即正常返回（无终止帧） | 代码 |
| 物理连接生命周期 | Gateway 打开后**空闲也保持**；每次请求只做一次物理连接尝试，重试调度归 Connection | 官方文档 |
| 心跳 | Host 每 `websocketHeartbeatIntervalMs`（默认 **2000 ms**）发 Ping 控制帧；浏览器/客户端在 WS 协议层自动回 Pong；Host 若在下一周期前未收到 Pong 则**主动终止**该 socket | 官方文档 |
| 并发 | 多条独立可取消的逻辑流共享同一条物理连接 | 官方文档 |

**待实测**（→ 已于 2026-09-10 用 `tools/protocol-enum.mjs` 部分关闭，见 §8 附录）：
- [x] `item` 帧的完整字段名 —— 见 §8.3
- [ ] `error` 帧结构（`frame.error.{code,message,details}`）—— 仍需真机抓包
- [x] 是否需要客户端主动发 Ping —— 见 §8.2（**不需要**，但需显式开客户端心跳，理由见该节）
- [ ] 单连接最大并发逻辑流数与背压行为

### 1.3 事件流（Gateway 内部逻辑流）

| 项 | 事实 | 来源 |
|---|---|---|
| 逻辑端点 | `$events`（`payload = { args: {} }`） | 代码（`stream-protocol.js`） |
| 结果回送端点 | `$events/result`（一元，用于 waterfall 结果回送），载荷含 `clientId`/`eventId`/`outcome` | 代码 |
| 首项 | 唯一一个 `{ type: 'ready', clientId, host: { home } }` | 官方文档 |
| 时机 | source factory 在返回 iterable 前**同步**挂好增量 listener，之后才发 ready ⇒ 客户端收到 ready 后发布的 baseline 不会丢增量 | 官方文档 |
| 订阅 | Client 侧 `$on()` 的合法键 = Host 装配声明的转发事件；每订阅归属调用方 fiber | 官方文档 |
| 注册时机 | Client Remote 服务激活时即把 `$events` pump 注册为 Connection generation source ⇒ **即使无订阅也会在连接循环启动时打开** | 官方文档 |
| 重放语义 | 普通通知重连后**不重放**；Agent-scoped waterfall 用同一 `eventId` 重放 | 官方文档 |

**待实测**：
- [ ] `clientId` 的格式与稳定性（是否跨重连不变）
- [ ] 事件项的实际结构（事件名 + 载荷形状），需按会话/工具/审批/提问分类枚举
- [ ] 重连后 baseline 的来源：客户端应调用哪个端点补齐「连接前」的状态（预期为会话快照/journal 流，需定位具体端点）

---

## 2. 认证与信任

### 2.1 认证流程

| 步骤 | 事实 | 来源 |
|---|---|---|
| 1 | 进程启动生成**随机启动 token**（每进程一个） | 官方文档 |
| 2 | `dsh web` 打印并打开 `http://<authority>/?token=<token>` | 官方文档 |
| 3 | 仅 `GET /` 接受该 token：校验通过 → 写入**绑定 authority 的签名 cookie** → **303 重定向到干净的 `/`** | 官方文档 |
| 4 | 静态资源保持公开；`/api` 全部方法与全部 WS 流要求同一 cookie | 官方文档 |
| 5 | 缺失/过期/畸形/authority 不匹配的 cookie ⇒ RPC 分发前 **401** | 官方文档 |
| 6 | HTTP 载体**不接受** query token（除根路径交换）与 `Authorization` header token | 官方文档 |

**签名 cookie 属性**：

| 属性 | 值 |
|---|---|
| 密钥 | `ctx.credentials` 中由 `client-connection/browser-session` 拥有的 grant 记录；本地提供方持久化到 `$DSH_HOME/.credentials.yaml` |
| 名称与载荷 | 确定性名称；payload 同时绑定**规范化 hostname 与 port** |
| 绑定 | host-only；`Path=/` |
| 标志 | `HttpOnly`、`SameSite=Strict`；**刻意不设 `Secure`**（载体是 loopback HTTP） |
| 有效期 | 绝对签发/过期区间；`cookieMaxAgeDays` 默认 **30 天** |
| 撤销 | 无 logout 操作；清除客户端 cookie 结束单个会话；删除 owner 凭据记录并重启 dsh 撤销全部会话 |

### 2.2 信任栅栏（`src/api-request-trust.ts`）

| 规则 | 结果 |
|---|---|
| `Host` 为 loopback（`localhost` / IPv6 loopback / `127.0.0.0/8`），或匹配 `trustedHosts` 条目 | 通过 |
| `trustedHosts` 匹配语义：带端口条目按 `host:port` **精确**匹配；不带端口条目匹配**任意端口**；两侧经 WHATWG 归一化 | 通过 |
| 附带 `Origin` 时必须**等于**该 `Host` | 否则 403 |
| `sec-fetch-site: cross-site` | 一律拒绝 |
| 畸形配置 authority | 插件**加载失败** |
| 校验失败 | **403** |
| Host 可信但未认证 | **401** |

> 这些检查**防御 DNS rebinding 与跨站请求，绝不建立身份**——身份由 2.1 的 cookie 承担。

### 2.3 绑定姿态

| 配置 | 行为 | 来源 |
|---|---|---|
| `host` 取值 | 只接受 `127.0.0.1`（默认，仅回环）与 `0.0.0.0`（有意向网络开放；服务器自身**不携带 TLS、认证或来源策略**） | 官方文档（webserver） |
| CLI 姿态 | `dsh web --host 0.0.0.0` **在启动时被拒绝**（安全考虑）；`--trusted-host` 可追加权威 | 官方文档（web-app） |
| LAN 信任采样 | `resolveLanTrust` 启动时采样一次：loopback 绑定不派生 LAN 地址；`0.0.0.0` 绑定加入每个非 internal IPv4 字面量 | 官方文档 |

**结论（本项目的部署红线）**：端侧**永不**依赖 `0.0.0.0` 绑定，也**永不**改写 `Host`/`Origin`。跨设备接入必须让请求在 Host 看来仍来自 loopback（隧道出口回 loopback）或其权威在 `trustedHosts` 中显式登记。

### 2.4 特权方法（loopback-only）

| 类别 | 方法 | 来源 |
|---|---|---|
| 设置 | `settings.*` | 社区工程实测（§18.7 问题 2）/ 待实测复核 |
| 凭据 | `credentials.*` | 同上 |
| 宿主交互 | `host.pickDirectory`、`host.openPath` | 同上 |
| Agent 预设 | `agentPreset.*` | 同上 |
| 模型发现 | `llm.discoverModels` | 同上 |

**待实测**：
- [ ] 逐方法枚举完整的 `PRIVILEGED_METHODS` 列表（从 `dsh-client-connection` 源码提取，作为套件用例）
- [ ] 在「真实 loopback + 真实 cookie」下逐方法确认可达（POC-3）
- [ ] 确认 `host.listDirectory` 是否也在特权集内（社区工程记录它可用且有默认起始目录语义）

---

## 3. 连接生命周期

| 状态 | 进入条件 | 来源 |
|---|---|---|
| `connecting` | 浏览器/客户端报告网络可用；或 `reconnect()` 被调用 | 官方文档 |
| `connected` | 收到 `$events` 的 ready 项 | 官方文档 |
| `disconnected`（终态） | 10s 档重试仍失败 | 官方文档 |
| 中断 | `offline` 事件：中断活动工作、发布 `disconnected`、暂停自动尝试 | 官方文档 |
| 恢复 | 下一次 `online`：重置序列，从 **500 ms** 档开始 | 官方文档 |

**退避表（确定性，必须逐档实现）**：

| 档 | 延迟 | 抖动 |
|---|---|---|
| 1 | 500 ms | 50%–100% |
| 2 | 1 s | 50%–100% |
| 3 | 2 s | 50%–100% |
| 4 | 4 s | 50%–100% |
| 5 | 8 s | 50%–100% |
| 6 | 10 s（上限） | 50%–100% |

**generation 失效条件（任一即失效）**：`$events` 结束 / Remote stream error / 收到**非 ready 首项** / 畸形事件项。

**重试前动作**：要求 mux **取消候选或活动 socket**，再重开 `$events`。

**待实测**：
- [ ] 端侧如何在 ArkTS 中获取「网络可用/offline」信号（`@ohos.net.connection` 能力确认）
- [ ] 后台切前后台的连接行为与系统限制（POC-4）

---

## 4. 客户端 UI 插件与前端资产（供参考，不作为端侧依赖）

| 项 | 事实 |
|---|---|
| 客户端插件形态 | `node_modules/@deepseek-ai/dsh-client-ui-*/client.js`，形如 `window.__ModuleLoader__.load({ id, factory })`（ESM-in-loader） |
| 数量级 | 本机 50+ 个 `dsh-client-ui-*` 包 |
| 布局包 | `dsh-client-ui-layout`：三栏 `AppFrame`（`sidebar` / `conversation` / `details` / `shell.overlay` 槽位）、让步链（空间不足先收详情栏，再自动关闭）、`ctx.layout` 面板几何服务、主题呈现器 |
| 关键槽位消费者 | `dsh-client-ui-sidebar`、`dsh-client-ui-conversation`、`dsh-client-ui-trajectory`、`dsh-client-ui-tool`、`dsh-client-ui-approval`、`dsh-client-ui-user-questions`、`dsh-client-ui-workspace`、`dsh-client-ui-settings*`、`dsh-client-ui-subagent`、`dsh-client-ui-goal`、`dsh-client-ui-jobs`、`dsh-client-ui-plan`、`dsh-client-ui-schedule`、`dsh-client-ui-skill`、`dsh-client-ui-deliverables`、`dsh-client-ui-message-feedback` |
| 前端 dist | `@deepseek-ai/dsh-web-frontend/dist/`（`index.html` + `assets/index-*.js` ~423 KB + `assets/vendor-*.js` ~740 KB + `assets/langs/*` 语法高亮 ~3 MB） |
| 端侧态度 | **不加载、不依赖**；仅作为「行为对照物」：同一场景下与官方 Web 客户端的行为差异必须能解释 |

> 端侧若在某处需要「与 Web 端一致」的语义（如审批三选、提问选项、命令解析），以对应 `dsh-client-ui-*` 的 `client.js` 为**行为参照**做黑盒对照测试，而不是移植其代码。

---

## 5. 备选协议：SDK JSON-RPC（stdio）

| 项 | 事实 | 来源 |
|---|---|---|
| 分帧 | 每个 `\n` 结尾行承载一条 JSON-RPC 2.0 消息 | 官方文档 |
| 帧判别 | 同时含 `id` + `method` ⇒ 请求；仅 `id` ⇒ 响应；仅 `method` ⇒ 通知；格式错误行**忽略** | 官方文档 |
| 错误码 | 无处理器 ⇒ `-32601`；处理器失败 ⇒ `-32603` | 官方文档 |
| 客户端→服务端 | `initialize`（`InitializeParams` → `InitializeResult`）、`session/prompt`（→ 持久入队回执）、`shutdown` | 官方文档 |
| 服务端→客户端 | `session.event`、`session.status`、`subagent.started`、`subagent.finished` | 官方文档 |
| `prompt` 内容块 | 普通内容 + `SdkEncodedImageBlock { type: "image", data, mimeType }`；服务端入队前转成持久引用 | 官方文档 |
| `serverInfo.name` | 固定 `deepseek-harness-sdk-runtime`（协议稳定值） | 官方文档 |
| 无版本协商 | 握手只带 `serverInfo.version`（`0.0.1`），客户端不校验 | 官方文档 |
| 已知限制 | 无取消方法、无会话关闭方法（客户端放弃轮次 = 关进程）；server→client 请求未使用 | 官方文档 |
| 服务端插件 | `@deepseek-ai/dsh-sdk-jsonrpc-server`，应用组合包 `dsh --profile sdk` | 官方文档 |

**本项目用途**：仅 POC-11（端侧 Host 可行性）与「无 webserver 场景」备选。**不得**作为 v1 主载体——它缺少取消/会话关闭，且 stdio 载体在移动端反而更难（需进程内管道）。

---

## 6. 协议探测工具要求（`tools/protocol-suite`）

| 能力 | 说明 |
|---|---|
| token 交换 | 从 Host 启动输出或参数取 token，完成 cookie 获取并持久化 |
| 端点列举 | 用 `settings.describe` / 会话查询等已知端点确认连通；用未知端点确认 404 语义 |
| 权威错误码表 | 对每个已确认端点记录：成功形状、业务错误 `code` 枚举、HTTP 状态码 |
| 帧抓取 | 记录 `/api/remote.mux` 全部收发帧（**不含用户内容**），产出结构化 fixture |
| 事件目录 | 订阅 `$events` 一段时间，产出事件名 → 载荷形状目录 |
| 特权方法表 | 提取 `PRIVILEGED_METHODS` 并与实测 403/200 对照 |
| 回归 | 上述产物纳入 CI，Host 版本变化时自动 diff，产出「协议漂移报告」 |

**产出物**：
- `tools/protocol-suite/fixtures/rpc/*.json`（信封样本）
- `tools/protocol-suite/fixtures/mux/*.jsonl`（帧序列）
- `tools/protocol-suite/fixtures/events/catalog.md`（事件目录）
- `tools/protocol-suite/reports/drift-<hostVersion>.md`（漂移报告）

---

## 7. 未决问题清单（阻塞实施前必须关闭）

| # | 问题 | 影响 | 关闭方式 |
|---|---|---|---|
| A-1 | `item`/`error` 帧完整结构 | 逻辑流解析 | **部分关闭**（§8.3 已实测字段名）；`error` 帧结构待真机抓包（POC-1） |
| A-2 | ArkTS WebSocket 是否自动回 Pong | 心跳存活 | **已关闭（设计侧）**：不依赖自动 Pong，改用 ArkTS 原生 `pingInterval/pongTimeout` 显式双向心跳（§8.2）；存活结论仍待 POC-1 真机 300s 验证 |
| A-3 | 重连后状态 baseline 的补齐端点 | 状态一致性（R5） | 实测 + 上游代码定位（POC-4） |
| A-4 | 特权方法完整清单与可达性 | 设置/凭据功能 | **部分关闭**：清单待从 `dsh-client-connection` 源码提取 + 实测（POC-3） |
| A-5 | 事件项的分类与载荷形状 | 状态投影正确性 | 事件目录（POC-1） |
| A-6 | 会话/工作区/设置的端点全清单 | 功能实现覆盖面 | **已关闭（枚举完成）**：见 §8.1 与 `.research/protocol/endpoints.json`（59 一元 + 3 流式） |
| A-7 | cookie 续期与失效重绑的最小可行路径 | 长稳（30 天后） | 实测（伪造过期 cookie 场景） |
| A-8 | 大 payload（图片）上限与传输时延 | 投喂体验 | 实测（POC-7） |
| A-9 | 端侧网络状态信号 API | 重连策略 | API 确认 |
| A-10 | 未配对设备在 L1 下的失败表现与审计 | 安全验收 S2 | 设计 + 实测（M7） |

---

## 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.1 | 2026-09-10 | 新增 §8 附录：Remote endpoint 枚举结果（关闭 A-6）、ArkTS 心跳对策（关闭 A-2 设计侧）、mux 帧字段实测结论（部分关闭 A-1） |
| v1.0 | 2026-09-10 | 首版：载体/端点、认证与信任、连接生命周期、UI 插件参考、SDK 备选协议、探测工具要求、未决问题 A-1~A-10 |

---

## 8. 附录：远程端点枚举与 ArkTS 侧实测结论（2026-09-10）

> 采集方式：`node tools/protocol-enum.mjs --json .research/protocol/endpoints.json`
> 采集对象：本机 `@deepseek-ai/dsh` **0.1.2-rc.1**（223 个 `@deepseek-ai/*` 包）
> 提取规则：命名空间来自 `namespace: "<ns>"` 声明；方法来自编译产物中的
> `_<prop>_decorators = [Remote("...")]` / `[Remote({ mode: "stream" })]`（后者方法名取属性名）。

### 8.1 已枚举的 endpoint 清单（关闭 A-6）

> 采集脚本：`tools/protocol-enum.mjs`（首版）与 `tools/protocol-enum2.mjs`（精确版，修正「命名空间跨文件声明」与「属性名≠线上名」两个盲区）。

**权威命名空间清单**（来自 `dsh-api-remotes` 的集中声明，共 16 个）：

```
agentPresets  commands  credentials  directoryPicker  dynamicCordisRunner
fileReferences  goals  llm  messageFeedback  pluginInventory
session  sessionReferenceResolver  settings  skills  subagents  workspace
```

**已验证的「包 → 命名空间」归属**（来自 `super(ctx, "<service>", { namespace })` 声明）：

| 包 | 声明的命名空间 |
|---|---|
| `dsh-api-session-controller` | `session`、`fileReferences`、`skills` |
| `dsh-api-workspace-controller` | `workspace`、`directoryPicker` |
| `dsh-api-settings-controller` | `settings`、`credentials` |
| `dsh-api-remotes` | 上表全部 16 个（集中声明处） |

**已验证的「包 → 线上方法名」**（`_<prop>_decorators = [Remote("<wire>")]`）：

| 包 | 线上方法名 |
|---|---|
| `dsh-api-session-controller` | `attachment` `cancel` `create` `fork` `list` `modelCatalog` `openWorkspacePath` `page` `prompt` `rename` `search` `selectModel` `updateQueue` + 流式 `follow` `control` |
| `dsh-api-workspace-controller` | `archiveSession` `create` `createDirectory` `delete` `insertBefore` `insertSessionBefore` `list` `pick` `rename` + 流式 `follow` |
| `dsh-agent-presets` | `list` `read` `copy` `deletePreset` `select` |
| `dsh-goal` | `create` `edit` `pause` `resume` `complete` `clear` |
| `dsh-subagent` | `list` `prompt` `interruptByParent` |
| `dsh-message-feedback` | `list` `put` `delete` |
| `dsh-session-reference` | `candidates` |
| `dsh-host-plugin-inventory` | `list` |
| `dsh-llm` / `dsh-tmux-context` / `dsh-repeat-tool-reminder` | `discoverModels` |
| `dsh-cordis-host-runner` | `getClientCode` `inventory` `invoke` `runHostHalf` `stopFromPanel` `undefineFromPanel` `settleUserRun` `resolveRequestRun` `resolveInspectQuery` `syncInspectManifest` `reportClientGuardFailure` `reportRenderFailure` |

**「属性名 ≠ 线上名」的实测映射**（首版脚本的漏抓原因，v2 已修正）：

| 包 | 属性名 | 线上名 |
|---|---|---|
| `dsh-agent-presets` | `remoteExportList` / `readDocument` / `remoteExportCopy` / `remoteExportDelete` | `list` / `read` / `copy` / `deletePreset` |
| `dsh-goal` | `remoteExportCreate` | `create` |
| `dsh-llm` 等 | `remoteDiscoverModels` | `discoverModels` |
| `dsh-session-reference` | `remoteExportCandidates` | `candidates` |
| `dsh-subagent` | `remoteExportList` | `list` |

> **注意（影响实现的坑）**：**不要**用「属性名」当线上方法名。`settings` / `credentials` / `commands` 等命名空间的
> 方法名**不在** `dsh-api-remotes` 内声明（该包只声明命名空间），必须从其**各自所有者包**提取；
> 上一版文档把 `settings` 命名空间的方法清单留白，正是因为这一点。**POC-3 需补齐 `settings.*` / `credentials.*` 的完整方法名。**

> **对 POC-1 的直接价值**：`session/list` 与 `workspace/list` 是**只读、无副作用**的可用性探测端点，
> 已作为 POC-1 步骤②的候选（见 `entry/src/main/ets/pages/Index.ets`）。
> endpoint 与 HTTP 路径的关系：`POST /api/<namespace>/<method>`。

### 8.2 ArkTS 心跳对策（关闭 A-2 的设计侧）

**新事实（ArkTS SDK，API 21+）**：`@ohos.net.webSocket` 的 `WebSocketRequestOptions` 提供
`pingInterval`（默认 30s，0 = 关闭）与 `pongTimeout`（默认 30s，须 ≤ `pingInterval`），
即**客户端可主动发起心跳**，但 SDK **不暴露** `pong()` 之类的控制帧 API。

**风险**：Host 每 2s 发 Ping 并「若下一周期前未收到 Pong 则主动终止」。若 ArkTS 客户端不自动回 Pong
（无法在 ArkTS 层验证），连接会在 ~4s 内被 Host 断开。

**本项目对策**：不假设自动 Pong，改为**显式开启客户端双向心跳** `pingInterval=5000ms, pongTimeout=4000ms`。
链路上因此始终存在双向流量；即使对端把「有流量」视为保活，也不会在 2s 判定窗口内被判静默。
该决策已实现于 `connection/src/main/ets/protocol/RemoteMux.ets`，并在 POC-1 步骤⑤以 300s 存活观察做最终判定。

### 8.3 mux 帧字段（部分关闭 A-1）

已确认的**客户端 → 服务端**帧字段名（来自网关客户端源码，已在本项目实现中固化）：

| 帧 | 字段 |
|---|---|
| 开流 | `type`（值 `"open"`）、`streamId`、`endpoint`、`payload`（形如 `{ args: {...} }`） |
| 取消 | `type`（值 `"cancel"`）、`streamId` |

已确认的**服务端 → 客户端**帧判别与取值方式：

| 判别 | 取值 |
|---|---|
| 逐项 | `type == "item"` → 业务值在 `value` 字段 |
| 失败 | `type == "error"` → `error.{code,message,details}`（**结构待真机抓包确认**） |
| 正常结束 | 流结束（无终止帧） |

**尚未确认**：未知帧类型是否需要回错（本实现选择「忽略，前向兼容」）；单连接最大并发逻辑流数。

### 8.4 对真实 Host 的实测矩阵（2026-09-10）

> 采集方式：`dsh web --no-open --port 3111 --host 127.0.0.1`（`DSH_HOME` 指向临时目录，不触碰用户既有会话）
> 探针：`node tools/protocol-probe.mjs --base http://127.0.0.1:3111 --token <token>`
> 探针只调用**只读**端点，不创建或修改任何会话与配置。

**认证流程实测（逐条命中 D2 §2.1）**

| 观测项 | 实测值 | 与文档一致性 |
|---|---|---|
| `GET /?token=<token>` | **HTTP 303**，`Location: /` | ✅ 与「303 重定向到干净的 `/`」一致 |
| `Set-Cookie` 数量 | 1 条 | ✅ |
| cookie 名 | `dsh-auth-1JE7j68MO-9Nw0tkJe_UeXpKdC-1-XB3CzmNnfT4tLY`（确定性名称 + 随机段） | ✅ |
| cookie 标志 | `HttpOnly=True`、`Path=/` | ✅ |
| cookie 有效期 | 签发 + 30 天（2026-10-10） | ✅ 与 `cookieMaxAgeDays` 默认 30 天一致 |
| `Secure` 标志 | **未设置** | ✅ 与「loopback HTTP 刻意不设 Secure」一致 |

**只读端点可用性（10/10 成功）**

| endpoint | HTTP | 耗时 | 结论 |
|---|---|---|---|
| `session/modelCatalog` | 200 | 41 ms | ok（返回 `default`/`routableProviders`/`groups`） |
| `pluginInventory/list` | 200 | 168 ms | ok（返回插件条目与 `fiberPhase`） |
| `settings/describe` | 200 | 10 ms | ok（返回 `writable`/`hasDocument`/`namespaces[].schema`） |
| `settings/canOpenAgentPresetDirectory` | 200 | 5 ms | ok（`true`） |
| `agentPresets/list` | 200 | 234 ms | ok（`presets[]`，含 `trust`/`isDefault`） |
| `llm/listProviders` | 200 | 5 ms | ok |
| `llm/listConfigurableProviders` | 200 | 4 ms | ok |
| `session/canOpenWorkspacePath` | 200 | 6 ms | ok（`true`） |
| `session/list`（带 `_request`） | 200 | 7 ms | ok（`{"items":[]}`） |
| `credentials/describe`（带 `refs`） | 200 | 5 ms | ok |

> **`credentials/describe` 在 loopback 下返回 200** ⇒ 特权方法在 L0 路径可达（**POC-3 的正面证据**）。
> 完整的特权集可达性仍需逐方法覆盖（POC-3 未关闭）。

**失败语义（三类，全部实测确认）**

| 场景 | 观测 | 语义 |
|---|---|---|
| 端点不存在（`workspace/list`、`does/notExist`） | **HTTP 404**（无信封、无 `rpcId`） | 未认领的 endpoint 由 `/api` handler 回答 404 |
| 参数与描述符不匹配（`session/list` 传 `bogus`） | HTTP 200 + `gateway/arguments-invalid`，消息形如 `args fields do not match the descriptor: missing "_request"; unexpected "bogus"` | 参数校验**在网关层**，错误消息精确指出缺失/多余字段 |
| 流式端点走一元载体（`workspace/follow`） | HTTP 200 + `gateway/signature-invalid`，消息 `stream Remote methods must be opened through the stream carrier` | **流式与一元必须走不同载体**，误用会被明确拒绝 |
| 域内业务拒绝（`llm/discoverModels`） | HTTP 200 + `llm/model-discovery-rejected` | 业务错误码按域命名，`code` 前缀可区分网关层与业务层 |

**信任栅栏与认证的实测边界（POC-3 的正面证据）**

| 场景 | 实测 | 对应文档 |
|---|---|---|
| 无 cookie 调 `POST /api/pluginInventory/list` | **401** | ✅ D2 §2.2「Host 可信但未认证 401」 |
| 有有效 cookie，但 `Host` 头伪造为 `192.168.1.50:3111`（非 loopback、未登记 `trustedHosts`） | **403** | ✅ D2 §2.2「Host/Origin 校验失败 403」 |
| 有有效 cookie + 真实 loopback | **200** | ✅ 基线 |
| `credentials/describe`（特权方法）经 loopback | **200** | ✅ 特权方法在 L0 路径可达 |

> **对 §6.4（设备接入与安全设计）的直接支撑**：该实验证明「伪造 `Host` 头」这条路**必然 403**——
> 这正是社区方案需要打 patch 才能绕过的围栏。本项目的 L1/L2 走**隧道出口回 loopback**，
> 因此无需伪造任何头即可满足该围栏；反之，任何试图直接连 Host 对外地址的做法都会被 403 拒绝。

**由此确立的三条实现纪律**

1. **404 与业务失败必须分开处理**：404 意味着「该 endpoint 在本 Host 上不存在」⇒ 上层应做**能力探测与入口置灰**（D1 7.9.2），而不是当作网络错误重试。
2. **错误码前缀是分层信号**：`gateway/*` = 调用契约错误（我方 bug，不应重试）；其他前缀 = 域内业务结果（应呈现给用户）。`Connection.call` 的 `RemoteFailure.code` 因此必须原样保留。
3. **参数包装名必须逐端点查描述符**：`session/list` 用 **`_request`**（带下划线），其余多数用 `request`，还有多个是扁平具名参数（`refs` / `id` / `namespace` 等）。**禁止按方法名猜参数形状**——这是本项目 v1 枚举阶段踩到的真实错误（首版假设 `workspace/list` 存在，实测 404）。

### 8.5 完整调用契约表（74 个 endpoint，由生成描述符提取）

> 采集方式：`node tools/protocol-contract.mjs --json .research/protocol/contracts.json`
> 来源：各包 `lib/*.js` 内的 generated `InvocationDescriptor`（含 `id` / `namespace` / `method` / `invocation.kind` / `parameters[].wire` / `cancellation` / `result.typeSymbol` / `sourceLocation`）。

**`session` 命名空间**（`sourceLocation` 指向 `packages/api/session-controller/src/index.ts`）

| method | 参数（`wire` 名） | 可取消 |
|---|---|---|
| `list` | **`_request`** | ✅ |
| `create` / `page` / `search` / `prompt` / `attachment` / `cancel` / `rename` / `fork` / `selectModel` / `openWorkspacePath` / `updateQueue` | `request` | 部分 ✅ |
| `follow`（流式） | `request` | ✅ |
| `control`（流式） | 无参数 | ✅ |
| `modelCatalog` / `canOpenWorkspacePath` | 无参数 | — |

**`settings` 命名空间**

| method | 参数 | 可取消 |
|---|---|---|
| `describe` / `canOpenAgentPresetDirectory` | 无参数 | — |
| `mutate` | `ns`、`ops`、`expectedRevision` | — |
| `replace` | `ns`、`section`、`expectedRevision` | — |
| `update` | `ns`、`patch`、`expectedRevision` | — |
| `openAgentPresetDirectory` | `agentPreset` | ✅ |
| `openSettingsDocument` | 无参数 | ✅ |

**`credentials`**：`describe`（`refs`）、`set`（`ref`、`value`）、`unset`（`ref`）
**`directoryPicker`**：`pick`（无参数，✅）、`list`（`path`，✅）、`createDirectory`（`path`、`name`）
**`workspace`**：`follow`（流式，无参数，✅）、`create` / `rename` / `delete` / `archiveSession` / `insertBefore` / `insertSessionBefore`（均为 `request`）
**`agentPresets`**：`list`（无参数）、`read`（`agentPreset`）、`select`（`agentId`、`agentPreset`）、`copy`（`from`、`id`、`name`）、`deletePreset`（`id`）
**`goals`**：`create`（`agentId`、`request`）、`edit`（`agentId`、`ref`、`request`）、`pause` / `resume` / `complete` / `clear`（`agentId`、`ref`）
**`subagents`**：`list`（`parentSessionId`，✅）、`prompt`（`request`，✅）、`interruptByParent`（`childSessionId`、`parentSessionId`、`mode`）
**`fileReferences`**：`list`（`agentId`、`query`，✅）
**`skills`**：`list`（`request`，✅）
**`sessionReferenceResolver`**：`candidates`（`agentId`、`query`，✅）
**`messageFeedback`**：`list` / `put` / `delete`（均为 `request`）
**`pluginInventory`**：`list`（无参数）
**`llm`**：`discoverModels`（`settingsNs`、`request`，✅）、`listProviders` / `listConfigurableProviders`（无参数）
**`commands`**：`list`（`agentId`）、`execute`（`agentId`、`line`、`images`，✅）
**`dynamicCordisRunner`**：`inventory`（无参数）、`invoke`（`pluginId`、`pluginRunId`、`method`、`args`）等 12 个

> **命名空间总数为 16，但描述符覆盖 74 个 endpoint**；两者不一致说明部分命名空间的方法来自其它服务包，
> 已在本表按 `namespace` 归组。**凡本表未列出的调用一律视为不可用**，实现前必须先跑一次 `protocol-contract.mjs` 核对。

### 8.6 三个被实测推翻的错误假设（教训留档）

| # | 曾经的假设 | 实测事实 | 影响 |
|---|---|---|---|
| 1 | `workspace/list` 存在（因为 `workspaceController._list_decorators = [Remote("list")]`） | **404**。该 `_list_decorators` 属于同文件的 **`DirectoryPickerController`**（namespace `directoryPicker`），与 `workspace` 无关 | 按「包 → 方法名」推断命名空间会错，必须读 `id: "<pkg>#<ns>/<method>"` |
| 2 | 一元调用只需传业务参数，空对象 `{}` 即可 | `session/list` 需要 **`_request`** 字段，否则 `gateway/arguments-invalid` | 参数包装名必须逐端点查描述符 |
| 3 | ArkTS WebSocket 会自动回 Pong，因此无需处理心跳 | SDK 不暴露 `pong()`；Host 每 2s Ping 且「未收到 Pong 即断开」 | 改为显式开客户端心跳（§8.2）——**但仍需真机验证** |

