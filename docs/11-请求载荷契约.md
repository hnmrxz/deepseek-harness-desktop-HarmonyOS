# 请求载荷契约（D2 附录 A）

| 项 | 内容 |
|---|---|
| 文档编号 | `docs/11-请求载荷契约.md` |
| 版本 | v1.0 |
| 日期 | 2026-09-10 |
| 状态 | 静态提取完成；标注「未确认」的条目**不得**据以实现 |
| 上位文档 | `docs/10-协议兼容事实基线.md`（D2）——本文是它的**附录级同伴**，不重复 §8.5 的端点清单与 §8.6 的错误假设，只补「参数包装之内是什么」与「流上推什么」 |
| 纪律 | **本文件只写事实，不写设计**。每行必须有 `source`。查不到就写 `未确认`，并说明缺什么证据 |
| 采集对象 | `@deepseek-ai/dsh` **0.1.5-rc.1** |

---

## 0. 版本与采集方法

| 项 | 值 | 来源 |
|---|---|---|
| 核心包版本 | `@deepseek-ai/dsh` **0.1.5-rc.1** | 代码（`.research/upstream-0.1.5/node_modules/@deepseek-ai/dsh/package.json` → `version`） |
| 端点总数 | **84**（一元 + 流式） | 代码（`dshcompat/src/main/ets/Endpoints.ets` → `UPSTREAM_IDENTITY.endpointCount`） |
| 运行时提取源 | `.research/upstream-0.1.5/node_modules/@deepseek-ai/` | 环境 |
| 源码提取源 | `.research/dsh-src`（tag `dsh-v0.1.5-rc.1`，commit `183f08e9c6dde7e36cd2318eaee70b0da08fb35e`） | 环境 |
| 机器可读副本 | `.research/protocol/request-schemas.json` | 代码（本文的 JSON 同伴） |

> **版本纪律**：`0.1.5-rc.1` 是 `@deepseek-ai/dsh` 自身 `package.json` 的 `version`。不要读外层打包包（`0.1.2-alpha.5` 一类的包装版本）。见 D2 §0。

### 0.1 实际执行的提取命令（可复现）

```text
node .research/protocol/extract-typert.mjs
   # 从生成的 FaceModel（lib/typert.host.js）+ zod 运行时 schema 提取每个 invocation 的
   # 参数 JSON Schema 与结果 JSON Schema；同时提取各包 model.events 与类型声明表
   # → .research/protocol/typert-extracted.json（30 个 face / 168 个 invocation）

node .research/protocol/dump-face-models.mjs
   # 把每个包的 FaceModel 事件表 + 类型声明表合并导出
   # → .research/protocol/face-model-all.{txt,json}（22 个事件 / 151 个 @Remote 成员 / 369 个类型声明）

node .research/protocol/dump-remote-signatures.mjs
   # 串接全部生成式 Remote 线上签名表（这是「客户端到底发什么」的权威表）
   # → .research/protocol/remote-signatures.txt

node .research/protocol/build-schemas.mjs
   # 生成交付物 JSON
   # → .research/protocol/request-schemas.json

   # 漂移核对（0.1.2-rc.1 提交版本 vs 0.1.5-rc.1 提取结果）
   # 注意：PowerShell 重定向会写成 UTF-16LE，reconcile 脚本已按 BOM 自动识别
git show HEAD:dshcompat/src/main/ets/Endpoints.ets > .research/protocol/endpoints-0.1.2.ets
node .research/protocol/reconcile-0.1.2-vs-0.1.5.mjs
```

**为什么这些产物可信**：`lib/typert.host.js` 是 `@deepseek-ai/dsh-typert-generator` 从 Host FaceModel 生成的，里面每个 invocation 内嵌**运行时 zod schema**（`z.toJSONSchema()` 可直接还原成 JSON Schema）；`lib/typert.remote-client.d.ts` 是同一 FaceModel 生成的**客户端线上签名表**，`parameters[].wire` 就是 `args` 对象的字段名。两者交叉一致时，本表的 `source` 会同时给出。

### 0.2 参数包装（`args` 的形状）——先看这个再查下面的表

D2 §8.5 已给出「包装名」。这里给出**判别规则**（来源：`.research/protocol/remote-signatures.txt`，每行形如 `'ns/method': (p1, p2, signal?) => ...`）：

| 线上签名形态 | `args` 该长什么样 | 例 |
|---|---|---|
| `(request: T)` | `args = { request: {...T 的字段...} }` | `session/prompt` |
| `(_request: T)` | `args = { _request: {...} }` | `session/list` |
| `(agentId, request: T)` | `args = { agentId, request: {...} }` | `fileUploads/upload` |
| `(from, id, name?)` | `args = { from, id, name }`（扁平具名） | `agentPresets/copy` |
| `()` | `args = {}` | `session/modelCatalog` |

> **`agentId` 的语义（易错）**：`agent` 参数在 Host 侧是 `source: "lookup"` 的 **Agent 作用域**，Host 端由 Typert 把线上 `agentId` 解析成 Agent 对象再注入。对客户端而言，`agentId` **就是 Session Id**（`SessionId` 类型）。也就是说 `session/prompt.request.sessionId`、`commands/execute.agentId`、`goals/*.agentId`、`fileUploads/upload.agentId` 是**同一个东西**。
> 来源：`.research/protocol/typert-extracted.json` → `faces["dsh-commands#host"].invocations[?method=list].parameters[0]` `{ name: "agent", wire: "agentId", source: "lookup", lookup: "agent" }`，其 `codec.typeSymbol` = `@deepseek-ai/dsh-session/types#SessionId`。

> **`signal` 不是 `args` 字段**。它是 `@Remote` 的取消参数，走 mux 的 `cancel` 帧或 HTTP `AbortSignal`，**绝不放进 `args`**。见 D2 §1.1/§1.2。

---

## 1. `session` 命名空间（18 个端点）

来源总表：`.research/upstream-0.1.5/node_modules/@deepseek-ai/dsh-api-session-controller/lib/typert.remote-client.d.ts` → `interface TypertRemoteMap`。

### 1.1 只读与会话生命周期

#### `session/list` — 包装 `_request`（**带下划线**）

- `args = { _request: {...} }`，`source`: `dsh-api-session-controller/lib/types/types.d.ts` → `interface SessionListRequest`

| 字段 | 类型 | 必填 | 含义 |
|---|---|---|---|
| `cursor` | `string` | 否 | 预留游标。0.1.5 的 Host 实现**忽略**该字段（`async list(_request, signal)` 不使用 `_request`）；不要依赖它做分页 |

- **回复** `SessionListValue` = `{ items: SessionSummary[] }`，`source`: 同上 → `interface SessionListValue` / `interface SessionSummary`

`SessionSummary` 字段：`sessionId`(string)、`updatedAt`(number, epoch ms)、`running`(boolean)、`blank`(boolean)、`parentSessionId?`(string)、`origin?`(`'subagent'`)、`cwd?`(string)、`projections?`(`{ asOfSeq: number; values: SessionProjectionValues }`)。

`projections.values` 是**稀疏对象**，可能出现的键见 §7.3 的 `SessionProjectionMap`。

> 实测佐证：本机 `session/list` 带 `_request` 返回 `200 {"items":[]}`；不带则 `gateway/arguments-invalid`（D2 §8.4/§8.6）。

#### `session/search` — 包装 `request`

`args = { request: { query: string } }`；`query` 必填（字面量内容查询，非正则）。
`source`: `.../types.d.ts` → `interface SessionSearchRequest`

**回复** `SessionSearchValue` = `{ items: SessionSearchItem[]; hasMore: boolean }`，`SessionSearchItem` = `{ sessionId, snippet }`。
上限：`SESSION_SEARCH_RESULT_LIMIT = 20`、`SESSION_SEARCH_SNIPPET_MAX_CODE_POINTS = 240`（同一 `.d.ts` L161/L163）。

#### `session/create` — 包装 `request`

`source`: `.../types.d.ts` → `interface SessionCreateRequest`

| 字段 | 类型 | 必填 | 含义 |
|---|---|---|---|
| `workspaceId` | `string` | 否 | 归属工作区；**与 `cwd` 互斥**（同传报 `gateway/bad-request`） |
| `cwd` | `string` | 否 | 直接指定目录 |
| `sessionId` | `string` | 否 | 显式指定/幂等认领既有 Session |
| `agentPreset` | `string` | 否 | Agent 预设 id |

**回复** `SessionCreateValue` = `{ sessionId, agentPreset? }`。

#### `session/rename` — 包装 `request`

`{ sessionId: string, title: string }`；**回复** `SessionRenameValue` = `{ title, seq }`（`seq` 是提交该标题的持久事件序号）。
`source`: `.../types.d.ts` → `interface SessionRenameRequest/Value`

#### `session/fork` — 包装 `request`

`{ sessionId: string, atSeq?: number }`（`atSeq` 必须是**已完成轮次**内的安全整数）；**回复** `{ sessionId }`。
失败码：`session/fork-unavailable`、`session/not-found`。
`source`: `.../types.d.ts` → `interface SessionForkRequest/Value`

#### `session/selectModel` — 包装 `request`

`source`: `.../types.d.ts` → `interface SessionSelectModelRequest extends ModelSelection`

| 字段 | 类型 | 必填 |
|---|---|---|
| `sessionId` | `string` | **是** |
| `provider` | `string` | **是**（来自 `ModelSelection`） |
| `model` | `string` | **是** |
| `reasoningEffort` | `string` | 否 |

**回复** `SessionSelectModelValue` = `{ selected: { provider, model, reasoningEffort? } }`（Host 归一化后的值，可能与请求不同）。

#### `session/openWorkspacePath` — 包装 `request`

`{ action?: 'reveal', path: string }`。`action` 省略 ⇒ 用默认程序打开；`'reveal'` ⇒ 在文件管理器中定位。`path` 不可为空字符串。
**回复** `{ opened: true }`。
`source`: `.../types.d.ts` → `interface SessionOpenWorkspacePathRequest/Value`

#### `session/canOpenWorkspacePath` — 无参数

`args = {}`；**回复** `boolean`（不是对象）。
`source`: `dsh-api-session-controller/lib/typert.remote-client.d.ts` L82 → `canOpenWorkspacePath: () => Promise<RemoteResult<boolean>>`

#### `session/modelCatalog` — 无参数

**回复** `ModelCatalog`，`source`: `.../types.d.ts` → `interface ModelCatalog`

```
{
  default: { provider, model, reasoningEffort? },      // ModelSelection
  routableProviders: string[],                          // 当前可路由的 provider id
  groups: { id, name, models: { id, name, description?,
              reasoning?: { efforts: {id,name,description?}[], defaultEffort? } }[] }[],
  failures: { id, name, message }[]                     // 模型目录加载失败的 provider
}
```

### 1.2 发消息、附件与队列

#### `session/prompt` — 包装 `request`（**最高频、最重要**）

`source`: `.../types.d.ts` → `interface SessionPromptRequest`（L291-300）+ `type PromptContentPart`（L64-75）

| 字段 | 类型 | 必填 | 含义 |
|---|---|---|---|
| `requestId` | `string`（brand `session-request-id`） | **是** | **客户端自铸**的幂等 id。Host 用它做去重（同 id 重复提交返回 `{accepted:true}`），并把它持久化进 `user/message.source.rpcId` 用于乐观回显对账。**必须客户端生成，不可复用** |
| `sessionId` | `string` | **是** | 目标 Session |
| `mode` | `'queue' \| 'steer'` | **是** | `'queue'` = 排到下一轮；`'steer'` = 插进当前轮最近的一步 |
| `content` | `PromptContentPart[]` | **是** | 见下表。必须至少含一个非空白文本片段**或**一个附件，否则 `gateway/bad-request` |
| `clientTimeZone` | `string` | 否 | `'UTC'` 或合法 IANA `Area/Location`。非法报 `session/invalid-time-zone` |

`PromptContentPart` 判别联合（`type` 为判别字段）：

| `type` | 字段 | 含义 |
|---|---|---|
| `'text'` | `text: string` | 纯文本 |
| `'image'` | `mediaType: 'image/png'\|'image/jpeg'\|'image/webp'\|'image/gif'`、`data: string`（**base64**）、`name?: string` | 内联图片；Host 会转成持久引用。模型不支持图片输入时报 `session/attachment-invalid` + `details.reason = 'MODEL_DOES_NOT_SUPPORT_IMAGES'` |
| `'file'` | `receiptId: string` | **0.1.5 新增**。指向此前 `fileUploads/upload` 返回的收据；Host 用 `fileUploads.resolve(agent, receiptId)` 换取持久引用，换不到报 `session/attachment-invalid` + `reason:'FILE_NOT_STAGED'` |

**回复** `SessionPromptValue` = `{ accepted: true }`（**只有这一个字段，没有 messageId**）。要拿到真实消息 id 必须靠 §7.1 的 `session/follow` 或 `session/page`。

> **对客户端的直接含义**：图片要么走 `session/prompt.content[].type='image'`（内联 base64，受 `imageLimits` 约束），要么先 `fileUploads/upload` 或 `POST /api/session/uploadFileBinary` 拿 `receiptId`，再用 `content[].type='file'`。两条路都合法。

#### `session/attachment` — 包装 `request`

`{ sessionId: string, attachmentId: string }`。
**回复** `SessionAttachmentValue` = `{ attachment: ImageAttachmentRef; data: string }`。
`ImageAttachmentRef` = `{ attachmentId, mediaType, bytes, width, height, name?, originalDimensions?: {width,height} }`；`data` 是 base64。
授权规则：`attachmentId` 必须被该 Session 日志引用过，否则 `session/attachment-invalid` + `reason:'ATTACHMENT_NOT_REFERENCED'`。
`source`: `.../types.d.ts` → `interface SessionAttachmentRequest/Value`；`dsh-api-session-controller/src/commands.ts` → `async attachment(request)`

#### `session/updateQueue` — 包装 `request`

`source`: `.../types.d.ts` → `interface SessionUpdateQueueRequest` + `type QueueAction`

| 字段 | 类型 | 必填 |
|---|---|---|
| `sessionId` | `string` | 是 |
| `itemId` | `string`（`MessageId`） | 是（来自 `SessionQueuedItem.id`） |
| `action` | `QueueAction` | 是 |

`QueueAction` 判别联合（`kind` 为判别字段）：

| `kind` | 附带字段 | 语义 |
|---|---|---|
| `'edit'` | `content: ContentBlock[]` | 替换队列项内容。**只接受纯文本块**，含非 `text` 块报 `session/attachment-invalid` + `reason:'QUEUE_EDIT_NON_TEXT'` |
| `'remove'` | — | 移除队列项 |
| `'steer'` | — | 把队列项改成 steer 插队。仅当该项位于 `next-turn` **且** Agent 正在运行，否则 `session/steer-unavailable` |

**回复** `{ accepted: true }`。

#### `session/cancel` — 包装 `request`

`{ sessionId: string }` ⇒ **回复** `{ accepted: true }`。
语义：取消**当前活动轮次**但**保留**待处理收件箱（`agent.cancel({kind:'user'}, {keepInbox:true})`）。
Session 未附着（无 live Agent）⇒ `session/not-found`。
`source`: `.../types.d.ts` → `interface SessionCancelRequest/Value`；`dsh-api-session-controller/src/commands.ts` → `cancel(request)`

#### `session/page` — 包装 `request`

`source`: `.../types.d.ts` → `interface SessionPageRequest`（L409-415）+ `type SessionAddress`（L357-365）

| 字段 | 类型 | 必填 | 含义 |
|---|---|---|---|
| `address` | `SessionAddress` | **是** | 见下 |
| `throughSeq` | `number` | **是** | 包含式日志切点。`-1` 表示「到当前末尾」。必须 `>= -1` 的安全整数 |
| `beforeSeq` | `number` | 否 | 向前分页游标，非负安全整数 |
| `maxMessages` | `number` | 否 | 正整数，默认 **50**（`DEFAULT_MAX_MESSAGES`） |

`SessionAddress` 判别联合（`kind` 为判别字段）：
- `{ kind:'session', sessionId }`
- `{ kind:'subagent', parentSessionId, childSessionId, mode:'one-shot'|'continuable' }`

**回复** `SessionPage` = `{ records: SessionHistoryRecord[]; hasMore: boolean }`，`SessionHistoryRecord` = `{ type:'event', event: SessionWireEvent }`。
`source`: `dsh-api-session-controller/src/history.ts` → `async page(request, signal)`（`DEFAULT_MAX_MESSAGES = 50`）

### 1.3 流式端点

`session/follow` 与 `session/control` 的逐项结构见 **§7**。

---

## 2. `workspaceFiles` 命名空间（**0.1.5-rc.1 新增，7 个端点**）

来源总表：`.research/upstream-0.1.5/node_modules/@deepseek-ai/dsh-api-workspace-files/lib/typert.remote-client.d.ts`（L20-26）。
类型权威：`.research/dsh-src/packages/api/workspace-files/src/types.ts` 与同包 `src/index.ts`。

### 2.0 `workspaceFileScopeId` 是什么？客户端从哪拿？

**结论（有确证）**：`workspaceFileScopeId` **就是 Session Id**（`SessionId`，字符串）。它**不是**工作区 id，也**不是**新造的句柄。

| 问题 | 答案 | 来源 |
|---|---|---|
| 线上字段类型 | `SessionId`（字符串） | `dsh-api-workspace-files/lib/typert.remote-client.d.ts` L11-17 → 签名第一个参数写作 `workspaceFileScopeId: SessionId` |
| Host 端如何解析 | Typert lookup：`register('workspaceFileScope', { parameter:'workspaceFileScope', wire:'workspaceFileScopeId', wireTypeSymbol:'@deepseek-ai/dsh-session/types#SessionId' })` | `dsh-api-workspace-files/lib/index.js` L373-376；同 `src/index.ts` L201-220 |
| lookup 返回什么 | `WorkspaceFileScope = { sessionId, workspaceRoot }`；`workspaceRoot` 取 `header.cwd`，无 cwd 时回落 `sandboxPolicy.workspaceRoot` | `dsh-api-workspace-files/lib/types/index.d.ts` L33-39；`src/index.ts` L207-218 |
| 作用域粒度 | **会话级**（每个 Session 一个 workspaceRoot）。是「哪个会话的根目录」，不是「哪个工作区」 | 同上 |
| 冷会话可用吗 | 可以。live Session 读内存 header，冷的走 `sessionPersistence.stat(sessionId)` 只读 header，**不激活 Agent、不读事件正文** | `src/index.ts` L207-220 |
| 客户端从哪拿 | **直接用自己正在看的 Session Id**。0.1.5 自己的客户端就是这么做的：`remote.workspaceFiles.stat(sessionId, path, signal)`，其中 `sessionId` 来自资源地址 `dsh-resource://file/session/<sessionId>/<path>` | `.research/dsh-src/packages/api/workspace-files/src/client/provider.ts` L58-61、L118-121 |

> **给 HarmonyOS 客户端的结论**：把 `workspaceFileScopeId` 填成当前 Session 的 `sessionId` 即可，不需要任何额外端点或事件去「取得」它。

> **错误码**：`workspace-file/not-found`、`workspace-file/outside-workspace`、`workspace-file/too-large`、`workspace-file/not-text`、`workspace-file/not-regular-file`、`workspace-file/not-directory`（`source`: `src/types.ts` L145-165 的 `RemoteErrorDetailsMap`）。

### 2.1 `workspaceFiles/list` — 命名参数

`args = { workspaceFileScopeId, path }`（两者都必填）。
`source`: `typert.remote-client.d.ts` L12；实现 `src/index.ts` → `async list(workspaceFileScope, path, signal)`

**回复** `WorkspaceDirectoryListing`（`source`: `src/types.ts` L89-104）：

```
{
  path: string,                    // 被列举目录的「工作区相对路径」；工作区根为空串
  entries: { name: string,         // 目录内 basename；子项全路径 = path + '/' + name
             type: 'file' | 'directory' | 'other',
             size?: number           // 仅常规文件且后端报告时存在
           }[],
  truncated: boolean               // 是否因条目上限被截断
}
```

**关键事实**：
- **不是树，是「某一层目录的直接子项」扁平列表**。要建树必须逐层调用（子项路径 = 父 `path` + `'/'` + `name`）。
- **无 `mtime` / `version` / `isDirectory` 布尔**：类型判别字段叫 `type`（取值 `file|directory|other`），不是 `kind`/`isDirectory`。
- **无分页**。用部署上限截断：`maxEntries` 默认 **2000**，超出部分**丢弃**并把 `truncated=true`。
- 符号链接报告**目标**类型；但 `read` 仍拒绝软链（`workspace-file/not-regular-file` + `kind:'symlink'`）。
- **只有 `list` 受工作区包含约束**：目标解析出工作区外报 `workspace-file/outside-workspace`。
- **路径词法**：`list` 说「工作区路径」（相对工作区根，或绝对）；`read`/`readBytes`/`stat`/`changes` 说「文件系统执行世界的绝对路径」。这是 `src/types.ts` 文件头明确写的两套词法。
- `source`: `src/index.ts` L337-353、L69-84（Config 默认值）

### 2.2 `workspaceFiles/stat` — 命名参数

`args = { workspaceFileScopeId, path }`。
**回复** `WorkspaceFileStat` = `{ absolutePath: string, version: string, bytes?: number }`。
- `version` 是**不透明新鲜度令牌，永不被解析**（`src/types.ts` L31-32）。客户端只做「与上次不同 ⇒ 内容变了」的比较。
- `bytes` 是**完整文件**大小（不是窗口大小），后端不报告时缺省。
- `source`: `typert.remote-client.d.ts` L17；`src/index.ts` L324-327

### 2.3 `workspaceFiles/read` — 命名参数（**文本按行**）

`args = { workspaceFileScopeId, path, range }`。`range` **必填**（可为 `{}`）。
`range: WorkspaceFileRange` = `{ offset?: number; limit?: number }`（`source`: `src/types.ts` L35-40）

| range 字段 | 类型 | 默认 | 语义 |
|---|---|---|---|
| `offset` | `number` | **1** | **行号，1-based**。不是字节偏移 |
| `limit` | `number` | `maxLines`（默认 **5000**） | **行数上限**，不得超过 `maxLines` |

**回复** `WorkspaceFileText` = `WorkspaceFileStat & { offset: number, text: string, lines: number, eof: boolean }`：
- `text` 是页内各行用 `'\n'` 连接，**末行后无终止符**。
- `lines` 是页内行数；`offset` 越过文件末行时为 **0**（此时 `text` 为空串）。
- `eof` 表示本页是否包含文件最后一行。
- 返回**文本**，不是 base64。
- 行按 `\n` 结束；文件末尾的 `\n` **终止最后一行**而不新起一个空行。

**拒答边界**：单页文本超过 `maxBytes`（默认 **2 MiB**）⇒ `workspace-file/too-large`（**拒绝而非截断**，理由：被静默截断的页读起来像完整页）。内容非 UTF-8 或含 NUL ⇒ `workspace-file/not-text`。非普通文件 ⇒ `workspace-file/not-regular-file`。
`source`: `src/index.ts` L231-245、L369-377、L105-159；`src/types.ts` L41-62

### 2.4 `workspaceFiles/readBytes` — 命名参数（**原始字节按窗口**）

`args = { workspaceFileScopeId, path, range }`，`range` **必填**。
`range: WorkspaceByteRange` = `{ offset?: number; length?: number }`（`source`: `src/types.ts` L56-62）

| range 字段 | 类型 | 默认 | 语义 |
|---|---|---|---|
| `offset` | `number` | **0** | **字节偏移，0-based** |
| `length` | `number` | `maxBytes`（默认 2 MiB） | **字节数**，不得超过 `maxBytes` |

**回复** `WorkspaceFileBytes` = `WorkspaceFileStat & { offset, data: string, eof: boolean }`：`data` 是窗口字节的 **base64**；`eof` 表示是否含文件最后一个字节。
与 `read` 的区别：**不解码、不拒绝二进制**（可以读 exe/zip 的片段）。
`source`: `src/index.ts` L256-268、L379-394；`src/types.ts` L63-83

### 2.5 `workspaceFiles/readAll` — 命名参数（**整文件**）

`args = { workspaceFileScopeId, path }`（**无 `range`**）。
**回复** 与 `readBytes` 同型：`WorkspaceFileBytes`，恒有 `offset = 0`、`eof = true`。
约束：完整文件超过 `maxFileBytes`（默认 **32 MiB**）⇒ `workspace-file/too-large`（**拒绝，永不截断**）。
适用场景：需要整个文件时；大文件应用 `readBytes` 分窗。
`source`: `src/index.ts` L277-289

### 2.6 `workspaceFiles/readRelated` — 命名参数

`args = { workspaceFileScopeId, path, relativePath }`（三字段全必填）。
`path` = 基准文件；`relativePath` = **相对文件系统路径**（不是 URL、不是绝对路径、不得含 NUL），相对**基准文件所在目录**解析。返回整文件（等价于对解析出的路径调 `readAll`，**同样受 `maxFileBytes` 与文件种类检查约束**）。
适用场景：一个文件引用另一个文件（如 `foo.ts` 里的 `./bar.ts`）。
非法 `relativePath` ⇒ `gateway/bad-request`。
`source`: `src/index.ts` L299-314

### 2.7 `workspaceFiles/changes` — **流式**，命名参数

`args = { workspaceFileScopeId }`。
逐项结构见 **§7.4**。
`source`: `src/index.ts` L364-367；`src/changes.ts`

> **`read` / `readBytes` / `readAll` / `readRelated` 不受工作区包含限制**——只做普通文件检查，允许读工作区外的可读路径。**只有 `list` 与 `changes` 是工作区受限的**。（`src/index.ts` 文件头注释 + `confine()` 仅被 `list` 调用。）

### 2.8 四个读端点速查（回答「`range` 是什么类型、返回文本还是 base64」）

| 端点 | `range` 类型与单位 | 回复里的内容字段 |
|---|---|---|
| `read` | **行**：`{ offset?: number, limit?: number }`，`offset` 1-based 行号、`limit` 行数 | `WorkspaceFileText`：`text`（UTF-8 文本）+ `lines` + `eof` |
| `readBytes` | **字节**：`{ offset?: number, length?: number }`，`offset` 0-based 字节偏移、`length` 字节数 | `WorkspaceFileBytes`：`data`（**base64**）+ `eof` |
| `readAll` | 无 `range`，读整文件 | `WorkspaceFileBytes`（`offset` 恒 0、`eof` 恒 `true`） |
| `readRelated` | 无 `range`，`relativePath` 相对**基准文件所在目录**解析 | `WorkspaceFileBytes`（等价于对解析结果调 `readAll`） |

> **`range` 在三个带 `range` 的端点上都是必填参数**（要么传 `{}` 用默认值，要么传具体字段），不是可省参数。省略整个 `range` 键会报 `gateway/arguments-invalid`。
> **单位不要混**：`read` 的 `offset`/`limit` 是**行**；`readBytes` 的 `offset`/`length` 是**字节**。字段名本身在提示这一点（`limit` vs `length`），这是刻意的不对称。
> **`read` 返回文本，`readBytes`/`readAll`/`readRelated` 返回 base64。** 只有 `read` 会拒绝二进制（`workspace-file/not-text`）。

---

## 3. `workspace` 与 `directoryPicker` 命名空间（10 个端点）

来源总表：`dsh-api-workspace-controller/lib/typert.remote-client.d.ts` L190-201。
类型权威：`dsh-api-workspace-controller/lib/types/types.d.ts` 与 `dsh-host-directory-picker/lib/types/*.d.ts`。

`WorkspaceView`（**所有 workspace 回复的基本行**，`source`: `types.d.ts` L12-24）：
`{ workspaceId, path, title, sessionIds: string[], createdAt: string(ISO-8601), updatedAt: string(ISO-8601) }`
> 注意 `createdAt`/`updatedAt` 是 **ISO 字符串**，而 Session 侧的时间戳是 epoch 毫秒的 number。别混用。

| 端点 | 包装 | `request` 字段 | 回复 |
|---|---|---|---|
| `workspace/create` | `request` | `path`(string, 必填) | `{ workspace: WorkspaceView, created: boolean }` |
| `workspace/rename` | `request` | `workspaceId`(string)、`title`(string) | `{ workspace: WorkspaceView }` |
| `workspace/delete` | `request` | `workspaceId`(string) | `{ deleted: true }` |
| `workspace/insertBefore` | `request` | `workspaceId`(string)、`beforeWorkspaceId?`(string) | `{ workspaceIds: string[] }`（**完整新顺序**） |
| `workspace/insertSessionBefore` | `request` | `workspaceId`、`sessionId`、`beforeSessionId?` | `{ workspace: WorkspaceView }` |
| `workspace/archiveSession` | `request` | `sessionId`(string) | `{ archivedSessionIds: string[] }`（**完整归档集**） |
| `workspace/follow` | **无参数**，流式 | — | 见 §7.5 |
| `directoryPicker/list` | 命名 | `path?: string`（**可省**；省 ⇒ Host 默认起始目录） | `DirectoryListing` = `{ path, home, crumbs: DirectoryEntry[], entries: DirectoryEntry[], truncated: boolean }`，`DirectoryEntry` = `{ name, path, hidden }` |
| `directoryPicker/pick` | **无参数** | — | `string \| null`（用户取消 ⇒ `null`） |
| `directoryPicker/createDirectory` | 命名 | `path`(string)、`name`(string) | `string`（新目录路径） |

`source`: `dsh-api-workspace-controller/lib/types/types.d.ts` L59-107（workspace）、`dsh-host-directory-picker` 类型经 `types.d.ts` L10 再导出；`typert.remote-client.d.ts` L177-200。

**`workspace` 域错误码**（`types.d.ts` L25-57）：`workspace/invalid-path`、`workspace/name-conflict`、`workspace/move-invalid`、`directory-picker/unavailable`、`directory-picker/unreadable`、`directory-picker/exists`、`directory-picker/create-failed`。

> `workspace/follow` 的基线是**完整**工作区表 + 完整归档集，增量是 `upsert`/`remove`/`order`/`archived` 四类——设计上就是「订阅一次即可维持客户端工作区侧栏」。

---

## 4. `settings` 与 `credentials` 命名空间（10 个端点）

来源总表：`dsh-api-settings-controller/lib/typert.remote-client.d.ts` L141-152。

| 端点 | 包装 | 参数 | 回复 |
|---|---|---|---|
| `settings/describe` | 无参数 | — | `SettingsDescribeValue` |
| `settings/update` | 命名 | `ns`(string)、`patch`(`Record<string, JsonValue>`)、`expectedRevision`(number \| undefined) | `SettingsNamespaceView` |
| `settings/replace` | 命名 | `ns`(string)、`section`(`Record<string, JsonValue>`)、`expectedRevision`(number \| undefined) | `SettingsNamespaceView` |
| `settings/mutate` | 命名 | `ns`(string)、`ops`(`SettingsPathOpView[]`)、`expectedRevision`(number \| undefined) | `SettingsNamespaceView` |
| `settings/canOpenAgentPresetDirectory` | 无参数 | — | `boolean` |
| `settings/openSettingsDocument` | 无参数（可取消） | — | `SettingsDocumentOpenValue` = `{ opened: true }` |
| `settings/openAgentPresetDirectory` | 命名（可取消） | `agentPreset`(string) | `{ opened: true } \| { opened: false; path: string }` |
| `credentials/describe` | 命名 | `refs: string[]` | `Record<string, CredentialInfo>` |
| `credentials/set` | 命名 | `ref`(string)、`value`(string) | `void`（见下方「void 回复的线上形态」） |
| `credentials/unset` | 命名 | `ref`(string) | `void` |

`settings` 类型（`source`: `dsh-settings` / `dsh-api-settings-controller` 的 `lib/types/*.d.ts`）：

```
SettingsDescribeValue   = { writable: boolean, hasDocument: boolean, namespaces: SettingsNamespaceView[] }
SettingsNamespaceView   = { ns: string, schema: JsonValue, value: JsonValue, base?: JsonValue,
                            user?: JsonValue, applies: 'live' | 'restart',
                            secrets: { path: string[], set: boolean }[], revision: number }
SettingsPathOpView      = { op:'set', path: string[], value: JsonValue } | { op:'unset', path: string[] }
CredentialInfo          = { configured: boolean, source?: string, writable: boolean }
```

**三个写端点的区别**（这是最容易混的地方，`source`: `dsh-api-settings-controller/lib/index.js` 的方法体）：
- **`update`**：`patch` 是**浅覆盖对象**（与现有 section 合并）。
- **`replace`**：`section` **整体替换**该命名空间的用户 section（未出现的键被删除）。
- **`mutate`**：`ops` 是**有序路径操作列表**（`set`/`unset` 到具体 `path`），适合深层字段与删除单个键。
- 三者都吃 `expectedRevision`：省略 = 不做乐观并发检查。

> **`expectedRevision` 在 `typert.remote-client.d.ts` 里类型是 `number | undefined`（不是可选 `?`）**，但 Typert 的实参序列化会跳过 `undefined`，所以客户端**省略该键**即可（`{ns, patch}`），Host 收到 `undefined`。这与 D2 §8.5 记的「`ns`、`patch`、`expectedRevision`」一致。

> `settings/*` 与 `credentials/*` 属 D2 §2.4 的**特权方法**（loopback-only）。实测在 loopback 下 `credentials/describe` 返回 200（D2 §8.4）。

### 4.1 「void 回复」的线上形态（**修正 D2 §1.1 的一条措辞**）

`credentials/set`、`credentials/unset`、`agentPresets/copy`、`agentPresets/deletePreset` 的 `result.typeSymbol` 是 `void`。它们的成功响应体是：

```json
{ "type": "server-response", "rpcId": "...", "result": { "ok": true } }
```

**`result.value` 键不存在**（不是 `null`）。原因：Host 构造 `{ ok: true, value: await this.invoke(...) }`，业务方法返回 `undefined`，随后 `JSON.stringify` **丢弃值为 `undefined` 的键**。
`source`: `dsh-api-gateway/lib/index.js` L728-737 → `invokeRpc()`；`dsh-client-connection/lib/index.js` L687-694 → `fullResponse(rpcId, result)` 用 `Response.json(body)` 序列化。

**对 D2 §1.1 的修正**：D2 写「`ok=true` 时须有 `value`」。**准确的规则**是：Client 的解析器 `parseConnectionResponse` **只要求** `result.ok === true`，然后无条件读 `result.value`；**缺失 `value` 是合法的**，解析结果是 `value === undefined`。
`source`: `dsh-client-connection/lib/client.js` → `parseConnectionResponse`（`if (result.ok === true) return { result: { ok: true, value: result.value } }`，**无存在性校验**）。

> **对端侧的直接含义**：ArkTS 的响应解析**不得**把「`ok:true` 但没有 `value` 字段」判为畸形信封，否则四个 void 端点会全部失败。这是一个会在真机上直接暴露的实现陷阱。

---

## 5. `llm` 与 `pluginInventory` 命名空间（4 个端点）

| 端点 | 包装 | 参数 | 回复 |
|---|---|---|---|
| `llm/listProviders` | 无参数 | — | `LlmProviderInfo[]` = `{ id: string, name: string }[]` |
| `llm/listConfigurableProviders` | 无参数 | — | `LlmConfigurableProvider[]` |
| `llm/discoverModels` | 命名（可取消） | `settingsNs`(string)、`request`(`LlmModelDiscoveryRequest`) | `LlmDiscoveredModel[]` |
| `pluginInventory/list` | 无参数 | — | `PluginInventorySnapshot` |

```
LlmConfigurableProvider = { provider: string, displayName: string, settingsNs: string,
                            settingsPath: string[], declared?: boolean, error?: string }
LlmModelDiscoveryRequest = { provider?: string, baseURL?: string, api?: string, apiKey?: string }
LlmDiscoveredModel       = { id: string, name?: string, contextWindow?: number, maxTokens?: number }
PluginInventorySnapshot  = { entries: { entryId, moduleName, enabled: boolean,
                                        fiberPhase: null|'pending'|'active'|'failed'|'loading'|'unloading' }[],
                             agentPresets?: { id, trust:'system'|'user', name?, isDefault: boolean,
                                              broken?, rows: { entryId: null|string, moduleName,
                                              enabled: false|true|'conditional', condition?,
                                              fiberPhase: ... }[] }[] }
```

`settingsNs` 必填（取 `listConfigurableProviders[].settingsNs`）。
`llm/discoverModels` 的业务拒答码为 `llm/model-discovery-rejected`（D2 §8.4 实测）。
`source`: `dsh-llm/lib/typert.remote-client.d.ts` L498-501；`dsh-host-plugin-inventory/lib/typert.remote-client.d.ts` L469-471；类型经 `dsh-llm/lib/types/*.d.ts` 与 `dsh-host-plugin-inventory/lib/types/*.d.ts`。

---

## 6. 其余一元命名空间

### 6.1 `commands`（2 个端点）

`source`: `dsh-commands/lib/typert.remote-client.d.ts` L327-329。

| 端点 | 包装 | 参数 | 回复 |
|---|---|---|---|
| `commands/list` | 命名 | `agentId`(string) | `CommandDescriptor[]` = `{ name, description, input?: { hint: string, attachments?: boolean } }[]` |
| `commands/execute` | 命名（可取消） | `agentId`(string)、`line`(string)、`submittedAttachments`(`CommandSubmitAttachment[]`) | `CommandExecution \| undefined` |

> ⚠️ **0.1.5-rc.1 的元素类型变更（已确认）**：0.1.2-rc.1 的第三个参数在源码里名为 `images`，其类型只接受 base64 图片数组；0.1.5-rc.1 改名为 `submittedAttachments`，其类型是 `CommandSubmitAttachment[]`（图片**或**文件收据的联合）。
> **但线上字段名两次都是 `submittedAttachments`**——0.1.2 的 `images` 只是 TypeScript 形参名，被 `@Remote` 投影成线上名 `submittedAttachments`。
> 证据：0.1.2 的 `contracts.json` 该端点 `name=["agent","line","submittedAttachments"]` / `wire=["agentId","line","submittedAttachments"]`。**所以这不是线上破坏性改名，而是形参改名 + 元素类型扩容。**
> 权威签名见 `dsh-commands/lib/typert.remote-client.d.ts` L328；元素类型见 `.research/dsh-src/packages/interaction/commands/src/types.ts` L14-17。
> **客户端仍然必须发 `submittedAttachments`**；发 `images` 会报 `gateway/arguments-invalid`（这一点 0.1.2 也一样）。

`CommandSubmitAttachment`（`source`: `.research/dsh-src/packages/interaction/commands/src/types.ts` L14-17）：

```ts
type CommandSubmitAttachment =
  | ({ readonly type: 'image' } & EncodedImageAttachment)   // { type:'image', mediaType, data(base64), name? }
  | { readonly type: 'file'; readonly receiptId: string }   // 指向 fileUploads/upload 的收据
```

`CommandExecution` = `{ commandId: string, result: CommandResult }`；
`CommandResult` = `{ kind:'success', text?, sourceEventSeq? } | { kind:'error', text: string }`。
`execute` 返回 **`undefined`** 表示 `line` 语法不成立或命令名未注册（**不是错误**）。
附件被拒时**不抛错**，而是返回 `{kind:'error', text:'/name does not accept attachments'}`。
`source`: `.research/dsh-src/packages/interaction/commands/src/index.ts` L355-410。

### 6.2 `fileUploads`（**0.1.5-rc.1 新增**）

`fileUploads/upload` — 命名（可取消）：`args = { agentId: string, request: EncodedFileUploadRequest }`。
`source`: `dsh-client-file-upload/lib/typert.remote-client.d.ts` L266-276。

| `request` 字段 | 类型 | 必填 | 含义 |
|---|---|---|---|
| `data` | `string` | **是** | 文件字节的 **base64**（规范编码） |
| `name` | `string` | 否 | 显示名；Host 会消毒成存储叶子名 |

**回复** `FileUploadValue` = `{ receiptId: string, file: FileAttachmentRef }`，`FileAttachmentRef` = `{ attachmentId, name, bytes }`。
`agentId` 必须是**普通会话**（非 subagent）；subagent 会话报 `subagent/attachment-invalid` + `reason:'SUBAGENT_FILE_UNSUPPORTED'`。
收据是 **Agent 作用域**的：换一个 Session 用同一 `receiptId` 会被 `fileUploads.resolve()` 判为未知 ⇒ `session/attachment-invalid` + `reason:'FILE_NOT_STAGED'`。
`source`: `.research/dsh-src/packages/client/file-upload/src/index.ts` L105-112、L183-214、L230-239；`src/types.ts` L7-22。

#### 完整上传流程（**两条路，都要知道**）

上传后拿到的 `receiptId` 有两个消费口：`session/prompt.request.content[].type='file'` 与 `commands/execute.submittedAttachments[].type='file'`。

| 路 | 步骤 | 载体 | 说明 |
|---|---|---|---|
| **A. RPC 内联 base64** | 一次 `fileUploads/upload`，请求体里带完整 base64 | 一元 RPC `POST /api/fileUploads/upload` | 实现简单，但 base64 会膨胀 ≈33%，且整个文件在一条 JSON 里；适合中小文件 |
| **B. 原始字节流式路由** | `POST /api/session/uploadFileBinary?sessionId=<id>[&name=<n>]` | **HTTP 路由，不是 RPC**；`content-type: application/octet-stream`；请求体是裸字节流 | **支持分块/流式上传，无 base64 膨胀**（Host 端 `request.body` 逐块读取）。**适合大文件** |

- 路径常量：`FILE_UPLOAD_PATH = '/api/session/uploadFileBinary'`（`source`: `.research/dsh-src/packages/client/file-upload/src/protocol.ts` L2）
- 响应：HTTP **200** + JSON `{ ok: true, value: FileUploadValue }` 或 `{ ok: false, error: { code, message, details } }`；非 `POST` ⇒ 405；`content-type` 不是 `application/octet-stream` ⇒ 415；缺 `sessionId` ⇒ 400
- 认证：与其他 `/api` 路由同一 cookie（D2 §2.1）
- `source`: `.research/dsh-src/packages/client/file-upload/src/http-route.ts` L22-67

**关于「进度」**（**已核实，不是未确认**）：
- **协议层（线上）没有任何进度帧**。`uploadFileBinary` 的响应是**一次性** JSON（上传完成后才返回），Host 不会中途回传已接收字节数。`fileUploads/upload` 更是一次性 base64 提交。
- **但客户端可以在发送侧拿到逐块进度**。0.1.5 的 Web 客户端就是这么做的：把请求体包成 `ReadableStream`，在 `pull()` 里累计 `loaded += chunk.byteLength` 并回调 `onProgress({ loaded, total? })`。
  `source`: `.research/dsh-src/packages/client/file-upload/src/client/runtime.ts` L111-131（worker 内 `scope.postMessage({ kind:'progress', loaded })`）与 L193-209（`upload(..., onProgress?)`）。
- **对端侧的结论**：ArkTS 侧要用 `@ohos.net.http` 的流式上传（或按块分写）**自己在发送侧统计已发送字节数**来驱动进度条；不要等 Host 回传进度。这与 Web 客户端的能力对等。

**收据的生命周期**：`receiptId` 在**成功进入某个 prompt 的收件箱**之前有效；一旦对应的 `user/message` 事件以该 prompt 的 `requestId` 落盘，Host 就**回收**收据（`retire`）。所以「上传后长期缓存收据、以后再发」是不成立的——必须在上传后尽快随 prompt 提交。
`source`: `.research/dsh-src/packages/client/file-upload/src/index.ts` L241-254、L152-181。

### 6.3 `fileReferences` 与 `sessionReferenceResolver`（2 个端点）

| 端点 | 包装 | 参数 | 回复 |
|---|---|---|---|
| `fileReferences/list` | 命名（可取消） | `agentId`(string)、`query`(string) | `FileReferenceCandidate[]` = `{ path: string, kind: 'file' \| 'directory' }[]` |
| `sessionReferenceResolver/candidates` | 命名（可取消） | `agentId`(string)、`query`(string) | `SessionReferenceMentionCandidate[]` |

`SessionReferenceMentionCandidate`（`source`: `dsh-session-reference` 类型 + `face-model-all.txt` L2012）= `{ mention, sessionId, label, cwd?, sameWorkspace: boolean, createdAt: number }`——`mention` 是可直接插入输入框的 `@` 提及文本。
`source`: `dsh-api-session-controller/lib/typert.remote-client.d.ts` L79；`dsh-session-reference/lib/typert.remote-client.d.ts` L560。

### 6.4 `skills`（1 个端点）

`skills/list` — 包装 `request`（可取消）：`{ sessionId: string }`。
**回复** `{ skills: SkillEntry[] }`，`SkillEntry` = `{ name, description, whenToUse?, modelInvocable: boolean }`。
`source`: `dsh-api-session-controller/lib/types/types.d.ts` L217-234；`typert.remote-client.d.ts` L96。

### 6.5 `goals`（7 个端点，含 **0.1.5-rc.1 新增的 `get`**）

`source`: `dsh-goal/lib/typert.remote-client.d.ts` L427-434；类型 `.research/dsh-src/packages/goal/goal/src/types.ts`。

| 端点 | 包装 | `args` 字段 | 回复 |
|---|---|---|---|
| `goals/get` | 命名 | `agentId` | `GoalView \| undefined`（无目标 ⇒ `undefined`） |
| `goals/create` | 命名 | `agentId`、`request: { objective: string, maxGoalRounds?: number }` | `{ ref: GoalRef }` |
| `goals/edit` | 命名 | `agentId`、`ref: GoalRef`、`request: { objective?: string, maxGoalRounds?: number }` | `GoalView` |
| `goals/pause` | 命名 | `agentId`、`ref: GoalRef` | `GoalView` |
| `goals/resume` | 命名 | `agentId`、`ref: GoalRef` | `GoalView` |
| `goals/complete` | 命名 | `agentId`、`ref: GoalRef` | `GoalView` |
| `goals/clear` | 命名 | `agentId`、`ref: GoalRef` | `GoalRef`（被清除的 ref） |

```
GoalRef  = { id: string, revision: number }          // CAS 身份：revision 每次持久变更 +1
GoalView = { id, revision, objective: string,
             phase: 'active'|'paused'|'blocked'|'complete',
             blockedReason?: { code: string, message: string },   // 仅 phase==='blocked' 时存在
             maxGoalRounds: number,
             roundsStarted: number, createdAt: number, updatedAt: number,
             activation: 'armed'|'disarmed' }                     // 进程本地，不持久化
```

> **`ref.revision` 必须来自最近一次读到的 `GoalView`**（CAS）。用陈旧 revision 调用会被拒。客户端应先用 `goals/get`（0.1.5 新增，正好补上这个缺口）拿当前 ref。

### 6.6 `subagents`（3 个端点）

`source`: `dsh-subagent/lib/typert.remote-client.d.ts` L592-595；类型 `.research/upstream-0.1.5/node_modules/@deepseek-ai/dsh-subagent/lib/types/control-types.d.ts`。

| 端点 | 包装 | `args` 字段 | 回复 |
|---|---|---|---|
| `subagents/list` | 命名（可取消） | `parentSessionId` | `SubagentCatalog` |
| `subagents/prompt` | 包装 `request`（可取消） | `request: SubagentPromptRequest` | `{ messageId: string }` |
| `subagents/interruptByParent` | 命名 | `childSessionId`、`parentSessionId`、`mode:'continuable'` | `{ accepted: true }` |

```
SubagentCatalog = { entries: SubagentListEntry[], parentAvailable: boolean }
SubagentListEntry =
  | { kind:'child', id, activity:'running'|'inactive', hasChildren: boolean,
      mode:'one-shot',  label?: string }
  | { kind:'child', id, activity:'running'|'inactive', hasChildren: boolean,
      mode:'continuable', label: string }
  | { kind:'diagnostic', id, reason:'corrupt'|'unsupported'|'unavailable' }

SubagentPromptRequest = { requestId: string,          // 客户端自铸，与 session/prompt 同一 brand
                          parentSessionId: string,
                          childSessionId: string,
                          mode: 'continuable',         // 仅此一值
                          delivery: 'queue' | 'steer',
                          content: PromptContentPart[],
                          clientTimeZone?: string }
```

> **`mode` 参数只接受字面量 `'continuable'`**（`typert.remote-client.d.ts` L588 与 L593 的 `interruptByParent` 参数类型）。`interruptByParent` 不是「按 parent 中断任意子代理」——它中断**一个可续对话子代理**。
> `subagents/prompt.content` 与 `session/prompt.content` 同型，**但 0.1.5 的 subagent 侧只列了 `text` 与 `image` 两态**（Host FaceModel 生成的 schema 无 `file` 分支）；`PromptContentPart` 类型定义本身是含 `file` 的三态联合（`control-types.d.ts` L8 引入 `@deepseek-ai/dsh-attachment/types` 的 `PromptContentPart`）。
> **这两者的差异未被证实**：`未确认`（缺证据：需要对 0.1.5-rc.1 真实 Host 发一个 `type:'file'` 的 subagent prompt，观察是 `gateway/arguments-invalid` 还是 `session/attachment-invalid`）。**最小实验**：对任一 continuable 子代理先 `fileUploads/upload` 拿 `receiptId`，再 `subagents/prompt` 带 `content:[{type:'file',receiptId}]`，记录错误码。

### 6.7 `messageFeedback`（3 个端点）

`source`: `dsh-message-feedback/lib/typert.remote-client.d.ts` L529-532；`lib/types/*.d.ts`。

| 端点 | `request` 字段 | 回复 |
|---|---|---|
| `messageFeedback/list` | `sessionId` | `MessageFeedbackListResult` |
| `messageFeedback/put` | `sessionId`、`messageId`、`rating`、`note?`、`category?`、`ifVersion: string \| null` | `MessageFeedbackPutResult` |
| `messageFeedback/delete` | `sessionId`、`messageId`、`ifVersion: string` | `MessageFeedbackDeleteResult` |

`MessageFeedbackItem` = `{ messageId, rating: 'positive'|'negative', note?, category?, version: string, createdAt: number, updatedAt: number }`
`category` 取值域 = `'task-result'|'instruction-following'|'product-interaction'|'service-stability'|'resource-cost'|'security-privacy-permission'|'other'`

> **这三个端点用「结果对象」而不是抛错**：回复恒为 `{ ok:true, value:... } | { ok:false, error:{ code, ... } }`。
> 错误码：`session-not-found`、`target-not-found`（仅 put）、`version-conflict`（带 `current: MessageFeedbackItem | null`）、`note-blank`、`note-too-large`（带 `maxBytes`、`actualBytes`）。
> `ifVersion: null` = 无条件写入；`list`/`put` 的 `ifVersion` 必须用上一次读到的 `version` 做 CAS。
> `source`: `face-model-all.txt` L3521-3629（`MessageFeedback*Result` 声明）

### 6.8 `agentPresets`（5 个端点）

`source`: `dsh-agent-presets/lib/typert.remote-client.d.ts` L21-27；类型 `face-model-all.txt` L416-472。

| 端点 | 包装 | 参数 | 回复 |
|---|---|---|---|
| `agentPresets/list` | 无参数 | — | `AgentPresetRoster` |
| `agentPresets/read` | 命名 | `agentPreset: string` | `AgentPresetDocument` |
| `agentPresets/select` | 命名 | `agentId`、`agentPreset: string` | `string`（生效的预设 id） |
| `agentPresets/copy` | 命名 | `from: string`、`id: string`、`name?: string` | `void` |
| `agentPresets/deletePreset` | 命名 | `id: string` | `void` |

```
AgentPresetRoster   = { presets: AgentPresetRow[], authorable: boolean }
AgentPresetRow      = { id, trust: PresetTrust, isDefault: boolean, name?, description?, broken? }
AgentPresetDocument = { agentPreset: string, trust: PresetTrust, content: string, name?, description? }
```

### 6.9 `sessionFeedback`（**0.1.5-rc.1 新增**）

`sessionFeedback/record` — 包装 `request`：`args = { request: SessionFeedbackRecordRequest }`。
`source`: `dsh-command-feedback/lib/typert.remote-client.d.ts` L299；类型 `.research/dsh-src/packages/feedback/command-feedback/src/types.ts`。

| 字段 | 类型 | 必填 | 含义 |
|---|---|---|---|
| `sessionId` | `string` | **是** | 被评价的**存活** Session |
| `text` | `string` | 否 | 自由文本；**首尾空白被去除**，全空白视为缺省 |
| `category` | `FeedbackCategory` | 否 | 见下 |

`FeedbackCategory` = `'task-result' | 'instruction-following' | 'product-interaction' | 'service-stability' | 'resource-cost' | 'security-privacy-permission' | 'other'`

**回复**是**结果对象，不抛错**：
- 成功：`{ ok: true, value: { recorded: true } }`
- Session 不在存活注册表中：`{ ok: false, error: { code: 'session-not-found', sessionId } }`

> `text` 与 `category` **可以都缺省**——「提交了空反馈」仍然合法，表示「请求审查该 Session」。落盘为 `feedback/record` 会话事件（log-only，永不进入模型上下文）。

### 6.10 `dynamicCordisRunner`（12 个端点）

属「动态 Cordis 插件宿主」能力，与端侧 v1 范围无交集。签名表在 `.research/protocol/remote-signatures.txt` L357-398（含 `agent:` 作用域变体）。**本表不展开**；若后续需要，按该文件逐行提取即可。

---

## 7. 事件流（本文的核心）

> 这一节回答「流上到底推什么」。所有判别字段都已给出**完整取值域**。

### 7.1 `session/follow` 的逐项结构

`args = { request: SessionFollowRequest }`，`SessionFollowRequest = { address: SessionAddress, maxMessages?: number, assistantStream?: true }`。
`source`: `dsh-api-session-controller/lib/types/types.d.ts` L417-422（请求）、L475-486（帧联合）。

**`type` 是判别字段，三种取值**：

| `type` | 何时出现 | 载荷 |
|---|---|---|
| `'snapshot'` | 每一代**恰好一次**，且是**首项** | `header`、`cursor`、`records`、`hasMore`、`projections`、`assistantStream?` |
| `'event'` | 快照之后的**持久事件**，`seq` 严格连续递增 | `event: SessionWireEvent` |
| `'assistant-stream'` | **仅当请求带 `assistantStream: true`**；进程本地，重连**不重放** | `frame: SessionAssistantStreamFrame` |

**`SessionWireEvent` 信封**（`source`: `types.d.ts` L397-407）：

```
{ type: string,          // 事件类型，取值域见下表
  seq: number,           // 持久日志序号，0-based 密集递增
  time: number,          // epoch 毫秒
  data: JsonValue,       // 事件专属载荷，见下表
  ignorable?: true,      // 该事件可被不认识的读者安全忽略
  sourceEventSeqs?: JsonValue,   // 仅在 surface 事件上：引用的更早序号
  surfaceOp?: JsonValue }        // 仅在 surface 事件上：'append' 或 {op:'replace',startSeq,endSeq}
```

**连续性纪律（客户端必须实现）**：Host 在 `follow` 里显式校验 `event.seq === expected`，不连续直接抛 `gateway/internal` + `session event stream skipped seq N`（`source`: `dsh-api-session-controller/src/history.ts` → `follow()` 的 `if (item.event.seq !== expectedSeq) throw ...`）。因此客户端也应把 `seq` 不连续视为**当前 generation 失效**并重开流。

#### 7.1.1 `SessionEventMap` 全量事件类型（40 种）

`source`: `dsh-api-session-controller/lib/typert.host.js` → `model.services[sessionController].types` 的 `SessionEventMap` 声明（该表已合并各包通过 `declare module '@deepseek-ai/dsh-session/types'` 的扩展；权威源在 `.research/dsh-src/packages/core/session/src/types.ts` L269-495 及各扩展包）。

| # | `event.type` | `event.data` 字段 | 类别 |
|---|---|---|---|
| 1 | `turn/start` | `{ turn: number }` | **轮次边界** |
| 2 | `turn/end` | `{ turn: number, reason: TurnEndReason }` | **轮次边界** |
| 3 | `step/start` | `{ turn: number, step: number }` | 步边界 |
| 4 | `step/end` | `{ turn: number, step: number }` | 步边界 |
| 5 | `user/message` | `UserMessage`（`{ id, role:'user', content: ContentBlock[], source }`） | **消息** |
| 6 | `system/message` | `{ turn, step, message: SystemMessage }` | 消息（系统提示渲染） |
| 7 | `assistant/message` | `{ turn, step, message: AssistantMessage, stream: AssistantStreamRecord[], usage?: TokenUsage, interrupted?: true }` | **消息 + 用量** |
| 8 | `assistant/attempt` | `{ turn, step, stream: AssistantStreamRecord[] }` | 未提交的模型尝试 |
| 9 | `tool/call` | `{ turn, step, callId, name: string, arguments: string }`（`arguments` 是**模型产出的原始 JSON 字符串，未解析**） | **工具调用**（开始） |
| 10 | `tool/result` | `{ turn, step, message: ToolResultMessage, error?: {name, code}, meta?: JsonValue }` | **工具调用**（结束） |
| 11 | `request/header` | `{ header: EpochHeader, reason: 'initial'\|'resume'\|'change'\|'series', startsSeries?: true }` | 请求快照 |
| 12 | `request/context` | `RequestContext` = `{ provider, model, contextWindow?, systemPromptUpdate? }` | 请求快照 |
| 13 | `session/end-seed` | `{ inherited?: true }` | 生命周期（fork 种子切点） |
| 14 | `agent/inbox/spliced` | `{ target, start, removedCount?, inserted: UserMessage[], outcome?: 'canceled' }` | 队列变更 |
| 15 | `approval/asked` | `{ id, toolName, callId?, reason? }` | **审批**（落盘审计对） |
| 16 | `approval/decided` | `{ id, outcome: ApprovalOutcome }` | **审批**（落盘审计对） |
| 17 | `approval/policy` | `{ policy: 'ask'\|'never', source?: 'delegation' }` | 审批策略 |
| 18 | `tool/ptc-dispatch-start` | `{ rootCallId, parentCallId, subCallId, name, arguments }` | PTC 子调用 |
| 19 | `tool/ptc-dispatch` | `{ isError: boolean, content: ContentBlock[] }` | PTC 子调用 |
| 20 | `agent-preset/selected` | `{ agentPreset: string }` | 组合变更 |
| 21 | `command/run` | `{ commandId, name, args?, source }` | **斜杠命令**（开始） |
| 22 | `command/done` | `{ commandId, kind: 'success'\|'error', text?, sourceEventSeq? }` | **斜杠命令**（结束） |
| 23 | `session/title` | `{ title, messageSeqs: number[], source: {kind:'fallback'}\|{kind:'provider',provider,model?}\|{kind:'user'} }` | 标题 |
| 24 | `todo/write` | `{ todos: TodoItem[] }`，`TodoItem = { content, status: 'pending'\|'in_progress'\|'completed' }` | **待办** |
| 25 | `model/selection` | `ModelSelection` = `{ provider, model, reasoningEffort? }` | 模型选择 |
| 26 | `subagent/descriptor` | `SubagentDescriptorData` | **子代理** |
| 27 | `sandbox/mode` | `{ mode: 'read-only'\|'workspace-write'\|'danger-full-access', source?: 'delegation' }` | 沙箱 |
| 28 | `subagent/catalog` | `{ version: 0, childId, childCreatedAt } & ({mode:'one-shot',label?}\|{mode:'continuable',label})` | **子代理** |
| 29 | `feedback/record` | `{ text?, category? }` | **反馈** |
| 30 | `team/member` | `{ version: 2, teamId, member: TeamMemberSnapshot }` | 团队 |
| 31 | `team/task` | `{ version: 2, teamId, task: TeamTaskSnapshot }` | 团队 |
| 32 | `team/message/queued` | `{ version: 2, teamId, message: TeamMessageSnapshot }` | 团队 |
| 33 | `team/message/delivered` | `{ version: 2, teamId, messageId, targetId }` | 团队 |
| 34 | `goal/change` | `GoalChangeMeta` | **目标** |
| 35 | `feedback/message-put` | `MessageFeedbackPut` | **反馈**（逐消息） |
| 36 | `feedback/message-delete` | `MessageFeedbackDelete` | **反馈**（逐消息） |
| 37 | `compaction/start` | `{ compactionId, sourceCommandId?, turn: number\|null }` | 上下文压缩 |
| 38 | `compaction/summary` | `{ compactionId, sourceCommandId?, summary: ContentBlock[], shadowedRange:{start,end}, shadowedSeqs: number[], shadowedTokenCount, provider, model, maxTokens?, usage? } & ({rawOutput,llmStreamCall:true}\|{rawOutput?,llmStreamCall?:never})` | 上下文压缩 |
| 39 | `compaction/end` | `{ compactionId, sourceCommandId?, turn: number\|null, error? }` | 上下文压缩 |
| 40 | `compaction/prune` | `{ shadowedRange, shadowedSeqs, shadowedTokenCount }` | 上下文压缩 |

**`TurnEndReason` 取值域**（`source`: `types.d.ts` → `TurnEndReasonMap`）：

| `reason.kind` | 额外字段 | 含义 |
|---|---|---|
| `'completed'` | — | 正常完成 |
| `'aborted'` | `reason: TurnEndCancelCause` = `{kind:'user'}` \| `{kind:'parent'}` \| `{kind:'hook',reason:string}` \| `{kind:'disposed'}` \| `{kind:'legacy'}` | **被取消**（客户端 `session/cancel` 产生 `{kind:'user'}`） |
| `'blocked'` | — | 被阻塞 |
| `'error'` | `error: LlmFailure` = `{message, code, status?, providerRetryAfterMs?, requestId?}` | **轮次错误事件** |
| `'max-tokens'` | — | 触及 token 上限 |
| `'interrupted'` | — | 中断 |

> **逐项回答「事件流能不能覆盖这些 UI 需求」**：
> - **消息** → `user/message`、`system/message`、`assistant/message`（第 7 条带 `usage` 与 `interrupted`）。
> - **思考（thinking/reasoning）** → **不是独立事件类型**。推理文本在 `assistant/message.data.stream` 的 `AssistantStreamRecord` 里，其判别字段 `type` 取值 `'text-chunks' | 'reasoning-chunks' | 'tool-call-chunks' | 'chunk'`。要**流式**渲染思考，必须请求 `assistantStream: true` 并消费 §7.1.2 的 `chunk` 帧中的 `StreamChunk.type === 'reasoning-delta'`。
> - **工具调用与生命周期** → 落盘对是 `tool/call`（开始）↔ `tool/result`（结束），用 `callId` 配对；流式增量走 `assistantStream` 的 `tool-call-delta`。`tool/result.data.error` 给出内部失败身份。
> - **子代理事件** → `session/follow` 上的 `subagent/catalog`、`subagent/descriptor`；**另**有 Host 事件 `subagent/start` / `subagent/end`，但它们**不在** `$events` 转发白名单里（§8.2），所以端侧只能通过父会话的 `subagent/catalog` 事件或 `subagents/list` 端点感知。
> - **目标事件** → `goal/change`（会话日志）；`$events` 上另有 `goal/activation-changed`（§8.2 第 10 项）。
> - **任务/作业事件** → **不在 `session/follow` 上**。作业在 `session/control` 的 `jobs` 帧（§7.2）。
> - **错误事件** → 两个层次：轮次内错误是 `turn/end` 的 `reason.kind==='error'`；轮次外错误走 `$events` 的 `api-session/error`（§8.2 第 5 项）。
> - **轮次边界** → `turn/start` / `turn/end`（另有 `step/start` / `step/end`）。
> - **token/用量** → `assistant/message.data.usage`（`TokenUsage = {inputTokens, outputTokens, totalTokens?, cacheReadTokens?, cacheWriteTokens?, reasoningTokens?}`）。**没有独立的 usage 事件**——`SessionEventMap` 的注释明确写「there is no separate usage record」。
> - **文件/交付物事件** → `session/follow` 上**没有**通用的「交付物」事件类型。文件相关的事实来自：`tool/call`/`tool/result`（写文件的工具）、`commands/execute` 的返回、以及独立的 `workspaceFiles/changes` 流（§7.4）。**「交付物」是 Web 客户端从原始事件派生出来的呈现，不是协议事件**（`source`: `.research/dsh-src/packages/` 中 `dsh-client-ui-deliverables` 的存在 + 无对应 `SessionEventMap` 成员）。

#### 7.1.2 `assistantStream` 三态帧

`source`: `types.d.ts` L441-468、L424-439。

| `frame.type` | 字段 |
|---|---|
| `'start'` | `{ attemptId, revision, startedAfterSeq: number \| -1, turn, step }` |
| `'chunk'` | `{ attemptId, revision, index, time, chunk: JsonValue }`——`chunk` 是 `StreamChunk` |
| `'end'` | `{ attemptId, revision, index, outcome: {kind:'committed', eventType:'assistant/message'\|'assistant/attempt', seq} \| {kind:'abandoned'} }` |

`StreamChunk` 取值域（`source`: `types.d.ts` / `face-model-all.txt` → `StreamChunk`）：

| `chunk.type` | 字段 |
|---|---|
| `'block-start'` | `{ index, blockType: ContentBlockType }` |
| `'text-delta'` | `{ index, text }` |
| `'reasoning-delta'` | `{ index, text }` ← **思考流** |
| `'tool-call-delta'` | `{ index, id, name?, argumentsDelta }` |
| `'block-end'` | `{ index, block: ContentBlock }` |
| `'usage'` | `{ usage: TokenUsage }` |
| `'finish'` | `{ reason: FinishReason, replayState? }` |

快照里的 `SessionAssistantStreamBaseline = { revision, activeAttempt?: { attemptId, startedAfterSeq, turn, step, nextIndex, stream: JsonValue[] } }`；`nextIndex` 是「下一个 live chunk 帧应有的密集序号」，客户端要据此校验。

### 7.2 `session/control` 的逐项结构

`args = {}`（无参数）。`source`: `dsh-api-session-controller/lib/types/types.d.ts` L509-536。

**`type` 是判别字段，四种取值**：

| `type` | 何时 | 载荷 |
|---|---|---|
| `'baseline'` | 每代恰好一次、首项 | `value: { queues: Record<SessionId, SessionQueuedItem[]>, jobs: Record<SessionId, SessionJob[]>, projections: Record<SessionId, SessionProjectionBaseline> }` |
| `'queue'` | 某会话队列变化 | `{ sessionId, items: SessionQueuedItem[] }`（**整体替换**） |
| `'jobs'` | 某会话作业变化 | `{ sessionId, jobs: SessionJob[] }`（**整体替换**） |
| `'projection'` | 某会话某投影键变化 | `{ sessionId, key: string, value: JsonValue, seq: number }` |

```
SessionQueuedItem = { id, placement: 'queued'|'steering'|'context', rpcId?, message: { id, content: JsonValue[] } }
SessionJob        = { id, kind: string, label: string,
                      status: 'running'|'stopping'|'completed'|'killed'|'failed',
                      detail?, startedAt: number, finishedAt?: number }
```

> **明确回答「`session/control` 里是不是审批/提问」：不是。** `SessionControlFrame` 的四个 `type` 只有 baseline/queue/jobs/projection。审批与提问走 **`$events` 的 waterfall 帧**（§8.2/§9）。这一条推翻了「审批走 control 流」的直觉，是本附录最重要的结论之一。
> `source`: `dsh-api-session-controller/lib/types/control.d.ts` → `control(signal): AsyncIterable<SessionControlFrame>`（无参数、无审批相关成员）
> 佐证：仓库自己的 `dshcompat/src/main/ets/Endpoints.ets` 把 `session/control` 标了 `capability: 'approvals'`；**该标注是错的**（见 §11 漂移发现）。

### 7.3 `projections` 的键与值

`SessionProjectionMap`（`source`: `.research/protocol/model-decls.txt` → `SessionProjectionMap`；权威源 `.research/dsh-src/packages/api/session-controller/src/types.ts`）：

| 键 | 值类型 |
|---|---|
| `inbox` | `{ 'next-turn': JsonValue[], 'next-step': JsonValue[] }` |
| `agentPreset` | `string \| null` |
| `title` | `string \| null` |
| `todos` | `TodoItem[] \| null` |
| `sessionListMetadata` | `{ blank: boolean, lastPromptAt: number \| null }` |
| `imageLimits` | `ImageAttachmentLimits` = `{ maxImageBytes, maxImagesPerMessage, maxMessageImageBytes, maxImagePixels, maxImageDimension, mediaTypes: ImageMediaType[] }` |
| `modelSelection` | `{ lastUsed: ModelSelection \| null, next: ModelSelection \| null }` |
| `subagentCatalog` | `SubagentCatalogEntry[]` |
| `subagentTiming` | `{ settledMs: number, active?: { since, through } }` |
| `subagent` | `SubagentIdentityProjection \| null` |
| `goal` | `GoalProjection \| null` |

`SessionProjectionValues = Partial<SessionProjectionMap> & Readonly<Record<string, JsonValue>>`——**稀疏 + 可扩展**。客户端必须对自己不认识的键做前向兼容忽略，不能因为出现未知键而判流畸形。

### 7.4 `workspaceFiles/changes` 的逐项结构

`args = { workspaceFileScopeId }`。`source`: `dsh-api-workspace-files/lib/types/types.d.ts` L105-131；`.research/dsh-src/packages/api/workspace-files/src/changes.ts`。

**`kind` 是判别字段，两种取值**：

| `kind` | 载荷 | 语义 |
|---|---|---|
| `'ready'` | — | Host 已开始观察 **instrumented 文件系统操作**并已解析工作区根。是每代**首项**，恰好一次 |
| `'change'` | `change` 字段，二者之一：`{ absolutePath: string, version: string }` 或 `{ absolutePath: string, absent: true }` | 一次观察 |

> **关键语义（直接决定客户端能不能用）**：
> 1. **帧是「观察」，不是「增删改差量」**。`source` 注释原文：*"Frames report observations, not deltas: a consumer already holding `version` learns nothing new from the frame and can ignore it."* 即客户端若已持有同一 `version`，该帧**可以安全忽略**。
> 2. **不是 OS 级文件监听**。只有经 `ctx.fs` 的 instrumented 操作会发 `fs/observed`；用别的程序改文件**不会**产生帧（`changes.ts` 文件头：*"Instrumented filesystem operations emit these observations; the operating system is not watched."*）。
> 3. **只有 `change` 帧带路径**；`ready` 无载荷。
> 4. `version` 是不透明令牌（§2.2），**只能比较相等，不能解析**。
> 5. 帧**按发射顺序**到达；工作区外目标被过滤掉（`ctx.fs.contains(root, target)`）。
> 6. 生成器在**根解析完成前**就挂好监听，所以解析期间产生的观察不会丢，会在 `ready` 之后按序补发。

### 7.5 `workspace/follow` 的逐项结构

`args = {}`（无参数）。`source`: `dsh-api-workspace-controller/lib/types/types.d.ts` L127-131。

**`type` 是判别字段，五种取值**：

| `type` | 载荷 |
|---|---|
| `'baseline'` | `value: { items: WorkspaceView[], archivedSessionIds: SessionId[] }`——每代**首项**，恰好一次 |
| `'upsert'` | `workspace: WorkspaceView`（整行替换） |
| `'remove'` | `workspaceId` |
| `'order'` | `workspaceIds: WorkspaceId[]`（**完整新顺序**） |
| `'archived'` | `archivedSessionIds: SessionId[]`（**完整新归档集**） |

---

## 8. 网关 `$events` 逻辑流（**19 个转发事件的完整目录**）

### 8.1 载体与帧

| 项 | 事实 | 来源 |
|---|---|---|
| 逻辑端点 | `$events`，`payload = { args: {} }`（`args` **必须**是空对象，多一个键即 `gateway/arguments-invalid`） | `dsh-api-gateway/lib/types/stream-protocol.js` → `REMOTE_EVENT_STREAM_ENDPOINT` / `REMOTE_EVENT_STREAM_PAYLOAD`；`lib/index.js` → `openRemoteEvents()` 的键数校验 |
| 结果回送 | `$events/result`（一元），载荷 `{ clientId, eventId, outcome }` | 同上 → `REMOTE_EVENT_RESULT_ENDPOINT` / `parseRemoteEventResult` |
| 传输 | `/api/remote.mux`（WebSocket），与其他逻辑流共用物理连接 | 同 `REMOTE_STREAM_MUX_PATH`；D2 §1.2 |
| 首项 | `{ type:'ready', clientId, host:{ home } }`；`host` **必须恰好一个键** `home`（= `os.homedir()`） | `lib/client.js` → `parseRemoteEventReady`；`lib/index.js` → `registerRemoteEvents(source, { home: homedir() })` |
| `clientId` 稳定性 | **每次 generation 重新 `randomUUID()`**，重连后必然变化。不是稳定客户端身份 | `lib/index.js` → `openRemoteEvents()` 的 `let clientId = randomUUID()` |
| 重放 | 普通通知**不重放**；waterfall 用同一 `eventId` 重发（`pendingRemoteEvents` 对新 client 重新投递） | `lib/index.js` → `deliverRemoteEvent`；D2 §1.3 |

**四种帧**（`source`: `dsh-api-gateway/lib/client.js` → `parseRemoteEventFrame`，每种的**精确键集**）：

| `type` | 精确键 | 载荷 |
|---|---|---|
| `'ready'` | `type, clientId, host` | `host: { home }` |
| `'emit'` | `type, event, args` | `event: string`；`args: JsonValue[]`（位置参数） |
| `'waterfall'` | `type, event, eventId, agentId, request` | `eventId: string`；`agentId: SessionId`；`request: object`（**保证不含 `agent`/`signal`**） |
| `'cancel'` | `type, eventId` | 撤回此前投递的 waterfall |

> 帧的键集是**精确匹配**（`hasExactRemoteEventKeys`）：多一个键或少一个键都判为畸形帧，客户端会 `close(4002, "invalid Remote stream frame")` 并让整个 generation 失效。**解析必须同样严格。**

### 8.2 转发事件白名单（唯一权威来源：`API_REMOTE_FORWARDED_EVENTS`）

`source`: `.research/upstream-0.1.5/node_modules/@deepseek-ai/dsh-api-remotes/lib/index.js` L17-94（同一数组也在 `lib/types/remote-events.js`）。
该数组**同时**是 Host 转发循环与客户端 `ctx.remote.$on` 的合法键集。

| # | `event` | 模式 | 位置参数 | **是否足以更新会话列表** |
|---|---|---|---|---|
| 1 | `agent-preset/selected` | emit | `(sessionId: SessionId, agentPreset: string)` | 否（只更新该会话的预设标记） |
| 2 | `approval/request` | **waterfall** | `request: ApprovalRequestEvent`（作用域 Agent） | 否 |
| 3 | `api-session/activity` | emit | `(sessionId, updatedAt: number)` | **部分**：能改 `updatedAt` 与重排，但**不带标题/cwd/running** |
| 4 | `api-session/added` | emit | `(summary: SessionSummary)` | **是**（完整行，含 `projections`） |
| 5 | `api-session/error` | emit | `(sessionId, message: string)` | 否（错误提示） |
| 6 | `api-session/removed` | emit | `(sessionId)` | **是**（删除行） |
| 7 | `api-session/status` | emit | `(sessionId, running: boolean)` | **部分**：只能改 `running` |
| 8 | `commands/change` | emit | `()` | 否（提示重读 `commands/list`） |
| 9 | `credentials/reference-updated` | emit | `(ref: CredentialRef)` | 否 |
| 10 | `goal/activation-changed` | emit | `(payload: GoalActivationChanged)` | 否 |
| 11 | `cordis/request-run` | emit | `(request: DynamicCordisRunRequest)` | 否 |
| 12 | `cordis/request-run-resolved` | emit | `(resolved: DynamicCordisRequestResolved)` | 否 |
| 13 | `cordis/dynamic-package` | emit | `(pkg: DynamicCordisPackage)` | 否 |
| 14 | `cordis/dynamic-retract` | emit | `(retracted: DynamicCordisRetracted)` | 否 |
| 15 | `cordis/inspect-query` | emit | `(request: CordisInspectQueryRequest)` | 否 |
| 16 | `cordis/inspect-query-resolved` | emit | `(resolved: CordisInspectQueryResolved)` | 否 |
| 17 | `llm/adapters-updated` | emit | `()` | 否（无载荷，提示重读 `llm/*`） |
| 18 | `settings/document-updated` | emit | 见注 | 否 |
| 19 | `user-questions/request` | **waterfall** | `request: AskUserQuestionRequestEvent`（作用域 Agent） | 否 |

> **注（第 18 项）**：`settings/document-updated` 与第 9 项 `credentials/reference-updated` 的**宿主事件签名不在生成的 FaceModel 里**（它们由 `dsh-settings` / `dsh-credentials` 用 `declare module` 扩展，`@mode emit` 注释存在，但未进入 `TYPERT.model.events`）。`source`: `grep 'settings/document-updated'` 命中 `dsh-settings/lib/types/index.js` 与 `dsh-credentials/lib/types/index.js`，但 `dsh-settings` / `dsh-credentials` **没有** `lib/typert.host.js`（不在生成面内）。
> 位置参数的**准确个数与类型为 `未确认`**。缺的证据：`dsh-settings` / `dsh-credentials` 未提供 `.d.ts` 里的事件签名（FaceModel 未覆盖这两个包）。**最小实验**：订阅 `$events`，改一次设置，记录该 emit 帧的 `args` 数组长度与元素形状。
> **可以确定的是**：这两项都**无业务载荷可用性**——即使拿到参数，也只是「某处变了，去重读」。

### 8.3 会话列表能否只靠 `$events` 维持？

**结论：不能，但可以只靠 `$events` + 一次 `session/list` 维系。**

- 初始 baseline 必须来自 **`session/list`**（`{ _request: {} }` ⇒ `{ items: SessionSummary[] }`）。`$events` 的第一个业务项之外的 `ready` **不带任何会话数据**（只有 `clientId` 与 `host.home`）。
- 之后：`api-session/added` 提供**完整**新行；`api-session/removed` 删除；`api-session/status` 只改 `running`；`api-session/activity` 只改 `updatedAt`。
- **缺口**：没有「标题变更」的转发事件。标题变化只能通过 `session/follow` 的 `session/title` 事件或 `projections`（`title` 键）感知——即**必须按会话订阅**，不能只订阅全局事件流。
- 因此端侧的正确形态是：`session/list`（列表 baseline）+ `$events`（增删/运行态/活跃度增量）+ 按需 `session/follow`（单会话内容与标题）。

### 8.4 `$events` 事件权威声明中**未转发**的事件（避免误等）

`TYPERT.model.events` 共 22 条 `@mode` 声明，其中以下 **5 条不在转发白名单**，端侧**永远收不到**：

| 事件 | 声明包 | 为什么收不到 |
|---|---|---|
| `feedback/committed` | `dsh-message-feedback` | 未列入 `API_REMOTE_FORWARDED_EVENTS`（`@mode parallel`，非 emit/waterfall） |
| `goal/changed` | `dsh-goal` | 未列入白名单（对应会话日志事件 `goal/change` 才是端侧可用的） |
| `llm/stream` | `dsh-llm` | `@mode waterfall`，是模型调用内部的瀑布，不转发 |
| `subagent/start` | `dsh-subagent` | 未列入白名单 |
| `subagent/end` | `dsh-subagent` | 未列入白名单 |

同理，`subagent/provider-added`、`subagent/provider-removed`、`authorization/settled`、`credentials/record-updated`、`domain/changed` 等宿主事件也**不在**白名单。
`source`: `.research/protocol/face-model-all.txt` L1-67（22 条声明）与 `dsh-api-remotes/lib/index.js` L17-94（19 条白名单）求差集。

---

## 9. 审批与用户提问的应答通路（**关键结论：不走 RPC 端点**）

### 9.1 总览

审批与提问都是 **`$events` 上的 waterfall 帧**，不是一元端点。客户端「应答」= 把结果 POST 回 `$events/result`。

```
Host: ApprovalService.request() / UserQuestions.ask()
   ↓ ctx.waterfall('approval/request' | 'user-questions/request', req, next)   [Agent 作用域]
dsh-api-remotes: 白名单命中 waterfall → 包装成 dispatch 推入 RemoteEventQueue
   ↓
dsh-api-gateway: startRemoteEvent() → 分配 eventId → 帧 { type:'waterfall', event, eventId, agentId, request }
   ↓ 广播给每个 $events 客户端
客户端: ctx.remote.$on('approval/request', (request, next) => ...)
   ↓ 用户作答
客户端: POST /api/$events/result  { args: { clientId, eventId, outcome } }
   ↓
dsh-api-gateway: receiveRemoteEventResult() → 解决该 pending waterfall
```

`source`: `.research/dsh-src/packages/interaction/user-questions/src/index.ts` L136-139；`dsh-user-approval/lib/types/index.js` → `decide()` 的 `this.ctx.waterfall(scopeTarget(req.agent, req.agent), 'approval/request', req, ...)`；`dsh-api-remotes/lib/index.js` → `forwardWaterfall()`；`dsh-api-gateway/lib/index.js` → `startRemoteEvent()`/`receiveRemoteEventResult()`。

### 9.2 应答审批

**接收**（`type='waterfall'`，`event='approval/request'` 时 `request` 的字段）：

`source`: `dsh-user-approval/lib/types/index.js`（`ApprovalRequestEvent` 声明）；`dsh-client-ui-approval/lib/client.js` L138-161（客户端投影）。

| 字段 | 类型 | 必填 | 含义 |
|---|---|---|---|
| `toolName` | `string` | **是** | 请求决策的工具名 |
| `callId` | `string` (`ToolCallId`) | 否 | 关联的工具调用，可用于拉取工具自有详情 |
| `reason` | `string` | 否 | 提问方给出的可读理由 |

> `ApprovalRequestEvent` 的**类型定义**含 `agent: Agent` 与 `signal?: AbortSignal` 两个字段，但 `projectRemoteEventRequest` 在出网前**显式剥离**这两个键（并把 `signal` 转成客户端侧的 delivery 生命周期）。所以**线上 `request` 里只有 `toolName`/`callId?`/`reason?`**。
> `source`: `dsh-api-gateway/lib/types/stream-protocol.js` → `projectRemoteEventRequest`（`if (key === 'agent' || key === 'signal') continue`）；`dsh-api-gateway/lib/client.js` → `parseRemoteEventFrame` 的 `!Object.hasOwn(value.request, "agent") && !Object.hasOwn(value.request, "signal")`。

**应答**：`POST /api/$events/result`，`args = { clientId, eventId, outcome }`：

| 字段 | 类型 | 来源 |
|---|---|---|
| `clientId` | `string` | 本代 `ready` 帧的 `clientId` |
| `eventId` | `string` | waterfall 帧的 `eventId` |
| `outcome` | 见下 | — |

**审批 `outcome` 取值域**（`source`: `dsh-user-approval/lib/types/index.js` → `OUTCOMES` 常量与 `ApprovalOutcome` 声明）：

| 值 | 谁产生 | 语义 |
|---|---|---|
| `'allowed-once'` | **客户端** | **唯一的授权结果**——只授权本次动作 |
| `'rejected'` | **客户端**（也由 `'never'` 策略直接产生） | 拒绝 |
| `'cancelled'` | Host（请求信号被中止） | 已取消（客户端**不必**发） |
| `'unavailable'` | Host（无 answerer / answerer 抛错 / 返回值不在词表内） | **fail-closed** 兜底（客户端**不必**发） |

> **只有两个字面量是客户端该发的**：`'allowed-once'` 与 `'rejected'`。Web 客户端的审批面板正好只有两个按钮（`answer("rejected")` / `answer("allowed-once")`），`source`: `dsh-client-ui-approval/lib/client.js` L86/L93。
> **没有「总是允许」客户端结果**。要「不再问」必须改策略：`approval/policy` 会话事件（`'ask' | 'never'`），由宿主侧策略变更产生，**不是**审批应答的一个 outcome。
> **fail-closed 语义**：answerer 缺失或抛错 ⇒ `'unavailable'`；返回词表外的值也归一成 `'unavailable'`。所以客户端**绝不能**靠「不发应答」来表达允许——不发就是拒绝。
> **策略 `'never'` 在服务自己内部判定**，在派发 waterfall 之前就返回 `'rejected'`：即使某个监听器用 `prepend: true` 抢在前面也无法绕过。`source`: `dsh-user-approval/lib/types/index.js` → `decide()` 的 `if (this.effectivePolicy(session) === 'never') return 'rejected'`。

**审计对**：每次审批都会在**发起它的会话日志**里落一对事件 `approval/asked`（`{id, toolName, callId?, reason?}`）→ `approval/decided`（`{id, outcome}`）。这对事件必须在**一个打开的轮次内**（`approval.request()` 在轮次外调用会直接抛错）。所以客户端也能从 `session/follow` 读到审批历史。
`source`: `dsh-user-approval/lib/types/index.js` → `request()` 与 `hasOpenTurn()`。

### 9.3 应答用户提问

**接收**（`type='waterfall'`，`event='user-questions/request'` 时 `request` 的字段）：

`source`: `.research/dsh-src/packages/interaction/user-questions/src/types.ts` L6-74。

| 字段 | 类型 | 必填 | 含义 |
|---|---|---|---|
| `questions` | `AskUserQuestionItem[]` | **是** | 问题批次 |
| `agent` | `Agent` | 否 | 出网前被剥离 |
| `signal` | `AbortSignal` | 否 | 出网前被剥离并转为客户端 delivery 生命周期 |

`AskUserQuestionItem`：

| 字段 | 类型 | 必填 | 含义 |
|---|---|---|---|
| `id` | `string` | **是** | 稳定问题 id，**应答里要原样回传** |
| `question` | `string` | **是** | 展示的问题文本 |
| `detail` | `string` | 否 | 附加说明（markdown），渲染在问题下方但**不进入选项标签** |
| `header` | `string` | 否 | 短标题/分组标签 |
| `options` | `{ label: string, description?: string }[]` | 否 | 可选项；**没有 id，应答用 `label` 字符串** |
| `multiSelect` | `boolean` | 否 | 是否多选（默认单选） |
| `intent` | `{ kind: 'plan-review', approve: string }` | 否 | **纯呈现意图**，不改变应答编码。`approve` 指定哪个**选项 label** 表示批准；其他选项表示拒绝 |

**应答**：同样的 `POST /api/$events/result`，`outcome = { kind: 'result', value: <AskUserQuestionAnswer> }`，其中：

`source`: 同 `types.ts` L50-64。

```
AskUserQuestionAnswer = { answers: AskUserQuestionAnswerItem[] }
AskUserQuestionAnswerItem = {
  id: string,          // 对应提问的 question.id
  selected: string[],  // 选中的【选项 label 字符串】；跳过的题传 []
  custom?: string      // 自由文本「其他」答案
}
```

**答案词表与约束**：
- `selected` 的元素是**选项的 `label` 原文**（不是索引、不是 id）。`source`: `.research/dsh-src/packages/interaction/user-questions/src/types.ts` L51-58；Web 客户端 `decide(label)` 传的就是 label（`dsh-client-ui-user-questions/lib/client.js`）。
- 多选题允许 `selected` 与 `custom` **同时**存在（类型注释：*"May accompany custom text for a multi-select question"*）。单选时客户端会二选一。
- **跳过**某题 = 该题 `{ id, selected: [] }`（`source`: client.js 的 `value.skipped` 分支）。
- **取消整组**在客户端是**拒绝**该 waterfall（`ASK_CANCELLED`），**不是**发一个空答案。`source`: `dsh-client-ui-user-questions/lib/client.js` → `PendingQuestion.cancel()` 的 `this.#reject(questionError(..., "ASK_CANCELLED"))`。
- 请求生命周期结束时客户端 `abort` ⇒ `ASK_ABORTED`，同样不是答案。
- 区分「空页」与「一页空行」的语义由 `lines` 字段承担（那是 `workspaceFiles/read` 的事，此处仅提示不要用 `text === ''` 推断）。

---

## 10. 取消运行中的轮次 / 发新 prompt（含图片与附件）

### 10.1 取消 / 中断正在运行的轮次

| 场景 | 做法 | 请求 | 回复 | 结果 |
|---|---|---|---|---|
| 取消**普通会话**当前轮次 | `session/cancel` | `args = { request: { sessionId } }` | `{ accepted: true }` | 保留收件箱（`keepInbox: true`）；落盘 `turn/end` 且 `reason = {kind:'aborted', reason:{kind:'user'}}` |
| 取消**可续子代理**当前运行 | `subagents/interruptByParent` | `args = { childSessionId, parentSessionId, mode: 'continuable' }` | `{ accepted: true }` | **不是** `session/cancel` 的替代：`session/cancel` 对 subagent 拥有权的会话会报 `apiSessionSubagentOwnershipError` |
| 取消**正在进行的 RPC 调用本身**（尚未进入轮次） | mux `cancel` 帧（流式）或 `AbortSignal`（一元） | `{ type:'cancel', streamId }` | 无 | 见 D2 §1.2 |

**要点**：
- `session/cancel` 只对 **live（已附着）** Agent 生效；Session 未附着 ⇒ `session/not-found`，消息是 `session "<id>" not found (not attached)`。取消**不会**把冷会话唤醒。
- `session/cancel` **不清空收件箱**。要清队列得逐项 `session/updateQueue` 的 `remove`。
- `session/cancel` 的 `request` **只有 `sessionId` 一个字段**（`SessionCancelRequest`），没有 `reason`、没有 `turn`。
- 取消是**异步生效**：`{ accepted: true }` 只表示「已把取消请求送达 live Agent」，**不表示轮次已结束**。真正的结束信号是 `session/follow` 上的 `turn/end`（`reason.kind==='aborted'`），或 `session/control` 上该会话的 `jobs` 帧。
- `source`: `.research/dsh-src/packages/api/session-controller/src/commands.ts` → `cancel(request)`；`subagent/lib/types/control-types.d.ts` L108-111；`typert.remote-client.d.ts` L588。

### 10.2 发新 prompt（含图片 / 附件）

**最小可用调用**：

```jsonc
// POST /api/session/prompt
{ "args": { "request": {
    "requestId": "<客户端自铸的 uuid>",
    "sessionId": "<sessionId>",
    "mode": "queue",                       // 或 "steer"
    "content": [ { "type": "text", "text": "你好" } ],
    "clientTimeZone": "Asia/Shanghai"      // 可选
} } }
// → { ok:true, value: { accepted: true } }
```

**带内联图片**：

```jsonc
"content": [
  { "type": "text",  "text": "这张图里有什么？" },
  { "type": "image", "mediaType": "image/png", "data": "<base64>", "name": "shot.png" }
]
```

**带已上传文件（0.1.5 新增路径）**：

```jsonc
"content": [
  { "type": "text", "text": "看看这个文件" },
  { "type": "file", "receiptId": "<fileUploads/upload 或 /api/session/uploadFileBinary 返回的 receiptId>" }
]
```

**要点与陷阱**：
1. **`requestId` 必须由客户端生成且每次唯一**。Host 用它做幂等：同一 `requestId` 重复提交返回 `{accepted:true}` 而不重复入队（`source`: `commands.ts` → `hasPromptRequest()`）。它同时被持久化进 `user/message.data.source.rpcId`，是乐观消息与落盘消息对账的唯一键。
2. **内容不能为空**：必须至少一个非空白 `text` 片段**或**一个附件，否则 `gateway/bad-request`（`prompt content must include non-whitespace text or an attachment`）。
3. **`mode: 'steer'` vs `'queue'`**：`'queue'` 排到当前轮之后的下一轮；`'steer'` 插进**当前轮最近的一步**（`agent.steer(message)` vs `agent.followup(message)`）。
4. **图片有服务端上限**：具体数值由 Host 的 `attachments.imageLimits` 决定，客户端应**从 `session/control` 的 `projections.imageLimits` 或 `session/list` 的 `projections.values.imageLimits` 读取**，不要硬编码。`ImageAttachmentLimits = { maxImageBytes, maxImagesPerMessage, maxMessageImageBytes, maxImagePixels, maxImageDimension, mediaTypes }`（`source`: `.research/protocol/model-decls.txt` → `ImageAttachmentLimits`；`dsh-api-session-controller/src/list.ts` → `imageLimitsSchema` 注册为投影 `imageLimits`）。
5. **模型必须支持图片**：否则 `session/attachment-invalid` + `details.reason = 'MODEL_DOES_NOT_SUPPORT_IMAGES'`。客户端应先用 `session/modelCatalog` 判断，或直接处理该错误码。
6. **图片上传是串行化的**：Host 用 `serializeImageAdmission(agent, ...)` 保证同一 Agent 的图片准入不并发（`source`: `commands.ts` → `prompt()`）。
7. **`{accepted:true}` 不是「模型已回复」**，只是「已进收件箱」。要看到消息必须消费 `session/follow`。
8. **不要用 `session/cancel` 来「清空重发」**；它对收件箱无效。

### 10.3 附件上传的两条路（回到 §6.2）

```
路 A（RPC）:   fileUploads/upload          { agentId, request:{ data(base64), name? } }  → { receiptId, file }
路 B（HTTP）:  POST /api/session/uploadFileBinary?sessionId=&name=   (application/octet-stream, 裸字节流)
                                            → 200 { ok:true, value:{ receiptId, file } }
                                 ↓ 任一
              session/prompt.request.content[].type='file'  { receiptId }
              或 commands/execute.submittedAttachments[].type='file' { receiptId }
```

**上传是否分块/带进度**：路 B 在**请求体层面**是流式的（Host 逐块读 `request.body`，浏览器侧用 `ReadableStream` + `duplex:'half'` 分块推送），**但协议不上报 Host 侧进度**——响应只在全部字节接收完后一次性返回。客户端进度来自**发送侧**逐块统计（见 §6.2 的「关于进度」）。路 A 一次性提交完整 base64，只有在 RPC 层的一次性往返，无分块可言。

---

## 11. 漂移发现（0.1.2-rc.1 → 0.1.5-rc.1）

> 本节是本次工作的**最高价值发现**，应同步回 D2。

### 11.1 已确认的线上契约变更（**0 处改名，1 处元素类型扩容**）

**结论：0.1.2-rc.1 → 0.1.5-rc.1 之间，74 个既有端点的 `args` 线上字段名、`args` 字段顺序、可取消性、流式/一元的划分，全部一致。**（比对方法：§0.1 的 `reconcile-0.1.2-vs-0.1.5.mjs`，拿 `git show HEAD:dshcompat/src/main/ets/Endpoints.ets` 的 `wire` 名与 0.1.5 提取结果逐端点比对。）

唯一实质变化是 `commands/execute` 第 3 参数的**元素类型**：

| 端点 | 0.1.2-rc.1 | 0.1.5-rc.1 | 影响 |
|---|---|---|---|
| `commands/execute` 第 3 参数 | 线上名 `submittedAttachments`，只接受 base64 图片数组 | 线上名不变，接受 `CommandSubmitAttachment[]`（图片**或** `{type:'file',receiptId}`） | **兼容**：旧的图片形状仍是合法元素。新增了文件收据这一支 |

`source`: 0.1.2 的 `contracts.json` 该端点 `name=["agent","line","submittedAttachments"]` / `wire=["agentId","line","submittedAttachments"]`；0.1.5 见 `dsh-commands/lib/typert.remote-client.d.ts` L328。

### 11.2 已确认的新增端点（10 个）

`workspaceFiles/{list,stat,read,readAll,readBytes,readRelated,changes}`、`fileUploads/upload`、`goals/get`、`sessionFeedback/record`。命名空间总数因此从 16 增至 18（新增 `workspaceFiles`、`fileUploads`、`sessionFeedback`）。
`source`: `reconcile-0.1.2-vs-0.1.5.mjs` 的输出（0.1.2 = 74，0.1.5 = 84，`REMOVED: (none)`）。

> 注：`.research/protocol/contracts.json` 已被上游升级流程**重新生成为 0.1.5**，当它与新提取结果同源时字段级 diff 恒为空。要复现 0.1.2 的差集必须用 `git show HEAD:dshcompat/src/main/ets/Endpoints.ets`（或 `contracts.json` 的历史版本），而不是当前的 `contracts.json`。**这一点必须在 D5 里写明**：`contracts.json` 不是历史快照，会被就地覆盖。这也是上面把 HEAD 版本落盘成 `endpoints-0.1.2.ets` 的原因。

### 11.3 「静默变更（参数名未变但结构变了）」

**本次未发现**（在可比对的精度内）。74 个既有端点的 `args` 线上名与可取消性完全一致。

**但结果类型的「内部字段」变化未被本次比对覆盖**——比对只到 `resultSymbol` 层面，未逐字段比对 0.1.2 与 0.1.5 的 JSON Schema。**这是明确的未确认项（U-3）**。**最小实验**：从 npm 取 `@deepseek-ai/dsh@0.1.2-rc.1` 到独立目录，跑 §0.1 的 `extract-typert.mjs`，与 `typert-extracted.json` 做逐字段 diff。

### 11.4 仓库内 `Endpoints.ets` 的两处**实质错误**（本次最高价值发现）

#### 11.4.1 `params` 存的是 **TypeScript 内部形参名**，不是**线上字段名**（**高严重度**）

`dshcompat/src/main/ets/Endpoints.ets` 里每条端点的 `params` 数组，来源是 `tools/gen-compat-endpoints.mjs` L208：

```js
params: d.params.map((p) => p.name),      // ← 名字，不是 wire
```

而 `tools/protocol-contract.mjs` L216-218 提取的是**成对**的 `{name, wire}`。因此对**所有 Agent 作用域端点**，上表记的是内部名 `agent`，而线上名是 **`agentId`**：

| 端点 | `Endpoints.ets` 记的 | **实际必须发** | 查得字段名会怎样 |
|---|---|---|---|
| `commands/list` | `['agent']` | `['agentId']` | `gateway/arguments-invalid` |
| `commands/execute` | `['agent','line','submittedAttachments']` | `['agentId','line','submittedAttachments']` | 同上 |
| `agentPresets/select` | `['agent','agentPreset']` | `['agentId','agentPreset']` | 同上 |
| `goals/*`（6 个） | `['agent', ...]` | `['agentId', ...]` | 同上 |
| `fileReferences/list` | `['agent','query']` | `['agentId','query']` | 同上 |
| `sessionReferenceResolver/candidates` | `['agent','query']` | `['agentId','query']` | 同上 |
| `subagents/*`（3 个） | `['agent', ...]` | `['agentId', ...]` | 同上 |
| `dynamicCordisRunner/*`（7 个） | `['agent', ...]` | `['agentId', ...]` | 同上 |
| `workspaceFiles/*`（7 个，0.1.5 新增） | `['workspaceFileScope', ...]` | `['workspaceFileScopeId', ...]` | 同上 |

**共 30 个端点的 `params` 与线上契约不符。**
`source`: `dshcompat/src/main/ets/Endpoints.ets`（`params: ['agent', …]` / `params: ['workspaceFileScope', …]`）；反面证据 `.research/protocol/contracts.json` 同一端点的 `wire` 字段与 `.research/upstream-0.1.5/node_modules/@deepseek-ai/*/lib/typert.remote-client.d.ts` 的实参名。

> **为什么会漏掉**：`Endpoints.ets` 只被**声明性**消费——仓库内当前**没有任何代码读取 `.params`**（`grep '\.params'` 在 `dshcompat/`、`connection/`、`appstate/`、`entry/` 下无命中）。所以这个错误不会立刻编译失败，只会在客户端按表构造 `args` 时变成线上 400。

#### 11.4.2 `compat-drift.mjs` 比的是内部名，因此**看不见线上改名**（**高严重度**）

`tools/compat-drift.mjs` L133 与 L183 用 `d.params.map((p) => p.name)` 与上表的 `params` 比对：

```js
params: d.params.map((p) => p.name),                       // L133
if (c.params.join(',') !== e.params.join(',')) diffs.push(...)   // L183
```

后果：**上游把线上字段改名（改 `wire`）而没改 TypeScript 形参名时，漂移门禁不会报警。** 这正好违反 D5 §5「门禁必须经注入式负测试验证其真的会失败」。
`source`: `tools/compat-drift.mjs` L120-133、L183；反例：`commands/execute` 的 0.1.2→0.1.5 形参 `images`→`submittedAttachments` 被门禁检出，而**线上名从未变**——检出的恰好是噪音，真信号（`wire` 变化）反而检不出。

**建议**（供上游升级流程处置，本文不代改代码）：
1. `tools/gen-compat-endpoints.mjs` L208 改为 `d.params.map((p) => p.wire)`，并把 `shapeOf` 也改成按 `wire` 判定（`request` / `_request` 恰好两边同名，所以 `shape` 不受影响）。
2. `tools/compat-drift.mjs` L133 同步改 `p.wire`，并**加一条注入式负测试**：人为把一个端点的 `wire` 改掉，断言门禁报出 diff。
3. 重新生成 `Endpoints.ets`。

#### 11.4.3 `session/control` 的 `capability` 标注错误（**中严重度**）

| 位置 | 仓库现状 | 本次事实 | 严重度 |
|---|---|---|---|
| `session/control` 的 `capability` | `'approvals'` | **错误**。`session/control` 的帧只有 `baseline`/`queue`/`jobs`/`projection`，**不含任何审批信息**。审批与提问在 `$events` 的 waterfall 帧上 | **中**——会让能力探测把「审批可用」判在该流上，而真正需要的 `$events` + `$events/result` 不在能力表里 |

`source`: `dshcompat/src/main/ets/Endpoints.ets` → `session/control` 条目的 `capability: 'approvals'`；反面证据 `dsh-api-session-controller/lib/types/types.d.ts` L523-536；`dsh-api-remotes/lib/index.js` L17-94（`approval/request` 的转发模式是 `waterfall`，与 `session/control` 无关）。
**建议**：把 `session/control` 的 capability 改为 `'sessions'`，并为 `$events` 的 waterfall 新增能力项（如 `approvals` → requires `$events` + `$events/result`，两者都是逻辑流/一元端点而非上表端点，需要能力表支持「非 Endpoints.ets 端点」的表达）。

### 11.5 对 D2 §1.1 的一处措辞修正：`ok=true` 时 `value` 可以缺失

D2 §1.1 的信封校验行写「`ok=true` 时须有 `value`」。这与上游实现的**实际**行为不符，且会导致四个 void 端点（`credentials/set`、`credentials/unset`、`agentPresets/copy`、`agentPresets/deletePreset`）在端侧被误判为畸形信封。详见 §4.1。
`source`: `dsh-client-connection/lib/client.js` → `parseConnectionResponse`（`ok===true` 分支不做 `value` 存在性校验）；`dsh-api-gateway/lib/index.js` L728-737；`dsh-client-connection/lib/index.js` L687-694。

---

## 12. 未确认清单（阻塞实现前必须关闭）

| # | 项 | 为什么未确认 | 最小实验 |
|---|---|---|---|
| U-1 | `settings/document-updated` 与 `credentials/reference-updated` 的**位置参数个数与类型** | `dsh-settings` / `dsh-credentials` 无生成的 `typert.host.js`，事件签名未进 FaceModel | 订阅 `$events`，改一次设置/凭据，记录 emit 帧 `args` 的 `length` 与元素形状 |
| U-2 | `subagents/prompt.content` 是否接受 `type:'file'` | 类型定义（`PromptContentPart`）含 `file`，但 Host FaceModel 生成的 schema 只列 `text`/`image` | 对 continuable 子代理发 `content:[{type:'file',receiptId}]`，记录错误码 |
| U-3 | 0.1.2→0.1.5 **结果类型内部字段**是否变过 | 只比对到 `resultSymbol` 层；0.1.2 的本机 FaceModel 已不可得 | 从 npm 装 `0.1.2-rc.1` 到独立目录，跑 `extract-typert.mjs` 做逐字段 diff |
| U-4 | `$events` 单连接最大并发逻辑流数、背压行为 | D2 §1.2 遗留问题，本次静态提取无法回答 | 真机并发开流压测（POC-1） |
| U-5 | `uploadFileBinary` 是否有请求体大小上限 | `handleFileUploadHttp` 未做体积检查，但 webserver 层有 `maxRequestBodyBytes`（D2 §1.1，默认 300 MiB） | 上传接近上限的文件观察 |
| U-6 | `imageLimits` 的实际数值 | 是部署配置（`attachments` 服务提供），不是协议常量 | `session/list` 或 `session/control` 的 `projections.imageLimits` 实测读取 |

---

## 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-09-10 | 首版：84 端点请求/回复载荷契约、`workspaceFiles` 新命名空间、`$events` 19 事件目录、四类流的逐项结构、审批/提问应答通路、0.1.2→0.1.5 漂移核对（线上名 0 改名）、仓库内三处实质错误（`Endpoints.ets` 记内部形参名而非线上名、`compat-drift.mjs` 因此看不见线上改名、`session/control` capability 错标）、D2 §1.1 的 `value` 措辞修正 |
