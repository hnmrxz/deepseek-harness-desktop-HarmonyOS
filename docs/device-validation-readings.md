# 真机验收实测读数（摘要）

> 本文档于 2026-09-19 精简（保留全部条目编号与判据）；完整版见 git 历史。
> 本文是 `device-validation.md`（D 系列判据）在真机上的实测台账（判据三档：通过 / 不通过 / 本机不可验；推断不算读数）。逐条原始读数已删，完整版见 git 历史。

## 验收会话元信息

| 项 | 值 |
|---|---|
| 设备 | HUAWEI Mate 70 Pro+（序列号 3EV0224B14000538，API 26 Release，phone；1316×2832px ≈ 376vp 单栏直板） |
| 日期 | 2026-09-16 / 2026-09-17 两轮验收 |
| 安装 | `entry-default-signed.hap`（274.5 MB），SignHap + `hdc install -r` 成功（D3 全局阻塞解除） |
| 核心 | `0.1.5-rc.2`，profile `ondevice`，`HDSH_PLATFORM=ohos`，`127.0.0.1:3120`，jitless |
| 连接与数据面 | `HDSH-AUTH connect ok=true sessions=33`；33 会话 / 4 模型目录 / 155 插件 / 40 设置项 / 1 凭据 |
| 取证通道 | `hdc fport tcp:3120` + `dist/lab/host-rpc.ps1` 直调端侧 Host 84 个端点；`uitest dumpLayout/uiInput` + 截图；`hilog -x`（读后清空，先落盘） |

## 缺陷台账 F1–F32 分布统计（共 32 条）

- ✅ **已修并真机复验（6）**：F1（`write`/`edit` 沙箱拒 `link()` EACCES ⇒ 降级 `copyFile(COPYFILE_EXCL)` 垫片）、F7（审批结果须为裸字符串 `"allowed-once"`，非双层包装）、F9（HAP 内 Host 入口脚本是旧版 ⇒ 新增 `check-host-app-fresh.mjs` 门禁）、F19（冷启重连竞态 ⇒ 未连上按 5 秒兜底重试，3/3 冷启通过）、F22（轨迹视图泄露完整系统提示词 ⇒ 渲染改走 `userFacingBodyOf()`）、F29（返回后重进同一条会话空屏 ⇒ 视图从中枢回填，3/3 通过）
- ✅ **已收敛（1）**：F17（D17/D22 对「插件清单」判据互斥，按 D22 收敛为七项分区）
- ⏪ **已撤回（1）**：F32（回合产出文件行"不通过"系读数时点问题，非缺陷；D39 §1 实际通过，但同处发现 D39 §2 点 chip 无跳转）
- ❌ **未修（24）**：
  - P0（7）：F2（子代理会话点开永久空白 + 每 3 秒无限重试）、F12（会话页待决卡主键被压至屏幕下沿仅 5px）、F18（工作区页待决卡被 `maxHeight 320vp` 裁掉整条动作行）、F21（待决在客户端重启后丢失，该轮永久卡死）、F24（斜杠命令在手机上无可达执行入口 ⇒ 被当提示词发给模型）、F25（端侧 `sharp` 未起来 ⇒ 发图必败 `session/attachment-invalid`）、F28（消息反馈双层包装：读不出也写不进）
  - P1（10）：F3（顶部错误横幅永不消失、逻辑流计数只增不减）、F4（「新建会话」不进入会话，两入口不一致）、F5（归档分组标题渲染成 `[object Object] · 4`）、F6（列表页待决卡被底部标签栏切掉按钮）、F10（发送后无乐观回显，整屏空白到回合结束）、F13（待决卡与输入区同时可交互）、F14（命令提示写进只在设置页渲染的通道）、F20（引用竖条被拉成近整屏高，为文字高 21 倍）、F27（原生件缺失原因只写在不渲染的 note 里）、F30（`@` 菜单点目录不钻取、无面包屑）
  - P2（7）：F8（「本地调试」破坏设置沉底）、F11（`elapsedMs` 恒为 0 ⇒ D8/D26 §1 不可能通过）、F15（`@` 候选对未命名会话显示原始 UUID）、F16（子代理会话进 `@` 候选与工作区树）、F23（未命名会话列表显示 `workspace`，与 F15 两套口径）、F26（「导出诊断」未接线）、F31（会话头统计不跟新回合）

> D 系列判据的逐条通过/不通过结论见 `device-validation.md` §1 状态列；修复方案与次序见 `docs/80-修复与优化方案.md`（含回扫新增的 F33：侧栏「工作区」席位删除后映射未跟着改，已修待验）；待复验清单见 `docs/81-待真机复验清单.md`。
