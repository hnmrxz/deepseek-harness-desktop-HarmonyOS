# 端侧核心运行架构（On-Device Core Architecture）—— D6

| 项 | 内容 |
|---|---|
| 文档编号 | `docs/50-端侧核心运行架构.md` |
| 版本 | v1.0 |
| 日期 | 2026-09-12 |
| 状态 | **生效**（本文件是目标与架构的权威更正；与 D1 冲突处以本文件为准，D1 同步修订见 §10） |
| 上位文档 | `docs/00-开发任务书.md`（D1） |
| 触发原因 | 原 D1 把「端侧跑完整 dsh Host」列为 **P3 非目标**（§3.3）并把「原生 Client + PC 侧 Host」定为主方案（§6.2），与本项目真实目标相反。本文件更正该目标并给出可行路径。 |

---

## 0. 一句话更正

> **HDSH 不是「PC 上 dsh 的遥控器」。HDSH 本体就是 DSH 的运行载体：应用内自带 DSH 核心运行能力，
> 在自己这一端把 Host 跑起来，客户端页面（ArkUI 原生页面）讲的是本地 Host 的协议；
> 并支持插件的增删启停与核心版本的安装 / 切换 / 回滚。**

原方案（远程客户端）保留为**一种可选接入方式**（连接别人的 Host），但**不再是产品目标**，
也不再是验收口径。凡是「必须有另一台机器在跑 dsh」才成立的功能，都不能算完成。

### 0.1 变更的原因（不是喜好问题，是三条事实变了）

| # | 事实 | 对目标的影响 |
|---|---|---|
| 1 | **Node.js 官方已支持 OpenHarmony**（`BUILDING.md` 平台表：`OpenHarmony / arm64 / >= 5.0 / Experimental`；支持 PR [#58350](https://github.com/nodejs/node/pull/58350) 已于 2025-05-21 合入 `main`，后续修复 [#59547](https://github.com/nodejs/node/pull/59547)、[#59804](https://github.com/nodejs/node/pull/59804)） | 「端侧没有 Node 运行时」这个**前提**已经不成立——原 D1 §6.2 判 B 方案「成本巨大」的依据过期了 |
| 2 | **dsh 的原生依赖在鸿蒙上已有现成移植**：npm 上存在 `@ohos-ports/*` 与 `@ohos-npm-ports/*` 组织，含 `@ohos-ports/koffi`、`@ohos-ports/node-pty`、`@ohos-ports/sharp`、`@ohos-ports/better-sqlite3`，甚至已有 **`@ohos-ports/deepseek-ai-dsh`**（`os:["openharmony"]`、`cpu:["arm64"]`、`bin.dsh=bin/dsh.sh`，依赖里把 `koffi/node-pty/sharp` 全部 alias 到鸿蒙移植版） | 原 D1 §1.3 里「原生模块无 aarch64 产物 → 终端/搜索/沙箱/图片全缺失」这条**反面对标的理由**同样过期 |
| 3 | 原方案最痛的**传输层**问题（长连接周期性断开、`hdc rport` 不可靠、跨设备通道）**在端侧自足模型下根本不存在**：Host 与客户端在同一个进程里，走 `127.0.0.1`，不跨设备、不转发、不需要隧道 | 原 D1 §6.4/§6.5（L1/L2 配对与洞道）从「关键路径」降级为「可选的远程接入能力」 |

---

## 1. 不变式（设计的硬边界）

1. **零上游 patch**：不 fork、不魔改 dsh 业务代码；端侧差异只走 dsh 自己的组合面（profile / `cordis.patch.yml` / bundle）。
2. **不打洞**：默认只监听 `127.0.0.1`；不绑 `0.0.0.0`、不伪造 `Host`/`Origin`。端侧 Host 与端侧客户端同进程，**这两条本来就自然满足**——这是本架构相对社区的 Electron-on-鸿蒙方案的结构性优势。
3. **状态与代码分离**：`$DSH_HOME`（会话/设置/凭据/工作区注册）在应用沙箱里**只有一份**，跨核心版本共享；核心代码树按版本各存一份。升级核心不得动用户数据。
4. **能力降级而非崩溃**：dsh 的 `boot()` 是 **fail-loud**（任一插件行挂载失败 → 进程 `exit(1)`），所以端侧必须用 profile 精确控制「哪些行启用」，而不是指望它自己降级。
5. **界面不说谎**：核心没起来、版本不匹配、插件装失败，页面必须如实说明并给出下一步动作（沿用 D3 §4 纪律）。
6. **上架优先：不申请任何特殊权限**（2026-09-12 新增）。不用 JIT 类 ACL 权限、不用需特批的能力；
   运行时统一以 **jitless** 方式工作，权限面只保留普通权限（网络 = `ohos.permission.INTERNET`）。
   **任何"靠申请特殊权限才能成立"的方案都不进入本项目的选型**（详见 §4.4）。

---

## 2. 目标架构

```
┌─ HDSH（HAP / APP）────────────────────────────────────────────────────────┐
│  UI 层（ArkUI 原生页面，一多自适应）        ← 「客户端页面」                │
│    └ 复用现有 9 个视图 + 三形态导航（手机/折叠屏/平板/2in1）               │
├──────────────────────────────────────────────────────────────────────────┤
│  状态层 appstate（SessionHub 单一连接与会话状态中枢）                      │
├──────────────────────────────────────────────────────────────────────────┤
│  协议层 connection + dshcompat（84 端点 / 4 流，漂移门禁保护）              │
├──────────────────────────────────────────────────────────────────────────┤
│  ★ 新增：端侧核心运行层 hostruntime                                       │
│    ├ DshHost      核心生命周期：准备 → 起 → 健康检查 → ready → 停/重启     │
│    ├ CoreStore    多版本核心仓库：解包 → 校验 → 激活 → 回滚（§6）          │
│    ├ Profiles     端侧 profile / bundle（哪些 dsh 行启用，§5）             │
│    ├ Plugins      插件清单与增删启停（§6.2）                              │
│    └ Runtime      运行时载体：libnode.so / NAPI 引导（§4）                │
├──────────────────────────────────────────────────────────────────────────┤
│  设备能力层 platform（通知 / 长时任务 / 剪贴板 / 分享 / 窗口 / 密钥存储）    │
└──────────────────────────────────────────────────────────────────────────┘
        │ 进程内 loopback：HTTP POST /api/<endpoint> + WS /api/remote.mux
        ▼
┌─ 端侧 dsh Host（Node 运行时，跑在本应用进程内的线程上）───────────────────┐
│  webserver(127.0.0.1:port) ├ /api ├ /api/remote.mux ├ $events            │
│  dsh 本体：agent / 工具 / 会话持久化 / 投影 / 设置 / 凭据 / 插件清单       │
│  DSH_HOME = <应用沙箱>/dsh/home（跨版本共享，唯一一份用户数据）           │
└──────────────────────────────────────────────────────────────────────────┘
```

**为什么是同进程（而不是 HNP 子进程）**：设备之间/进程之间的 loopback 在鸿蒙 NEXT 上可能被隔离
（社区 Electron 方案必须绑 `0.0.0.0` + 改写 `Host` 头来绕开，见 `.research/ref-harmony` 的
`src-main/main.js` 与 `profiles/desktop/cordis.patch.yml`）。**同进程内 loopback 不跨隔离边界**，
因此本架构不需要绕，也就没有「必须打洞」的结构性压力。
子进程（HNP / `childProcessManager`）作为**崩溃隔离**的后续优化项，不作为 v1 前提。

---

## 3. 可行性证据（写下来是为了不再重复论证）

| # | 命题 | 证据 | 置信度 |
|---|---|---|---|
| E1 | Node 能在 OpenHarmony/aarch64 上构建运行 | Node `BUILDING.md` 平台表列 `OpenHarmony arm64 >= 5.0`（Experimental）；[#58350](https://github.com/nodejs/node/pull/58350) 已合入 `main`（只动了 `configure.py`/`common.gypi`/`node.gyp`/`deps/*`/`tools/v8_gypfiles`） | 高（官方） |
| E2 | 官方**不提供** ohos 预编译包 | `nodejs.org/dist/index.json` 全量 865 个版本中**无任何 ohos 产物** | 高（实测） |
| E3 | 鸿蒙生态里有可用的构建/分发通道 | `ohos-npm-ports` 用 arm64 鸿蒙容器（`ghcr.io/hqzing/dockerharmony:6.1`）+ Harmonybrew 安装 `node/python/devel-base`；`build_in_harmonyos`（Conan 框架）已收录 427 款软件 / node 26.7.0 适配 | 中高 |
| E4 | dsh 的原生依赖已有鸿蒙移植 | `@ohos-ports/koffi@2.16.2-beta.0`、`@ohos-ports/node-pty@1.1.0-beta.0`、`@ohos-ports/sharp`（libvips 8.18.3）、`@ohos-ports/better-sqlite3`；`@ohos-ports/deepseek-ai-dsh@0.1.0-beta.3` 把它们按 `npm:` 别名接进 dsh | 高（npm 元数据实测） |
| E5 | **鸿蒙商用版校验 ELF 代码签名** | `ohos-npm-ports` 贡献指南第 3 条明确要求 `.node` 带代码签名；工具 `ohos-bst-light/self-sign.py`（[仓库](https://github.com/hqzing/ohos-bst-light)）；Harmonybrew 的 ohos-sdk 产物自动带签名 | 高（社区文档） |
| E6 | HAP 打不动 5 万+ 小文件 | 社区方案把 dsh 产物打成 `dsh-dist.tar.gz`，首启流式解压（其产物 555 MB / 解压约 45 s） | 高（社区实测） |
| E7 | 鸿蒙支持随包分发**可执行原生程序** | `electron` 模块 `module.json5` 里有 `hnpPackages` 与 `executableBinaryPaths`（社区仓库中被注释保留，配套 `electron_exec_path_ohos` 元数据） | 中（配置证据，未实测） |
| E8 | dsh 的 boot 是 fail-loud | `profile-boot-*.js` 的 `installFailLoud` → `exit(1)`；`assertEntriesActivated` 聚合所有失败行后抛错 | 高（源码，见 §5） |
| E9 | **端侧核心树可在 Windows 上直接物化成 openharmony/arm64 形态** | **本项目实测**：`npm install --os=openharmony --cpu=arm64 --ignore-scripts` + `overrides` 把 `node-pty/koffi/sharp` 别名到 `@ohos-ports/*`，得到 518 包 / 25741 文件 / 292.8 MB（未裁剪）；`node_modules/koffi/build/koffi/openharmony_arm64/koffi.node`、`node-pty/prebuilds/openharmony-arm64/{pty.node,spawn-helper}`、`@ohos-ports/img-sharp-openharmony-arm64/lib/sharp-openharmony-arm64.node` 均在位 | 高（本仓库实测） |
| E10 | **这些移植件已带鸿蒙代码签名** | 本项目实测：对上述三个 `.node` 跑 NDK 的 `llvm-readelf -S`，三者都同时有 `.note.ohos.ident` 与 `.codesign` 段 | 高（本仓库实测） |
| E11 | **有可用的、已签名的 ohos-aarch64 `node` 可执行文件**（但没有 `libnode.so`） | `hqzing/ohos-node` 提供 v22.17.0…v26.8.2 的发行包（如 `node-v22.23.2-openharmony-arm64.tar.xz`），并自建 `https://ohos-node.com/dist`（兼容 nvm/fnm）；实测下载 v24.21.0 校验 SHA256 与 `SHASUMS256.txt` 完全一致，`bin/node` 为 ELF64 AArch64、`ET_DYN`、带 `.note.ohos.ident`，**包内无任何 `.so`** | 高（第三方 + 实测校验） |
| E12 | **无预编译 `libnode.so`；Node 的 `--shared` 在 OpenHarmony 上「未测试」** | Node 官方文档把 shared library 支持的已测平台限定为 Linux/macOS/Windows/AIX；`node.gyp` 里 ohos 的 shared 分支虽已接线但未验证 | 中高 |
| E13 | **JIT 是权限闸门，且只对平板 / PC-2in1 开放** | V8 需要可写可执行匿名内存；鸿蒙默认拦截，须申请 ACL 权限 `ohos.permission.kernel.ALLOW_WRITABLE_CODE_MEMORY`，而华为明确：**当前仅平板、PC/2in1 设备应用可申请**；否则要 `v8_jitless=true`，且 WebAssembly 变为 `undefined` | 高（官方答复） |
| E14 | **`.node`/`.so` 必须随 HAP 打包；热更新下载的 so 会被拦** | 华为 FAQ：发布阶段三方应用所有 so 需打包在 HAP 内；**通过热更新下载的 so 因缺乏合法签名会被系统拦截** | 高（官方 FAQ） |
| E15 | **原生子进程（`childProcessManager.startNativeChildProcess`）仅在平板 / PC-2in1 可用**；手机禁止三方应用 fork/创建进程 | 官方接口说明：其他设备类型返回错误码；手机产品不允许普通应用直接创建进程 | 高（官方文档） |
| E16 | 监听 `127.0.0.1` 只需普通权限 | `ohos.permission.INTERNET`（normal，system_grant）；官方 seccomp 白名单含 `socket/bind/listen/accept/connect`；应用**不能**建网络命名空间 | 高（官方文档） |
| E17 | **端侧核心的原生码只有 3 个包；插件行的可安装性可在核心树上静态判定** | `tools/scan-core-plugins.mjs` 扫真实核心树：518 包 / 27250 文件，含 `.node` 的包恰为 `koffi`、`node-pty`、`@ohos-ports/img-sharp-openharmony-arm64`；152 个插件行判定为 146 纯 JS / 6 依赖原生 / 0 待确认。**原生性属依赖闭包而非插件包自身**——6 行无一自带 `.node`（详见 §6.2.1） | 高（自测，可复跑） |
| E18 | **打包流程从不给原生 `.so` 签名**（`.so` 在 HAP 内是原样字节） | `tools/node-runtime/check-hap-native-signing.sh` 从构建出的 HAP 里取出全部 4 个 `libs/arm64-v8a/*.so`：均为 `ELF64/AArch64/DYN`、**均只有 `.note.ohos.ident`、均无 `.codesign`**，且与 `entry/libs/arm64-v8a/` 下的源文件**逐字节相同**（sha256 一致）⇒ 打包既没签名也没改写。HAP 自身的签名是包级的，不会回溯给内嵌 `.so` | 高（自测，可复跑） |
| E19 | **`--jitless` 是 Node 的一等选项，且它真的关掉整条 JIT 链并把 WebAssembly 变成 undefined** | `src/node_options.cc:1068` 有 `AddOption("--jitless", …)`（不是碰运气透传给 V8 的未知参数）；V8 `flag-definitions.h:726-738` 的 `DEFINE_NEG_IMPLICATION(jitless, …)` 关掉 `turbofan`/`sparkplug`/`always_sparkplug`/`maglev`；同文件 `1524-1525`：`--jitless` 还隐含 `--no-expose-wasm`。另有 `DEFINE_BOOL_READONLY(jitless, true, …)`（部分平台强置）与 `DEFINE_IMPLICATION(lite_mode, jitless)`（`--lite-mode` 会连带打开 jitless） | 高（源码可查） |
| E20 | **核心树里唯一的 WASM 产物是 sharp 的 wasm32 回退变体** | 对真实核心树 glob `**/*.wasm`，全树**只有一个**：`node_modules/@ohos-ports/img-sharp-wasm32/lib/sharp-wasm32-0.35.3.node.wasm`。它属于**平台回退包**（与上游 `@img/sharp-wasm32` 同类），而我们实际用的是原生变体 `@ohos-ports/img-sharp-openharmony-arm64`。⇒ 在 E19 的"jitless 下 WASM 为 undefined"这一前提下，**dsh 实际加载的东西没有任何一个依赖 WASM**，jitless 路线对本依赖集成立 | 高（自测，可复跑） |
| E21 | **`main.js` 能跑在裸 libnode 下（不依赖 Electron），但因此阶段二完全依赖 env 通道** | `hostcore/app/main.js` 的 `electronUserData()` 把 `require('electron')` 包在 try/catch 里（第 34–45 行，注释就写明"离线跑 Node 时会抛错，走 env 回退"）⇒ 在 libnode 下它**必然返回空串**，于是 `DSH_BASE` 为空、`currentCoreDir()` 直接返回 `''`。结论两条：① **阶段二不需要改 main.js**；② **阶段二没有任何自定位能力**，`HDSH_CORE_DIR`/`HDSH_HOME`/`HDSH_SANDBOX_HOME` 全部必须由引导层 `setenv` 传入——这正是 §"Host 启动契约"必须完整且被单测的原因 | 高（源码可查） |
| E22 | **gyp 的 `OS` 不是 `openharmony` ⇒ Node 所有 `OS=="openharmony"` 分支都是死代码，第三方依赖静默走了 Linux/glibc 配置** | `out/Release/libnode.so.127` **链接成功（138 MB）**，但链接 `embedtest` 时报 `ld.lld: error: undefined reference due to --no-allow-shlib-undefined: getservbyport_r`。`getservbyport_r` 是 **glibc 专有**、musl/OHOS 不提供 ⇒ **libnode.so 里带着一个在设备上解析不了的符号，加载会失败**。查到根因：① `config.gypi` 里**没有** `openharmony`；② 生成的 `out/deps/cares/cares.target.mk` 用的是 `-I…/deps/cares/config/linux`，**没有** `config/openharmony`。Node 明明**自带** `deps/cares/config/openharmony/ares_config.h`（其中 `HAVE_GETSERVBYPORT_R` 是 `#undef`），`cares.gyp:252` 也写了 `OS=="openharmony"` 才选它——但该条件**从未命中**。⇒ `--dest-os=openharmony` 只写进 configure 自己的字段，**不改变 gyp 的 `OS`**（后者由构建宿主机决定，这里是 linux），于是**所有依赖 `OS=="openharmony"` 的分支静默失效**。<br>**这次是幸运的**：`embedtest` 的 `--no-allow-shlib-undefined` 把一个只会在设备上暴露的加载失败，变成了本机可见的链接错误。**还有多少同类配置错误是沉默的，目前未知** | 高（自测 + 生成物可查） |
| E23 | **E18 判定完毕：包内未签名 `.so` 能正常加载并执行**；且**阶段一的 `exit(1)` 不是原生引导崩溃，而是 JS 调用了 `exit()`**——我此前"没有 `[hdsh-host]` 日志 ⇒ 崩在原生引导里"的推断**是错的** | 真机（Mate 70 Pro+ / PLA-AL10 / 7.0.0.105）崩溃日志：`Reason:Signal:SIGABRT`、`Process life time:2s`、`LastFatalMessage:[appspawn_server.c:69]Unexpected call: exit(1)`；调用栈 `#02 libappspawn_helper.z.so(exit+144)` ← `#03 libelectron.so` ← `#12 v8::internal::Execution::Call` ← `#14 node::InternalCallbackScope::Close` ← `#16 node::StartExecution` ← `#17 node::LoadEnvironment` ← `#28 ElectronMain+424` ← `libadapter.so`。三条结论：<br>① **`libelectron.so` 已成功加载并跑到了执行 JS**（`ElectronMain`→`LoadEnvironment`→`StartExecution`→`Expression Call`），⇒ **E18 的两种解释里 ① 成立：包内 `.so` 不需要 `.codesign` 也能加载**，阶段二的 `libnode.so` 同理；<br>② `exit()` 是被 **JS 回调**调用的（栈里明确有 `v8::Function::Call`），**不是**原生引导在跑我们的 JS 之前就崩；<br>③ 之前"日志里一行 `[hdsh-host]` 都没有"**不能**推出"我们的 JS 没跑"——Node 的 `console.log` 走 **stdout**，在应用进程里**不进 hilog**。这个误判导致我把排查方向错了很久。<br>**⚠ 由此得到一条对阶段二最关键的硬约束**：OHOS 用 `libappspawn_helper` 拦截应用进程里的 `exit()`，**任何 `process.exit()` 都会变成 SIGABRT**。而 dsh 的启动是 **fail-loud**（`installFailLoud` → `exit(1)`）——**这在端侧会直接把应用打崩**，必须在阶段二处理（或确保失败路径不触发它） | 高（真机崩溃日志） |
| E24 | **加了"落文件 + 拦截 `process.exit`"的诊断后，设备上仍找不到日志 ⇒ 那个 `exit(1)` 很可能出自 Electron 自己的 bootstrap，而不是我们的入口** | 给 `hostcore/app/main.js` 加了诊断（把输出写 `<userData>/hdsh-host.log`、拦截 `process.exit` 记栈且不真退出），`place-host-app.ps1` 确认 `main.js 15878 bytes` 已进资源、重建、`hdc install` 成功。启动后 14 秒 `pidof` 仍为空，且 `/data/app/el2/100/base/com.hnmrxz.hdsh/haps/entry/files/` 为空、`…/files/` 下只有早先探针留下的 `probe-*`、`find … -name hdsh-host.log` 无结果。⇒ 我们的入口**很可能从未执行**。**注意不确定性**：`app.getPath('userData')` 在 OHOS 上的映射未经证实（部分沙箱子目录 `Permission denied`），所以是"很可能"而非"已证明" | 中（真机观察 + 一处未证实的映射） |
| E27 | **阶段一那个 `exit(1)` 是"挂载 Electron 宿主"引起的回归；不再挂载它之后应用稳定运行并正常渲染** | 根 Stack 上常驻挂载 `RuntimeHost()`（`Index.ets`，为触发 `runBrowser` 而设）会让应用在 2–4 秒内以 `Unexpected call: exit(1)` 结束。改为**默认不挂载**（`MOUNT_ELECTRON_RUNTIME_HOST = false`，保留一行开关供将来诊断）后实测：启动 15 秒后 `hdc shell pidof com.hnmrxz.hdsh` 返回 **56683**（进程存活），`snapshot_display` 截图显示界面正常——标题「会话」、空态文案「本机核心尚未接线 / 运行时载体还没接上，会话暂时不可用。到「核心」页可以看到运行时与版本的真实状态。」、底部五页签（待决/会话/工作区/**核心**/设置）。<br>**处置理由**：阶段一在 D6 §4.0 里本来就只是**开发期验证通道**（依赖 JIT、与本项目"不申请特殊权限、可正常上架"冲突），**不是上架形态**；阶段二（`libnode.so` + `libdshhost.so`）才是。所以这里不是"绕过一个 bug"，而是**把不必要的组件从默认路径上移开**，让运行时只由阶段二提供。<br>**注意**：这**没有**回答"Electron 引导里是谁调的 `exit(1)`"——那条路径仍有未解之谜，只是它不再挡路。 | 高（真机进程存活 + 截图） |
| E28 | **`.so` 被加载、构造器执行，但 `Init` 从未被调用 ⇒ ArkTS 的导入根本没有走到我们的模块**（名字口径已排除） | 在构造器与 `Init()` 各打一条 `OH_LOG_Print` 后真机读数：构造器两条都在（`constructor ran … registration calls returned`），**没有** `Init called`；ArkTS 侧仍是 `runtimeVersion is undefined` + ArkUI 节点导出。⇒ 运行时 `dlopen` 了我们的 `.so`、`napi_module_register` 也返回了，但**从未用我们的模块去实例化**。四种合理名字（`dshhost` / `libdshhost.so` / `libdshhost` / `dshhost.so`）已全试，**改名这条支路用尽**。SDK 侧只有经典的 `napi_module_register`，无 `*_by_name` 之类新 API（grep 整个 `native/sysroot/usr/include` 无匹配）。 | 高（真机两条日志的对照） |
| E29 | **`nm_filename` 也不是原因；"改结构体字段"这条路也走尽了 ⇒ 应当放弃手工放 `.so` 的方式，改走 DevEco 的 CMake 路径** | 把四个注册结构体的 `nm_filename` 从 `nullptr` 改成 `__FILE__`（Node 的 `NODE_MODULE` 宏与 DevEco 样例都这么写，这是我们与规范写法**唯一**剩下的差异）后重建、安装、启动，症状**逐字相同**：构造器两条日志照旧，`Init` 依旧从不被调用，ArkTS 侧仍是 `runtimeVersion is undefined` + ArkUI 节点导出。<br>⇒ 至此已排除：`.so` 本体（ELF/重定位/NEEDED/strip 前后一致）、四种模块名、`nm_filename`、依赖声明位置（`dependencies` vs `devDependencies`）、types 包形状（对齐过仓库内先例）。**"逐项改字段试错"的收益已经耗尽。**<br>**决定改变做法**：不再手工把预编译 `.so` 放进 `entry/libs/arm64-v8a/`（这条路 DevEco 没走过，我们也不确定 hvigor 是否为此生成原生模块映射），改为**走 DevEco 的 CMake 路径**——在 `entry/src/main/cpp/` 放 `CMakeLists.txt` 与源文件、在 `entry/build-profile.json5` 声明 `externalNativeOptions`，让 **hvigor 自己编译并链接出 `libdshhost.so`**。这样"映射从哪来"这一环就由工具链负责，而不是由我们猜。代价：CMake 需要 Node 头文件与 `libnode.so.127`（后者已在本仓库 `entry/libs/` 下，可指定绝对路径链接）。 | 高（真机读数逐字相同） |
 把四个注册结构体的 `nm_filename` 从 `nullptr` 改成 `__FILE__`（Node 的 `NODE_MODULE` 宏与 DevEco 样例都这么写，这是我们与规范写法**唯一**剩下的差异）后重建、安装、启动，症状**逐字相同**：构造器两条日志照旧，`Init` 依旧从不被调用，ArkTS 侧仍是 `runtimeVersion is undefined` + ArkUI 节点导出。<br>⇒ 至此已排除：`.so` 本体（ELF/重定位/NEEDED/strip 前后一致）、四种模块名、`nm_filename`、依赖声明位置（`dependencies` vs `devDependencies`）、types 包形状（对齐过仓库内先例）。**"逐项改字段试错"的收益已经耗尽。**<br>**决定改变做法**：不再手工把预编译 `.so` 放进 `entry/libs/arm64-v8a/`（这条路 DevEco 没走过，我们也不确定 hvigor 是否为此生成原生模块映射），改为**走 DevEco 的 CMake 路径**——在 `entry/src/main/cpp/` 放 `CMakeLists.txt` 与源文件、在 `entry/build-profile.json5` 声明 `externalNativeOptions`，让 **hvigor 自己编译并链接出 `libdshhost.so`**。这样"映射从哪来"这一环就由工具链负责，而不是由我们猜。代价：CMake 需要 Node 头文件与 `libnode.so.127`（后者已在本仓库 `entry/libs/` 下，可指定绝对路径链接）。 | 高（真机读数逐字相同） |
 在构造器与 `Init()` 各打一条 `OH_LOG_Print` 后真机读数：<br>`HDSH-SHIM: constructor ran: registering 4 name forms (dshhost / libdshhost.so / libdshhost / dshhost.so)`<br>`HDSH-SHIM: registration calls returned`<br>—— **没有** `HDSH-SHIM: Init called` 这一行；而 ArkTS 侧仍是 `runtimeVersion is undefined` + 导出键为 ArkUI 节点 API。<br>⇒ 序列是：运行时 `dlopen` 了我们的 `.so`（构造器跑了）、`napi_module_register` 返回了，但**运行时从未用我们的模块去实例化**。因此问题不在"名字怎么拼"（四种合理口径已全试；改名这一招**已用尽**），而在**"ArkTS 的 `import … from 'libdshhost.so'` 到底被解析成了什么"**。<br>SDK 侧已查：`native_api.h` 只有经典的 `napi_module_register`，**没有** `napi_module_register_by_name` / `load_native_module` 之类的新 API（grep 整个 `native/sysroot/usr/include` 无匹配）。<br>**下一步应换方向**：① 看 ArkTS 侧那个 `dshhost` 对象**到底是谁**（用 `import { NodeController } from '@ohos.arkui.node'` 交叉验证是否同源）；② 检查 `module.json5` / `build-profile.json5` 是否有"原生模块白名单/映射"一类的配置项；③ 考虑改用 **HSP/HAR 里的 native module** 形式，或按 DevEco 的 CMake 路径让 hvigor 自己生成映射（我们是手工把预编译 `.so` 丢进 `libs/`，**这可能正是缺的那一环**） | 高（真机两条日志的对照） |
| E30 | **根因确定：手工放进 `entry/libs/` 的预编译 `.so` 绑不上；走 hvigor 的 CMake 路径构建的原生模块能绑上** | 在**同一个文件、同一个进程、相隔几毫秒**内并列调用两个模块：<br>`HDSH-PROBE cmake 构建的原生模块可绑定，返回值=hdsh-probe-ok` ✅<br>`HDSH-RUNTIME 调用原生模块失败：runtimeVersion is undefined`（导出键仍是 ArkUI 节点 API）❌<br>两者的唯一差别就是**来源**：探针 `libentryprobe.so` 由 `entry/src/main/cpp/CMakeLists.txt` + `build-profile.json5` 的 `externalNativeOptions` 交给 **hvigor 自己编译链接**（构建日志：`:entry:default@BuildNativeWithCmake... after 7 s`）；而 `libdshhost.so` 是用 `tools/node-runtime/build-dshhost.sh` 独立编好后**手工拷进** `entry/libs/arm64-v8a/`。<br>⇒ **本项目里"手工放预编译 .so"不会生成 ArkTS↔原生模块的映射**（这也解释了为何同方式的 `web_engine/libadapter.so` 同样绑不上、且被兜底代码掩盖，见 E26）。这是 E25–E29 五轮排查的答案。<br>**下一步（已明确）**：把 `dshhost` 也改成 **CMake 构建**——在同一份 `CMakeLists.txt` 里加 `add_library(dshhost SHARED …/dshhost.cc)`，需要 Node 头文件（`src` / `deps/v8/include` / `deps/uv/include`）与 `libnode.so.127`（已在本仓库，可绝对路径链接）；同时**从 `entry/libs/` 移除手工放的 `libdshhost.so`**，避免同名库冲突。 | 高（同进程并列对照） |
| E26 | **原生模块绑不上是"项目级"现象，不是 `libdshhost` 特有**；且 `web_engine` 早已用兜底代码把它掩盖了 | ① 对齐先例后症状不变：把 `libdshhost.so` 的依赖从 `dependencies` 移到 **`devDependencies`**（与 `web_engine` 对 `libadapter.so` 的写法一致）、types 的 `oh-package.json5` 去掉 `main` 并把 `version` 置空串——仍然是 `runtimeVersion is undefined` + 导出键为 ArkUI 节点 API。② **更关键**：`web_engine/src/main/ets/utils/JsBindingUtils.ets` 用**完全相同的方式**声明并导入 `libadapter.so`（`import adapter from 'libadapter.so'`），而它的调用点旁边就写着兜底：`'SetContextPaths: ContextPathAdapter not available'`、`'adapter.SetContextPaths not found in C++ module'`——**而早先的真机日志里那些兜底分支确实被走到了**（`[WebEngine]: ContextPathAdapter#getFilesDir() in / out => ""`）。⇒ 这个项目里声明的原生模块**普遍没绑上**，所以根因在项目/工具链侧，不在我们的 C++ 或声明形状。<br>③ 库落地位置是对的：崩溃栈里 `libelectron.so` 的路径是 `/data/storage/el1/bundle/libs/arm64/…`，即 HAP 内 `libs/arm64-v8a/` 的映射（`hdc shell ls` 该目录被拒，无法直接列）。<br>④ **二分结果（构造器日志）**：在构造器里 `OH_LOG_Print` 后真机打出 `HDSH-SHIM: constructor ran … registration calls returned` ⇒ **`.so` 确实被 `dlopen` 了、构造器跑了、注册调用返回了**，问题**纯在模块名查找**，不在加载。下一步应把注册名再补上 `libdshhost`（带 lib 前缀、不带扩展名）这一形式 | 高（真机读数 + 仓库内先例的调用点证据） |
| E25 | **`libdshhost.so` 本体是正确的，但 ArkTS 的 `import … from 'libdshhost.so'` 绑到的不是我们的模块**（拿到的是 ArkUI 的节点 API） | 真机读数（hilog，`entry/ability` 标签）：<br>`HDSH-RUNTIME 调用原生模块失败：runtimeVersion is not callable, runtimeVersion is undefined`<br>`HDSH-RUNTIME 模块类型=object 导出键=["NodeController","BuilderNode","BaseNode","RenderNode","FrameNode","typeNode","ComponentContent",…]`<br>—— 导出键**全是 ArkUI 的节点 API**，说明导入**没有抛加载错误**却绑到了别的模块。<br>**已排除 `.so` 自身**：`DT_INIT_ARRAY` 有 3 项且重定位为 `0x58c0→__do_init`、**`0x58d0→_GLOBAL__sub_I_dshhost.cc`**、`0x58e0→__do_finalize`，即**加载时一定会执行我们的注册构造器**；`nm -D` 里 `RegisterDshHostModule` 为 `T`（已导出）；`NEEDED` 三项（`libnode.so.127`/`libc++_shared.so`/`libc.so`）齐全；我甚至已把模块**按两个名字各注册一次**（`dshhost` 与 `libdshhost.so`）以排除匹配口径问题——仍然如此。编译产物里导入被正确规范化为 `@normalized:Y&&&libdshhost.so&`，说明接线**被识别**了 | 高（真机读数 + 本机 ELF 证据） |
| E31 | **✅ 阶段二的运行时门通过：自建 `libnode` 在真机上可加载、可调用** | 把 `dshhost` 改为由 hvigor/CMake 构建（E30 的结论）后，真机 hilog：<br>`HDSH-PROBE cmake 构建的原生模块可绑定，返回值=hdsh-probe-ok`<br>**`HDSH-RUNTIME libnode 版本 = 22.23.2`**<br>这一行同时证明三件事：① `libdshhost.so` 在设备上加载并绑定成功；② 它 `NEEDED` 的 **`libnode.so.127`（我们自建的 Node 运行时）也在设备上加载成功**——否则动态链接失败、模块起不来；③ 版本 **22.23.2** ≥ 下限 22.17.0。<br>配套事实：构建日志 `:entry:default@BuildNativeWithCmake...`；手工拷贝的 `libdshhost.so` 已从 `entry/libs/` 移除（避免同名冲突）；Node 头文件由 `tools/node-runtime/sync-node-headers.sh` 同步到 `entry/src/main/cpp/node-headers/`（已 gitignore，3.7 MB）。<br>**阶段一口径的更新**：目标里"第一阶段先打通运行时可行性门（端侧 libnode/Node 运行时 + dsh web 最小可用）"的**前半（libnode/Node 运行时）已在真机证明**；剩下的后半是"dsh web 最小可用"——即真正把 Host 起在回环上，这一步还没做。 | 高（真机读数） |
| E32 | **端侧「安装内置核心 → 启动 Host」第一次真机跑通前半段，并暴露两个真实缺陷** | 把载体换成 `NodeRuntime`（阶段二）并加了一条不依赖点击的自动诊断后，真机读数：<br>`HDSH-SHIM: Init called: creating exports object` ← 绑定完全正常（对照 E28 的"Init 从不被调用"）<br>`HDSH-AUTO 探测：available=true node=22.23.2`<br>**`HDSH-AUTO 安装内置核心：installed=["0.1.5-rc.2"] failed=0`** ← **核心树在设备上装好了**（约 5 秒）<br>`HDSH-AUTO 启动 Host：ok=false note=启动失败：Node 已启动，但 60000 ms 内 3120 端口未应答（Node 线程已经退出）`<br>入口脚本路径也确认：`/data/storage/el1/bundle/entry/resources/resfile/resources/app/main.js`。<br>**缺陷 ①（已修）**：OHOS 的 `fs.mkdirSync(path, true)` 与 Node 的 `recursive:true` **语义不同**——目标目录已存在时抛 `13900015 File exists`，于是"幂等"的 `ensureLayout()` 第二次调用必然失败，而安装与启动都会调它（首轮读数就是 `建立端侧核心目录失败：13900015 File exists`）。改为先 `fs.accessSync` 判存在。<br>**缺陷 ②（已改）**：`hdc shell` 对 `…/el2/base/…` 下的 **cacheDir 无读权限**（`Permission denied`），而 `files` 目录可读 ⇒ 入口脚本的诊断日志落点从 `cacheDir` 改为 **`filesDir`**（`HDSH_SANDBOX_HOME` 一并指向 filesDir，语义上也更对）。<br>**下一步要回答的**：Node 线程为何起来又退出——原因仍打在 stdout（设备不可见），而现在诊断日志会落在 `filesDir/hdsh-host.log`，取回即可定位。<br>**注意（环境类，非代码）**：期间连续两次 `aa start` 失败，真因是 `Error Code:10106102 The device screen is locked during the application launch`——**手机锁屏**，不是应用故障（最新崩溃日志仍是旧的 12:13 记录，没有新崩溃）。 | 高（真机读数 + 错误码） |
| E33 | **把 Node 的 stdout 抓进文件再由 shim 转成 hilog，终于看见了 Host 失败的真正原因（两条）** | 在 `dshhost.cc` 的 Node 线程里 `freopen(HDSH_SANDBOX_HOME/node-output.log, "w", stdout)` + `dup2(1,2)`，`node::Start` 返回后把该文件内容逐行 `OH_LOG_Print`（tag `HDSH-NODEOUT`）。真机读数：<br>`Warning: disabling flag --expose_wasm due to conflicting flags`（E19 预言的行为）<br>`[hdsh-host] profile 已就位：…/files/dsh/home/profiles/ondevice` ✅<br>`[hdsh-host] 导入 …/@deepseek-ai/dsh/lib/profile-boot-BP_C0vpU.js` ✅ → **dsh 本体确实被加载起来了**<br>`[hdsh-host] runProfile profile=ondevice args=--port 3120 --host 127.0.0.1 --no-open --skip-auth`<br>**`error: unknown option '--skip-auth'`** ← 根因①：这个选项在当前版本里不存在，于是 Host 从未起监听（`node::Start returned rc=1`）<br>**`!! unhandledRejection: ReferenceError: WebAssembly is not defined` at `lazyllhttp (node:internal/deps/undici/undici:5988:9)`** ← 根因②：**`--jitless` 让 `WebAssembly` 为 undefined，而 Node 自带的 undici（fetch/HTTP 客户端）会惰性加载 WASM 版 llhttp**。<br>⇒ 两条都要处理，且**顺序明确**：先修①（去掉/换掉那个选项，Host 才可能起监听，这是"dsh web 最小可用"的直接 blocker）；再处理②（它影响的是**对外** HTTP——LLM 调用，不影响端口是否监听）。<br>**方法论收获**：应用进程的 stdout 在设备上不可见（E23），`filesDir` 又**不给 hdc 读**（`Permission denied`）——所以"自己抓 stdout 再转 hilog"是这类问题的通用解法，值得保留在 shim 里。 | 高（真机读数 + `rc=1`） |
| E34 | **去掉不存在的 `--skip-auth` 后 `rc` 从 1 变 0；但 `jitless ⇒ 无 WASM ⇒ Node 自带 undici 起不来` 成为新 blocker** | 以 `dsh-web-app/lib/startup.js` 的权威选项表为准（合法项只有 `--host` / `--no-open` / `--port` / `--trusted-host`），把 `main.js` 的 args 改为 `['--port', PORT, '--host', '127.0.0.1', '--no-open']`。真机读数：<br>`[hdsh-host] runProfile profile=ondevice args=--port 3120 --host 127.0.0.1 --no-open`<br>**`HDSH-SHIM: node::Start returned rc=0`**（此前 `rc=1`）⇒ 选项问题已解决。<br>但 `unhandledRejection: ReferenceError: WebAssembly is not defined at lazyllhttp (node:internal/deps/undici/undici:5988:9)` 仍在，且**端口始终未应答**。<br>**分析**：undici 那条栈是 **client-h1.js（对外 HTTP）**，而 `--jitless` 让 `WebAssembly` 为 undefined（E19 已预言）。我们的 `main.js` 装了 `unhandledRejection` 处理器（诊断用），于是这个 rejection 被**吞掉而不是按 Node 默认的 throw 退出**——事件循环随之耗尽，`node::Start` 以 `rc=0` 正常返回，而 Web 服务**从未绑定端口**。⇒ **这是"不申请特殊权限"前提下的核心矛盾**：jitless ⇒ 无 WASM ⇒ Node 自带 fetch/undici 无法初始化。要么改掉核心树对这一路径的依赖（例如替换 `fetch` 实现），要么重新审视 jitless 的取舍——这是一个**架构级决策**，不是配置问题。<br>**本轮还有一个测量缺陷要修**：shim 只 `fread` 了前 2999 字节，而 Host 打印的带 token 的 `authenticatedUrl` 在**输出末尾**——被截掉了。下一步应改为读**末尾** N KB（或全量分行打）。 | 高（真机读数 + 调用栈） |
| E35 | **`jitless ⇒ 无 WASM ⇒ Node 自带 undici 无法初始化`是「dsh web 最小可用」的确切 blocker；`--no-experimental-fetch` 无效** | 把 shim 的 stdout 捕获改为**读全量**（262 KB 缓冲）后，完整因果链已取得：<br>`[hdsh-host] profile 已就位` → `导入 …/profile-boot-BP_C0vpU.js` → `runProfile … --port 3120 --host 127.0.0.1 --no-open` →<br>`unhandledRejection: ReferenceError: WebAssembly is not defined`，栈为 `lazyllhttp ← lib/dispatcher/client-h1.js ← client.js ← pool.js ← agent.js ← lib/global.js`（**undici 的全局 dispatcher 初始化**）→<br>`[hdsh-host] FATAL runProfile 返回了但没有 webServer` → `process.exit(1)` **由 dsh 自己调用**（`@deepseek-ai/dsh-app-boot/lib/index.js:1420`，正是 E23 警告过的 fail-loud；被我们的拦截器挡住，所以进程活下来、日志才拿得到）。<br>**实验否证**：在 argv 里加 `--no-experimental-fetch`（意图是不装 `globalThis.fetch`、从而不初始化 undici 全局 dispatcher）——**症状完全不变**（flag 被接受，无未知选项报错），说明仍有代码显式 require/使用 undici。<br>⇒ 结论：**这是架构级取舍，不是配置问题**。可选路线（尚未决定，代价都不同）：① 在**我们随包的核心树**里替换掉对外 HTTP 的实现（不依赖 undici/fetch）；② 改 Node 的 undici 让 llhttp 走非 wasm 实现（上游无此开关，需要自己改 Node）；③ 重新审视 jitless——但它正是"不申请 `ALLOW_WRITABLE_CODE_MEMORY`、保证顺利上架"的前提（E13：该权限在手机上不可申请）。**在没想清代价前我不动它。** | 高（真机读数 + 完整因果链 + 否证实验） |
| E36 | **本机可完整复现设备的 jitless/undici 失败（秒级迭代）；并否证"覆盖 fetch 就能绕开"** | ① **本地复现成立**：本机 Node **v22.22.0** 在 `--jitless` 下 `WebAssembly` 亦为 `undefined`，且同样打出 `Warning: disabling flag --expose_wasm due to conflicting flags`；用**真实入口脚本 + 真实物化核心树**（`dist/core/work/dsh-core-0.1.5-rc.2`）跑 `node --jitless hostcore/app/main.js`，得到与真机**逐帧相同**的 undici 栈与 `dsh: fatal load failure: ReferenceError: WebAssembly is not defined`。⇒ 这条链路此后可以**在 PC 上秒级迭代**，只把最终结论拿到设备上验证。<br>② **新增证据**：dsh 自己报的是 **`fatal load failure`**，即失败发生在 **profile/插件加载期**，而不是运行期请求。<br>③ **否证**：`Object.defineProperty(globalThis,'fetch',…)` 在任何人访问前直接覆盖（**不能先读原值**——Node 用 getter 惰性装 `fetch`，一读就触发 undici 初始化；我先前那行诊断 `const f = globalThis.fetch` 正是这种陷阱）——症状**不变**。⇒ 触发 undici 的不是 `fetch`。<br>**下一步**：用 `Module._load` 钩子 + 调用栈定位"哪个包在加载期拉进 undici"，再决定是**用 profile 的 patch 层禁掉该行**（`hostcore/profile/ondevice/cordis.patch.yml` 本就是为此设计的）还是替换其 HTTP 实现。 | 高（本机复现 + 否证） |
| E37 | **定位到两个 undici 使用者（`dsh-http-proxy` 与我们的调用栈完全吻合）；但"在 profile patch 层禁掉它们"两次都无效——而 patch 格式本身尚未验证** | 全量检索 `@deepseek-ai/*`：只有两个包引用 undici——<br>① `@deepseek-ai/dsh-web-fetch-http/lib/index.js:154,193` `await import("undici")`；<br>② **`@deepseek-ai/dsh-http-proxy/lib/index.js:389,427,442` `await import("undici")`，其中 442 行用 `getGlobalDispatcher`/`setGlobalDispatcher`** —— 与真机/本机看到的栈（`lib/global.js`，undici 的**全局 dispatcher**）**完全吻合**，所以它才是首要嫌疑（我第一轮禁错了对象）。<br>核心树里确实带 npm 的 `undici` 包（`node_modules/undici/package.json` 存在）。<br>**但两次禁用实验（`- id: web-fetch-http` / `- id: http-proxy` + `disabled: true`）症状都不变。**<br>**关键保留意见（必须下一轮先解决）**：上面那个 patch 写法**是我自己推断的、从未验证过**。如果格式不对，这两次否定结果**什么也证明不了**——它们只说明"我写的那份 patch 没起作用"。<br>**下一轮第一步**：先验证 patch 模式（查 dsh 的 patch 解析实现，或让 dsh 把"某行被禁用/某行加载失败"打到日志里），确认禁用**确实生效**之后，再重做这两次实验。 | 高（全量检索 + 与栈吻合） |
| E38 | **触发点确定到 `node:http` 的惰性 undici 加载器（`lazyUndici`）——即"谁碰了 `http.Agent`"** | 加大栈深度后拿到关键帧：<br>`… ← lib/global.js ← node:internal/deps/undici/undici:14896:52` ← **`at lazyUndici (node:http:123:21)`**。<br>⇒ 触发 undici 初始化的**不是 `fetch`**（覆盖 fetch 无效，E36），而是 **`node:http` 自带的惰性 undici 加载器**（Node 22 用 undici 实现 `http.Agent` / `globalAgent` 一类 API）。所以真正要找的是"**哪个模块在加载期访问了 `http.Agent`**"——`dsh-http-proxy` 仍是首要嫌疑（它配置代理必然涉及 agent/dispatcher）。<br>**另一条重要旁证**：把 `dsh-http-proxy` 从工作树移走后，错误**变成** `ERR_MODULE_NOT_FOUND: Cannot find package '@deepseek-ai/dsh-http-proxy' imported from …/dsh/lib/profile-boot-Dk-7KqJc.js` ⇒ 该包是被 **profile boot 在加载期静态 import** 的，**不能靠删包绕开**；同时 boot 文件哈希从 `BP_C0vpU` 变为 `Dk-7KqJc`，说明**我那份 patch 确实生效了**（patch 格式并非无效），只是禁行并不能阻止 boot 的静态 import。<br>**下一轮**：在 `dsh-http-proxy/lib/index.js` 里找模块级（非函数内）对 `http.Agent`/`globalAgent` 的访问；若确实在模块级，则可行的做法是在**入口脚本里预先定义 `http.Agent`**（或改走 dsh 支持的配置面关掉代理），而不是删包。 | 高（关键栈帧 + 删包实验） |
| E39 | **WASM 阻塞已消除（封 `node:http` 惰性 getter）；平台别名已生效；但撞上 E14 那条硬约束——从沙箱 dlopen 原生库被系统拦** | ① **WASM**：把 `node:http` 上全部惰性 getter（实测 `maxHeaderSize, globalAgent, WebSocket, CloseEvent, MessageEvent`）用 `defineProperty` 封掉后，`WebAssembly is not defined` 在**本机与真机上都消失**。触发点确认是 `at lazyUndici (node:http:123:21)`（E38）。<br>② **平台别名**：真机读数为 `platform=linux arch=arm64`（与 E22 同源：gyp `OS=linux` ⇒ Node 按 linux 编译），而移植件放在 `openharmony_arm64` ⇒ koffi 按 `platform_arch` 找不到。`pack-core.mjs` 增加**复制**形式的别名（`linux_arm64`/`musl_arm64`/node-pty 的 `linux-arm64`；沙箱禁符号链接）。生效后错误里已是 `…/build/koffi/linux_arm64/koffi.node`。<br>③ **换包必须能生效**：`BundledCore` 原先"版本已装即跳过"，导致换了核心包端侧仍跑旧树（实测：补别名后仍报旧错误）。改为用**资源包大小**做指纹（写在 `<coreDir>/.hdsh-bundled-stamp`），不一致就删旧树重装。这不只是为调试——**否则"核心版本安装/切换/回滚"这条功能形同虚设**。<br>④ **新 blocker（架构级）**：`Error loading shared library …/cores/0.1.5-rc.2/node_modules/koffi/build/koffi/linux_arm64/koffi.node: No error information` —— 即 **E14 那条**：运行时解包到**沙箱**里的原生库，`dlopen` 会被系统拦；而从 HAP 的 `libs/` 里加载则不会（E18 已证，且那里没有 `.codesign` 也能加载）。⇒ **核心树里的原生件（koffi / node-pty / sharp，共 3 个包）必须由 HAP 的 `libs/` 提供，不能靠运行时解包**。这也意味着"核心版本切换"只能切换纯 JS 部分，原生部分与**应用版本**绑定。<br>**下一步候选**（按代价排序）：把 3 个原生件放进 `entry/libs/`，并在入口脚本里设 `process.resourcesPath` 指向一个**能命中 loader 候选路径**的位置（koffi 会尝试 `process.resourcesPath` 下的若干相对路径，见其 index.js:469-481）；或对这三个包改用"随应用发版"的加载路径。 | 高（真机读数 + 与 E14 吻合） |
| E40 | **绕过沙箱 dlopen 限制的办法有效（重定向到 HAP 的 libs/）；但暴露出 musl 的符号作用域问题** | ① 实测 **hvigor 只打包扁平的 `libs/<abi>/*.so`**（把 `build/koffi/linux_arm64/koffi.node` 放进 `entry/libs/arm64-v8a/` 后，构建产物里**没有**它）⇒ 不能按 loader 的候选路径原样摆放。<br>② **有效办法**（不碰任何第三方包）：loader 都会先 `fs.existsSync(候选路径)`，而 `.node` 真正加载必经 `Module._extensions['.node']`——**同时接管这两处**，即可把沙箱内的 `.node` 请求重定向到 HAP `libs/` 下的扁平文件（约定 `lib<stem>.so`）。真机验证：`原生库重定向已启用：libs=/data/storage/el1/bundle/libs/arm64`，且错误从"沙箱加载被拦"变成 **`Error relocating …/libs/arm64/libkoffi.so: napi_fatal_error: symbol not found`** ⇒ **沙箱那条限制确实绕过了**。<br>③ 新问题：koffi 需要 85 个 `napi_*` 符号（含 `napi_fatal_error`），而 `libnode.so.127` **确实导出**它们（`nm -D` 可见 196 个 `napi_*`、`T napi_fatal_error`）。但**符号仍解析不到**——因为 Node 在端侧是**共享库**（非可执行文件），其符号默认不在全局作用域；在构造函数里补 `dlopen("libnode.so.127", RTLD_GLOBAL)` 返回 `ok` 却无效（musl 对"已以局部作用域加载过的库"提升为全局支持有限，而 `libdshhost.so` 的 `DT_NEEDED` 已先把它载入）。<br>**下一步（思路已明确）**：让 libdshhost **不要**在 `DT_NEEDED` 里带 libnode，改为在构造函数里**首次** `dlopen("libnode.so.127", RTLD_NOW|RTLD_GLOBAL)`，并用 `dlsym` 取 `_ZN4node5StartEiPPc`（该 mangled 名已在 `nm -D libdshhost.so` 的未定义符号里确认）。这样 libnode 的符号从一开始就在全局作用域，`.node` 才能解析 `napi_*`。 | 高（真机读数 + `nm -D` 双向核对） |
| E41 | **打通了原生库的符号作用域（已确证）；但"Host 是否起来"仍未读到，且中途亲手制造过一次启动回归** | ① **先制造了回归**：按 E40 的思路把 `libnode` 从 `libdshhost.so` 的 `DT_NEEDED` 里去掉后，真机出现**零日志**（连一条 `HDSH-*` 都没有），`libdshhost` 很可能根本没加载起来。原因：去掉 NEEDED 后 `dlopen("libnode.so.127")` 是按**裸 soname** 找库，而 bundle 的 libs 目录未必在默认搜索路径里。<br>② **修法（已确证）**：用 **`dladdr` 问出本 `.so` 自己的路径**，同目录下的 `libnode.so.127` 用**绝对路径**首载。真机读数：<br>`first-load /data/storage/el1/bundle/libs/arm64/libnode.so.127 (RTLD_GLOBAL): handle=ok node::Start=ok`<br>`constructor ran: registering 4 name forms` → `Init called: creating exports object`<br>⇒ 应用正常加载原生模块（无回归）、libnode 成为**首个加载者**并进全局作用域、`node::Start` 经 `dlsym` 解析成功、模块注册与 `Init` 均执行。**E40 那一步落地并验证。**<br>③ **未读到**：`HDSH-AUTO 启动 Host` 一行三次都没读到（26 秒时恰好未落盘，34 秒时缓冲已滚）。`HDSH-NODEOUT` 也为空——**注意它只在 `node::Start` 返回时才打印**，所以"空"**可能**意味着 Node 一直跑着（即 koffi 已解析到 `napi_*`、Web 服务起来）。**这是推断，不是证据；不算通过。**<br>④ **工具教训**：多次读不到日志的根因是 **hilog 默认缓冲太小**。`hilog -G 64M` 会被拒（上限 16M），**应当用 `hilog -G 16M`**——这一步我一直没做，白费了数轮读数。 | 高（真机读数）+ 未验证项已标注 |

| E42 | **libuv io_uring 补丁生效：Node 首次真正跑起来，失败点从"被 seccomp 杀"推进到 dsh 自身逻辑** | 按 E41 的线索改 `deps/uv/src/unix/linux.c` 的 `uv__use_io_uring` 为**无条件 `return 0`**（根因见 E41：`linux.c:482` 仅在 `!(flags & UV__IORING_SETUP_SQPOLL)` 时才读 `UV_USE_IO_URING`，所以环境变量那条路根本走不到），重建 libnode（`MAKE_EXIT=0`）后真机读数：<br>`platform=linux arch=arm64 versions={"node":"22.23.2",…,"uv":"1.51.0","v8":"12.4.254.21-node.56",…}`<br>⇒ Node 主循环、模块加载、我们入口脚本的**全部前置 shim**（`node:http` 惰性 getter 封堵、原生库重定向 `libs=/data/storage/el1/bundle/libs/arm64`）都执行到了。`SIGSYS(SYS_SECCOMP) syscall 425` 不再出现。**这是"自建 Node 能在鸿蒙上跑"的第一条完整正面证据。** | 高（真机读数 + 修复前后对照） |
| E43 | **koffi「`napi_fatal_error: symbol not found`」的真因不是符号不存在，而是 dlopen 出来的对象看不到 libnode 的符号；修法是给它一个依赖闭包** | ① **并列读数**（同在 `libdshhost.so` 构造函数里打印，`/system/lib64/…` 为设备真实路径）：<br>`diag1 RTLD_DEFAULT napi_fatal_error <- /system/lib64/platformsdk/libace_napi.z.so`<br>`diag2 RTLD_DEFAULT napi_get_undefined <- 同上`<br>`diag3 dlopen libkoffi.so = Error relocating …: napi_fatal_error: symbol not found`<br>② **`napi_fatal_error` 是 `libkoffi.dynsym` 里序号最小的 napi 符号**（index 5；紧接着 index 15/16 是 `uv_poll_start`/`uv_strerror`）⇒ 加载器只是报了**第一个**失败者，真实情况是"koffi 的作用域里**一个 Node 运行期符号都没有**"（它只有 `libc++_shared.so` / `libc.so` 两个 `DT_NEEDED`）。dlopen 出来的对象按「自身 + 依赖闭包 + 全局组」解析，而 libnode 以 `RTLD_GLOBAL` 首载也**进不了**后续 `.node` 的可见集（与 E40/E41 的实测一致）。<br>③ **修法（已验证）**：原地改写 `libkoffi.so` 的 `.dynstr`，把 `DT_NEEDED: libc++_shared.so`（16 字节）改成 `libnode.so.127`（14 字节）+ NUL 补齐——**等长内更短 ⇒ 纯字节替换，不动 ELF 任何结构/偏移**；libc++ 不丢：libnode 自己就 `DT_NEEDED libc++_shared.so`，传递依赖同样参与解析。工具 `tools/patch-native-needed.mjs`（幂等、带 `.orig` 备份与校验）。<br>④ 真机读数：`diag3 dlopen …/libkoffi.so = ok`。 | 高（诊断读数 + 修复验证） |
| E44 | **同一根因波及 `libpty.so`；且 node-pty 的报错文本会把人带偏** | ① `libpty.so`（node-pty）的 `DT_NEEDED` 同样只有 `libc++_shared.so`/`libc.so` ⇒ 同法修补后可用。<br>② **报错文本陷阱**：node-pty 的 `loadNativeModule`（`lib/utils.js:17-37`）把每个候选的异常都记进 `lastError` 再统一抛，最后留下的是**最后一个候选**的 `Cannot find module './prebuilds/linux-arm64//pty.node'`——而真正失败的是前一个候选（`../prebuilds/linux-arm64/pty.node` 解析成功、**重定位失败**）。若照着它的文本去补文件，会白忙。 | 高（源码 + 真机对照） |
| E45 | **换包触发核心重装时，同步删树会把 ArkUI 主线程锁死 → 系统以 `LIFECYCLE_TIMEOUT`+`SYS_FREEZE` 杀进程** | 现象：换了核心包后启动，应用"起来就消失"，`pidof` 为空且**连一条 `HDSH-*` 日志都没有**（SHIM 只到 `Init called`）。系统日志给出真因：<br>`AppLifeCycleManager: onProcessDied … pid:62616`<br>`Faultlogger_Base: AddFaultLog: … Reason:LIFECYCLE_TIMEOUT`<br>`FreezeDetector: SaveFreezeExtInfoToFile …`<br>根因：`BundledCore.installBundledCores` 用 `fs.rmdirSync(旧核心树)` 删 **216 MB / 25k 文件**，这是在**主线程**上做的（异步函数里的同步段同样阻塞 ArkUI 线程）。<br>**修法**：改为 `fs.renameSync` 挪成 `<coreDir>.stale-<ts>.tmp`（毫秒级元数据操作；`.tmp` 后缀被 `CoreStore.listInstalled` 明确忽略，不会被当成已安装版本），安装末尾再用**异步** `fs.rmdir` 清理；同时把 `CoreStore` 里 `stageFromZip` 的 `rmdirSync(target/staging)` 与 `evict()` 一并改成异步。**纪律：安装/删除路径上不得出现任何"删几万个文件"的同步调用。** | 高（系统日志 + 修复后进程存活） |
| E46 | **`dsh-attachment-local`（sharp）在端侧当前不可用，已在 profile patch 层首个"有证据的禁用行"** | 三个**彼此独立**的阻塞点：<br>① `libsharp-openharmony-arm64.so` 的 `DT_NEEDED` 含 `libvips-cpp.so.42`，而 HAP `libs/arm64-v8a/` 里没有它（`llvm-readelf -d` 实测）；<br>② sharp 的 JS 加载器要在 `@img/` 下**建软链**（`sharp-libvips-<platform>` → `img-sharp-libvips-<platform>`），沙箱**禁止符号链接**；<br>③ WASM 回退分支在 `--jitless` 下必然 `WebAssembly is not defined`。<br>⇒ 写入 `hostcore/profile/ondevice/cordis.patch.yml`：`- id: attachment-local` + `disabled: true`（dsh 的 fail-loud 会让它拖垮整棵树）。<br>**恢复线索**：`@ohos-ports/img-sharp-libvips-openharmony-arm64/lib/libvips-cpp.so.42.20.3` **确实在核心树里**（pack-core 输出可见），将来把它连同依赖搬进 HAP libs 并处理带版本号的 SONAME 即可。 | 高（`readelf` + 核心树实证） |
| E47 | **新架构级约束：dsh 全线要求 `koffi ^3.1.0`，而鸿蒙只有 2.16.2 移植版** | ① 实测：`dsh-win32-process` / `dsh-subprocess-local` / `dsh-sandbox-windows-acl` / `dsh-fs-local` 的 `package.json` 都声明 `koffi: ^3.1.0`；而核心树装的是 `@ohos-ports/koffi@2.16.2-beta.0`（npm 上该包**只有这一个版本**）。<br>② 后果：2.x 的 `koffi.struct(...).size` 为 `undefined` ⇒ `dsh-win32-process` **模块顶层**断言 `STARTUPINFOW layout mismatch: koffi computed undefined, expected 104` 抛错 ⇒ `subprocess-local` 与 `sandbox-local` 双双 import 失败 ⇒ 整棵插件树 fail-loud（`index 29/30`）。<br>③ 上游 3.x 走 `@koromix/koffi-<os>-<arch>` 分包，**没有 openharmony 变体**；其 `linux-arm64` 包内的 `musl_arm64/koffi.node` 的 `DT_NEEDED` 是 `libstdc++.so.6` / `libgcc_s.so.1` / `libc.musl-aarch64.so.1`——**鸿蒙都不提供**（鸿蒙是 libc++/libc 命名）⇒ 不能直接借用。<br>④ 结论：**必须自建 koffi 3.x 的 OHOS 版**（源码随包发布：`src/koffi/src/**` + `vendor/node-api-headers`，且 3.x 自带 FFI 实现、不依赖 libffi）。 | 高（版本声明 + 真机报错 + `readelf`） |
| E48 | **转模拟器路线的前提与已完成的第一步** | 本开发机为 **AMD64** ⇒ 本地鸿蒙模拟器（`devecocli emulator`，Pura 90 / HarmonyOS 6.1.1）镜像为 **x86_64**，而现有原生栈（自建 `libnode.so.127`、koffi、node-pty）**只有 `openharmony-arm64`**（`@ohos-ports` 无 x86_64 变体）⇒ 转模拟器必须先把这些原生件做成 x86_64。<br>已完成：`toolchain-env.sh` 与 `build-node-ohos.sh` 参数化 `OHOS_ARCH=arm64\|x64`（triple、`--dest-cpu`、按架构分离构建日志，arm64 专属的 zlib CRC32 修补在 x64 下跳过）；顺带修掉一个调用坑：`HERE` 原先在脚本中途 `cd` 到源码树**之后**才用相对路径解析，以相对路径调用必然失败（`cd: tools/node-runtime: No such file or directory`）。 | 高（命令实测） |

| E49 | **阶段一 Electron 链彻底移除；应用首次在模拟器（x86_64）上常驻** | ① **崩因定位**：`AppAbilityStage` 原先继承 `web_engine` 的 `WebAbilityStage`，其 `onCreate` 会 `initNativeContext(libadapter.so)`；x86_64 上该库不存在 ⇒ 模拟器日志<br>`MMG: load libadapter.so failed` → `ArkCompiler: TypeError: Cannot read property getNativeContext of undefined` → `AppKit: com.hnmrxz.hdsh is about to exit due to RuntimeError`。<br>② **处置**：改为继承裸 `AbilityStage`；删掉 `view/RuntimeHost.ets`、`Index.ets` 的挂载点与 `MOUNT_ELECTRON_RUNTIME_HOST`、`entry` 对 `web_engine` 的依赖，以及 `libadapter.so`/`libelectron.so`/`libffmpeg.so`（**HAP 因此减约 190 MB**）。端侧运行时此后只有一条路径：`libdshhost.so` → `libnode.so.127`。<br>③ `entry/build-profile.json5` 增加 `abiFilters: ["arm64-v8a","x86_64"]` ⇒ HAP 内同时含 `libs/arm64-v8a/*` 与 `libs/x86_64/libdshhost.so`。<br>④ **模拟器实测**（`devecocli emulator start "Pura 90"`；hdc `127.0.0.1:5555`；锁屏须先 `power-shell wakeup` + `uitest uiInput swipe`）：安装成功、启动成功、**进程常驻（pid 6356）**，`HDSH-SHIM` 打出 `diag2`/`diag3`/`constructor ran`/`Init called` ⇒ **ArkTS↔原生模块在 x86_64 上同样绑得上**。 | 高（模拟器实测） |
| E50 | **koffi 3.x 的 OHOS 自建要素已逐条摸清（下一段关键路径）** | `koffi@3.2.1` 的 npm 包**带完整源码**：源码清单 `src/{ffi,call,interp,parser,type,util,uv,win32}.cc` + `lib/native/base/base.cc` + 按架构 `src/abi/{arm64.cc,arm64_asm.S}`（arm64）或 `src/abi/{x64sysv.cc,x64sysv_asm.S}`（x64）；需生成 `trampolines/gnu.inc`（`node src/trampolines.cjs <dir> 8192`）；宏 `FELIX_TARGET=koffi NAPI_VERSION=NAPI_VERSION_EXPERIMENTAL NODE_ADDON_API_DISABLE_CPP_EXCEPTIONS NODE_ADDON_API_REQUIRE_BASIC_FINALIZERS`，`VERSION=<ver>` 仅加在 `ffi.cc`；选项 `-fno-exceptions -fno-rtti -fwrapv -fno-semantic-interposition -fvisibility=hidden -fdata-sections -ffunction-sections`、链接 `-Wl,--gc-sections`；`add_node_addon`（CNoke）等价于 `add_library(koffi MODULE …)` 且 `PREFIX=""`、`SUFFIX=".node"`；另需 `vendor/node-addon-api`（npm 包**未随包发布**，需另装）。<br>另外：x86_64 的 Node 交叉编译已按 `OHOS_ARCH=x64` 启动，并确认**目标对象确实由 OHOS clang 编译**（日志中 `x86_64-unknown-linux-ohos-clang++` 出现 1495 次，未见宿主 g++ 编译目标对象——这正是 `toolchain-env.sh` 注释里警告过的那类静默失败）。 | 高（源码/构建清单逐条核对） |

| E51 | **koffi 3.x 已用应用自身的 OHOS 工具链自建成功（arm64），并把 E43 的"依赖闭包"变成结构性事实** | ① **取源与生成**：`node tools/fetch-koffi.mjs` —— koffi 的 npm 包**自带** `vendor/node-addon-api` 与 `vendor/node-api-headers`，唯一需要生成的是 `trampolines/*.inc`（用 host node 跑 `src/koffi/src/trampolines.cjs`）。<br>② **编入方式**：不走 WSL（那里**没有 node、没有 cmake**），而是把 koffi 作为 **CMake `MODULE` 目标 `koffi`** 加进 `entry/src/main/cpp/CMakeLists.txt`——hvigor 天然按 ABI 各编一份、按 `lib<name>.so` 打进 HAP，目标名 `koffi` ⇒ `libs/<abi>/libkoffi.so`，**正好命中原生库重定向的命名约定**（E40）。<br>③ **读数**：`BUILD SUCCESSFUL`；HAP 内 `libs/arm64-v8a/libkoffi.so` = **1,600,256 B**（原 `@ohos-ports` 2.16.2 预编译件为 4,639,168 B）；`llvm-readelf`：`Class: ELF64 / Machine: AArch64`，`NEEDED: [libnode.so.127, libc++_shared.so, libc.so]` ⇒ 依赖闭包**由链接器保证**，不再依赖事后字节补丁。（dynsym 里仍有 88 个 `UND napi_*` 是正常的：链接共享库不会内联符号，运行期由 DT_NEEDED 解析。）<br>④ **守卫**：某 ABI 若没有 `libnode.so.127`，就**不编** koffi——OHOS 工具链默认带 `-Wl,--no-undefined`，硬编只会得到一个运行时必崩的 `.node`；x86_64 因此在 x64 libnode 到位前自动跳过。<br>⑤ **仍待**：删掉 profile 里那两条临时禁用行（`subprocess-local`/`sandbox-local`）并在有 koffi 的架构上复测。 | 高（HAP 产物 + `llvm-readelf` 读数） |

| E52 | **加入启动阶段标记（BOOT_xx）；并借此在**本机以端侧同参**跑通整条 Host 启动链——"dsh web 最小可用"这条 gate 达成** | ① **动机**：端侧只有 hilog 可看（应用进程 stdout 不可见，E23），而"Host 没起来"最贵的成本是**猜停在哪一步**。于是把启动切成显式阶段：`BOOT_00_NODE_START` → `BOOT_10_ENV_READY` → `BOOT_20_CORE_FOUND` → `BOOT_30_PROFILE_READY` → `BOOT_40_PROFILE_BOOT` → `BOOT_50_DSH_INIT` → `BOOT_60_HTTP_BIND` → `BOOT_70_HTTP_READY`；失败一律打 `BOOT_ERR after=<最后一个成功阶段>`。`BOOT_70` 是**自探**（`node:http` GET 一次 `/`），因为"dsh 说它绑了端口"与"端口真的应答"是两件事。<br>② **本机读数**（`node --jitless --no-experimental-fetch hostcore/app/main.js`，env/argv 与端侧同一口径）：<br>`BOOT_40_PROFILE_BOOT entry=profile-boot-BP_C0vpU.js (+139ms)`<br>`dsh web: http://127.0.0.1:3121/?token=U6kr…` ← **带 token 的 authenticatedUrl**（正是端侧客户端需要的东西）<br>`BOOT_50_DSH_INIT runProfile 已返回 (+9297ms)` → `BOOT_60_HTTP_BIND port=3121` → `HDSH_READY {"port":3121,…}`<br>`BOOT_70_HTTP_READY GET / → HTTP 401（43ms）`<br>⇒ **Host 绑定成功、URL 已打印、端口真的应答**（401 是 dsh 的 browser-trust fence：无 token 的正确响应）。**E31 以来一直在追的"Host 是否真的起监听"，在本机以端侧同参得到肯定回答。** | 高（本机读数 + 阶段化） |
| E53 | **在 dsh 里"禁一行"不是局部操作：会沿服务依赖链把消费者拖成 pending，而 boot 要求所有行都 activate** | 第一轮本地读数（只禁了 `subprocess`/`sandbox`/`bash-sandbox`/`pwsh-sandbox`/`tool-bash`/`tool-pwsh`/`attachment-local`/`open-in-app`）：<br>`dsh: 4 entries did not activate`<br>`permission-presets: pending (waiting for service: shell)`<br>`api-session-controller: pending (waiting for services: attachments, fileUploads)`<br>`client-file-upload: pending (waiting for service: attachments)`<br>`client-ui-deliverables: pending (waiting for service: sessionController)`<br>⇒ 两条链：`attachment-local`(attachments) → `file-upload`(fileUploads) → `session-controller`(sessionController) → 客户端行；`bash-sandbox`/`pwsh-sandbox`(shell) → `permission`(permission-presets)。<br>**另一个坑**：patch 里的 **id 与包名不同名**——`@deepseek-ai/dsh-permission-presets` 的行 id 是 **`permission`**。第一轮我写成 `permission-presets`，patch **静默不生效**（没有任何报错），表现为"禁了却仍 pending"。写 patch 前必须回查 `hdsh-core.json` 的 `plugins[].id`。 | 高（本地读数 + 静默失效的实证） |

| E54 | **真机（arm64）达成主链：应用自足跑起 dsh Host，`127.0.0.1:3120` 处于 LISTEN，ArkUI 客户端已接线** | 装机启动后（`pid=43478`）逐阶段读数：<br>`BOOT_00_NODE_START pid=43478 node=v22.23.2 platform=linux/arm64 jitless=true`<br>`BOOT_10_ENV_READY core=…/dsh/cores/0.1.5-rc.2 … port=3120` → `BOOT_20_CORE_FOUND` → `BOOT_30_PROFILE_READY` → `BOOT_40_PROFILE_BOOT`<br>`dsh web: http://127.0.0.1:3120/?token=QMHMWOSL…`（**带 token 的 authenticatedUrl**）<br>`BOOT_50_DSH_INIT (+2065ms)` → `BOOT_60_HTTP_BIND port=3120` → `HDSH_READY {"port":3120,…}` → `BOOT_70_HTTP_READY GET / → HTTP 401（10ms）`<br>**内核态证据**（`cat /proc/net/tcp`）：`0100007F:0C30 … 0A`（**LISTEN**）与 `0100007F:0C30 0100007F:946E 05`（**已建立**）⇒ 端口真的在听，且已有回环连接。<br>**ArkTS 侧**：`HDSH-AUTO 探测：available=true node=22.23.2` → `安装内置核心：installed=["0.1.5-rc.2"] failed=0` → `启动 Host：ok=true url=http://127.0.0.1:3120` → `HDSH-NodeRuntime: Host 已就绪：127.0.0.1:3120`；NETSTACK 记录 `response_code:401 … dst_port:3120`（无 token 时的正确响应）。<br>⇒ **"HAP 内嵌 Node 运行时 + dsh 核心、本端起 Host、ArkUI 作为其客户端页面"这条主链在真机上首次完整跑通**（E31 起追的目标达成）。<br>**紧接着的一件事**：客户端目前只探 `/` 拿到 401——要真正驱动会话 UI，必须把 dsh 打印的 **token** 接到客户端（现成的取法：入口脚本在 runProfile 期间拦截 stdout，抓 `dsh web: http://…?token=…` 后写进 `HDSH_READY`）。 | 高（真机逐阶段 + 内核态 socket + ArkTS 侧读数） |

| E55 | **"端口通了但页面说没有启动核心"的根因是状态源错了层；token 已接通客户端** | ① **根因**：`DshHost.status()` 用 `this.handle !== null` 判断"已就绪"，而 `handle` 是**实例字段**；页面每次刷新都 `new DshHost(...)`（Index 的 `coreHost`）⇒ 新实例永远报 `IDLE`"核心已安装但未启动"，而事实是端口已在监听。**修法**：给 `RuntimePort` 加 `isRunning()`（进程级事实），`NodeRuntime` 实现为问引导层 `dshhost.isHostRunning()`，未接线载体如实返回 false；`status()` 改为 `this.handle !== null \|\| this.port.isRunning()`。**教训：进程级事实不能记在可多次构造的对象里。**<br>② **token 接线**：dsh 的 `/api` 前是 browser-trust fence，不带 token 一律 401（入口自探得到 401 是**正确**响应），而 token 只出现在 `dsh web: http://127.0.0.1:3120/?token=…` 这一行里；`libdshhost` 没有"读 Node 输出"的 API ⇒ 通道走**文件**：入口脚本在 stdout 上做一次性匹配（对上游零 patch）写 `<HDSH_HOME>/host-ready.json`，并打 `BOOT_65_AUTH_URL`；`NodeRuntime.start()` 读该文件把**带 token 的 URL** 放进句柄；`EntryAbility.adoptLocalHost()` 复用既有的 `stripTokenQuery` + `persistConnectedHost` 路径发布到 `KEY_HOST_URL/KEY_HOST_TOKEN/…`（页面用 `@StorageProp` 消费，改动零侵入）。<br>③ **真机读数**：`BOOT_65_AUTH_URL port=3120 tokenLen=43 → host-ready.json` → `已从 host-ready.json 取到带 token 的地址（tokenLen=43）` → `HDSH-AUTO 已接入本端 Host：base=http://127.0.0.1:3120/ tokenLen=43`；`/proc/net/tcp` 出现到 `0C30` 的 ESTABLISHED 连接。 | 高（真机读数 + 症状-根因对应） |

| E56 | **token 已到 AppStorage，但页面的自动连接路径**从未被执行**——已加 `HDSH-CONN` 诊断定位** | ① 真机读数（E55 的 token 接线生效）：`HDSH-AUTO 已接入本端 Host：base=http://127.0.0.1:3120 tokenLen=43`，`/proc/net/tcp` 有到 `0C30` 的 ESTABLISHED；但 NETSTACK 里到 3120 **只有就绪探测那一次 401**，没有任何认证请求 ⇒ 客户端确实没连。② 逐层排除：把 `waitForRememberedHost()` 的有界等待从 **4 秒**放到 **45 秒**（本端 Host 要 15~20 秒才发布 token）、把 `baseUrl` 的**尾斜杠**规范化（`stripTokenQuery` 会留下 `…:3120/`，而 `parseTarget` 期望 `scheme://authority`；带尾斜杠时 `SessionHub.configure` 静默返回 false ⇒ 既不请求也不报错）——**两处都不是原因**。③ **❗上一轮记错的一条，这里更正**：当时写的是"加了 `HDSH-CONN` 日志后一条都没有 ⇒ `waitForRememberedHost()` 从未被调用"。**该结论不成立**——那次查询期间真机已经断开（`hdc` 报 `Device not found`），所以连 `BOOT_` 日志都取不到，空读数**什么也不能证明**。实际调用点就在 `pages/Index.ets:624`（`aboutToAppear` 里），路径**是被走的**。<br>⇒ 真正要查的是**它内部为什么早退**。**已按代码推演定位（不需要设备）**：Ability 在"还没有记住的 Host"时会把 `KEY_HOST_TOKEN_STATE` 置为 **`absent`**，而页面的轮询只在 `state === 'pending'` 时继续等 ⇒ **250 ms 就退出**、并因 `rememberedUrl` 为空直接 `return`，此时 `autoConnectAttempted` 已置位 ⇒ 15~20 秒后本端 Host 发布 url+token 时**再没有人重试**。**修法**：判据从"状态是不是 `pending`"改成"**是否真的具备连接条件**"（`state === 'found' && url 非空 && token 非空`），否则在 45 秒预算内继续等（`absent` 与 `pending` 一视同仁）。诊断日志（`HDSH-CONN` 开始等待/等待结束/无记住的地址/`configure` 被拒）已就位，**待有设备时验证**。<br>⑤ **形态核对（零设备，已做完）**：`composeHostUrl('http://127.0.0.1:3120', token)` 走的是 `url + '/?token=' + token`（`Index.ets:3061`），与 dsh 自己打印的 `…/?token=…` **逐字一致**；`parseTarget`（`HostAddress.ets:157`）用 `extractToken` + `parseHostAddress` 解析，接受该形态；尾斜杠已在 `adoptLocalHost` 里去掉，token 字符集是 `[A-Za-z0-9_-]`（无编码问题）。⇒ 组合/解析这一环**没有剩余障碍**，下一轮有设备时应能直接连上。 | 中（两处排除成立；早退分支已定位并修复，缺端侧验证） |④ 顺带修掉一个编译坑：`Index.ets` 里没有 `hilog`/`DOMAIN`/`TAG`，加诊断时一度把构建打挂（这类错误必须先看 `CompileArkTS` 的 ERROR 明细，别被一堆 WARN 带偏）。 | 高（逐层排除 + 决定性读数） |

| E58 | **客户端已连上本端 Host（token 被接受），剩下的是请求形态被判 400** | ① **否证一个假设**：上一轮怀疑"上一个进程遗留的 Host 占着 3120 导致新进程起不来"。实测 `aa force-stop` 后**进程与端口都释放了**（`pidof` 空、`/proc/net/tcp` 无 `0C30`）⇒ **没有孤儿 Host**，该假设不成立。<br>② **完整读数**（同一次运行）：`BOOT_00…BOOT_40` → `BOOT_65_AUTH_URL tokenLen=43` → `BOOT_50/60` → `BOOT_70_HTTP_READY GET / → 401（+1442ms）`；`HDSH-CONN` 打出 **第 1 次**与**第 2 次**自动连接（后者即 token 刷新后按 `(url,token)` 变化重连——E57 的修复在真机上生效），随后 `等待结束 waited=45000ms attempts=2`。<br>③ **流量**：NETSTACK 统计到 3120 上 **401 ×1**（入口的就绪探测，无 token 的正确响应）+ **400 ×8** ⇒ 客户端确实发出了 8 个请求，**token 已被接受**（不再是 401），但**请求形态被 dsh 判为 400**。<br>**下一轮的唯一起点**：抓到那 8 个请求的形态（路径/方法/`content-type`/body 封装）与 dsh 的期望对照——`connection` 侧走的是 `POST <baseUrl>/api/<endpoint>` + JSON 封装（`HttpClient.ets`），而 mux 是 WS；400 通常意味着封装或 `content-type` 不符。建议直接从 dsh 自己的输出取（`node-output.log` 已 tail 到 hilog 的 `HDSH-NODELIVE`），它比 NETSTACK 更接近原因。 | 高（真假假设各一条 + 明确下一步） |

| E59 | **400 的真身是 WebSocket 升级被 403（dsh 的 lws 自己说了）；方向是 token→cookie 兑换缺失** | 从 dsh 自身输出（`HDSH-NODELIVE`，即 `node-output.log` 的 tail）拿到决定性一行：<br>`N: lws_create_context: LWS: 4.3.3-OpenHarmony, NET CLI SRV H1 H2 WS ConMon IPV6-on`<br>`W: [wsicli|0|WS/h1/default/127.0.0.1]: lws_client_ws_upgrade: got bad HTTP response '403'`<br>⇒ ① Host 的 HTTP/WS 栈是 **libwebsockets**；② NETSTACK 里那 `400 ×8` 对应的是**客户端反复尝试 WS 升级**（每次 connect 一个 lws context，日志里可见 3 个）；③ **升级被 403 拒**——token 在 HTTP 层已被"看见"，但 `/api/remote.mux` 的 WS 握手仍过不了 browser-trust fence。<br>**判断（待验证）**：缺 **token → cookie 兑换**这一步——dsh 的 fence 用的是 cookie；客户端 `AuthSession.ets:78` 正是在拼 `…/?token=…` 去换 cookie，之后的 `/api` 与 WS 都必须带它。下一步应查 `SessionHub.connect()` 的顺序：是否先完成兑换、是否把 `Set-Cookie` 存进 `Cookies` 并在 WS 升级时带上；同时确认兑换请求本身是否成功（它若失败，客户端可能仍继续去升 WS，于是只看到 403）。 | 高（dsh 原始日志） |

| E60 | **请求序列落地：客户端在发 `/api` 的 POST RPC，被 dsh 判 400（不是认证问题）** | 按时间序取 NETSTACK 的方法+状态码（同一真机运行）：<br>`GET / → 401  upload=0`（入口就绪探测，无 token 的正确响应）<br>`POST → 400  upload=131/126/126/123/131/126/126/123`（**8 次，body 约 123~131 字节**）<br>⇒ ① 客户端发的是 **POST + 小 JSON 封装**，即 `HttpClient` 的 `/api/<endpoint>` RPC（**不是** WS 握手）；② dsh 用 **400** 拒绝。**关键判据**：`GET /` 无 token 得到的是 **401**，所以 400 不是"没认证"，而是**请求形态不对**——路径、`content-type`、封装三者之一与 dsh 的期望不符（dsh 的 400 响应里 `content-type` 为空）。<br>③ 同时 dsh 的 lws 日志记录 `ws_upgrade: got bad HTTP response '403'`（E59）⇒ **两条通道都不通**，但 400 是更靠前、更可修的一环。<br>**下一轮的唯一起点**：对照 `connection/.../HttpClient.ets` 实际发出的「路径 + `content-type` + body」与 dsh 期望的 `/api/<endpoint>` + JSON 封装；最省事的取证方式是在 Host 侧把每个请求的 `method + url + content-type` 打到 `HDSH-NODELIVE`（入口脚本可挂一个只读的请求记录器），一次运行即可定论。 | 高（方法/状态/体积按序读数） |

| E61 | **客户端"跳过 token→cookie 兑换、直接打 `/api`"——这是 400/403 的共同上游原因** | ① **封装格式本身没问题**：`HttpClient.send()`（`connection/.../HttpClient.ets:64-90`）在 POST 时设 `content-type: application/json`、带 `accept`、有 cookie 就加 `cookie` 头、body 走 `extraData` ⇒ E60 里那 8 次 `POST`（body 123~131B）形态是**合法**的。<br>② **真正的缺口**：按协议（`connection/.../Cookies.ets:5` 的注释）认证流程是 **`GET /?token=<token>` → 303 + `Set-Cookie`**，之后 `/api` 与 mux 都要带该 cookie。而把 E60 的序列按"谁发的"对齐：`GET / → 401` 是**入口脚本**的就绪探测（`node:http`，同进程所以也进 NETSTACK），**其余 8 次 POST 全部来自客户端**——序列里**没有那条带 token 的兑换请求**。⇒ 客户端在 cookie jar 为空的情况下直接打 `/api`：dsh 以 **400** 拒（mux 侧则 **403**）。<br>③ **下一步（已收敛）**：查 `SessionHub.connect()` → `Connection`（其 `auth = new AuthSession(jar, http, address)`，见 `Connection.ets:233/263/285`）为什么没走兑换——要么兑换未触发（例如把"已授权"状态当了真），要么它失败后仍继续后续请求。**取证方式**：在 `AuthSession` 发起兑换处与其解析 `Set-Cookie` 处各打一行 hilog，一次运行即可区分"没发"与"发了但没存住"。 | 高（把"谁发的"对齐后定位到缺失的那一步） |

| E62 | **收窄到最后一个分支：兑换被"已有会话"跳过（持久化的旧 cookie 被复用）** | 逐步核对调用链，**排除**了"没接上"的可能：<br>· `Connection.configure(input)` 用 `parseTarget` 解析并 `this.pendingToken = target.token`（`Connection.ets:224/225/232`）⇒ 只要地址串带 `?token=`，token 就进了 `pendingToken`；<br>· `SessionHub.connect()` **确实**在第 535 行调用 `this.conn.authenticate()`；<br>· `AuthSession.exchange()` 的实现本身正确（`GET <baseUrl>/?token=<token>`，`send(..., false, undefined, 15000, true)`，成功判据是 cookie jar 有值）。<br>⇒ 既然 NETSTACK 里**没有**那条兑换请求，只剩"它被跳过或提前返回"。最符合全部读数的解释：客户端**复用了上一个进程持久化的 cookie**（`persistCookies` 会落盘），于是认为"已有会话"而不去兑换；而 **dsh 每个进程重新生成信任密钥**（token 也是每次新生成），旧 cookie 因此被拒——`/api` 得到 **400**、WS 升级得到 **403**，两者与"cookie 存在但不被承认"完全吻合（若完全没 cookie，`/api` 更可能是 401）。<br>**下一轮（一步可验）**：在 `SessionHub.connect()` 的 `authenticate()` 前后各打一行（是否走了 authenticate、`hasSession()` 当时是真还是假），并在本端 Host 的 `adoptLocalHost` 里**先清掉该 authority 的 cookie 再连**（本端 Host 每次重启都是新信任密钥，旧 cookie 必须作废）。 | 中高（调用链逐段排除 + 与状态码吻合的解释，尚缺一行日志确认） |

| E63 | **造出第二个核心版本的测试夹具（不必依赖设备），为"安装/切换/回滚"的真机验收做准备** | 背景：核心版本管理（`CoreStore`/`decideActivation`/`decideRollback`/`evictionCandidates`）此前**只有纯函数单测**，缺"真实两个版本"的端到端验证；而造第二个版本不需要设备，于是先把它做好。<br>做法（都对现有可用形态零影响）：<br>① 复制 stage：`dist/core/work/dsh-core-0.1.5-rc.2` → `…-rc.3`（内容相同，只换版本号——验收的是**版本管理**逻辑，不是两版代码差异）；<br>② 生成 `hostcore/core-recipe-rc3.json`（`pack-core.mjs` 支持 `--recipe`），只改 `coreVersion`；<br>③ `node tools/pack-core.mjs --recipe hostcore/core-recipe-rc3.json --skip-install` ⇒ **已产出** `dist/core/dsh-core-0.1.5-rc.3-openharmony-arm64.zip`（66.0 MB / 28990 条目 / sha256 `63a1b238…`）。<br>**已知瑕疵（刻意留着，够用即可）**：打包汇总行仍打印"核心版本 0.1.5-rc.2"——因为 stage 是从 rc.2 复制的，树内 `hdsh-core.json` 的 `coreVersion` 仍是 rc.2。端侧的**版本识别走归档文件名**（`parseArchiveName` + `topLevelDir`），两棵树的目录名/包名都是 rc.3/rc.2 ✓，所以装两份、切换、回滚仍然成立；只是核心页展示的树内元数据会显示 rc.2，验收时不要把它当成失败。若要更干净，重跑 `pack-core` 时让它重写树内元数据即可（同一命令已支持）。<br>**踩坑记录**：第一次生成 recipe 用 `Get-Content -Raw` + `-replace` + `Set-Content`，写进了一个控制字符，`pack-core` 在 `JSON.parse` 直接报 `Bad control character in string literal`。改用 `[System.IO.File]::ReadAllText(path, UTF8)` + `String.Replace` + `WriteAllText(…, UTF8Encoding($false))` 后正常——**改 JSON/文本文件时不要经过 PowerShell 的默认编码与正则**。<br>验收步骤（待设备）：装 rc.2 → 起 Host（已达成）→ 放入 rc.3 zip 并重装 → 期望 `installed=[rc.2, rc.3]`、`BundledCore` 装出第二棵树 → 切到 rc.3（`activate`）→ Host 重启后 `state.current=rc.3` → 回滚（`decideRollback`）→ `current=rc.2` 且 rc.3 树仍在（不误删 current/previous）。 | 高（夹具已产出，验证待设备） |

| E64 | **恢复 `attachment-local` 的前置已量化：46 个依赖库 / 30.5 MB，且 SONAME 普遍带版本号** | 实测 `dist/core/work/…/node_modules/@ohos-ports/img-sharp-libvips-openharmony-arm64/lib/`：**46 个文件、合计 30.5 MB**，且除 `index.js` 外**几乎都带版本号文件名**（`libglib-2.0.so.0.8800.2`、`libcairo.so.2.11804.4`、`libfreetype.so.6.20.6`、`libexpat.so.1.12.3`、`libffi.so.8.5.0`…）。<br>这直接决定工作量：① hvigor 只打包 `libs/<abi>/*.so`（E40 实测），所以**每个库都要改名**成 `*.so`；② 改名后，**它自己的 SONAME 必须同步改**（否则别人按旧 SONAME 找不到它）；③ **每个依赖方的 `DT_NEEDED` 也要逐个改**成新名字。三者缺一，运行时就是 `cannot open shared object` 或 `symbol not found`。<br>现状：`tools/patch-native-needed.mjs` 只覆盖第 ③ 类（原地改写 `DT_NEEDED`，零结构风险），**还缺** `--soname` 模式与"按清单批量改名"的一步。另：`libsharp-openharmony-arm64.so` 自身还 `DT_NEEDED libvips-cpp.so.42`，sharp 的加载器还要在 `@img/` 下建软链（沙箱禁止，E46）——**软链那一条必须换成"直接给对路径"或去掉依赖**。<br>结论：这**不是"搬几个文件"**，而是一次有明确清单的 46 库打包工程；放到"协议连通 + 两版本验收"之后再动，优先级低于当前唯一阻塞（客户端连不上 Host）。<br>**可复跑步骤（工具已齐：E65 的 `--set-soname`/`--rename-file` + 原有 DT_NEEDED 模式）**：<br>① 取旧→新名字映射：每个库的 SONAME 用 `llvm-readelf -d` 读出来（例：文件 `libglib-2.0.so.0.8800.2` → SONAME `libglib-2.0.so.0`），新名 = SONAME 去掉 `.so` 之后的版本段（`libglib-2.0.so`）；<br>② 按新名拷进 `entry/libs/arm64-v8a/`，对每个副本执行 `--set-soname <新名>`；<br>③ 对**每个**副本，把仍指向旧 SONAME 的 `DT_NEEDED` 改成新名（对 46 个库两两比对一遍）；<br>④ 最后把 `libsharp-openharmony-arm64.so` 的 `DT_NEEDED libvips-cpp.so.42` 改成新名，并解决它加载器要建软链那一步（E46）。<br>**【必须记的一条】**新版名字若**比旧 SONAME 长**就无法原地改写（工具会明确报错而不是静默）——遇到这种库要么选更短的别名，要么走"重建该库"的路。 | 高（清单、体积与工具能力均为实测） |

| E65 | **认证已通；唯一剩下的错误是 WS 升级被 403（且 dsh 侧无任何 API/封装报错）** | 真机读数（同一次运行）：<br>`HDSH-CONN 第 1 次` → `HDSH-AUTH ok=false err=无法连接到 127.0.0.1:3120（code=2300007）`（250 ms 时 Host 还没起监听；Host 实测约 1.4 s 就绪）<br>`HDSH-CONN 第 2 次` → **`HDSH-AUTH ok=true`**（token→cookie 兑换成功）→ `err=会话列表获取失败：连接在传输中断开`<br>dsh 自身输出（`HDSH-NODELIVE`）里**只有一种错误、且每 ~6.5 s 重复一次**（=客户端重连周期）：<br>`W: [wsicli|0|WS/h1/default/127.0.0.1]: lws_client_ws_upgrade: got bad HTTP response '403'`<br>⇒ ① **认证不再是问题**（E61/E62 的候选 A"未提供 token"与 B"旧 cookie 无效"**都被这条读数排除**：兑换确实成功）；② 剩下的唯一硬错误是 **mux 的 WebSocket 升级被 fence 拒 403**；③ 数据流（会话列表）失败与之因果一致——数据流经 mux，mux 不通即"连接在传输中断开"。NETSTACK 里 `GET / → 401`（入口就绪探测）与 `POST → 400`（body 123~131B）是旁证。<br>**下一轮唯一检查点**：`RemoteMux` 的 WS 握手**是否真的带上了 cookie**——其构造收到 `cookieProvider`（`RemoteMux.ets:117/120`）并在 `:216-220` 取用、塞进 `options.header` 后交给 `socket.connect(url, options)`（`:242`）。<br>**已用官方文档排除"API 不支持"这一可能**（`devecocli docs read` 读 `API参考/网络/Network_Kit_网络服务/ArkTS_API/ohos_net_webSocket_WebSocket连接_/js-apis-websocket`）：`connect(url, options?)` 的说明是"**根据 URL 地址和 header** 建立一个 WebSocket 连接"，且 `WebSocketRequestOptions.header: Object` **受支持**（官方示例即 `options.header = {…}`）。⇒ 403 的原因收窄为三种：**(a)** 那一刻 cookie 为空；**(b)** 有 cookie 但头名/格式未被服务端识别（文档未承诺 `cookie` 小写名一定被认可）；**(c)** authority 不匹配导致 `Cookies.headerFor` 取不到值（与 (a) 同症状）。三者由**已进构建的诊断**区分：`RemoteMux` 在 `ws connect` 失败分支把 `cookie=无/NNB` 并进 `lastError`，经 `SessionHub.lastError` 显示到 `HDSH-AUTH connect 结果 … err=`。 | 高（官方文档 + 真机读数） |

| E68 | **自查自纠：`--trusted-host` 放错层会让 Host 完全起不来（Node 吃掉该开关）；以及"装机总失败"的真因是 hdc 服务端僵死会话** | ① **我引入并修掉的回归**：为对齐"浏览器等价"把 `--trusted-host 127.0.0.1:3120` 加进了 `buildHostArgv()`。真机读数：<br>`node: bad option: --trusted-host`<br>`HDSH-AUTO 启动 Host：ok=false … 1500 ms 内 3120 端口未应答（Node 线程已经退出）`<br>且该次运行**完全没有 3120 流量**（连就绪探测的 401 都没有）——症状与"某个插件加载失败"完全不同。根因：`buildHostArgv()` 返回的是**交给 `node::Start` 的 Node 自身 argv**，Node 不认识的开关会让进程**死在 CLI 解析阶段**；而 dsh 的应用级选项（`--port/--host/--no-open/--trusted-host`，dsh-web-app `startup.js` 定义的那四个）一律走入口脚本的 `runProfile({ args })` → `ctx.cmdlineArgs`。**修法**：从 `buildHostArgv` 撤掉并留注释警示，改放进 `hostcore/app/main.js` 的 `args`。<br>**教训（写给后续的自己）**：改了 argv/env 之后，第一件事必须是**跑一次完整启动看 `BOOT_*` 是否仍全绿**——只确认 `BUILD SUCCESSFUL` 完全不够，这次差错就是这样漏过去的。<br>② **运维要点**：本会话多次出现装机报 `[Fail]Bind target session is dead`（而设备**在** `list targets` 里），`hdc kill` 重启服务端后立刻 `install bundle successfully`。下次遇到"设备在列表里但任何操作都失败"，先 `hdc kill`，不要怀疑应用。 | 高（真机日志 + 修复后构建通过） |

| E69 | **403 的判定规则读出来了；且按时间戳对齐，那串 403 不是我们的 mux——我们真正的失败是"流中断"** | ① **规则**（`dsh-client-connection/lib/index.js:201-215` 的 `isTrustedApiRequest`，`:554` 是 `return 403` 的落点）：`host` 头必须有；**回环主机名直接可信**（`isLoopbackHostname` 短路，不查 `trustedHosts`）；`sec-fetch-site === 'cross-site'` 直接拒；**`Origin` 缺席 ⇒ 通过**；`Origin` 存在 ⇒ 必须 `new URL(origin).host === host`。<br>⇒ **推翻我上一轮的"缺 Origin"假设**：回环本就在白名单内，而"不带 Origin"反而可信 ⇒ 我加的 `origin` 头**无必要但不破坏**（与 Host 同源），`--trusted-host` 对回环同样非必需；实测 403 未变与此一致。<br>② **时间戳对齐（决定性）**：客户端第 1 次 `17:27:15.487`（失败）→ 第 2 次 `17:27:19.495` **`ok=true @17:27:19.631`**；而 dsh 的 `ws_upgrade 403` 序列**从 `17:28:23` 才开始**、每 ~6.5 s 一次，至少持续到 `17:28:49`。⇒ 那串 403 发生在**客户端已结束尝试之后**且有独立周期 ⇒ 它是 **dsh 自己的内部 WS 客户端**在反复升级失败，**不是**我们的 mux 握手（这也解释了 `RemoteMux` 的 `cookie=` 诊断为何一次都没打印：我们的 `socket.connect()` 没走到失败分支）。<br>③ **我们真正的症状是另一条**：`HDSH-AUTH ok=true` 之后的 `err=会话列表获取失败：连接在传输中断开` ⇒ **流打开后中断**。下一步必须把两件事分开查：先定位 dsh 内部那个 6.5 s 周期的 WS 客户端是谁（与我们的失败是否同源），再查我们这条流在哪一步断（在 `RemoteMux`/`SessionHub` 打印 `MuxState` 与 `onEnd/onError` 原因码）。 | 高（源码规则 + 时间戳对齐） |

| E70 | **"流中断"的头号嫌疑：ArkTS 在链路健康时抛 `error(code=200)`，而我们可能在 `send()` 失败处误判为断线** | 读 `connection/.../RemoteMux.ets:183-214` 发现文件内已记着一条实测结论：ArkTS 的 `socket.on('error')` 在**链路健康时**也会触发，内容恒为 `code=200 message=undefined`（升级后 73–216 ms 到达），而连接**存活整个 8 s 观察窗**、从无 `close`。该文件注明：旧实现"一见 error 就 `onSocketClosed()`"会让**全部逻辑流被以 `carrier/closed` 终止**，症状正是 **"open 后几十毫秒就 closed"**——并因此让项目长期记着一个错结论。<br>而我们真机观察到的客户端错误恰是这一族：`HDSH-AUTH ok=true` → `err=会话列表获取失败：连接在传输中断开——中间转发通道不稳定，或 Host 已停止`（典型的 `carrier/closed` 文案）。同段注释还写明："真正的收尾放在**确实发不出去**的地方：`open()` 里 `socket.send()` 的失败会调用 `onSocketClosed()`"。<br>⇒ **下一轮第一检查点（已具体到分支）**：在 `open()` 的 `socket.send()` 失败分支把**真实原因**（ArkTS 返回的 `code/message` + 当时 `MuxState`）并进错误文本。若那里出现 `code=200`，则"流中断"是我们对**良性 error 的误判**，而非 dsh 或转发通道问题——这也与 E59 以来"网络/通道"方向的猜测相反。 | 高（源码内已有实测结论 + 症状文案吻合） |

| E71 | **决定性读数：`err` 的底层原因是 `HTTP 400（session/list）`——不是探活/WS/转发通道** | 按上一轮改动（把 `reason` 透出到文案）重装后，真机一次运行给出：<br>`HDSH-AUTH connect 结果：ok=true sessions=0`<br>`err=会话列表获取失败：连接在传输中断开——…（底层原因：传输失败：HTTP 400（session/list））`<br>⇒ ① **上一轮列的三个候选全部排除**（探活超时 / `send 失败 code=-1` / `code=200` 误判）：`carrier/closed` 只是**上层对一次 HTTP 失败的归类包装**，与 WS 载体无关；② 真正的失败是 **`session/list` 这条 `/api` 调用被 dsh 判 400**，与 E60 里"8 次 `POST → 400`（body 123~131 B）"**完全对上**；③ 由此也确认 E69 的判断：那串 `ws_upgrade 403` 与我们的失败**无关**（它是 dsh 内部客户端）。<br>**教训（方法层面）**：把底层 `reason` 透出来这一步，价值高于前面数轮的推断——**先让错误自己说话，再谈修法**。 | 高（真机读数） |

| E75 | **`Request` 全局缺失：空体 400 的第一处根因** | 探针（同参数 `--jitless --no-experimental-fetch`）实测 `Request/Response/Headers/fetch` **全部 `undefined`**；而 dsh 的 `/api` 挂载点会 `new Request(url, {method, headers, body: Buffer.concat(chunks), signal})`（`dsh-client-connection/lib/index.js:68`，buffered 模式）⇒ `ReferenceError` ⇒ 被 `dsh-host-webserver` 的 catch-all（`lib/index.js:253 res.writeHead(400); res.end()`）兜成**空体 400**。修法：`fetch-shim.js` 补 `HdshRequest`（`url/method/headers/signal` + `text/json/arrayBuffer`），并把安装逻辑改为"缺哪个全局就补哪个"。 |
| E76 | **`Response` 签名不标准：第二处根因** | dsh 的 web 栈到处 `new Response(body, {status, headers})`（`:415/:404/:400` 与 `fullResponse`），而我原本实现为 `(stream, init)` 且 `headers = init.headers`（常 `undefined`）⇒ 桥层 `Object.fromEntries(response.headers.entries())`（`:83`）抛 `TypeError` ⇒ 又是空体 400。修法：改成标准 Fetch 签名（支持 string/Buffer/Blob/ReadableStream/空体，且总是给出 `Headers`）。 |
| E77 | **`Response.json` 静态方法缺失：第三处根因** | 补签名后处理器**已可达**（`content-type: text/plain` 变体由 400 空体变为 **415 `content type must be application/json`**），但合法请求报 `handler failure: TypeError: Response.json is not a function` ⇒ 端点处理器用标准静态构造器。修法：补 `static json/redirect/error`。 |
| E78 | **本机 `/api` 返回 200（400 之谜终结）；附带一条工具陷阱** | `curl --data-binary @file`：`POST /api/settings/describe` 与 `/api/pluginInventory/list` 均 **HTTP 200**，返回完整 `server-response`。**陷阱**：PowerShell 用 `--data '<json>'` 传体会被引号处理破坏（服务端只收到 `{` 一个字节），我因此一度误判"请求体为空"——**调 API 一律用 `--data-binary @file`**。 |
| E79 | **sharp stub 解开 attachments 链（会话端点的前置）** | `dsh-attachment-local` 对 sharp 只需**加载期 import 成功**（真正调用只在图像路径 `lib/index.js:164/181/216/787`）⇒ 用最小 stub 替换即可让 `attachments → fileUploads → sessionController` 整条服务链成立。做法：`node_modules/sharp` 换 stub（真件留 `sharp.real`），profile 里把 `attachment-local/file-upload/session-controller/ui-deliverables` 改回启用（`permission` 仍禁用，其 `shell` provider 端侧不可用）。本机 `session/list` 200。代价：图片附件在使用时明确报错（诚实降级），完整恢复见 E64（**已由 E93 完成**：libvips 全套 45 库随包 + sharp 调度器；若真件加载失败，本行描述的降级仍然成立）。 |
| E80 | **端侧 preset：`session/create` 的真正前置** | `session/create` 曾报 `agent-preset/invalid`：preset `standard` 的 `tool-pwsh`（`condition: process.platform !== 'win32'` ⇒ 端侧启用）、`tool-bash`、`tool-fs-search`（多行 inject 含 `"subprocess"`，走 ripgrep）都依赖 `ctx.subprocess`，而**鸿蒙不支持进程创建**（E15）⇒ 让 shell 链"看起来可用"是错方向。**正确解法是配置层**：新增 `presets/ondevice/`（复制 standard 并禁用这三行）+ profile 里 `agent-presets.default: ondevice`。本机验证：`session/create` **200** 且返回 `sessionId` 与 `agentPreset: ondevice`，随后 `session/list` 返回该会话。并已在 `tools/pack-core.mjs` 加 `addOnDevicePreset()` **固化**（否则只存在于工作树，全新克隆会丢）。 |

| E81 | **WS 升级真正的失败点定案：`Origin` 被 libwebsockets 附加成"逗号列表"，fence 恒判 403** | 这一条结束了从 E59 起"WS 升级为什么失败"的长期悬案。做法是**两侧同时插桩**（此前只有客户端二手结论，无法区分"路径不对/升级头没发/cookie 没带"三种原因）：<br>· 宿主侧（`hostcore/app/main.js` 包装 `node:http` 的 `createServer`，挂只读监听器，不改 dsh 一行）：<br>`IN-UPGRADE GET /api/remote.mux conn=Upgrade upgrade=websocket key=yes ver=13 cookie=224B origin=http://127.0.0.1, ws://127.0.0.1:3120`<br>· 客户端侧（`RemoteMux` 打出真正用于 connect 的同一个值）：`HDSH-MUX connect url=ws://127.0.0.1:3120/api/remote.mux headerKeys=cookie,origin cookieLen=224`<br>⇒ ① **路径、升级头、cookie 全部正确**（三条候选一次排除）；② 真凶是 `Origin`：**libwebsockets 一定会自己附一个 `Origin`，且按 URL 推导时丢掉端口**（`ws://127.0.0.1:3120` → `http://127.0.0.1`）；而 `WebSocketRequestOptions.header` 里的 `origin` 是**追加而不是替换**，于是线上成为逗号列表。fence 按**整串** `new URL(origin).host === hostUrl.host` 判定（E69 的规则）⇒ 永远不等 ⇒ **每一次升级都 403**；③ 客户端把这个失败报成 `error code=200`（"升级响应不是 101"），正因如此，E70 那条"error code=200 是良性噪音、链路其实是好的"的结论被**推翻**——它不是噪音，它就是升级失败本身，只是码值误导。<br>**方法教训**：跨端问题时，"两端各自的原始读数 + 同一次握手的对齐"比任何单侧推断都便宜。这一条只花了一次插桩构建。 |
| E82 | **修法（两侧各一处）+ 可复跑的回归检查** | ① **宿主侧**：`tools/pack-core.mjs` 新增 `allowOriginList()`，把 `isTrustedApiRequest` 的 Origin 判定改成**逗号分隔列表、任一项同源即通过**（跨源项一律不认，语义不变）。**上游若改了这段实现，打包会直接报错退出**（不静默跳过）——悄悄发出一个"WS 永远连不上"的包，比打包失败难查得多。<br>② **客户端侧**：`RemoteMux` 注入的 Origin 改用 **http(s) 形态**（浏览器发 WS 升级时 `Origin` 就是 http(s) 源，不是 `ws:`），让列表里至少有一项语义正确。<br>③ **回归检查 `tools/check-origin-fence.mjs`**（本地无设备，自起 Host + 自铸 cookie + 裸 `http` 升级请求，含正/反控制组）：<br>补丁前：`clean=101 ok / absent=101 ok / **duplicated=403 MISMATCH** / foreign=403 ok / no-cookie=401 ok` → `RESULT: FAIL`<br>补丁后：`clean/absent/duplicated=101 ok；foreign=403 ok；no-cookie=401 ok` → `RESULT: PASS`<br>⇒ 正向（多值 Origin 被接受）与反向（跨源、无 cookie 仍被拒）**同时**被钉住——只测正向会把"栅栏被关掉"误判成修复成功。 |
| E83 | **真机端到端打通：逻辑流通、会话实时可见；顺带修掉"空态文案冻结"** | ① 装好带 E82 补丁的核心包后（`BundledCore` 按 zip 大小变化重装），真机读数：`IN-UPGRADE … origin=http://127.0.0.1, http://127.0.0.1:3120` → **`HDSH-AUTH connect 结果：ok=true sessions=0 err=(none)`**，此后 2 分钟+ 观察窗内**再无** `carrier/closed` / `degraded` / 重连（旧症状是 ~6 s 后掉线）。② **端到端实证**：经 `hdc fport` 从开发机对设备内核建会话（`session/create` → `{"sessionId":"session-65ef5506-…","agentPreset":"ondevice"}`），**页面无需刷新即出现「新会话 / 刚刚 / 18:34」** ⇒ WS 逻辑流（`$events` 推送）在真机上首次工作，`session/create` 的 preset 前置（E80）在真机同样成立。③ **同时暴露并修掉一个界面自相矛盾**：核心页显示「就绪 · 已安装 1 个版本 · 0.1.5-rc.2」，会话页却写着「还没有安装核心」。根因：会话页空态文案由 `sessionEmptyCopy(coreView.phase)` 决定，而首启时序是**页面先渲染（阶段=未安装）、核心后就绪**，中途无人重读 ⇒ 文案冻结在旧阶段。修法：自动重连成功时、以及切入会话页时各刷新一次核心阶段（空态文案是给人指路的，指错方向比不写更糟）。 |
| E84 | **两处"必然发生的端侧故障"被提前拆掉：孤儿写锁 + 临时目录/代理** | ① **孤儿写锁**（真机与开发机都会遇到）：`dsh-atomic-write` 的 `withFileLock` 用 `wx` 建同级 `<file>.lock`（内容 = 持有者 pid，释放写在 `finally`），上游注释明确写着 *contender never removes an existing lock … orphan recovery is an operator action*。也就是说**持有者被强杀时锁永久残留**，此后每个新进程都会在超时后失败：`atomic-write: timed out waiting for the writer lock at …`。开发机上这只是"手工删一下"，端侧却是**用户级故障**：用户划掉应用、或系统因内存压力回收应用 ⇒ 宿主被杀 ⇒ **下次启动起不来，而用户没有任何手段删那个文件**。<br>本机实测复现：我用 SIGKILL 收掉上一轮宿主后，下一次启动立刻死在同一个地方。修法（`hostcore/app/main.js` 的 `recoverOrphanLocks`，在 `runProfile` 之前跑）：巡检 `$DSH_HOME`（深度 ≤3、跳过 `node_modules`、最多 200 个锁 / 20000 个目录项）里的 `*.lock`，**只删 pid 明确已不存在的**——读不出 pid 不动、pid 是本进程不动、pid 仍存活（含 EPERM）不动。误判方向永远是"少删"，最坏回到上游行为。实测日志：`孤儿写锁已清理：…\.credentials.yaml.lock（持有者 pid=29912 已不存在）`。<br>② **临时目录 / 代理**：`os.tmpdir()` 是与 `os.homedir()` 同类的沙箱探测（后者在鸿蒙指向沙箱外目录并 EPERM，E 系列早已记录），而 `dsh-spill-local`（大输出落盘）与两个 worker-thread 运行器真的用它；`NO_PROXY` 则是回环流量的保险（有系统代理时，回环被代理走会表现成"端口通了但连不上"）。两者都在入口脚本里钉死/补齐，**不设不覆盖已有值**。 |
| E85 | **端侧不是 Web GUI：显式关掉 `surfaceContext` 注入的假系统提示** | `dsh-web-app` 的三个默认值都是 true（`lib/index.js:34-36`），其中 `surfaceContext: true` 会在 `:177` **注入一段系统提示**：大意是"你正在通过 DeepSeek Harness Web GUI 与用户交互……浏览器不提供隐式 DOM/路由/截图上下文，客户端插件 HMR 接收器是活的"。我们的端侧页面是**原生 ArkUI**，不是 Web GUI——留着它等于对模型说了一句假话，还会塑造模型对"界面能做什么"的判断。修法：profile 里对 `web-runtime` 行显式覆盖 `surfaceContext: false`、`printUrl: true`（**必须保留**：我们的 token 正是从这行 stdout 抓的，关掉等于关掉自己的凭据来源）、`openBrowser: false`（端侧没有可打开的浏览器；命令行层已有 `--no-open`，部署层再写一遍，任一侧被改动另一侧仍成立）。本地启动验证：覆盖后宿主照常就绪并返回模型目录（4 个可路由 provider）。<br>**同批对齐的参考项目经验**：其 README 里三条"最常被抄"的做法对本版本**已失效**——`PRIVILEGED_METHODS` 在 0.1.5-rc.2 里已不存在（现在是全局 cookie 认证）；HMR 用 `patchReload: 'startup'` + `DSH_DISABLE_HMR=1` 声明式关掉即可，**不需要**它的 `dsh-disable-hmr.patch`；SQLite 已改用 `node:sqlite`（我们的启动参数里本来就有 `--experimental-sqlite`），**不需要**它注入 better-sqlite3。真正适用的是它的**沙箱适配清单**与"打包后一致性守卫"思路。 |
| E86 | **插件在端侧"只能随包发版"是硬边界，UI 文案按事实改写** | 上游的插件安装通道是 `dsh plugin add`，其实现就是**创建子进程**：`dsh/lib/plugin-Ddi42qoW.js:109` → `spawnSync("pnpm", …)`。而鸿蒙不允许应用创建进程（D6 E15）⇒ **运行时安装/卸载插件在端侧永远不成立**，与"插件是纯 JS 还是含 .node"**无关**——此前 UI 上"纯 JS 插件可以在运行时安装/停用"的说法是错的（参考项目也承认它的 `dsh plugin` 通道在设备上没做成）。修法：`CorePane.pluginsCard` 与 `SettingsPane.pluginsTab` 改为如实陈述（列出的是随核心包发版的插件与启停状态；不提供任何安装/市场入口——原来那个点了没反应的「市场」文字本就是假入口）。这属于"去掉无用功能"，而不是降级。 |
| E87 | **解包后的关键件哨兵：把"装上了却起不来"提前成"安装就失败"** | `@ohos.zlib.decompressFile` 会校验条目 CRC，所以截断的 zip 通常在解压阶段就失败；但"解压成功、树却不对"仍有真实可能（容器不是我们的、打包漏了某类文件、磁盘不足时提前返回），而它的症状最难查：**装上了、也激活了，直到启动时才以"某个插件加载失败"的形态冒出来**。修法：`CoreStore.verifyStagedTree()` 在做完顶层目录/元数据校验后，再核三件"缺了宿主必然起不来"的哨兵——`@deepseek-ai/dsh/package.json`（宿主本体）、`koffi.node`（原生件，装错平台立刻暴露）、`profiles/ondevice/cordis.patch.yml`（端侧 profile）。**刻意不做全树计数**：2.5 万个文件的遍历正是端侧要避免的同步大目录操作（E45/E47），哨兵只花几次 `accessSync`。校验不过就清掉 staging 并**如实返回失败原因**，绝不留下半成品被当成"已安装"。 |
| E88 | **核心页的「未探测」变成实测事实：运行时自述 + 原生件加载结论** | 核心页长期有三项写着「未探测」（`zlib zstd` / 原生件加载 / 监听地址）。**那在当时是对的**——ArkTS 侧的原生模块只暴露 `runtimeVersion/startHost/isHostRunning/stopHost`，这几项只有宿主进程内部知道，而本项目的纪律是不猜。<br>本轮把它变成实测：入口脚本在写 `host-ready.json`（token 的主通道）时，顺手落一段 `runtime` 事实——`process.version`、`process.platform/arch`、`process.execArgv` 里是否真有 `--jitless`、`node:zlib` 有没有 `zstdCompressSync`、监听地址，以及**当场 `require` 三个硬原生依赖**（koffi / sharp / node-pty）的结论（失败时带原始错误文本）。ArkTS 侧由 `DshHost.mergeRuntimeFacts()` 在 `port.isRunning()` 为真时才读（**否则旧文件会把上一版核心的事当成现在的事**），且**已有读数优先**。<br>本机自检读数：`nodeVersion v22.22.0 / platform win32/x64 / jitless true / zstd true / listenAddress 127.0.0.1:3392 / koffi ok=false（Cannot find the native Koffi module…）/ sharp ok=true（stub）/ node-pty ok=true`。**这行 koffi=false 正是本机真实情况**：核心树只保留 OHOS/Linux 的 koffi 预编译（win32 那份被裁剪），也正是"本机跑不出完整一转"的原因——真机上应为 true，设备一接上即可对照。<br>**顺带解决一个悬案**：会话持久化用 `session.v3.jsonl.zstd`，而我们的启动参数里**没有** `--experimental-zstd`。实测 Node 22.22 **默认就有** `zstdCompressSync`（`--experimental-zstd` 在这个版本上反而是 `bad option`），且 `--jitless --no-experimental-fetch` 组合下依然可用 ⇒ 这一项无需任何改动，且现在每次启动都会被实测一次（换 libnode 版本时会立刻给出结论）。 |
| E89 | **摘掉阶段一的 Electron 残留 + 把"上架红线"变成可复跑守卫** | ① **清理**：`web_engine` 模块虽然早已没有任何代码 import（entry 的依赖只剩 connection/appstate/dshcompat/platform/hostruntime），但它**仍在 `build-profile.json5` 的模块列表里、161 个文件也仍在版本库里**；`runtime/`（阶段一从华为产物导入的 Electron 运行时，201 MB）同样堆在工作目录。本轮把模块从构建里摘除、`git rm` 掉那 161 个文件、删除 `runtime/`，并**在同一轮重新构建验证**：`BUILD SUCCESSFUL`；HAP 共 27 个条目，关键件齐全（`libkoffi.so` / `libdshhost.so` / `libnode.so.127` / 核心 zip / `resources/app/*`），**Electron 痕迹为 0**。<br>② **守卫**：新增 `tools/check-store-readiness.mjs`，把四条"一旦回退只会在**安装期**才炸"的红线钉死——包名必须是 `com.hnmrxz.hdsh`；任何**声明**里不得出现 `ALLOW_WRITABLE_CODE_MEMORY` 或任何 `ohos.permission.kernel.*`（**注释里讨论它是允许的**，所以扫描前先剥注释）；每个构建模块的 `deviceTypes` 必须覆盖 phone/tablet/2in1；`buildHostArgv` 必须保留 `--jitless` 且不得出现 `--experimental-fetch`；另加"阶段一模块不得回到构建路径"。本机读数 `RESULT: PASS`。<br>**顺手抓到的真实瑕疵**：仓库里确实有带 UTF-8 BOM 的 `module.json5`（`JSON.parse` 直接抛），守卫因此先剥 BOM——这正是"守卫要比人更能发现小事"的例子。 |
| E90 | **把"停止核心"从空按钮变成真能力：协作式停止（请求文件 → dsh shutdown → 干净退出）** | 之前 `stopHost()` 只能如实返回"做不到"：原生层（`dshhost.cc`）只在 `node::Start` **返回**时才把 `g_running` 置假，而没有任何外部手段能让它返回（ArkTS 不能给已启动的线程发信号，也不能改它的环境变量）⇒ 核心页那个「停止核心」按下去不会有任何效果——正是本项目一直在清理的"假入口"。而**核心切换/回滚必须以"能停"为前提**（`CoreStore.stageFromZip` 明确拒绝覆盖正在使用中的版本），所以这不是锦上添花。<br>做法（三处，各管一段）：① 入口脚本在 `runProfile` 成功后挂一个 1.5 s 巡检，看到 `$DSH_HOME/host-stop-request` 就调 dsh 自己的 `shutdown.shutdown(0)`，1.5 s 兜底再显式退出——为此给 `process.exit` 拦截器加了一个**只由停止路径**使用的放行开关 `ALLOW_EXIT`，其余 `process.exit` 仍被拦住并记录（诊断期的保护不能丢）；② `DshHost.stop()` 写请求文件（`$DSH_HOME` 的布局知识留在 hostruntime）并等待结果，**失败时必须把文件删掉**——留着它会让下一次启动的宿主刚起来就自杀，那比停止失败本身糟得多；③ `NodeRuntime.stop()` 只做等待端（轮询 `isHostRunning()`），超时才退回原生通道并如实抛错。<br>**本机实测（可复跑）**：`node tools/check-model-roundtrip.mjs --probe-stop` → `stop 端口停止应答=true (+1536ms)`、`Node 退出：code=0 signal=null`、`RESULT: PASS`。**退出码 0 是关键**：不是被信号打死，是 Node 自己收工——这正是"停完还能再启动一次"能干净成立的前提。 |
| E91 | **插件管理落地：能启停随包发版的行（安装/卸载仍是硬边界）** | 目标里有"插件管理"。先前 `hostruntime/src/main/ets/Index.ets` 自己写着"启停/安装插件尚未实现"——本轮把它做实了**一半**，并把另一半的边界写清楚。<br>① **不可做的那半**：安装/卸载插件永远不行（上游 `dsh plugin add` = `spawnSync("pnpm", …)`，鸿蒙不允许创建进程，E86）。<br>② **可做的那半**：启停**已经随核心包发版的行**是纯文件操作。但路径不能猜：dsh 的用户 patch 层是 `<profile 目录>/cordis.patch.yml`（`dsh-app-boot/lib/index.js:861-862`，**不是** `$DSH_HOME/cordis.patch.yml`——我们 profile 注释里那句写错了，已改），而且入口脚本**每次启动都会用核心种子覆盖它**（否则核心升级的 profile 变更进不来）⇒ 直接写它必然丢。于是拆成"种子 + 用户行"：新增 `<profile>/.hdsh-plugin-rows.yml` 只放用户行（谁都不覆盖），入口脚本 `composeUserRows()` 每次启动把 `cordis.patch.yml` 重新拼成 **种子 + 用户行**（后写覆盖先写，用户行天然优先）。"恢复默认"因此就是删掉那份文件。<br>③ **行 id 用哪一个**：用 **profile 行 id**（`ui-deliverables`），不是 Loader 报的 entryId（`include:ui-deliverables`）——patch 行按前者匹配。这个坑是实测出来的（第一次用 entryId 写，行没动）。<br>④ **本机实测（可复跑）**：`node tools/check-plugin-toggle.mjs` —— 基线 `ui-deliverables enabled=true` → 写用户行 → 重启后 `enabled=false`、`RESULT: PASS`；脚本自带负面对照（早先用错文件路径/错 id 形式时读数不变）。<br>⑤ 界面：插件行上给开关（**只对有行 id 的行显示**；没有 id 就给开关等于造假入口）、一个「恢复默认启停」、以及"重启核心后生效"的即时提示；`DshHost.plugins()` 会把用户行**叠加**到清单上，于是界面显示的是"重启后会生效的状态"，与提示语自洽——否则用户会以为改动没保存。 |
| E92 | **核心版本切换/回滚动作接线：停 → 起候选做健康检查 → 通过才激活；失败就把旧版本起回来** | 判据与事务（`Types.decideActivation/decideRollback` + `CoreStore.activate/rollback`）一直就位，但**界面动作没接**：`onCoreSelectVersion` 只弹一句"运行时接线后才能切换"（运行时早已接线，属于过时文案），`rollbackCore` 只调 `rollback()` 写状态、提示语还写着"核心并未随之重启"——后者更糟：它会造成"界面显示的版本 ≠ 正在跑的版本"，而这正是最难查的一类不一致。<br>本轮补上真正的动作（`DshHost.switchTo()` / `rollbackTo()`）：**先停**（用 E90 的协作式停止）→ **把候选真的起一次**（`decideActivation` 要求健康检查 PASS，而端侧不存在"静态检查就能证明这份核心能跑"的手段——原生件/profile 行/Node 版本都只有起来才知道，所以"起一次"就是唯一的健康检查）→ 通过才 `activate`；**候选起不来就把它停掉、把旧版本起回来**，并把每一步原因写进返回文案（结果是"切换失败，但设备仍可用"）。回滚同理：停 → `rollback()` → 起回滚后的版本。<br>**顺带补上一个必然踩到的坑**：切换/回滚都会重启宿主，而 dsh **每个进程重新生成启动 token**（cookie 也绑进程）⇒ 旧凭据立刻作废；不重连就会表现为"刚切完就掉线"，看起来像切换把连接弄坏了。所以成功后自动读回 `host-ready.json` 的启动链接并重连（`adoptLocalHostAfterRestart`），读不到就如实提示去「设置」页重新接入。<br>**仍待真机验收**：动作逻辑依赖设备上的 `CoreStore` 与原生运行时，开发机跑不了（夹具已备好：rc.3 包 sha256 `7ca555e6…`，见 E63/上轮）。 |
| E93 | **图片附件恢复：libvips 全套（45 库）搬进 HAP，并把"到底能不能用"交给运行时实测** | 这一步取消 E79 的诚实降级。E64 早已量化的"三件套"（改名 + 改自己的 SONAME + 改依赖方的 `DT_NEEDED`）之外，实测还多出一件**不做就白忙**的事：sharp 原生件里写死了 **RPATH**（`$ORIGIN/../../sharp-libvips-openharmony-arm64/lib:…`，指向 npm 安装布局），而那个布局在 HAP 里**不存在** ⇒ 即使 46 个库都拷进去，加载器也找不到 ⇒ 必须把 RPATH 改成 **`$ORIGIN`**（新串短得多，与 `DT_NEEDED`/`SONAME` 同一套"原地改写 + NUL 补齐"，零结构风险）。<br>**工具**：`tools/patch-native-needed.mjs` 新增 `--set-rpath` 与 `--list`（JSON 输出，ELF 解析只存一处）；新增 `tools/collect-libvips.mjs`（45 个库 → `entry/libs/arm64-v8a/`：改写 103 处 `DT_NEEDED`、1 处 RPATH、清理 42 个 `.orig`、30.5 MB，并写清单 `dist/core/libvips-libs.json`）；新增 `tools/check-native-closure.mjs`（**静态依赖闭包检查**：SONAME 必须等于文件名——但只对**被别的库 NEEDED 点名**的库作硬要求，顶层由路径 dlopen 的库不受此约束；每条 `DT_NEEDED` 必须解析到随包库或系统白名单，白名单是显式列出的，不是"未知即通过"）。<br>**读数**：收集前 `RESULT: FAIL`（`libsharp` 有 1 个未解析依赖 `libvips-cpp.so.42` + RPATH 指向不存在的布局）；收集后 **PASS**（45 库、零未解析、仅 2 条已解释的警告）；`devecocli build` 后产物目录 **52 个库、闭包仍 PASS**；HAP **183.2 → 206.4 MB**（+23 MB 压缩后）。<br>**为什么不是直接放真件**：真件是**鸿蒙 arm64** 原生件，开发机上必然加载失败，而开发机要跑**同一棵核心树**做本地回归（三个 check 工具全靠它）。于是 sharp 换成**调度器**（`pack-core` 的 `wrapSharp()`：真件放在 `node_modules/sharp.impl`，加载失败则退回"会报错但能挂载"的 stub）。<br>**顺带修掉一处会撒谎的读数**：`require('sharp.impl')` 在 Windows 上**也会成功**（sharp 的原生绑定是**惰性加载**的）——只看它会把"已降级"报成"可用"。所以运行时事实（E88）改成直接探**原生绑定本身**（那一次 dlopen 才是事实）。本机读数：`sharp: ok=false | 真件原生绑定加载失败，图片附件已降级：…`（设备上应为 `ok=true`，**待真机确认**）。 |
| E94 | **系统返回键：自绘导航必须先消费返回事件（真机已修）+ POC 页自动填入本机核心** | ① **返回键**：用户报告"许多页面用系统返回会直接退回桌面"。根因：本应用的导航是**自绘状态机**（`tab` / `stackPage` / `wsDrill` / 各种浮层），不经过 router 栈 ⇒ 系统返回事件**没有任何人消费**，默认行为就是退出应用。修法：`Index.ets` 实现 `onBackPress()`，按"从最上面那层往回退"消费——浮层/编辑态（凭据、设置编辑、搜索、选择器）→ 二级页（会话/详情/诊断/添加 Host）→ 会话详情 → 工作区下钻 → 文件预览 → 非首页签 → **真到根了才返回 false**（那时系统最小化应用才是对的，吞掉会变成"按返回没反应"）。每步打 `HDSH-BACK`。真机读数：在「核心」页按返回 → `HDSH-BACK 回首页签（会话）`、进程仍在前台、截图停在会话页 ✓。<br>② **POC 页**：用户说"无法手动调试，也不知道该填什么"。该页要的正是**本机正在跑的 Host 的地址与令牌**，而令牌每个进程都会重新生成 ⇒ 人工从 hilog 抄既易错又无意义。改为进入页面即读入口脚本写下的 `host-ready.json`（与客户端自动接入同源）自动填入：真机读数 `POC 已预填本机核心：authority=127.0.0.1:3120 tokenLen=43`，页面显示"已自动填入本机核心…可直接点「① 认证并跑通」"；默认端口也从误导性的 3000 改成真实的 **3120**；读不到时如实说明。 |
| E95 | **凭据行铺满整屏：ArkUI 里嵌套的 `layoutWeight` 会把父级撑到最大约束** | 用户报告"凭据页 apikey 铺满了"。真机截图确认：凭据行卡片纵向占满整屏、`apiKey` 居中、说明被挤到底边。根因：凭据行里 `Text(c.ref)` 带了 `.layoutWeight(1)`，而它在**内层 Column** 里 ⇒ 让这段文本吃掉纵向剩余空间，外层 Row 随之被撑到父级最大高度（同页模型卡片没有这层嵌套，所以一直正常）。修法：去掉文本上的 `layoutWeight`，改内容自适应（`width('100%')` + `maxLines` + 省略号）；同时给凭据浮层输入框显式 `.height(44)`、浮层面板 `.constraintSize({maxHeight:'80%'})`。真机复验：恢复为紧凑卡片（`apiKey` / 未配置 · 可在此授权或设置 / 配置）✓。**教训**：`layoutWeight` 只该用在**布局轴**上占剩余空间（Row 里横向、Column 里纵向）；给文本加权重等于让文本去撑容器。 |
| E96 | **模型页对齐官方 UI 的分工：设置页管"提供方与密钥"，选模型在会话输入区** | 读官方 `dsh-client-ui-settings-models` 的说明得到真实分工：Models 页**以提供方为单位**（一行一个提供方，展开是模型清单与设置），**API 密钥是该页主字段**（只写不读，落 profile 的凭据引用）；"选哪个模型 / 推理强度"属于 `dsh-client-ui-model-selection`，写 `session/selectModel`——**按会话生效**。据此改我们的模型页：① 新增「模型凭据」区（与「凭据」页共用同一写入通道），让"模型调不通是因为没配密钥"在同一页可见；② 「全部模型」按**提供方分组**（组头 = 提供方名 + 模型数），每行标「当前」「N 档推理」与说明；③ 「当前模型」「推理强度」改为**只读展示**并写明"切换模型在「会话」页的输入区进行（本机核心按会话记录选择）"——因为 Index 里原 `onPickModel` 是**空实现**、`onPickEffort` 只改本地状态不落 Host，那是点了没反应的假入口；④ 真机复验：显示「模型凭据 / apiKey 未配置·配置」「当前模型（Host 的默认选择）」「推理强度（当前默认）」「全部模型 → DeepSeek 4 个模型」✓。**仍欠**：把 `session/selectModel` 接进会话输入区（需在兼容面补该端点并绑到当前会话）。 |
| E97 | **轨迹流报 `gateway/internal …reading 'kind'`：定位到"页面响应不带 projections"＋待复现的服务端一抛** | 用户报告「轨迹流失败」。真机截图给出两条线索：横幅 `轨迹流失败：gateway/internal Cannot read properties of undefined (reading 'kind')`（Host 127.0.0.1:3120 · 连接 live · 逻辑流 2）与日志行 `page skip: 取不到游标（projections.asOfSeq）`、`ws list fail gateway/internal cannot list …`。<br>**已查清的一半**：本机复现（`node tools/check-model-roundtrip.mjs --no-prompt`：新建空会话 + 打开 `session/follow`）显示 **follow 开帧里确有 `projections.asOfSeq`**（`{"type":"snapshot",…,"cursor":-1,…,"projections":{"asOfSeq":-1,…}}`），而 **`session/page` 的响应里没有 `projections`** ⇒ 客户端 `refreshTrajectoryByPage()` 的"取游标"分支在当前核心版本上**必然走空**（`page skip` 是能力不匹配，不是失败）。<br>**仍待查的一半**：`gateway/internal …'kind'` 是**服务端**在 `session/follow` 上抛的（客户端 `onError` 分支），本机空会话不触发 ⇒ 需要一个**有失败回合记录**的会话来复现（设备上那个「新会话」很可能有：未配密钥时的一次 prompt）。工具已备好：`--list` 列出会话、`--session <id> --follow` 对真实会话开流、`--no-prompt` 不发模型请求。 |
| E98 | **默认工作区：真机上 `cwd=/` 是"轨迹流失败"的共同上游（已修）** | 用户报告「轨迹流失败」，并给出关键线索：**添加会话之后**才出现。真机取证：`session/list` 里会话的 `"cwd":"/"` —— 宿主进程的工作目录继承自应用进程（`/`），于是"默认工作区"落在根目录。后果不是路径难看，而是功能性故障：`session/follow` 折叠工作区投影时抛 `gateway/internal Cannot read properties of undefined (reading 'kind')`（用户看到的横幅）、`workspaceFiles/list` 必然 `cannot list`、旧会话甚至把 `session/list` 一起拖坏（界面显示"还没有会话"）。<br>**开发机为什么一直没暴露**：本地跑时 cwd 恰好是仓库根（可读可写），完全正常——又一处"开发机跑通、设备才炸"。<br>修法（三段，缺一不可）：① 入口脚本准备 `<SANDBOX_HOME>/workspace` 并把路径写进 `host-ready.json`（不 `process.chdir()`：Node 跑在应用进程的一个线程里，POSIX `chdir` 会改**整个进程**的 cwd，影响 ArkUI 线程）；② 客户端连上就调 **`workspace/create`** 注册它（在已有目录上创建或幂等解析一个工作区），拿到 `workspaceId`；③ 建会话带上 `workspaceId`（并保留 `cwd`）。<br>**踩到的形状坑**：`dshcompat.buildArgs` 对 `ArgsShape.REQUEST` 的端点**自己会包一层 `request`**，我又手工包了一次 ⇒ 宿主收到 `request.request` ⇒ `path` 变 undefined，真机报 `workspace/invalid-path cannot create a Workspace at "undefined"`。改正后真机读数：`HDSH-CONN 工作区注册：工作区已注册：/data/storage/el2/base/haps/entry/files/workspace` ✓。 |
| E99 | **三处"界面把正常状态说成失败"的修正（真机已验其一）** | ① **首启不该说"还没有安装核心"**：内置核心的解包（66 MB / 2.5 万文件、数十秒）发生在 **EntryAbility 的 `DshHost` 实例**里，而页面用的是另一个实例 ⇒ `transient=EXTRACTING` 页面看不到、`installed` 又是空 ⇒ 首启会话页显示「还没有安装核心」，事实正相反。改为用 AppStorage 跨实例广播"正在解包"，阶段纠正为 `EXTRACTING`。**真机复验（全新安装 12 秒时截图）**：会话页显示「**正在解包核心** / 首次启动需要解包一次（约 63 MB 压缩包 / 2.5 万个文件），完成后这里会出现会话。」✓<br>② **"已启动过"不是失败**：原生层一个进程只允许一个 Node 实例，重复 `startHost` 返回 `started=false`（note=「运行时已经启动过」），而核心其实**正在跑**——此前把它当异常抛出，界面显示「启动失败：运行时已经启动过；同一进程只允许一个 Node 实例」，用户会以为核心坏了（核心页的启动/重启/切换都会撞）。现若 `isHostRunning()` 为真则视为"已在运行"，继续等端口应答。另外 `DshHost.start()` 入口先查 `port.isRunning()`，已在跑就直接返回成功（幂等）。<br>③ **切换/回滚先确认旧实例真的退了**：停之后若 `isRunning()` 仍为真（Node 还在退出中），给出「上一个核心实例还在退出…请稍等几秒后重试」这种**可执行**提示，而不是把底层限制当"切换失败"抛出去。 |
| E102 | **协议参数"双重 request 包裹"——真机上"发消息没反应 / 轨迹流失败"的总根因** | `dshcompat.buildArgs` 对 `ArgsShape.REQUEST` 的端点**自己会包一层** `request`（`{'request': body}`），而 `SessionHub` 又手工包了一次 ⇒ 线上是 `request.request` ⇒ **Host 收不到任何字段**（`cwd`/`workspaceId`/prompt 全被静默丢弃）。真机取证链：① 会话 `cwd` 恒为 `/`（请求体里明明带了路径）；② `session/follow` 折叠快照时抛 `gateway/internal Cannot read properties of undefined (reading 'kind')`（用户看到的「轨迹流失败」）；③ 修掉双包后，Host 立刻回了一条**明确的校验错误**——`session.create accepts workspaceId or cwd, not both`，证明参数**第一次真正送达**。<br>修法：`session/create`、`session/follow`、`session/cancel`（以及 prompt 链路）改为把**内层请求对象**交给 `argsFor`；并按 Host 校验改成 `workspaceId` 与 `cwd` 二选一。真机复验：会话创建成功、直接进入会话页、**轨迹流错误横幅消失** ✓。<br>**教训**：一处"多包了一层"能伪装成四五种完全不同的症状（参数不生效、投影崩溃、文件列表失败、建会话失败）——因为它丢掉的是**所有**字段。判据只有一个：看**请求体与描述符**，别看客户端的二手结论。 |
| E103 | **flock 原生件：npm 平台上没有、预编译件又缺 libnode，只能自建（真机已验证加载）** | Agent 轮次在设备上报 `Cannot find module '@deepseek-ai/node-addon-system-linux-arm64/package.json'`：`dsh-session-persistence-jsonl` 用 `@deepseek-ai/node-addon-system/flock` 给会话日志加**排他锁**，而 npm 在 Windows 上装树时**不会**装 linux-arm64 平台包（optionalDependencies 只装当前平台）。<br>① 先试 npm 预编译件（`bin/musl/system.node`）：它只 `DT_NEEDED libc.so` ⇒ dlopen 后 napi 符号解析不到（E43/E44 同一个坑），而 `libc.so` 是必需项、无法像 koffi 那样替换掉 ⇒ 不能用。<br>② 改为**自建**：给 `entry/src/main/cpp/CMakeLists.txt` 加 `system` 目标（编 `node-addon-system/src/flock.c`，显式链接 `libnode.so.127`）⇒ 产出 `libs/<abi>/libsystem.so`，命中入口脚本的 `lib<stem>.so` 重定向约定。真机读数：`NEEDED [libnode.so.127, libc.so]`、闭包检查 PASS、**不再报缺件**。<br>③ 还差一步（踩到了）：Node 的模块解析走**内部 stat**（`Module._findPath`），不是我们 hook 的 `fs.existsSync` ⇒ 目标路径**必须物理存在**；于是 `pack-core` 在树里补出平台包清单**并放占位 `.node`**（内容无所谓，真正加载一定经过 `Module._extensions['.node']` 被改写到 HAP libs）。 |
| E104 | **鸿蒙沙箱禁止 `link(2)`：会话日志写不进去 ⇒「发消息后没反应、详情数量不变」** | E103 之后轮次推进到写日志，撞上第二条沙箱禁令：<br>`EACCES: permission denied, link '…/sessions/--…-workspace--/session-…/session.v3.jsonl.zstd.2112db587c…'`<br>`dsh-session-persistence-jsonl` 用 `link(2)` 把写好的临时文件**排他发布**成正式日志（link 天生带 EEXIST 语义），而鸿蒙应用沙箱禁止硬链接（与符号链接禁令 E46 同类）。<br>修法（`pack-core` 的 `patchLinkForSandbox()`，上游若改则**报错退出**）：把两处 `link(...)` 换成"**存在性检查 + rename**"——rename 同文件系统原子，目标不存在时改名过去与 link 语义一致；唯一弱化是极端 TOCTOU 窗口可能覆盖同名文件，而两个调用点在发布前都已经检查过目标。补丁后真机：会话日志开始正常落盘（follow 快照 `cursor: 21`、`cwd` = 工作区），并且**观测到真实模型调用**（dsh 的会话标题生成请求走的就是模型，说明 jitless 下的 fetch 垫片 + SSE 通路在设备上真的能用）。 |
| E105 | **POC 并入「本地调试」；页头 POC 入口移除；文件夹选择优先走系统选择器** | ① 用户要求把 POC 合并进本地调试、并删掉页面上的 POC 入口：页头那个常驻 `Button('POC')` 已移除，入口只保留在「设置 → 通用 → 本地调试 → 协议往返验证」（诊断页内一键下钻：结论 → 逐步证据）。② 用户实测**手机上可以选本地目录**，因此工作区的「选择文件夹」改为**优先**拉起系统文件夹选择器（`DocumentSelectMode.FOLDER` + `fileUri` → 真实路径），失败才回退到沙箱浏览并把原因写在浮层里。**公共存储的 ACL 权限（`READ_WRITE_*_DIRECTORY`）不加**：它属 `system_basic`+ACL，普通应用装上会 `grant request permissions failed`，与"顺利上架"的硬约束冲突；系统选择器拿到的授权是免权限路径。 |
| E106 | **端侧会话真正跑通（真机截图确认）**；凭据引用名必须按适配器**派生的那个**写 | E103/E104 之后，轮次走到了模型调用，但每轮都以 `MISSING_CREDENTIAL` 结束：<br>`llm-deepseek: no API key for provider route "deepseek-official"; store DEEPSEEK_API_KEY through the credentials service`<br>根因是**凭据引用名**：设置里声明的槽位叫 `apiKey`（界面照它写、显示"已配置"），而 DeepSeek 适配器读的是 **`DEEPSEEK_API_KEY`** ⇒ 界面说配好了、模型说没有密钥。改成按"提供方 id 首段 + `_API_KEY`"给出**适配器默认引用**（`deepseek-official` → `DEEPSEEK_API_KEY`，与官方 Models 页派生 `<ROUTE>_API_KEY` 一致）后：<br>**真机 API 侧**：`assistant "pong"`（+3068 ms / 24 帧）✓<br>**真机 UI 侧**（截图）：`pong` → 「你好」→「你好！有什么我可以帮你的吗？」→「hi」→「Hi! What can I help you with?」✓<br>至此端侧闭环：**应用内置 Node + 本地 Host + 真实模型 + ArkUI 页面**。遗留观感问题：推理文本与正文一起显示（渲染层待细分）。 |
| E107 | **思考与正文分开渲染（官方 web 的形态）** | 真机截图显示：一条"助手"消息里先是一段英文自述（模型思考），紧接着才是回答——因为 `textFromBlocks` 把内容块**全部拼成一段**。修法：`TrajectoryItem` 增加 `reasoning` 字段，解析改按块类型拆分（`reasoning`/`thinking`/`analysis` 归思考，`text` 归正文），会话页把思考渲染成**正文上方的折叠块**（"思考过程 · N 字"，默认收起）。真机确认：折叠块出现、正文不再混入思考 ✓ |
| E108 | **导航收敛：删「待决」「会话」→ 并入工作区** | 目标要求"删掉待决和会话，合并到工作区"。做法：`NavTab.SESSIONS` **别名**到 `'workspaces'`（既有 `selectTab(NavTab.SESSIONS)` 调用点无需逐个改），`PENDING` 在 `selectTab` 里归一化到工作区；工作区视图改为**工作区为组、会话挂组下**（组头给路径/会话数/「文件」入口，组内行点开进对话），待决内联在列表顶部（它属于"现在就要动手"的事）。真机截图确认：页签变为三项、两个工作区分组（含用户用系统选择器选的 `/storage/Users/currentUser/Download/…` 公共存储目录）与各自会话 ✓ |
| E109 | **系统提示词不再冒充"助手消息"** | `system/message` 与 `user/message` 同属 `EventFamily.MESSAGE`，而角色只做了"用户或助手"两分 ⇒ 真机上出现一条"助手说：You are an AI agent powered by DeepSeek Harness…"。修法：`Speaker.SYSTEM` 早已存在但从未赋值，新增 `isSystemAuthoredEvent`（放 dshcompat，与 `isUserAuthoredEvent` 同源），映射时三分角色；对话视图**隐去**系统消息、轨迹视图仍可查。 |
| E110 | **核心并入设置** | 核心不是"另一个世界"，而是"这台设备上 DSH 怎么跑"的配置（版本/插件/运行时事实），官方 web 也没有独立"核心"页。做法：`TABS` 收敛为 **工作区 / 设置**，`NavTab.CORE` 别名归一化到设置；原核心页内容抽成 `@Builder coreTabContent()`，通过 `@BuilderParam coreSlot` 注入设置页的**第一个页签「核心」**（避免十几个回调再串一层透传）。真机截图：底部两项、核心内容随设置页呈现 ✓ |
| E111 | **启动瞬间不再闪"无法连接核心"** | 启动时要先等本机核心就绪再完成 token→cookie 兑换（真机约数十秒），这期间中枢的 `lastError` 可能带中间态文本，而状态条是**无条件**渲染的 ⇒ 用户看到报错闪一下再消失。判据改成"有没有真的试过并且失败"：新增 `connectSettled`（连接尝试结束时置真），未结束或未尝试时显示**中性**的「正在连接本机核心…」。真机启动 9 秒截图：无错误横幅 ✓ |
| E112 | **工具调用显示为工具卡（不再漏成正文/占位）** | 真机截图：助手消息里夹带的 `tool-call` 块被渲染成 `[tool-call]` 占位，甚至把整段 `{"id":…,"toolName":"write","callId":…}` 当正文显示。修法：新增 `projectWireRecords()`（一条记录 → 多条条目），把工具调用块取成 **TOOL 条目**（工具名/参数/调用 id 按多候选字段取，取不到名字就不造条目——不做"没有名字的工具卡"），`splitBlocks` 同时跳过工具块。三个调用点（page / follow 快照 / 事件流）改走新函数。 |
| E113 | **会话智能贴底（官方 web 的跟随行为）** | 目标：默认滚到最新；**手动上翻历史时不要被拽回底部**；用户自己拉回底部后**恢复跟随**。实现：`stickToBottom` 用可见区间判定（`onScrollIndex` 的 `end` 是否覆盖最后一条 ⇒ 回到底部即恢复），进入会话先贴底；`itemCount` 变化时若在跟随则贴底；**运行中**再叠一个 900 ms 定时器——流式输出时"条目数不变而正文在长"，只靠条目数会停在半截；用户上翻即停表。 |
| E114 | **设置页对齐官方 web 的层级** | 读官方包弄清结构（不靠猜）：`dsh-client-ui-settings-general` 的 README 明确「**通用分区没有内置行**，行由功能包通过 `settings.general.item` 注册（如「外观」来自 ui-theme）」，导航是 `settings.section` 账本的投影；`-plugins` 分区有**配置/列表两个子页签**（配置=可展开卡片+重置为部署默认；列表=只读清单、按作用域分组、可搜索）。<br>落到我们这边：① 分区顺序与命名对齐（**通用 / 模型 / 插件 / 核心 / 设备**，「模型与密钥」正名为「模型」——官方把密钥当该分区里的字段）；② 「通用」按同样定义**策展**：真机 12 个命名空间里只放用户面向的 `ui-theme`/`locale`/`ui-conversation`/`ui-chat`/`ui-onboarding`，模型类归「模型」、引擎类（`agent-loop`/`agent-presets`）不占版面；用户**显式改过**的项一律收进「其他已修改项」（既不占版面又撤得回）；③ 每行加「**默认**」/「**已覆盖**」徽标与「**重置**」（走 `settings/unset` 回落到部署默认）；④ 插件分区拆成**配置 / 列表**两个子页签，列表带关键词过滤且只读（官方正是把只读清单单独成页，避免误触）。<br>**真机截图确认**：分区顺序与命名 ✓、通用只余 5 个用户面向命名空间且逐行带「默认」徽标 ✓。 |
| E115 | **审批弹在会话里（一次点击即可放行）** | 以前审批/提问只在「待决」页签里，用户得先切页签再点，而它恰恰是**卡住当前这一轮**的东西。改为放在会话**输入区正上方**（眼睛在对话上、手指就在按钮附近），只在这一轮真的有待决时出现（平时不占一行）。真机待验：需要一次会触发审批的操作。 |
| E116 | **权限预设：与官方 `dsh-client-ui-permission-presets` 逐条对齐（下一轮执行）** | 官方规格（读其 README 得到，不是猜的）：① **通用设置行**选"**以后新建会话**的默认预设"，取值来自宿主 `defaultPreset` 枚举，写一条设置变更，**绝不影响当前会话**；② **`/permission <preset>` 选择器**只切换**当前会话**，选中即提交命令；③ 内置标签：英文 `Read Only` / `Workspace Write` / `Full access`，中文「**仅可查看**」「**工作区内修改**」「**完全权限**」；④ **完全权限必须显式确认风险**；⑤ 两个界面都**只在宿主推送权限状态之后**才确认变更；⑥ **宿主没有权限组合时，选择器与设置行都不显示**（不能假装有）。<br>落地步骤：先确认宿主是否投影 `defaultPreset`（我们端侧 preset 目前禁用了 `permission` 行，很可能没有 ⇒ 要么按 ⑥ 不显示、要么先启用该行再做，二者都不能靠猜）；再把"会话内选择器"接到已有的 `/命令` 通道上。 |
| E117 | **界面语言跟随系统、默认中文（第一批已落地）** | 机制：`resources/base/element/string.json` 为**默认（中文）**，新增 `resources/en_US/element/string.json` 供英文系统，系统按语言自动选择；界面改用 `$r('app.string.*')`。**视图层约束**：`appstate` 是纯 TS 模块，不能用 `$r` ⇒ 页签/分区这类"要在界面上显示的名字"留在视图层做映射（如 Index 的 `navTabLabelRes`），appstate 里的纯字符串版本继续供非 UI 用途。<br>**第一批（真机截图确认）**：一级页签（工作区/设置）、设置五个分区（通用/模型/插件/核心/设备）全部走资源，中文渲染正常。`tabChip` 的标签参数类型同时放宽为 `ResourceStr`（ArkUI 的 `string | Resource`）——写死 `string` 会让资源引用编译不过。<br>**后续批次**：常用动作（新建会话/选择文件夹/文件/重置/默认/已覆盖/对话/轨迹/本地调试——资源条目已备好待接线）→ 设置项标签与说明（来自 Host，属上游文案，需评估是否翻译）→ 长文案与空态。每批真机切系统语言验收。 |
| E118 | **点开「核心」分区会 JS 崩溃——已修（真机复现）** | 真机点按「核心」直接杀进程：`AppKilledReporter: reason: JsError`（`/data/log/faultlog/JS_ERROR1789315029777`，报告文件 hdc 读不到）。可疑点正是 E110 的 `@BuilderParam coreSlot`（把父组件的 `@Builder` 注入子组件再调用）。改为**直接传 6 个属性**（`coreView` / `coreNotice` / `pluginNotice` + 三个回调），数据流一眼能看完，也不依赖"父 Builder 在子组件里被调用"这条机制。**修后真机复验：进程仍在（pid 2649），核心内容正常渲染**（就绪 · 运行时事实 · 两版各带「切换」）。 |
| E119 | **核心版本切换/重启失败的根因：原生 `g_started` 置真后从不复位（真机点按抓到）** | 点「切换」到 0.1.5-rc.3，真机返回：`切换失败：运行时已经启动过；同一进程只允许一个 Node 实例`，而 BOOT 日志显示旧核心仍在跑。查 `hostruntime/src/main/cpp/dshhost.cc`：`startHost` 的守卫查 `g_started`，它在第 224 行置真后**从未复位**（只有 `g_running` 在线程退出时置 false，而 `isHostRunning()` 读的正是 `g_running`——所以"能不能再启动"与"现在有没有在跑"两个判断长期不一致）。修法：Node 线程退出（含 `node::Start` 未解析到的失败路径）时把 `g_started` 一并复位——它的语义本来就是"当前**有**一个 Node 线程在跑"，仍拦住真正的并发启动，但不再拦住"停掉之后再起"。这是**版本切换/回滚此前一直跑不通**的直接原因。 |
| E120 | **设置「通用」中文措辞与官方一致（文案来源：官方包内嵌 locale 表）** | 官方 web 的中文文案**内嵌在客户端包里**（`dsh-client-ui-theme/lib/client.js` 的 `"appearance.title": "外观"` 等）。据此在视图层做映射：`ui-theme.preference` →「**外观**」（值「浅色/深色/跟随系统」）、`ui-theme.fontSize` →「**字号大小**」（描述「仅影响会话内容的字号」）、`locale.preference` →「**语言**」、`ui-chat.transcriptView` →「**对话显示**」（值「标准/紧凑」）、`ui-onboarding.welcomeNoticeVersion` →「欢迎提示版本」、`ui-conversation.busyEnter` →「回车键行为」。**覆盖不到的键保持 Host 原文**：宁可显示英文，也不自创官方没有的说法。理由：Host 给的 label/值是英文（`Preference`/`Font size`/`system`/`compact`），而用户要求"默认中文、措辞与官方一致"。 |
| E121 | **「2/3 原生件」现在会点名是谁没加载** | koffi / node-pty / sharp 三件里有一件未加载，而界面只写"2/3 已加载"——排查者无从下手。改为把**未加载者的名字与原因**一并显示（原因取自 Host 的真实 `require` 报错）。受影响的能力各自明确：koffi → 进程/沙箱链，node-pty → 终端，sharp → 图片处理。真机再连时这一行会直接给出答案。 |
| E122 | **外部项目核对：`fellow99/deepseek-harness-harmony`** | 该仓库自述为 "An Electron-based desktop wrapper for deepseek-harness on HarmonyOS or OpenHarmony" ⇒ 与我们**阶段一**同类（Electron 封装、面向鸿蒙 PC/2in1），**不解决手机侧 pty/shell**：pty 需要创建进程，而手机侧应用沙箱不允许（E15）。我们的路线（应用本体自足运行 + 端侧原生 ArkUI 页面）与本目标一致，不因此改道。同批检索到的相关项目：`QinpanWan/dsh-harmonyos-pc`（缓存优化预设 + 启动补丁 + 工具链）、`ystyle/dsh-harmonyos`、`shenjackyuanjie/dsh-ohos-patch`。 |
| E116 | **权限三档：端侧被上游硬约束挡住（实测结论，不是推测）** | 官方规格（`dsh-client-ui-permission-presets` / `dsh-permission-presets` 的 README）本已逐条对齐：**通用设置行**选"以后新建会话的默认预设"、**`/permission <preset>`** 只切当前会话、内置三档 `SANDBOX_MODES = ['read-only','workspace-write','danger-full-access']`（中文标签「仅可查看 / 工作区内修改 / 完全权限」）、完全权限需显式确认、**宿主没有权限组合时不显示**。<br>于是动手把它打开（端侧 preset 里给 `permission` 配三档、默认 `workspace-write`）并**实测**：<br>`dsh: plugin tree failed to load: 1 entry did not activate`<br>`@deepseek-ai/dsh-permission-presets: pending (waiting for service: shell)` ⇒ **整棵插件树启动失败**（dsh 的 boot 要求所有行 activate）。<br>结论：该插件需要 `shell` 服务，而 shell 只能由 `bash-sandbox`/`pwsh-sandbox` 提供，两者在鸿蒙上没有对应物（无法创建进程，E15）⇒ **上游依赖链的硬约束**，不是配置问题。已把 patch 回退为禁用（否则核心起不来），原因写进 patch 注释与界面。<br>客户端据官方规则 ⑥ 处理：**不给可切换的假档位**，改为在「通用」放一行**只读说明**——当前文件策略确实是 `workspace-write`（系统提示里写着 `Current DSH file policy: workspace-write`），但"捆绑成用户可选预设"的这一层在端侧缺席。 |
| E123 | **E117 第二批：常用动作/徽标/视图切换接资源；输入区改用官方措辞** | 接线完成（均走 `$r('app.string.*')`，跟随系统语言、默认中文）：新建会话（`+` 与文案分开渲染，避免 Resource 与字符串拼接）、选择文件夹、文件、重置、默认、已覆盖、对话、轨迹、本地调试；`navPanelAction` / `viewChip` 的标签参数同步放宽为 `ResourceStr`（与 `tabChip` 同理——写死 `string` 会让资源引用编译不过）。输入区占位改为**官方措辞**：「发消息或创建任务, / 调用指令, @ 文件或对话」（离线时「会话不可用」），英文对应 "Send a message or create a task, / for commands, @ for files or conversations"——取自官方 `dsh-client-ui-conversation` 的内嵌 locale 表。**设备本轮已断开**，以构建通过作为验证（`devecocli build` SUCCESSFUL）。 |
| E124 | **权限模式改为显示宿主**真实**报告的档位（此前是写死的字符串）** | 翻线协议文档时发现：会话投影里**本来就带** `"permissions":{"options":[…],"currentValue":"workspace-write"}`（`Wire.ets` 的投影样例），而我们的投影代码只读了 `title`——也就是说界面上那句「工作区内修改（workspace-write）」是我**写死**的，不是宿主的事实。修法：新增投影键常量 `PROJECTION_PERMISSIONS`（落在 dshcompat，与 `PROJECTION_TITLE` 同源），`applySessionProjections` 按结构取出 `currentValue` / `options`，经 `HubSnapshot` 送到界面；设置「通用」的权限行现在显示**宿主报告值 + 它自己给出的可选集合**（官方译名：仅可查看 / 工作区内修改 / 完全权限），未投影时如实写"宿主暂未报告"。**不给可切换的假档位**（切换需要端侧无法启用的 `dsh-permission-presets`，E116）。 |
| E126 | **外部同类项目对照（优化思路）+ 清理** | 检索到的同类项目：`fellow99/deepseek-harness-harmony`（Electron 桌面封装，已核 E122）、`QinpanWan/dsh-harmonyos-pc`、`ystyle/dsh-harmonyos`、`shizhonggang/dsh-harmonyos`（自称"幂等补丁 + 安装/升级脚本 + 平台文档"，面向鸿蒙 PC）、`shenjackyuanjie/dsh-ohos-patch`。**最有价值的一份是 npm 上的 `@ohos-ports/deepseek-ai-dsh`（0.1.0-beta.3 鸿蒙移植版）**，它把自己的兼容补丁列成了清单，逐条与我们的实测对照：<br>① **硬链接回退**（hmfs 不支持 `link()` → 回退 `rename`/`copyFile`）——与我们 E104 的修法**完全一致**（独立佐证：这不是我们的误判，而是平台事实）；<br>② **凭据权限检查豁免**（hmfs 强制文件权限 660，`chmod 600` 无效）——**我们没有这条**，值得后续核实（若上游凭据存储以"权限必须 600"为前置，端侧会被它挡住或误判；本轮已确认我们树里的 `chmod` 调用点在 `dsh-fs-local`/`dsh-attachment-local` 等，尚未定位到凭据写入路径）；<br>③ **沙箱降级**：OHOS 无 bwrap/landlock/seatbelt ⇒ 他们以 unconfined 跑并设 `DSH_PERMISSION_MODE=danger-full-access`。**注意：`DSH_PERMISSION_MODE` 在我们用的 0.1.5-rc.2 里不存在**（全树 grep 无命中）——那是旧 0.1.0 线/移植分支的机制，所以 E116 的"三档不可切换"结论不变；升级上游时应重新检查这条线索；<br>④ Ripgrep 回退系统 `rg`——我们按预设禁用 `tool-fs-search`（端侧沙箱内没有系统 `rg`，禁用仍是对的选择）；<br>⑤ koffi 2.x 兼容 / sharp `LD_LIBRARY_PATH`——我们分别用**自建 koffi 3.x** 与 **RPATH `$ORIGIN`** 解决，比它们的补丁更彻底。<br>**清理（本轮）**：`dist/localtest` 下 73 个文件 + 21 个脚手架目录（≈8.1 MB，全部可再生）已清；文档按"只标历史、不删契约"处理（见 `docs/README.md` 的新纪律小节）。 |
| E127 | **凭据"仅属主可读"检查的鸿蒙豁免（预防一类"填了密钥却说没有密钥"）** | 由 E126 的外部对照牵出：`@ohos-ports/deepseek-ai-dsh` 记录"hmfs 强制文件权限 660，`chmod 600` 无效"。核对我们的树——`dsh-credentials-local/lib/index.js` 的 `assertOwnerOnly()` 正是读 mode 后**抛错**：<br>`credentials-local: … is readable beyond its owner (mode 660); run "chmod 600 …"`<br>在 660 恒成立的平台（鸿蒙）上，这个桌面 POSIX 的合理防护会**永远无法通过**，把"凭据确实写进去了"判成读取失败。<br>修法（`pack-core.patchCredentialsOwnerCheck()`，上游漂移即报错退出）：在既有的 `win32` 分支之后加一条**平台豁免**——条件是入口脚本自己设的 `HDSH_PLATFORM === 'ohos'`（`SANDBOX_HOME`/`HOME_DIR` 以 `/data/storage` 开头时置位；端侧 `process.platform` 是 `linux`，与桌面无法区分，所以不能用它）。桌面/CI 上该变量不存在 ⇒ 检查照旧生效。<br>**这不是放宽安全**：端侧凭据在**应用私有沙箱**内，其它应用本就进不来，这条检查在端侧只提供假失败。真机复验方式：连机后确认 `HDSH-NODELIVE` 里出现「平台标识：HDSH_PLATFORM=ohos」，且凭据行状态不受 mode 影响。 |
| E128 | **设置-模型页按官方形态改造：提供方卡片（密钥优先）** | 官方规格（`dsh-client-ui-settings-models` 的说明，逐条读来）：**每个已配置的提供方一行**、一次只展开一张卡；卡片的**主字段是单独一个 API 密钥输入框**（只写；页面从不询问环境变量名，profile 无引用时派生 `<ROUTE>_API_KEY`）；折叠的**「自定义设置」**放 `baseURL` 与该适配器的**模型目录**；**推理等级刻意不可编辑**（它是**按模型的能力**，提供方级控件只会被部分模型拒绝）；密钥状态点**只有确认已配置才绿、只有确认缺失才红**，其余是"未知"。<br>落地：模型页顶部改为**提供方卡片**——状态点（按上述三态语义，取 `<ROUTE>_API_KEY` 的 `configured`）+ 首要动作「配置密钥」（走同一条 `credentials/set` 通道）+ 可展开的「自定义设置」（**直接复用设置项编辑器**，因此 `baseURL`、模型目录这类 TEXT/STRUCT 字段都能改；找不到匹配分区时如实说明而不造空编辑器）+ 一行说明"推理等级不在提供方级编辑"。为让模型页能读到 `llm-*` 分区，Index 另传 `allGroups`（「通用」页继续用策展后的 `groups`，见 E114）。<br>**未对齐的部分（如实留档）**：官方的**提供方增删流程**（`llm/discoverModels` 可搜索选择器、手工声明 pi-ai 路由、按用户层判断可删除）与**首次运行弹窗**（版本化声明 + DeepSeek 密钥步骤）本轮未实现——前者需要新的编辑器形态与一次 Remote 查询，后者属于引导流程；两者都已具备数据基础（`llm-pi-ai` 分区、`agent-default-model`）。 |
| E130 | **死按钮排查（用户要求"确保所有已展示的按钮功能可用"）：新增可复跑扫描器 + 修掉能修的 + 留档不能修的** | 新增 `tools/check-dead-handlers.mjs`：扫出**空箭头函数体/空方法体**（"点了没反应"的代码形态）。第一版把块注释整段换成空格 ⇒ 行号错位、报出大量假阳性（指向 `@Prop`、注释行）——**假阳性比漏报更糟**（会让人不再相信报告），已改为"保留换行的去注释"，行号随即对齐。<br>**已修**：诊断页「清理离线缓存」此前是空实现 ⇒ 现在真的清（新增 `SessionHub.clearLocalCache()`：旁路日志 / 未识别事件清单 / 缓存的轨迹与待决视图；**不碰 Host 侧任何数据**——诊断页不该有"顺手删远端东西"的按钮）。<br>**留档待改（不是"没实现"，而是"承诺了不存在的能力"）**：`ConnectPane` 的「选择已保存 Host / 移除 / 发现」与设置-设备页的「吊销」——我们的架构**只记一个 Host**（`hosts` 由单一 `rememberedUrl` 投影而来，见 `updateHosts()`），根本没有"多 Host 管理"这件事。正确处置是**移除这些控件**（而非硬造实现），下一轮执行；`DetailPane.onLineTap` 的空实现同批处理（详情行的可点性要么给出真实动作如"复制该行"，要么取消可点）。 |
| E131 | **死按钮收尾 + 三项新需求的线协议事实（可直接开工）** | **本轮修掉**：① `DetailPane` 行点击——根因是回调只给 `(section, index)`，宿主**拿不到文本**所以实现只能是空的；改为把**行文本**交给宿主，现在点行即复制（视图知道自己渲染了什么，就该直接给它）。② `ConnectPane` 的「使用/移除」已在上轮改为「重新接入」；「发现」按钮本身**已经是诚实的**（不可用即禁用 + 写明需要 hostkit 才可能发现），保留。<br>**三项新需求的事实（本轮取证，下一步实现）**：<br>· **工作区删除**：`workspace/delete` 请求 `{workspaceId}` → 回复 `{deleted:true}`。**关键依赖**：删除需要**注册表 id**，而我们的工作区列表是从会话 `cwd` 投影出来的（没有 id）⇒ 必须先接 `workspace/follow`。<br>· **归档**：`workspace/archiveSession` 请求 `{sessionId}` → 回复 `{archivedSessionIds}`（mutation 后的**完整**归档集合）。<br>· **`workspace/follow` 帧形状**（两者共同的数据源）：`{type:'baseline', value:{items: WorkspaceView[], archivedSessionIds}}`，其后是增量 `{type:'upsert', workspace}` 等 ⇒ 一次订阅即可同时供"工作区列表（带 id）+ 归档列表"。<br>· **定时任务**：官方为 `dsh-schedule` + `dsh-client-ui-schedule`（另有 `dsh-jobs`/`dsh-jobs-local`/`dsh-client-ui-jobs`/`dsh-tool-jobs`、`cordis-plugin-timer`）。**注意：我们的兼容面里没有 schedule/jobs 的 Remote 端点** ⇒ 它们的状态大概率走事件或投影，需要先在兼容面/事件表里定位，不能凭包名就写 UI。<br>· **技能**：官方为 `dsh-skill` + `dsh-client-ui-skill`（+ `dsh-skill-filesystem`/`dsh-skill-badge`/`dsh-tool-skill`）；**我们的兼容面里已有 `skills/list` 端点**（`ns: 'skills'`，能力 `skills`）⇒ 这一项可以直接接线。 |
| E132 | **工作区注册表数据层落地（删除/归档的前置）** | 兼容面**已有** `workspace/follow`（`shape: NONE`、stream ✓），只缺别名常量。本轮补 `WORKSPACE_FOLLOW_ENDPOINT` / `WORKSPACE_DELETE_ENDPOINT` / `WORKSPACE_ARCHIVE_SESSION_ENDPOINT`（dshcompat）并实现数据层：<br>① `openWorkspaceStream()`（连接时打开，与 `session/control` 并列）——基线 `{items: WorkspaceView[], archivedSessionIds}` 整表替换、增量 `{type:'upsert', workspace}` 按 id 合并；解析成 `WorkspaceRow{id,path,title,sessionCount}`（**没有 id 就不造行**：id 是删除/归档唯一的键）；<br>② `deleteWorkspace(workspaceId)`：`{request:{workspaceId}}` → `{deleted:true}`，成功后**本地立即过滤**（不等增量帧）；界面文案明确"**只删注册，不删目录**"——否则没人敢按；<br>③ `archiveSession(sessionId)`：`{request:{sessionId}}` → 用回复里的**完整**归档集合覆盖本地 `archivedIds`；<br>④ 快照暴露 `workspaceRows` / `archivedSessionIds`。<br>**为什么必须先做它**：我们原有的工作区列表是从会话 `cwd` 归并出来的，**没有注册表 id**，所以删除与归档此前根本无从下手。<br>**踩到的两条 ArkTS 约束**（都已修）：`(() => void) | undefined` 不能赋给 `| null` 字段；`argsFor(端点, {})` 的无类型空对象字面量会被 `arkts-no-untyped-obj-literals` 拦下 ⇒ 先声明 `Record<string, Object>` 再传。<br>**界面接线（下一轮，方法已定）**：Index 加 `workspaceRows/archivedSessionIds/confirmingDeletePath` 三个 `@State`，在 `workspaceGroup` 组头按**路径**匹配注册表 id，加「删除」（**两步确认**：先变成"确认删除/取消"）与「归档」动作，常规列表按 `archivedSessionIds` 过滤、另开「归档」分组列出。 |
| E133 | **工作区删除接到界面 + 归档集合开始生效** | ① **「删除」只在真有注册表 id 时出现**：列表里的工作区由会话 `cwd` 归并而来（无 id），注册表行才有 `workspaceId`，两者唯一可靠的共同键是**目录路径** ⇒ `registryIdOf(w)` 按归一化路径匹配，**匹配不到就不显示按钮**（而不是给一个点了没反应的入口）。② **两步确认**：同一位置先变成「确认删除 / 取消」，并在提示里写清"**只移除登记，磁盘上的目录与文件不会被删除**"——一句说不清的确认弹窗塞不进 Row，而这句必须让人看见。③ **归档集合开始生效**：`sessionsOf(w)` 过滤 `archivedSessionIds`，已归档会话从常规分组隐去（归档集是注册表全局的，与官方 `workspace/follow` 基线同源）。<br>**顺带**：`WorkspaceRow` 在 appstate 对外转出（声明在 SessionHub）。<br>**仍欠**：单独的「归档」视图与「归档会话」入口（`archiveSession()` 已在数据层就绪，只差列表与动作）；此项与「技能（`skills/list`）」是下一批。 |
| E134 | **归档闭环（动作 + 归档分组）** | ① 会话行新增「归档」→ `archiveSession(sessionId)`（`{request:{sessionId}}`，用回复里的**完整**归档集覆盖本地）；② 工作区视图新增「**归档 · N**」分组：列出归档集里的会话（图标 + 标题 + 时间，可点开查看），空集时整段不出现。<br>**为什么归档组是只读的（如实说明）**：线协议里只有 `workspace/archiveSession`，**没有反向端点** ⇒ 不提供"恢复"（那会是个点不动的按钮）。官方文档也只描述了归档集本身，没有描述取消归档。 |
| E135 | **技能模块（`skills/list` 接线）** | 官方对应 `dsh-skill` + `dsh-client-ui-skill`（另含 `dsh-skill-filesystem`/`dsh-skill-badge`/`dsh-tool-skill`）；**我们的兼容面里已有 `skills/list`**（`ns:'skills'`、REQUEST 形态），所以这一项可以直接接线。<br>**已落地**：① `SKILLS_LIST_ENDPOINT`（dshcompat）；② `SessionHub.refreshSkills()`——进设置页且已连接时读一次，解析成 `SkillRow{name,description,whenToUse,provider}`；③ 设置新增「**技能**」分区：卡片列出名称/提供方/描述/适用场景，空态与"读不到"分开写。<br>**关键诚实点**：类型定义只给了**单个技能摘要**的形状，**没说列表挂在哪个外层键**（`skills`/`items`/裸数组都可能）⇒ 解析**三种都试**，都不中时**不猜**：把回复的**真实顶层键**记进旁路日志、并在界面上写明"形状未识别，待按真实形状收紧"——绝不把"没读出来"渲染成"没有技能"（那会让人以为宿主没装技能）。下一次连真机时这一条会直接给出答案。 |
| E136 | **定时任务：本版 dsh 不暴露给客户端线协议（取证结论，附可行替代）** | 用户要求"与官方一样具备定时任务"。逐条取证：<br>① 官方 UI 是 `dsh-client-ui-schedule`，其 `lib/client.js` 是一个**目录动作**（`ScheduleCatalogAction`），消费的是带 `scheduledAt` 的**记录**（它只做"频率/本地时间/相对时间"的格式化），**不是**从某个 Remote 端点拉列表；<br>② 宿主侧 `dsh-schedule` / `dsh-jobs` / `dsh-jobs-local` / `dsh-tool-jobs` 四个包的 `lib/` 里**都没有 `typert.host.js`** ⇒ **它们没有注册任何 Remote 端点**（有端点的包必有该文件，本项目其余端点全部据此确认）；<br>③ 我们的**事件表里也没有** schedule/jobs 事件类型（`dshcompat/RemoteEvents.ets` 与 `EventShape.ets` 全表检索无命中）。<br>⇒ **结论**：定时/作业在这一版 dsh 里是**宿主侧能力**（由 `dsh-tool-jobs` 作为工具提供给智能体），**没有面向客户端的查询面**。客户端要显示它只能靠"自己发明一套接口"，本项目不做这种事。<br>**可行替代（已具备，无需新代码）**：智能体创建/查看作业是**工具调用**，它们落在会话轨迹里 ⇒ 我们的**工具卡**已经能看到这些动作发生（E112）。也就是说：**能看见它在做什么，但没有独立的"定时任务"页**——这是上游能力的边界，不是本客户端的缺口。<br>**若将来要做独立页面**，前置条件是上游把它投影出来（例如某个投影键或事件）；届时可先在一次真机上 dump `projections` 的键名来确认（本项目已有 `--dump` 与旁路日志基础）。 |
| E140 | **多语言的两种机制必须分开（本轮踩到并纠正）** | 语言批次三推进时发现一个**会静默出错**的区分：<br>· **渲染绑定**（`Text('…')`、`placeholder:`）→ 用 `$r('app.string.*')` ✓ 系统按语言解析；<br>· **运行时状态字符串**（如 `this.settingWriteNote`、中枢里的提示语）→ **不能**用 `$r` 赋值：它是 `string` 字段，ArkTS 不会去解析资源引用（真按资源对象赋进去要么编译不过、要么在别处被当作字符串用而露馅）。这类文案只能走资源管理器取值：`getContext().getResourceManager().getStringSync($r('app.string.x').id)`。<br>本轮据此**只改渲染绑定**（如「正在连接本机核心…」），把状态字符串留待统一走 `resourceManager` 取值——并**先确认编译与行为再改**，不为了"看起来全绿"把资源对象塞进字符串字段。<br>这条区分对后续批次是硬约束：**先分类，再替换**。 |
| E142 | **多形态复核发现一处真回归：待决徽标变成死分支（已修）** | 无设备条件下复核折叠屏/平板/2in1 的布局分派（SINGLE / DOUBLE / TRIPLE 三条分支 + `detailPanelAvailable` 门控 + `tabContent(compact)` 透传），结构本身完好；但顺着"已删页签还有谁在用"这条线查出一个**真回归**：`pendingBadge`（有待决时页签上的角标）仍挂在 `tab === NavTab.PENDING` 上，而该页签在 E108 已被删除 ⇒ **角标永远不会显示**——用户失去了"有东西在等我"的一眼信号。三处（底栏 / 侧栏 / 面板三种呈现）统一改挂到 **`NavTab.WORKSPACES`**（待决现在就落在工作区视图里）。<br>**教训**：把页签合并掉时，**挂在旧页签上的"信号类"逻辑最容易漏**——它不报错、不崩溃，只是安静地不再出现。合并类改动的复核清单应当包含"旧枚举成员的所有引用点按用途分类：归一化 / 死分支 / 信号"。 |
| E143 | **状态文案的架构边界：多数文案来自 appstate，不能"就地换资源"** | 铺开 E141 时查清了一件事，它决定后续多语言该在哪一层做：**状态文案的来源分两层**——<br>· **Index 内**（`this.settingWriteNote = …`）→ 有 UI 上下文，可用 `localized()` 经资源管理器取值（E141 机制 ✓）；<br>· **appstate 内** → 实测 **32+ 处**（`SessionHub` 的设置/凭据/工作区/技能操作结果与失败原因、`Settings` 模型的输入校验提示，形如 `message: \`未生效：${friendlyFailure(...)}\``），而 **appstate 是纯 TS 模块、没有 UI 上下文** ⇒ `$r` 与 `resourceManager` 都用不了。<br>**可选两条路**：① 中枢返回**码 + 参数**（如 `{code:'settings.write.failed', params:{...}}`），由视图层本地化——干净但要把 32+ 处逐一改造，并保持现有的"可执行建议"文案质量；② 在 `configure()` 时把一个**本地化函数**注入中枢（`(key, params) => string`），改动面小、但把 UI 依赖带进了中枢。<br>**建议走 ①**（与项目既有纪律一致：上游协议事实集中在 dshcompat、展示逻辑归视图层；中枢不该知道"哪种语言"）。**在没做这件事之前，不要半途把资源对象塞进这些 string 字段**——那会得到一个"看着改完了、运行时是对象或空串"的界面。<br>真机不受影响：这些文案目前是中文，与"默认中文"的目标一致；缺的是**英文系统下的回落**。 |
| E145 | **E143 方案①动工：结构化回执类型 + 文案资源就位（附加式，构建已验）** | 按 E143 选定的方案①（中枢产出**码 + 参数**、视图层本地化）落地第一步，且**只做附加式改动**，以免在无设备期间留下半改的树：<br>① `appstate` 新增 `ActionNotice{code, params}` 与唯一构造点 `notice()`——`code` 用点分命名（`workspace.deleted`），`params` **只放数据**（名字、id、Host 给的原因），**不放整句文案**；并写明视图层的兜底纪律：**遇到不认识的码必须显示"码 + 数据"，而不是空白**（空白会让人以为操作没发生）。<br>② 中英资源就位：`note_workspace_deleted` / `note_session_archived` / `note_no_workspace_id` / `note_archive_no_session` / `note_offline`。<br>**下一步（有整轮上下文时做）**：把 `deleteWorkspace()` / `archiveSession()` 的返回值由字符串改为 `ActionNotice`（两处调用点在 Index，用 `localized()` 渲染；Host 侧的失败原因作为 `params.reason` **原样透传**——上游文案不是我们的 UI 文案，不翻译），随后按同一模式迁移余下的 30 余处。 |
| E148 | **真机验收（首轮）：两处发布件缺陷被真机抓出并修复，三项 PASS** | 设备接回后按 §12 执行，**第一轮就抓到两个只有真机能发现的缺陷**：<br>① **入口脚本没进包**：E127 给 `hostcore/app/main.js` 加的 `HDSH_PLATFORM=ohos` 只跑了 `pack-core --place-in-app`（放的是**核心 zip**），而**入口脚本要 `tools/place-host-app.mjs`** ⇒ 包内是旧脚本，**凭据豁免在真机上根本没生效**。补齐放置后重装，读数 `平台标识：HDSH_PLATFORM=ohos` ✓（**12.7 PASS**）。教训：**改入口脚本必须重放**，否则"改了、构建绿、设备上没生效"。<br>② **E121 信息放错位置**：缺失原生件名单放在 `fact()` 的 note 参数，而核心页这一版式**不渲染 note** ⇒ 真机仍只显示"2/3 已加载"。改为写进**值**：读数 `原生件加载 2/3 已加载 · 缺 koffi` ✓（**12.3 PASS**）。**这同时回答了长期悬着的问题：缺的是 `koffi` 而非 node-pty**；且 `diag3 dlopen …libkoffi.so = ok` 表明**库能加载**，失败在 koffi 的 **JS 加载器路径**（`build/koffi/${platform}_${arch}`）——下一步据此收窄。<br>③ **归档链路 PASS**：真机点归档后页面顶部显示 E146 那条**本地化回执**「已归档（可在「归档」里查看）。」✓ ⇒"中枢产出码 + 视图本地化"闭环在设备上成立。<br>④ 其他读数：`HDSH-AUTH connect 结果：ok=true sessions=7`；运行时事实 `Node 22.23.2 / zstd 可用 / jitless / 127.0.0.1:3120 / 已安装 2 个版本 / 插件清单 152 行`。<br>**12.1 未完成**：两次点「切换」都没命中（无 `HDSH-CORE 切换结果`），第二次点按后应用退到桌面、进程消失且无 rc.3 启动记录 ⇒ 下次用更准坐标重试并同时抓 `AppKilledReporter`。 |
| E149 | **12.1 决定性的坏消息：点「切换」让应用原生崩溃（SIGABRT）——目标里"切换/回滚"仍不成立** | 先解决"点不中"的问题：用 `uitest dumpLayout -p …` 取**精确 bounds**（早先靠估算坐标两次失手；dump 出的 rc.3「切换」是 `[1105,2333][1190,2382]`，说明**我上次的 (1128,2348) 其实命中了**——所以不是点不中，是**动作本身把进程干掉了**）。精确点击后取证：<br>`reason=Cpp Crash, FOREGROUND=1, killId=2004`<br>`CppCrash, isForeground: 1`<br>`exitSigno = 6` ⇒ **SIGABRT（原生崩溃）**<br>现象链完整：点切换 → 进程消失 → 无 `HDSH-CORE 切换结果` → 无 rc.3 的 `BOOT_10_ENV_READY`（日志里仍是 rc.2 的那次）。<br>**含义**：E119 修的 `g_started` 复位是**必要但不充分**——现在能进到"停旧 + 起新"这条路径了，而**再次 `node::Start`（同一进程内第二次启动 Node）会让进程 abort**。这与本项目早先记录的物理约束一致：libnode 在应用进程的一个线程里只被设计为**启动一次**。<br>**下一步方向（三选一，待验证）**：① 让"激活新版本"**不重启 Node**——改为把核心目录切换后由 Node 侧重新 `runProfile`（若上游支持热切换）；② **停止时真正结束进程**（由系统重新拉起一个新的应用进程承载新核心），即把"切换"实现为"自杀 + 重启"；③ 查明 SIGABRT 的确切位置（faultlog 需要更高权限，或用 `hdc shell aa dump`/`hilog` 里的 native backtrace 字段）后再决定。**在解决之前，界面上"切换/回滚"不应声称可用**——按"界面不撒谎"的纪律，这属于必须显式告知的能力缺口。 |
| E150 | **切换入口下线：界面不再提供会崩的操作（真机已验证）** | 按"界面不撒谎"的纪律处置 E149 的结论：核心页的「切换」在两行版本上都改为**灰色「切换暂不可用」且不可点**（由 `CORE_SWITCH_AVAILABLE = false` 单点控制；正确实现落地后改为 true 即恢复），并在代码里写明原因（libnode 单进程只允许启动一次 ⇒ 停旧+起新会 SIGABRT）。**真机截图确认**：`0.1.5-rc.2 [已安装] 切换暂不可用` / `0.1.5-rc.3 [已安装] 切换暂不可用`，同页 `原生件加载 2/3 已加载 · 缺 koffi`（E121）与运行时事实照旧正常。<br>**踩到的重复错误**：又把模块级常量插到了 `@Component` 与 `export struct` 之间 ⇒ `Decorators are not valid here`（同一坑本会话内第二次）。**记录在案：视图文件里加模块级声明，必须放在 `@Component` 之前**。 |
| E151 | **切换的可用替代：安排激活 + 重启应用生效（数据层已改，UI 门控暂留）** | 读 `DshHost.switchTo()` 看清了崩溃的确切位置：它的**健康检查是"把候选真起一次"**（注释写着"为什么健康检查只能是'真的起一次'"），而这正是同进程第二次 `node::Start` ⇒ SIGABRT（E149）。<br>**关键洞察**：激活本身只是 `store.activate()` **写状态**，起候选只是为了"健康检查" ⇒ 在端侧可以**不起**候选：改为**静态通过 + 记录激活**，并明确告知"**完全退出应用后重新打开，新版本即生效**"（下次启动时入口脚本读到的 `current` 就是新版本，行为与切换成功等价）。宿主**未运行**时仍走原路径（那时是本次进程的第一次启动，不会崩）。已落地 `switchTo()`（构建通过）。<br>**为什么 UI 门控暂时不解除**：`rollbackTo()` 是**同构的崩溃路径**（同样"停旧起新"），而我还没有确认它取"上一版"的内部 API ⇒ **不猜**。半修好的能力暴露给用户，和没修一样糟。**下一步**：按同一模式改 `rollbackTo()`（先读清它如何取上一版），再把 `CORE_SWITCH_AVAILABLE` 置真、把界面文案改成"重启应用后生效"。 |
| E152 | **🎯 12.1 PASS：核心版本切换在真机上端到端跑通（"安排激活 + 重启生效"）** | `switchTo` / `rollbackTo` 同批改为"宿主运行时不起候选、只记录激活，重启应用生效"（`rollbackTo` 用的是 `store.rollback()`，同样是**写状态**；宿主未运行时仍走原路径）。解除 UI 门控后真机验证：<br>① 点「切换」→ **不再崩溃**：`HDSH-CORE 切换结果：ok=true note=已安排切换到 0.1.5-rc.3（当前 ）：请完全退出应用后重新打开，新版本即生效。`，`pidof` 仍返回进程号 ✓（此前是同一步 `CppCrash / exitSigno=6`）；<br>② **完全退出并重新打开** → `BOOT_10_ENV_READY core=…/cores/**0.1.5-rc.3**` ⇒ **新版本真的在跑**；<br>③ 自动接入成功：`HDSH-AUTH connect 结果：ok=true sessions=7 err=(none)`（宿主每进程重生成 token，客户端已重新接管）。<br>**结论**：目标里"核心版本的安装 / 切换 / 回滚"这条**在端侧成立**，代价是一次应用重启（与桌面 IDE 更新同类语义），而不是崩溃。<br>**遗留小瑕疵**：结果文案里"（当前 ）"为空——`store.readState().current` 在端侧读到空串；不影响功能，但应改成"读到空就不显示这一段"。<br>**下一步**：用同一条链路验一次「回滚到上一版」（应回到 rc.2），即完成 §12.1 的两条。 |
| E153 | **§12.1 收尾取证：切换 PASS、回滚未触发（如实记录）** | ① **切换那半已在真机闭环**（E152）：安排 → 重启 → `BOOT_10_ENV_READY core=…/0.1.5-rc.3` + `HDSH-AUTH connect ok=true sessions=7`，且点按不再崩溃。<br>② **回滚那半未触发**：点「回滚到上一版」后**无任何回滚日志**，重启后 `BOOT_10_ENV_READY` 仍是 `0.1.5-rc.3` ⇒ 要么点按未命中，要么按钮处于「**没有可回滚的上一版本**」的禁用态（`decision.next.previous` 为空）。**不把它算作通过**——需要下一次用 `uitest dumpLayout` 取该按钮的 `enabled` 与文案再判定：若为禁用，则问题在"激活时没有记下 previous"，属状态记录缺陷；若为启用，则是我点按坐标的问题。<br>③ 顺带一条真实读数：未选中会话时，设置-通用的**权限模式显示「未报告 / 宿主暂未报告当前访问模式」** ✓ —— 这正是 E124 想要的诚实形态（宿主没投影就不编一个默认值），而非缺陷。<br>④ 也再次验证了启动期的连接竞争是**良性**的：先出现一次 `ok=false … 无法连接到 127.0.0.1:3120`，随后自动重连 `ok=true sessions=7`（E111 的中性"正在连接"文案正是为此）。 |
| E154 | **真机使用反馈六条（用户提出，逐条定位；下一轮执行）** | ① **新建会话应挂在工作区上**：现在「+ 新建会话」在工作区视图**顶层**（`Index.workspaceHub` 的动作行），用户看不出"这会给哪个工作区加会话"。**改法**：把入口移进**每个工作区组头**，并新增"在该工作区建会话"的路径——`hub.ensureWorkspace(w.path)` 后再 `createSession()`（`ensureWorkspace` 已存在，E98）。<br>② **归档后的会话仍在工作区里出现**：过滤逻辑已写（`Index.sessionsOf` 按 `archivedSessionIds` 过滤），所以问题在**数据来源**——`archivedSessionIds` 目前只来自 `workspace/follow` 基线与归档回复；若该流在设备上没跑起来，重启后集合为空 ⇒ 归档会话"复活"。**先查**：`hilog | grep 'workspace baseline'`（`openWorkspaceStream` 有记录行）；若流没起来，退路是**把归档集持久化**（本机偏好）以在会话内保持过滤。<br>③ **预设页没有中文**：`预设` 分区直接渲染宿主给的分区（`agent-default-model` 的 Provider/Model/Reasoning effort、`agent-loop` 的 Max parallel tool calls、`agent-presets` 的 Default）。**改法**：把这些键加进已有的 `officialSettingLabel()` 映射表（E120 的机制，官方中文取自客户端包内嵌 locale）。<br>④ **模型页重复要密钥**：提供方卡片的「配置密钥」与下方「**模型凭据**」区块（`SettingsPane.ets:914` 起）指向同一个引用 ⇒ **删掉「模型凭据」区块**（提供方卡片是官方形态，E128）。<br>⑤ **推理强度默认 high 未被选中**：档位高亮取的是 `this.selection.effort`，而它与宿主默认（`deepseek-flash` 的 high）没对齐 ⇒ 应在拿到目录/默认选择时把 `selection.effort` 初始化为宿主的默认档（而不是留空）。<br>⑥ **核心页不需要括号内容**：如「核心已在运行（libnode.so（自建 Node））」「jitless（无 JIT）」等——括号里的实现细节对使用者无意义。**改法**：清理核心页标题与事实值的括号（来源在 `Index` 的 `buildCoreView` 参数与 `CorePane` 渲染，逐条去掉）。 |
| E156 | **用户纠正：「Agent 预设」的语义我做错了——它是"会话的 Agent 插件组装"，不是模型设置** | 用户原话：**"预设即一个会话的 Agent 所运行的插件组装 —— 它的工具、提示词与能力。复制一份既有预设改成自己的，或用「创造模式」让 Agent 帮你创建。"** 我此前（E129）把「预设」页做成了**把 `agent-*` 设置命名空间（provider/model/推理强度/最大并行工具调用数）列出来**——那是**模型设置**，与预设无关，语义确实错了。<br>**官方行为**（`dsh-client-ui-agent-preset` 的 README 早已写清，是我没照做）：设置分区把**名单**呈现为卡片；**复制对话框是创建 preset 的唯一入口**（浏览器不编辑任何组装文本）；自定义卡片保留"**打开 preset 自身文件**的位置动作"；**删除**移除 preset 目录（已据其组装的会话继续运行）；随附 preset 在**只读查看器**中打开、不提供位置与删除；`broken` 行渲染为标记卡片；新建会话 chip 以部署默认值打开、暂存一个选择落到下一个空白会话上；名单含自指 `cordis` preset 时还有一张"虚线添加卡"。<br>**本轮取证到的好消息**：预设的线协议**早就存在，而且已在我们兼容表里**（`dshcompat/Endpoints.ets:23/35/47`）——`agentPresets/list`、`agentPresets/copy`、`agentPresets/deletePreset`、`agentPresets/read`（宿主侧 `dsh-agent-presets` 有 `typert.host.js` ✓）。⇒ 官方那套"复制 / 打开位置 / 删除 / 只读查看"**都可以照实现**，不是能力缺口，是我没接。<br>**下一轮改造计划**：① 数据层：`AGENT_PRESETS_*` 四个端点常量（dshcompat）+ 中枢方法（list/copy/delete/read，解析容忍形状并如实记账，沿用 E135 的纪律）；② 「预设」页改为**名单卡片**：名称 + 描述（来自 preset 自身）+ 「设为默认」+ 自定义项的「复制 / 打开位置 / 删除」，随附项只读；③ 把当前误放的模型设置项移回它们该在的地方（模型页已有提供方卡片承载）。 |
| E159 | **预设页的线协议契约与真机数据（`agentPresets/list`），以及一个**反直觉的形状事实**** | 用探针直连设备取证（比读类型定义更硬）：<br>① **`agentPresets/list` 是具名参数形态**——带 `request` 包裹会被拒：`gateway/arguments-invalid: … unexpected "request"`；传 `{}` 才成功（`{"scope":null}` 也被拒）。**这与 session/* 那一族的 `request` 包裹相反**，是本项目又一次"形状不能凭经验推"的实例。<br>② **真机返回的完整名单（5 项，全部 `trust:"system"` ⇒ 随附只读）**：<br>· `ondevice`「**端侧模式（无 Shell）**」`isDefault:true` — "端侧编码 Agent：文件编辑、检索、Skills、计划、目标、子代理与工作流；不含 Shell 工具（鸿蒙不支持进程创建）。"<br>· `standard`「标准模式」— 功能完整的编码 Agent，支持文件编辑、Shell、文件与网页检索、Skills、计划、目标、子代理和工作流。<br>· `ptc`「PTC 模式」— 默认不提供 workflow 工具，其他工具通过 PTC SDK 呈现，让模型用一个 TypeScript 程序组合多步操作。<br>· `minimal`「极简模式」— 仅提供持久 shell 的单工具编码 Agent。<br>· `cordis`（自指预设，官方文档提到名单含它时会出现"虚线添加卡"）。<br>**每项字段**：`id` / `trust` / `isDefault` / **`name`** / **`description`** ⇒ 正好是官方卡片要显示的两行（名称 + 预设自己的描述），**不需要我们编文案**。<br>③ 同时确认 `provider/model` 等 `agent-*` **设置命名空间与预设是两码事**（E156 的纠正成立）：预设名单来自 `agentPresets/*`，模型设置来自 `agent-*` 命名空间。<br>**下一轮**：中枢接 `list`（已加常量）+ 预设页改为名单卡片（名称 + 描述 + 「设为默认」；自定义项「复制 / 打开位置 / 删除」，`trust:"system"` 的只读）；`copy`/`deletePreset`/`read` 的形状同样**先用探针确证再实现**（本轮的教训：形状不能推）。 |
| E164 | **诊断通道本身是坏的：`record()` 既不写 hilog、又被开关挡住——修好后立刻推翻了一个错误结论** | 为定位"归档会话复活"（用户反馈②），我在 `openWorkspaceStream()` 加了一行开流记账，真机复跑却**什么都看不到**。查 `record()` 实现才发现两个"看不见"叠加：<br>① **只写应用内 trace、不写 hilog** ⇒ `hdc` 侧 grep 不到；<br>② 而且**被 `traceEnabled`（界面上的旁路日志开关）挡着**，默认关闭 ⇒ **平时一行都不产出**。<br>⇒ 本项目一路写的"如实记账"（工作区流、技能/预设形状、归档/删除结果…）在真机排障时**等于不存在**；我因此在"流没开"与"开了没数据"之间**只能猜**，并基于猜测写下过错误结论（E154 的第②条）。<br>**修法**：`record()` 改为**无条件 `console.info('HDSH-TRACE …')`**（ArkTS 的 console 进 hilog，tag=JSAPP，hdc 可直接 grep），应用内 trace 仍尊重开关（那是给用户看的界面，不是排障通道）。<br>**修好后第一跑就推翻了旧结论**：`workspace baseline items=2 archived=3` + `archive ok …（归档集 4）` ⇒ **`workspace/follow` 一直是好的**，归档集也在正常累积。**教训**：诊断通道必须先自证可用，否则"没有输出"会被误读成"没有发生"。 |
| E168 | **预设三动作的契约探针结果（copy / deletePreset / read）** | 沿用"让宿主自己报出可接受字段"的办法（E159 那一招），三个动作各探一轮：<br>· **`deletePreset`：字段就是 `id`** ✓，且**随附预设被宿主正确拒绝**：<br>`agent-presets: preset "standard" cannot be written: it ships with the deployment` ⇒ 官方"随附只读"的规则**在宿主侧就已强制**，客户端不必自己兜。<br>· **`copy`：接受 `{id, …}`，但还需要至少一个字段**——`copy {id, newId}` 被描述符拒（`unexpected "newId"`）、`copy {id, name}` **通过描述符却在处理函数里崩**（`Cannot read properties of undefined (reading 'length')`）⇒ 字段名里有 `id` 与 `name`，另有一个必填字段缺失（名字待定：可能是 `description` 或目标 id 之类）。<br>· **`read`：字段名仍未知**——`id` / `presetId` / `name` / `preset` / `{id,path}` **全部被描述符拒**。<br>**结论**：`deletePreset` 可以立刻接（字段确定、只读保护由宿主给出）；`copy` 与 `read` 还差最后一两个字段名，需要再探（或直接读宿主侧 `typert.host.js` 的 schema——那条路比试探更快）。<br>**副产物**：`copy {}`/`read {}`/`deletePreset {}` 都触发 `Cannot read properties of undefined (reading 'length')` ⇒ 这几个处理函数**缺少参数校验**（少参时抛的是内部 TypeError 而非 `gateway/arguments-invalid`）。我们能做的是**永远不空参调用**，并把这条记在案（属上游健壮性问题，不是我们的缺陷）。 |
| E171 | **集中测试就绪态核对：`切换` 已开放、插件页就绪（待用户点按）** | 真机 `dumpLayout` 两项事实：<br>① **核心页的「切换」已恢复可点**（`CORE_SWITCH_AVAILABLE = true`，页面显示「切换」而非「切换暂不可用」）⇒ 目标里"核心版本的安装 / 切换 / 回滚"**三项都是可用入口**，其中切换在 E152 已验证端到端（安排激活 → 重启 → 新版本运行 + 自动接入），回滚走同一机制。**代价是一次应用重启**（与桌面 IDE 更新同类语义），不是崩溃。<br>② **插件页（12.2）就绪**：配置子页签列出各行与状态，形如 `cordis:include｜include · 已启用 · active｜待确认｜第一方`、`include:hmr · 已禁用`、`@deepseek-ai/dsh-llm` ⇒ **"待确认"这一列正是"已写入但需重启核心才生效"的如实标记**（E91 的设计：`patchReload: 'startup'`）。<br>**下一步（用户点按即可判定）**：点某行的启停 → 看状态是否转为待确认、提示是否给出原因；重启核心 → 看是否生效。 |
| E176 | **模型页对齐官方：已删数据源，下一步删空壳；并已定位「全部模型」的真实形态** | 用户要求："提供方里已经配置密钥 ⇒ 不需要额外的模型凭据 apiKey 设置了；**全部模型也不需要单独放出来，应当在提供方的展开里设置**，与官方一致；并支持**添加多个提供方**（含主流提供方）。"<br>**本轮进展**：<br>① 凭据区的两个数据源都已置空（E157 的 `orphanCredentials()` + E158 的 `providerKeyRefs()`）⇒ 内容不再出现；但那个外层 `Column`（**约 1108–1213 行**，含 `.padding(Sp.M)` 与 `.backgroundColor(background_secondary)`）**仍会渲染成一条空白灰条** ⇒ 下一轮按此范围**整段删除**（标题的条件渲染只挡住了标题，挡不住这个列本身）。<br>② **「全部模型」的真实形态**：三次按字面检索只命中注释 ⇒ 它不是以 `Text('全部模型')` 呈现的，而是**「当前模型（Host 的默认选择）」+「推理强度（当前默认）」这一组只读区块**（`modelTab()` 里位于凭据区之后，约 1214–1257 行起）——用户说的"不需要单独放出来"指的应是这组：官方把"选哪个模型/强度"放在**会话输入区**（`dsh-client-ui-model-selection`，按会话写 `session/selectModel`），设置页只管**提供方与密钥**。⇒ 下一轮把这两个只读区块**收进提供方卡片**或**移除**（保留必要说明一行）。<br>③ **添加提供方的官方做法已取证**（下一轮实施）：`llm/listProviders`（已注册路由）**∪** `llm/listConfigurableProviders`（可配置提供方目录，NONE 形态）⇒ 未配置者以"可添加"呈现；添加 = 写该提供方的**设置命名空间**（走既有设置写入通道）；模型目录在该命名空间下用 `llm/discoverModels`（NAMED）发现——这正是"模型在提供方展开里设置"。<br>**声明**：本轮及此后**无真机可验**，所有结论以**构建 + 门禁**为准，并在条目里标注（本条尚未真机验证）。 |
| E182 | **命令面板取证：端点早就在兼容表里（`commands/list` / `commands/execute`），且是会话范围的** | 对着官方 `dsh-client-ui-commands` 取证，得到可直接实施的完整事实：<br>① **端点**：`commands/list` 与 `commands/execute` **已经在我们兼容表里**（`dshcompat/Endpoints.ets:83/95`，`ns:'commands'`）⇒ 不需要新通道，是"没接"。<br>② **调用形态**：官方客户端写的是 `ctx.remote.commands.list(sessionId)` ⇒ **`commands/list` 是会话范围的**（与 E174 的 `skills/list` 同一类：能力随会话而定）。**这正好解释了技能那次为什么报 `failed to observe session "undefined"`——同一族端点都要求会话**。<br>③ **官方命令（宿主提供，客户端只渲染）**：`compact`「压缩以上对话内容」、`export`「将当前会话内容导出为 ZIP」、`feedback`「发送关于当前会话的反馈」、`goal`「设置或查看长期任务目标」、`permission`「切换权限预设（沙箱模式与审批策略）」、`plan`「进入或退出计划模式」——**文案由宿主给**（`description.<id>`），所以我们**不该硬编码**这些名字。<br>④ **相关事件**：`commands/change`（名单变化）、`agent-preset/selected`（会话选定的预设变化）⇒ 已属我们已知事件族，可挂到既有事件处理上。<br>**实施设计（下一轮）**：中枢 `refreshCommands(sessionId)`（未选中会话时不发请求并如实说明，沿用 E174 的教训）→ 会话输入区一个入口按钮 → 弹出的列表渲染宿主给的命令与描述 → 选中后 `commands/execute`（带 sessionId + 命令 id）。**不做硬编码命令**，因为官方也是"列宿主给的"。 |
**当前状态（本轮结束）**：⓪ **端侧已真机跑通**：内嵌 Node + 本地 Host、真实模型对话、工作区/设置双页签、对话与轨迹、审批内联、语言、权限真实值、会话统计、工作区删除、归档、预览/预设/技能。① 证据表 **E1–E171**；构建与门禁全绿（上架红线 PASS、原生依赖闭包 PASS、死按钮扫描 67 处空实现待逐条判断）。② **已真机验收**：核心版本**切换**（E152：安排激活 → 重启 → 新版本运行 + 自动接入；安装/回滚同机制同入口，E171）、工作区删除/归档（含 E146 本地化回执）、凭据权限豁免（E127/E148 的 `HDSH_PLATFORM=ohos`）、未加载原生件点名（**缺 koffi**，E148）、预设页（E161/E162/E170：名单+复制+自定义项删除）、模型页去重与推理强度默认（E158/E167）、核心页去括号（E166）、反馈①②③④⑤⑥ 全部处理。③ **仍待**：插件启停与多形态（折叠屏/平板/2in1）的**点按判定**、预设 `read` 只读查看器、英文系统下的状态文案回落（E143 方案①已闭环、余下 30 余处待迁移）。④ 逐项步骤与预期读数见 **§12 设备复验清单**与 **§12.9 记录模板**；**旧结论以最新条目为准**（例：E154 的第②条"`workspace/follow` 没起来"已被 E164 推翻——真机 `workspace baseline items=2 archived=4` 证明该流一直正常）。


**结论**：端侧自足运行 dsh **没有原理性障碍**；真正的工程量集中在
**① 拿到一个能被 HAP 加载的 Node 运行时（`libnode.so`，且完成签名）**，
**② 端侧 profile 把不可用的原生行精确关掉、可用的行打开**，
**③ 核心/插件的版本管理（dsh 本身**没有**提供，必须我们自己建，见 §6）**。

---

## 4. 运行时路线（含回退）

### 4.0 已决策：分两阶段（2026-09-12 拍板）

| 阶段 | 载体 | 目的 | 状态 |
|---|---|---|---|
| **阶段一（打通）** | **复用 Electron-on-鸿蒙 运行时**（`harmonypc-electron`，Electron 37 / Node 22.17.0）——**只跑 Node 与 dsh Host，不创建 Chromium 窗口** | 最快拿到「端侧自足 Host + 现有 ArkUI 页面 + @ohos-ports 原生能力」的**可运行系统**，把所有端侧集成问题（沙箱 HOME、symlink、loopback、profile、解包、核心版本切换）一次问清楚 | ← 当前 |
| **阶段二（目标形态）** | **自建 `libnode.so`**（官方 Node 源码 `--dest-os=ohos --dest-cpu=arm64 --shared`）+ NAPI 引导，替换掉 Electron 运行时 | 去掉 ≈172 MB 的 Chromium 负载、摆脱第三方运行时依赖、覆盖手机形态（HNP/`executableBinaryPaths` 主要在 2in1/tablet 可用） | 阶段一跑通后启动 |

> 两阶段的**接口必须一致**：ArkTS 侧只见 `DshHost`（start / status / stop），底下换载体不动上层。
> 这也是把 §4.1 与 §4.2 分开写的原因——**阶段一的产物不能泄漏到状态层与 UI 层**。

#### 4.1.1 阶段一实测状态（2026-09-13，产物已到位）

用户已提供华为侧产物 `v37.2.3-20260825.1-release.zip`（353.3 MB）。解包与校验已脚本化并入库
（`tools/electron-runtime/{extract.ps1,verify.ps1,README.md}`）。实测：

| 项 | 实测值 |
|---|---|
| release zip 内条目 | **1 个**：`libelectron_138.tar.gz`（373.9 MB） |
| 解开后的关键库 | `libelectron.so` **172.7 MB**、`libadapter.so` 7.33 MB、`libffmpeg.so` 2.10 MB |
| `libc++_shared.so` | 包里**没有**，从 OHOS SDK 的 `native/llvm/lib/aarch64-linux-ohos/` 复制补齐（1.20 MB） |
| 可直接引入的模块 | `libelectron/ohos_hap/{electron,web_engine}` + `{AppConfig,hvigor}` |
| 未 strip 的符号版 | `libelectron/lib.unstripped/libelectron.so` 1.19 GB —— **解包时排除** |
| ELF | 四个库均为 ELF64 / AArch64 / `ET_DYN` |
| **签名** | 四个库**都只有 `.note.ohos.ident`、没有 `.codesign`** ⇒ **未签名** |

**对"未签名为何仍可用"的判断依据（写清楚以免被误读成乐观）**：XPM 拦的是**执行**未签名二进制
（社区方案被拒绝的正是 `resources/resfile/electron` 这个**可执行路径**）；而这四个 `.so` 是
**随 HAP 打包、由 `dlopen` 加载**，签名覆盖来自 **HAP 自身**；同一批产物已被社区在真机
（HarmonyOS 6.1.0.135 / API 24）上跑通。**同时如实记为风险**：若平台对 in-HAP 的 `.so` 也强制
代码签名，需用 `binary-sign-tool` / `self-sign.py` 补签后再打包。

> 阶段二的自建路线同样在推进：OpenHarmony 公开 SDK（含 Linux NDK，clang 15.0.4）已成功获取，
> Node v22.23.2 源码已解包并推进到 `deps/ncrypto` 编译；当前阻塞点是
> **该文件用了 C++20 的 `<=>`，而 gyp 传的是 `-std=gnu++17`**（且 SDK 自带 clang 15 偏旧，
> 上游 OHOS 移植 CI 用的是 LLVM-19）。阶段一不再依赖它，因此它转为并行轨道。

#### 4.1.2 阶段一的启动链与三条硬约束（源码精读结论，2026-09-13）

对解包出的 `electron` / `web_engine` 两个模块做了逐文件精读（含 `libadapter.so` / `libelectron.so` 的符号与字符串），结论如下：

**启动链（谁真正把 Node 起起来）**
```
module.json5 srcEntry → AbilityStage extends WebAbilityStage
  → WebAbilityStage.onCreate → runTaskAsync → JsBindingUtils.initNativeContext(0)
      → libadapter.so 的 GetNativeContextByType （**只返回上下文，不启动 Node**）
  → JsBindingMethod.bind()（注册约 40 组 *AdapterBind）+ Inject + SetContextPaths
  → 【关键】WebWindow 的 XComponent .onLoad → nativeContext.runBrowser(argv)
      → libadapter.so 的 RunBrowser/ChildMain → libelectron.so（内含 Node 22.17.0 + Electron）
      → 按 Electron 原生机制加载 resources/resfile/resources/app/package.json 的 main
```

**三条因此被钉死的约束：**

1. **必须有 ArkUI 窗口/表面，但不需要 Electron 窗口。**
   `runBrowser` **只能**从组件生命周期触达（`XComponent.onLoad` / `aboutToAppear`），而 `.onLoad` 要在
   `windowStage.loadContent()` 之后、表面就绪才触发 ⇒ **ArkTS 侧删不掉窗口**。
   但 Node 在那之前就已经起来了，`main.js` **只要不 `new BrowserWindow`、不 `loadURL`，就不会有 Chromium 渲染进程**。
   ⇒ 端侧形态是"一个不可见/最小化的窗口 + 我们的 ArkUI 页面"，**不能 `terminateSelf`/`destroy()`**（表面一没，运行时即死）。

2. **`web_engine` 删不掉**——启动链全在它里面；`electron` 模块只是几十行的薄壳子类。
   精简只能到"保留启动所需的那几个类 + `JsBindingMethod` 裁剪"，属阶段二优化。

3. **阶段一需要 JIT 权限，与"不申请特殊权限"冲突（已在 D6 §4.4 预言）。**
   `web_engine` 自行声明 `ohos.permission.kernel.ALLOW_WRITABLE_CODE_MEMORY`（`definePermissions`），
   而它是 V8 JIT 的前提。⇒ **阶段一只能作为开发期验证通道**（决策 D-7 得到证实），
   上架形态必须是阶段二自建 **jitless** 的 `libnode.so`。

**其它已确认的事实（写下来以免重复考古）**：
- `main.js` 的落点是 **`web_engine/src/main/resources/resfile/resources/app/main.js`**，靠 Electron 原生
  `process.resourcesPath + appSearchPaths` 解析（两个 `.so` 里都**没有**硬编码 `resources/app`），
  所以**不需要**改运行时、也不需要 HNP。
- 原版 `deviceTypes` 只有 `2in1`/`tablet` ⇒ **手机装不上**，必须加 `phone`（我们的真机正是 phone）。
- 参考工程里 `WebAppAdapter*` / `KVStore` 是**死代码**（`WebAppAdapterBind` 从未被 `bind()` 调用）。
- 无窗口时 `app.on('window-all-closed')` **永不触发**，别把停机逻辑挂在那儿。

#### 4.1.3 P5 探针真机结论（2026-09-13，**两条都取证了**）

在真机（HUAWEI Mate 70 Pro+ / `deviceType=phone` / API 26 / OpenHarmony-7.0.0.105）上，
通过 `entry@ohosTest` 的 `OnDeviceProbe` 用例取证（`aa test`，165 用例 / 164 通过）：

| 问题 | 真机读数 | 结论 |
|---|---|---|
| **沙箱能否创建符号链接**（R3） | `symlinkOk=false errCode=13900012 msg=Permission denied` | **不能**。dsh 启动时由 `healProfilesModuleFallback` 在 `$DSH_HOME/profiles/node_modules` 下建符号链接的那条路**在这台设备上必然失败** ⇒ 必须走替代方案（预置结构 / 走 pkg-ESM-proxy 分支 / 登记为端侧差异），**不能假设它会成功** |
| **同进程回环是否可用**（R4） | `listenOk=true connectOk=true`（listen 127.0.0.1 成功，同进程 connect 成功） | **可用**。⇒ §2 的架构前提成立：**不绑 `0.0.0.0`、不改写 `Host` 头、不需要隧道**这条主线是对的 |

> **R3 的处置（下一步）**：`13900012` 是权限拒绝，不是"路径不存在"。因此顺序是：
> ① 先看 dsh 是否有"不建符号链接"的组合路径（它对自己 pkg 形态有 ESM-proxy 分支）；
> ② 否则在端侧预先把 `profiles/node_modules` 物化成**真实目录/硬链接**；
> ③ 仍不行才作为端侧差异登记（并记入 D5 的降级策略）。
> **注意**：这与"对 dsh 上游零 patch"的纪律直接相关——优选项是 ①，最次才是 ③。

> **R4 的另一半仍待验**：本探针证明的是**同一进程内**回环可用。
> 若将来把 Host 挪到子进程（崩溃隔离），跨进程回环是否可用**需要重新取证**，
> 不能把这条结论外推（社区正是在"渲染进程 ↔ 主进程"之间被隔离的）。

#### 4.1.4 R3 的处置：**用 dsh 自带的"ESM proxy"分支，零上游 patch**（2026-09-13）

R3 的实测结论是"沙箱全局禁符号链接"（三个目录全部 `13900012`）。顺着这个约束读 dsh 源码后，
找到了一条**不需要改上游**的路：

`dsh-app-boot` 的 `resolveModuleFallbackEntries` 里，链接形态是**二选一**的：

```js
entries: !isPackagedExecutable()
  ? [...links].map(... { kind: "symlink" })    // 普通 Node → 写符号链接（鸿蒙禁止）
  : [...links].flatMap(... { kind: "proxy" })  // pkg 打包 → 写真实目录 + entry-N.js
```

而：

| 事实 | 依据 |
|---|---|
| `isPackagedExecutable()` 的实现只有一句 `return process.pkg !== void 0` | `dsh-app-boot/lib/index.js:485-486` |
| proxy 分支的实现 `ensureModuleProxy` **只用普通文件 API**（`mkdirSync` + `writeFileSync`），写出的 `entry-N.js` 是 `export * from "<file:// URL>"` | 同文件 `:548-579` |
| `packageProxySource` 也只用真实路径（`packageDir` + `pathToFileURL`） | 同文件 `:510-540` |
| pkg 只出现在那段代码的**动机注释**里，运行路径上没有 pkg 虚拟文件系统依赖 | 同文件 `:542-547` |

⇒ **处置**：在我们的宿主入口 `hostcore/app/main.js` 里，于导入 dsh 之前设置
`process.pkg = process.pkg || {}`，让 dsh 选 proxy 分支。**不改 dsh 一行代码**，
`$DSH_HOME/profiles/node_modules` 会变成一堆小目录而不是一堆链接。

**风险（必须真机验证，不得当成已解决）**：
1. 其它依赖若也探测 `process.pkg` 并据此改变行为，可能被这一行影响；
2. 文件数变多（每个包一个目录 + N 个 entry 文件）；
3. 这条路径原本是为 pkg 设计的，**没有人在非 pkg 场景下验证过它**——
   与 `--shared` 在 OpenHarmony 上"已接线但未测试"属同一类风险。

#### 4.1.5 启动触发的接线进度（2026-09-13）

| 步骤 | 状态 | 证据 |
|---|---|---|
| `entry` 依赖 `web_engine`、`.so` 进 `entry/libs/arm64-v8a/`、我们的 `main.js` 落位 resfile | ✅ | 读产出的 HAP：`libelectron.so` 181.0 MB / `libadapter.so` 7.7 MB / `icudtl.dat` 10.5 MB / `resources/app/main.js` 10,331 B / 内置核心 zip 66.2 MB（HAP 共 267.6 MB） |
| **① AbilityStage 换基类为 `WebAbilityStage`** | ✅ 真机验证 | `aa start` → `start ability successfully`；截图确认 UI 正常渲染（五页签 + 阶段化空态），**183 MB 原生库进 HAP 后未导致崩溃**，我们自己的 `onAcceptWant`（多窗口实例 key）仍工作 |
| ② 页面挂载不可见 `WebWindow()` 触发 `runBrowser` | ⏳ 未做 | 需要 `ComponentConfig{nativeContext, moduleName, xComponentId, uri, cmdArgs}`，且 `WebAbility.onWindowStageCreate` 注入的 LocalStorage（`updateStyle`/`xcomponentId`）我们的 `EntryAbility` 要补上等价注入 |

#### 4.1.8 阶段一在真机上的确切阻塞（2026-09-13，**留给真机回来后的第一件事**）

`RuntimeHost`（挂载 `WebWindow`）接进去之后，真机表现是**应用自杀**：

```
APPSPAWN: Unexpected call: exit(1)
Current abort crash
```

已排除的（我们自己的问题，均已修并提交）：
1. 我们的 `main.js` 曾在 FATAL 时 `process.exit(1)` —— `libelectron.so` 是**同进程**跑的，
   这一句等于杀掉宿主应用。已改为记录 + throw；
2. Electron 的"无窗口即退出"默认行为 —— 已订阅 `window-all-closed` 阻止。

**仍未解决**：修完之后依旧 `exit(1)`，而且 **hilog 里一行 `[hdsh-host]` 都没有** ⇒
我们的 `main.js` **根本没被执行到**，`exit(1)` 出在 `libelectron`/`runBrowser` 的**原生引导阶段**。

真机回来后按这三条查（顺序即优先级）：
1. `CommandLineAdapter` 传给 `runBrowser` 的 argv 是否合规（其中**无条件带 `--inspect`**，
   在设备上是否会因 inspector 端口不可用而退出）；
2. 同进程模式是否另需 `hnpPackages` / `executableBinaryPaths`（原版这两项是注释掉的，
   但那是在**子进程模式**下验证的）；
3. **四个 `.so` 都没有 `.codesign`**（`tools/electron-runtime/verify.ps1` 实测）。
   当时我判为"由 HAP 签名覆盖、可用"，现在必须**重新怀疑**：平台可能对 in-HAP 的原生库
   也要求自身签名，未签名时以 `exit(1)` 收场。

#### 4.1.9 阶段二（自建 libnode.so）当前进展

| 步骤 | 状态 |
|---|---|
| 取 OpenHarmony 公开 SDK（Linux NDK，clang 15.0.4） | ✅ `tools/node-runtime/fetch-ohos-sdk.sh` |
| 取 Node v22.23.2 源码 | ✅ |
| **C++ 标准不匹配**（`ncrypto.cc` 用 C++20 `<=>` 而 gyp 给 `-std=gnu++17`） | ✅ 已修：`tools/node-runtime/fix-cxx-std.sh` 把 `common.gypi:514` 改为 `gnu++20` |
| **zlib ARMv8 CRC32 SIMD 编不过**（OHOS clang 15 无法选择 `%llvm.aarch64.crc32b`） | ✅ 已修：`tools/node-runtime/fix-zlib-crc32.sh` 关掉这条可选 SIMD 路径（详见该脚本头与 `tools/node-runtime/README.md`）。**这是本轮卡住全量编译的唯一原因** |
| 上述两个修补已接线进 `build-node-ohos.sh`（configure 之前） | ✅ 从零重跑不会再撞这两个坑 |
| **续跑脚本漏导出交叉工具链** → make 静默用宿主 g++ 编译**目标**对象 | ✅ 已修：`out/Makefile` 是 `CC.target ?= $(CC)`，目标编译器取自环境变量。上一轮续跑没导出 `CC`/`CXX`，2410 个目标对象里 **1112 个是 x86-64**（编译期不报错，直到宿主 gcc 撞上 `asm/hwcap.h` / `arm_neon.h` 才暴露）。现在工具链环境只有一份 `tools/node-runtime/toolchain-env.sh`，`build-node-ohos.sh` 与 `resume-make.sh` 都 source 它；`resume-make.sh` 还会在续跑前**清除所有非 AArch64 的目标对象** |
| **目标链接要 `-latomic`，而 OHOS SDK 没有这个库** | ✅ 已修：`node.gyp:507` 的 `['OS=="linux" and clang==1', {'libraries': ['-latomic']}]` 把 OHOS 当成 linux。`find $SDK/native/sysroot -name '*atomic*'` 只有头文件，链接报 `ld.lld: error: unable to find library -latomic`。修法：`tools/node-runtime/fix-latomic.sh` 从**目标**链接行去掉它（宿主工具保留）。aarch64 的 1/2/4/8 字节原子操作由 clang 内联下发，真需要 16 字节原子时会**响亮地**报未定义符号，不会静默产出坏库 |
| `make -j8` 全量编译 | ⏳ **进行中且健康**：目标侧对象约 2570 个（aarch64，齐全）；宿主侧仍在增长，当前阶段是 `v8_turboshaft`。**具体数字不写在这里**——它每小时都变，写进散文只会变成假信息；用 `bash tools/node-runtime/status-brief.sh` 取实时值。链接期的三个问题（zlib CRC32 / 工具链环境 / `-latomic`）都已修掉并接线进 `build-node-ohos.sh`。<br>**如何判定"慢"与"卡"**（我自己被误导过）：对象计数与 `g++` 调用次数在编大文件时会**连续两轮不变**，那不是卡死。可靠判据是 `ps -eo pid,etimes,pcpu,comm --sort=-pcpu`：健康的构建会看到 **8 个 `cc1plus` 全在 90%+ CPU**（`nproc=8`）；配合日志 mtime 距今几秒内。`status-brief.sh` 已把这两项做成读数 |
| 产物 ELF + `.codesign` 校验 | ⏳ 待产物 |
| NAPI 引导模块（`libdshhost.so`） | 🔨 **已写出并编译通过**（`hostruntime/src/main/cpp/dshhost.cc` + `tools/node-runtime/build-dshhost.sh --compile-only`，27520 字节目标文件）。它只做引导：`runtimeVersion()` / `startHost(argv, envPairs)` / `isHostRunning()` / `stopHost()`。**链接必须等 `libnode.so` 产出**；`entry/oh-package.json5` 的原生库声明与 `.d.ts` 也**必须等到链接之后**再加（提前声明一个不存在的原生库会把当前可用的构建与页面一起弄坏） |
| Host 启动契约（argv + env） | ✅ 已定下并单测：**配置通道是环境变量而非 argv**——`hostcore/app/main.js` 读 `HDSH_CORE_DIR/HDSH_HOME/HDSH_SANDBOX_HOME/HDSH_PORT/HDSH_PROFILE`，而 ArkTS 侧无法设置原生进程环境变量，只能由引导层 `setenv()`。契约在 `RuntimePort.ets` 里是纯函数 `buildHostArgv()` / `buildHostEnv()` / `isWellFormedEnvPair()`。**写错不会报错**（键名拼错 → Host 用默认目录、表面正常却指向错的 `$DSH_HOME`），所以必须有单测；`--jitless` 必须排在脚本路径之前 |

> **一条会浪费一小时的构建机制事实**：Node 生成的 `Makefile` **没有** `GYPFILES` 规则，
> 所以改 `deps/zlib/zlib.gyp` **不会**让 `make` 重新生成 makefile——生成的
> `*.target.mk` 在 `out/<gyp 目录>/` 下（`out/deps/zlib/…`），**不在 gyp 旁边**。
> 只改 `.gyp` 会得到一个"看起来改了但没生效"的构建。

> **一条会悄悄毁掉整棵目标树的构建机制事实**：`out/Makefile` 第 43 行是
> `CC.target ?= $(CC)`——**目标编译器来自环境变量**。任何一次不带 `CC`/`CXX` 导出的
> `make`，都会静默改用宿主 `cc`/`g++` 去编译 `obj.target/` 下的对象，**编译期完全不报错**；
> 要等到几千个对象之后，某个 OHOS 专属头文件（`asm/hwcap.h`、`arm_neon.h`）在宿主
> gcc 下找不到，才以一个看起来毫不相关的错误暴露。核对手段：
> `bash tools/node-runtime/diagnose-toolchain.sh` 会统计 `obj.target` 下对象里
> 有多少不是 AArch64——**这个数必须是 0**。

> **E18 的含义与它引出的、必须上设备回答的问题**：实测我们的打包流程**从不给内嵌 `.so`
> 签名**（原样字节进包）。于是"内嵌 `.so` 到底要不要 `.codesign`"有两种互斥解释，
> 而我们**现在无法区分**：
> ① 包内 `.so` 本就受信任，`.codesign` 只对**包外**动态加载的库有意义
>    （E14 说的"热更新下载的 so 被拦"正是包外那一类）⇒ 现状即可；
> ② 包内 `.so` 同样要有 `.codesign`，那么**现在这四个库都是不合规的**——
>    而阶段一那个"原生引导里 `exit(1)`、且一行我们的 JS 日志都没有"的现象，
>    恰好与"原生库校验失败"相符。
> **上设备时的判定动作**：在应用内 `dlopen` 一个包内 `.so` 并读错误码；
> 若报签名类错误，则必须在 `pack`/`collect` 之后、HAP 组装之前补一道
> `binary-sign-tool`（或 `ohos-bst-light` 的 `self-sign.py`）签名步骤——
> 这一步的位置很重要：**必须在 HAP 组装之前**，组装后再改包内字节会破坏 HAP 签名。


> **口径**：在 `out/Release/libnode.so` 出现、并通过 `ELF64/AArch64/ET_DYN` 与 `.codesign`
> 两项检查之前，**不得声明 Node 运行时已可用**。

> **口径**：① 只证明"原生桥接没有把应用搞崩"，**不证明 `initNativeContext` 真的成功**——
> 那要等 `runBrowser` 真把 Node 起起来才算数（届时 `HDSH_READY` 会打到 stdout）。
> 在端侧真的起出 Host 之前，一律不得声称运行时已经可用。

#### 4.1.6 挂载 RuntimeHost 后的真机诊断：缺的是**能力代理**，不是运行时（2026-09-13）

清空 hilog 缓冲后重新启动，终于读到应用自身的 JS 日志（此前几次读不到是**缓冲区被系统日志冲掉**，
不是代码没跑）。读数分两半，正好指向同一个结论：

**跑起来的（说明基类链条通了）**
```
[WebEngine]: ContextPathAdapter#getFilesDir() in / out => ""
[WebEngine]: AbilityManager --> getProxy failed, proxyId: not found.
[WebEngine]: ContextAdapter --> No active proxy. use default proxy
[WebEngine]: ContextAdapter --> No proxy can be used
entry/abilitystage: onAcceptWant → 实例 key = main
entry/ability: 窗口记忆已就绪（preferences） / 窗口已登记 id=69 label=主窗
```
⇒ `WebAbilityStage.onCreate` 确实执行了（web_engine 的 `Inject`/`JsBindingUtils` 都在跑），
我们自己的 AbilityStage 与 EntryAbility 也正常。

**缺的（决定 Node 能不能起）**
- `AbilityManager.getProxy failed` + `ContextAdapter: No proxy can be used`
- 所有 `ContextPathAdapter#getXxxDir()` 返回**空串**

⇒ **根因**：我们的 `EntryAbility` 继承的是裸的 `UIAbility`，**没有把自己作为代理注册进
`AbilityManager`**（参考实现里这是 `WebAbility` 在 `onWindowStageCreate` 里做的
`abilityManager.addProxy(this)`）。代理没注册 ⇒ 原生侧拿不到沙箱路径 ⇒ 即便 `runBrowser` 被调用，
Node 也会在"不知道该把 HOME/DSH_HOME 放哪"的状态下起来。

**下一步（明确）**：让 `EntryAbility extends WebAbility`（web_engine），或至少补上等价的
`addProxy` 注册与上下文路径回调；同时确认 `WebBaseAbility.getContentPath()` 返回的
`pages/Index` 会落到**我们的**页面（本模块同名的 `pages/Index` 正好是我们挂 RuntimeHost 的那页）。

#### 4.1.7 换基类的可行性与一个必须堵住的坑（2026-09-13）

**代理就是 `WebAbility` 自己**：`WebAbility.onWindowStageCreate` 里做的是
`this.abilityManager?.addProxy(this)`（`web_engine/.../WebAbility.ets:79`），
而 `addProxy(proxy: WebProxy)` 要求传入者实现 `WebProxy` 接口——
这正是不写 `WebAbility` 就注册不了代理的原因，所以正路是让 `EntryAbility extends WebAbility`。

**但换基类会连带换掉"加载哪个页面"**，`WebBaseAbility.getContentPath()`（`:107-113`）是二选一：

```ts
protected getContentPath(): string {
  if (this.nativeContext.IsSupportNodeHandleFeature()) return 'pages/NodeHandleWindow';
  else return 'pages/Index';
}
```

⇒ 分支由 **native 侧**决定，我们无法先验知道这台设备走哪一支。**幸运的是两支我们都能满足**：
本模块的 `pages/Index` 已经是我们挂 `RuntimeHost` 的那一页；
只需**再补一个同名 `pages/NodeHandleWindow`**（内容同样是挂 `RuntimeHost` 的极简页），
两支就都会落到"能触发 runBrowser 的页面"上。

> 这正是"不要假定一个你无法先验的分支"的实例：与其赌 native 返回哪一支，
> 不如**两个页面名都提供**，把不确定性消掉。

### 4.1 阶段一：Electron-on-鸿蒙 运行时（只当 Node 用）

| 步骤 | 内容 | 说明 |
|---|---|---|
| P1 | 取运行时工程与其产物：**候选 A** `harmonypc-electron`（<https://atomgit.com/jianguoxu/harmonypc-electron>，Electron 37）；**候选 B（更新，优先核对）** `gitcode.com/CPF-Electron/Electron`，分支有 `v40.1.0-openharmony`（Chromium 144 / 上游 Node 24.11.1）、`v37.2.0-openharmony`、`v34.0.2-openharmony`。产物为 `libelectron.so` / `libadapter.so` / `libffmpeg.so` + `libc++_shared.so` | 预编译包在华为 DevCloud CodeHub，**需华为账号登录、无公开 URL** ⇒ 这一步需要人来取（见本轮交付说明）。同一仓库的 `docs/electron-loading-addon-guide/README.md` 与 `docs/hnp-packaging-guide/README.md` 是必读：**`.node` 必须用绝对路径从 `/data/storage/el1/bundle/libs/arm64/` require**，且需 `NAPI_VERSION=8` + `libshim.a` + `HIDE_NAPI_AND_UV` |
| P2 | 引入其 `electron` + `web_engine` 两个模块（copy 产物，不做 submodule） | 与社区方案一致；我们的差异是 **main.js 不建 `BrowserWindow`** |
| P3 | 我们的主进程入口只做：设沙箱 `HOME`/`DSH_HOME` → 解包核心树（§4.2 R5）→ `runProfile({profile:'ondevice'})` → 把 `{port, url}` 交给 `DshHost` | 不加载官方 Web UI、不做 `Host` 头改写 |
| P4 | **loopback 验证（本阶段最重要的实验）**：Node 侧 `127.0.0.1` 上的 Host，**同进程**的 ArkTS 客户端能否直连 | 社区在「Chromium 渲染进程 ↔ 主进程」之间被隔离，故必须实测「同一进程内」是否也隔离；结论直接决定 §2 的架构是否成立 |
| P5 | 已知差异处理：鸿蒙沙箱**禁 symlink**（社区实测 `EACCES`，并为此打了 `dsh-symlink-to-copy.patch`）。我们先**实测**我们的 `$DSH_HOME` 是否也禁；若禁，按 D6 §1.1「零上游 patch」原则优先考虑「预置好 `profiles/node_modules` 结构 + 让 dsh 走其 pkg/ESM-proxy 分支」，最后才考虑登记为上游缺口 | 这条不解决会在 `boot()` 阶段直接失败（E8/R3） |

### 4.2 阶段二（目标形态）：`libnode.so` + NAPI 引导（同进程）

| 步骤 | 内容 | 说明 |
|---|---|---|
| R1 | 在 Linux（WSL2 或 arm64 容器）用 **官方 Node 源码** 构建：`./configure --dest-os=**openharmony** --dest-cpu=arm64 --cross-compiling --openssl-no-asm --shared` → `libnode.so` | 需要 OHOS NDK（Linux 宿主）。**已把这条路径脚本化并入库**：`tools/node-runtime/{fetch-ohos-sdk.sh,build-node-ohos.sh,status.sh}`（含用法与已知风险说明）。Windows 版 NDK 在 `D:\Huawei\DevEco Studio\sdk\default\openharmony\native`，但 Node 的构建脚本需要 POSIX + make，因此宿主用 WSL2。**注意 `--dest-os` 的合法取值是 `openharmony`（不是 `ohos`）**；`--cross-compiling` 与 `--openssl-no-asm` 是必须的 |
| R2 | 写一个极薄 NAPI 模块 `libdshhost.so`：导出 `start(argvJson)` / `stop()` / `status()`，内部在独立线程调用 Node 的启动入口，把 stdout/stderr 回调给 ArkTS | 即 nodejs-mobile 的模式 |
| R3 | 把 `libnode.so` + `libdshhost.so` 放进 `entry/libs/arm64-v8a/`（或 hostruntime 模块的 libs），ArkTS 侧 `import dshHost from 'libdshhost.so'` | |
| R4 | **签名**：对 ELF 打 OHOS 代码签名（E5），否则商用版拒绝加载 | 门禁判据必须是 **`.codesign` 段的存在**：实测对照——`binary-sign-tool` 签过的 `bin/node` 同时有 `.codesign` 与 `.note.ohos.ident`，而**未签名的** `libelectron` 只有 `.note.ohos.ident`，且正是它出现在华为 XPM 的拒绝日志里。**只有 `.note.ohos.ident` 不能作为"已签名"的证据** |
| R5 | 首启把打包的核心分发**解压**到 `<files>/dsh/cores/<ver>/`（E6） | **容器用 zip**：鸿蒙侧只有 `@ohos.zlib.decompressFile`（zip），没有 tar/gzip 等价 API ⇒ 用 zip 可以只调一个系统 API，避免在 ArkTS 里手写 tar 解析 + gzip 解压（实测规模：2.5 万条目 / 解包约 120 MB，未触及 zip64 边界） |
| R6 | 设 `DSH_HOME=<files>/dsh/home`、`HOME=<files>`（沙箱），起 `dsh web --no-open --host 127.0.0.1 --port 0 --skip-auth` | `--skip-auth` 已存在于上游 `dsh-web-app/startup.js`；Host/Origin 围栏仍在 |
| R7 | 从启动日志/回调里拿 `dsh web: http://127.0.0.1:<port>/?token=…`，走**现有** `SessionHub.configure(url)` 接上客户端 | 客户端与状态层**零改动**即可复用 |

**为什么用 `--skip-auth`**：客户端与 Host 同进程，token 交换这一步只增加失败面；围栏（Host 必须是 loopback、`Origin` 必须一致）仍然生效，安全语义不降级。若后续要允许别的进程/设备接入，再打开认证。

### 4.3 回退路线（阶段一/二受阻时按序尝试）

| # | 回退 | 触发条件 | 代价 |
|---|---|---|---|
| F1 | **HNP 分发 `node` 可执行文件**（E7）：`hnpPackages` + `executableBinaryPaths`，以子进程方式跑 node | 阶段二构建受阻，或需要崩溃隔离 | **⚠️ 手机上不成立**（E15：原生子进程仅平板/PC-2in1，手机禁止三方应用创建进程）→ **手机唯一可行路线是同进程 `libnode.so` + jitless**；跨进程后 loopback 可能被隔离，需重新验证 |
| F2 | 停留在阶段一（Electron 运行时）并接受体积 | 阶段二在限时内未通过 | 体积 ≈ +172 MB；手机形态可用性待验 |
| F3 | 端侧只读缓存 + 可选远程 Host | 全部失败 | 退回原方案：**不满足本项目目标**，必须如实关闭该目标而不是伪装完成 |

### 4.4 JIT：**本项目不申请该特殊权限**（为顺利上架，各端统一 jitless）

> **产品决策（2026-09-12，用户拍板）**：**各端都不涉及 JIT 特殊权限相关的能力**，以确保顺利上架。
> 即：**不申请** ACL 权限 `ohos.permission.kernel.ALLOW_WRITABLE_CODE_MEMORY`，也不使用任何依赖它的方案。
> 运行时必须在**无 JIT 权限**的前提下工作 —— 这对**所有设备形态**成立，不只是手机。

V8 依赖**可写可执行的匿名内存**，鸿蒙默认拦截。两条路的对比（本项目只走右边那列）：

| | ❌ 有 JIT（本项目不采用） | ✅ **无 JIT（本项目唯一形态）** |
|---|---|---|
| 权限 | 需 ACL 权限 `ohos.permission.kernel.ALLOW_WRITABLE_CODE_MEMORY` | **不申请任何特殊权限** |
| 可获得性 | **仅平板 / PC-2in1 应用可申请**，且须适配「坚盾守护模式」 | 各端一致，无差别 |
| 上架 | 特殊权限 + 额外适配，风险与成本都高 | **普通权限即可**（网络只需 `ohos.permission.INTERNET`） |
| 运行时要求 | 默认构建即可 | 必须以 **jitless** 方式运行 V8（`--jitless` / `v8_jitless=true` 构建） |
| 代价 | — | 计算密集 JS 显著变慢、**WebAssembly 不可用** |

**由此推出的两条硬约束（会影响选型，必须前置验证）**：

1. **运行时不能"默认启用 JIT"**。阶段一若走 Electron-on-鸿蒙 运行时，它的 V8 是带 JIT 编译的；
   必须验证它能否在设备上以 jitless 方式启动（Node 侧 `--jitless`，或 `--js-flags`），
   **否则阶段一只能当"开发期验证通道"，不能作为上架形态**；上架形态回到阶段二自建 `v8_jitless=true` 的 `libnode.so`。
   （社区资料亦指出 Electron-on-鸿蒙 的 fork/子进程方案本身依赖 JIT —— 这与"不申请该权限"直接冲突，必须实测。）
2. **依赖 WebAssembly 的功能要提前排查**。jitless 下 `WebAssembly` 为 `undefined`，
   凡是靠 wasm 的依赖（部分解析器/图像/加密库）在端侧会直接不可用。这一项要作为端侧 profile 的能力探测项之一。

**POC-13（项目级门，不再是"手机专用"）**：在**无 JIT 权限**的前提下跑真实会话——

1. 确认运行时确实以 jitless 起来（能读到 V8 的 jitless 状态，且没有申请任何特殊权限）；
2. 跑一次真实会话（含工具调用与长轨迹渲染），记录：首 token 延迟、单次 RPC 处理耗时、轨迹滚动帧率、内存峰值；
3. 判据：端侧额外开销相对网络往返**不成为瓶颈**（例如单次 RPC 处理 < 50 ms、轨迹渲染不掉帧）即为可用。

> dsh 的端侧负载以**网络等待 + I/O + JSON 处理**为主，不是计算密集；"慢 10~100 倍"针对计算密集场景，
> 因此 jitless 是否可接受必须**用真实会话实测**，而不是按经验判死。

**本项目对机型覆盖的判断（在"统一 jitless"下重新表述）**：
jitless 让**各端的技术约束变得一致**（不再有"平板能申请 JIT、手机不能"的差异），
这反而简化了目标：**四形态共用同一份运行时与同一套 profile**，差别只在布局与输入。
剩下的风险从"权限可得性"变成"**jitless 的性能是否够用**"，由 POC-13 用真实会话回答。

> **机型覆盖结论（当前）**：四形态**同技术前提**（不申请任何特殊权限、统一 jitless），
> 因此**四形态都在目标内**；某个形态若被 POC-13 判为不可用，才**如实降级**该形态的能力
> （而不是假装各端等价，也不是靠申请特殊权限去换性能）。
> 另一条好消息：无 JIT 权限后，**上架不再受"仅平板/PC 可申请"的限制**，手机形态也不再有权限层面的排他。

### 4.5 运行时必须满足的清单（来自 dsh 源码实测）

| 需求 | 来源 | 不满足的后果 |
|---|---|---|
| **`node:zlib` 的 zstd**（`createZstdDecompress`） | `dsh-session-persistence-jsonl` | 会话持久化不可用 → 需要 Node ≥ 22.15 / 23.8 |
| **版本下限 = `v22.17.0`** | 两条约束的交集：OHOS 支持落在 v22.17.0 / v24.4.0 / v25+ / v26+；zstd 需要 ≥ 22.15 | **统一 pin 到 22.17.0 及以上**（阶段一的 Electron 37 内置 Node 22.x 正好落在这个区间） |
| **`node:worker_threads`** | 会话持久化、`code-runtime` | 相关行必须禁用 |
| **`process.execPath` 可再拉起** | `dsh-web-app` 调 `open`、沙箱 runner | 用 `--no-open` 规避前者 |
| **`$DSH_HOME` 允许建符号链接** | `healProfilesModuleFallback` 每次启动建 `profiles/node_modules` 符号链接 | **启动即失败**（鸿蒙沙箱若禁 symlink，这条会先炸） |
| **`HOME` 指向沙箱可写目录** | 社区实测：`os.homedir()` 返回 `/storage/Users/currentUser` → EPERM | 目录选择器一打开就报错 |
| **不依赖系统 shell** | 正常启动路径**不 spawn 任何子进程**（唯一 spawn 是 `dsh plugin …` 调 pnpm） | 没有 shell 也能起核心；只是工具面变窄 |

---

## 5. 端侧 profile：哪些 dsh 行必须关、哪些可以开

dsh 的 `boot()` 只有在 `assertEntriesActivated()` 通过后才算 ready；**任一必装行挂载失败 → 进程 `exit(1)`**（E8）。
因此端侧 profile 不是「优化」，是**能不能起来**的前提。

### 5.1 必须禁用（否则起不来）

| 行 id | 包 | 硬依赖 |
|---|---|---|
| `subprocess` | `@deepseek-ai/dsh-subprocess-local` | `node-pty` + `koffi`（且静态 import `dsh-win32-process`） |
| `sandbox` | `@deepseek-ai/dsh-sandbox-local` | `koffi`（经 `dsh-sandbox-windows-acl`） |
| `attachment-local` | `@deepseek-ai/dsh-attachment-local` | `sharp` |

> **已决策（2026-09-12）：优先换成鸿蒙移植版，而不是一上来就禁用。**
> 即把这三个包按 `npm:@ohos-ports/...` 别名接进端侧核心树，尽量保留完整能力（终端 / 沙箱 / 图片）。
> 但必须带上失败预案：那批移植件对应的是**更早的 dsh 一代 + 不同的 Node ABI**（阶段一是 Electron 37 / Node 22.17），
> 因此**每一个移植件都要单独验证能不能在本运行时里加载并工作**；
> 验证不通过的，退回「禁用该行 + 在界面如实标注能力缺失」，不允许让核心起不来。
>
> 验证顺序（按风险从低到高）：`sharp`（Node-API，ABI 稳定）→ `koffi`（Node-API）→ `node-pty`（历史上有 NAN 成分，ABI 相关）。

### 5.2 建议关闭 / 需要显式选择

| 行 id | 原因 | 处置 |
|---|---|---|
| `tool-fs-search` | 依赖随包的 `rg` 可执行文件 | 有鸿蒙可执行的 rg 就开，否则关（按调用失败降级不致命） |
| `session-query-sqlite` | 用 `node:sqlite`（惰性、配置门控） | 保持 `openAt: never`，或确认运行时有 `node:sqlite` |
| `directory-picker` | `auto` 会落到 `-native`（需要 koffi） | 直接挂 `-browse` |
| `open-in-app` / `ui-open-in-app` | 需要拉起系统应用 | 按需 |
| `session-telemetry-otel` | 出网遥测 | 默认关（`DSH_TELEMETRY_DISABLED`） |
| `client-hmr` | 开发期热重载 | 生产关 |

### 5.3 端侧 profile 的落地形态

沿用 dsh 自己的扩展点（**零上游 patch**）：

1. 一个 bundle 包 `@hdsh/bundle-ondevice`，声明 `"dsh": { "bundle": { "patch": "./cordis.patch.yml" } }`；
2. profile 目录 `<cores>/<ver>/profiles/ondevice/package.json` 里 `dsh.profile.bundles = ["@deepseek-ai/dsh-base", "@deepseek-ai/dsh-web-app", "@hdsh/bundle-ondevice"]`；
3. 能力**探测结果**（运行时有没有 koffi/sharp/pty、有没有 rg）在启动前由 ArkTS 侧算好，作为 `--patch` 覆盖层或生成 profile 的 `cordis.patch.yml`——**同一份核心树可以按设备能力起出不同能力的 Host**。

---

## 6. 核心版本与插件（dsh 没提供，本项目自建）

### 6.1 核心版本：激活事务（stage → verify → switch → rollback）

dsh 现状（实测结论）：**没有** `$DSH_HOME/packages`、没有候选核心的 staging 目录、没有健康检查、
没有 activate/rollback、没有 A/B 启动、没有自更新。「切版本」今天只有一种办法：换掉那一棵扁平安装树再重启进程。

因此端侧实现：

```
<files>/dsh/cores/
  ├─ 0.1.5-rc.2/            # 已激活
  ├─ 0.1.6-rc.1.staging/    # 正在解包/校验（未激活）
  └─ …
<files>/dsh/state.json      # { current: "0.1.5-rc.2", previous: "...", history: [...] }
```

切换流程（每一步都可失败且可回滚）：

| 步 | 动作 | 失败处置 |
|---|---|---|
| 1 | 下载/导入新核心包到 `.staging` | 保持 current 不变 |
| 2 | 校验（清单/大小/摘要；离线包校验签名） | 删除 staging |
| 3 | 解包到 `cores/<ver>/`（原子 rename） | 删除半成品目录 |
| 4 | **健康检查**：用该版本起一次 Host，直到 `/api` 可用且 `$events` 收到 `ready`；超时即失败 | 记 `health: fail` 并保留证据（日志摘要） |
| 5 | **激活**：写 `state.json.current = <ver>`，重启核心 | 重启失败 → 自动回滚到 `previous` |
| 6 | 清理旧版本（保留最近 N 个 + 永远保留 previous） | — |

**用户数据不动**：`$DSH_HOME` 独立于核心树，升级/回滚不迁移、不删除会话。

### 6.2 插件

dsh 现状（实测结论）：插件**没有** Remote 安装/启停端点；`pluginInventory.list` 只读；
唯一变更路径是改 profile 的 `dsh.profile.bundles` / `cordis.patch.yml`，或 `dsh plugin --profile <name> <pnpm args>`
（它 `spawnSync("pnpm", …)`）。因此：

| 能力 | 端侧做法 |
|---|---|
| 列出/查看状态 | 直接用现有 `pluginInventory.list`（只读，已经有） |
| 安装/卸载 | 端侧包管理：把插件包下载/解包进 `<files>/dsh/plugins/<name>`，用 Node 侧脚本改 profile 的 `dependencies` + `bundles`（**不依赖系统 pnpm**：可以自带一个纯 JS 安装器，或在可用时用运行时内的 pnpm） |
| 启用/停用 | 写 profile 的 `cordis.patch.yml`（`{id, disabled:true}`），核心重启或热加载生效 |
| 回滚 | profile 目录整体做快照，失败即还原 |
| **含原生 `.node` 的插件** | **不能热装**：鸿蒙只允许加载 HAP 内的、已签名的 `.so`（E14）。这类插件只能两种方式支持：① 随应用发布（打包进 HAP，作为"随包插件"）；② 走 HNP 分发（E7/E15，且仅平板/PC）。**界面必须把"纯 JS 插件可装、原生插件需更新应用"如实区分**，不允许点了没反应 |

> **这条约束会让"支持各类插件"的语义变具体**：纯 JS 插件可以运行时安装；带原生模块的插件必须经过应用发版。
> 比例不是靠"dsh 的插件大多是 JS"这种印象说的，而是实测出来的——见 §6.2.1。

> 界面上「插件」页从「只读清单」升级为**可操作**，但每个动作都要有
> 成功/失败/回滚三态，不允许"点了没反应"。

#### 6.2.1 实测：152 个插件行的可安装性判定（2026-09-13）

**方法**：`node tools/scan-core-plugins.mjs`，输入是**真实物化出来的核心树**
（`dist/core/work/dsh-core-0.1.5-rc.2`，即 `pack-core.mjs` 打进 HAP 的那棵树）。
脚本可复跑，产物 `plugin-scan.json` / `plugin-scan.md` 落在 `dist/`（**不进版本库**：
进库的是生成方法与下面的结论）。

**核心树规模**：`node_modules` 下 518 个包、27250 个文件。

**含原生模块的包：恰好 3 个**（这是端侧核心的全部原生码，也就是上架风险面）：

| 包 | 原生文件 |
|---|---|
| `koffi` | `build/koffi/openharmony_arm64/koffi.node` |
| `node-pty` | `prebuilds/openharmony-arm64/pty.node` |
| `@ohos-ports/img-sharp-openharmony-arm64` | `lib/sharp-openharmony-arm64.node` |

**插件行：152 行**（`dsh-base` 84 + `dsh-web-app` 68），判定结果：

| 判定 | 行数 | 含义 |
|---|---|---|
| `PURE_JS` | **146** | 依赖闭包内无原生码 → 原则上可运行时安装 |
| `NATIVE` | **6** | 闭包内有原生码 → **运行时不可安装**，只能随应用发版 |
| `UNKNOWN` | **0** | 全部可判定，插件页不该再出现「待确认」 |

依赖原生码的 6 行（`经由`＝闭包内命中的原生包）：

| 行 id | 经由 |
|---|---|
| `session-persistence-jsonl` | `koffi` |
| `attachment-local` | `@ohos-ports/img-sharp-openharmony-arm64` |
| `subprocess` | `koffi`, `node-pty` |
| `sandbox` | `koffi` |
| `web-startup` | `koffi` |
| `web-runtime` | `koffi` |

默认禁用 3 行：`hmr`、`skill-badge`、`ui-schedule`。

**两条方法上的纠正（都会让朴素实现给出错误答案）**：

1. **原生性不是插件包自己的属性，是依赖闭包的属性。**
   上面 6 行**没有一个自己包含 `.node`**——它们全都经由传递依赖摸到 `koffi`/`node-pty`/`sharp`。
   所以「这个插件包里有没有 `.node`」是**错判据**：用它会把 `sandbox`、`subprocess` 这类
   实际依赖原生的插件标成「纯 JS · 可安装」，用户装完必然起不来。
   判据必须沿 `dependencies` 闭包走到底。
2. **patch 行的 `name` 可能是包的子路径导出，不是包名。**
   实测有 3 行如此：`@deepseek-ai/dsh-web-app/startup`、
   `@deepseek-ai/dsh-tool-subagent-control/list-agents`、`@deepseek-ai/dsh-tool-subagent/model-selection-settings`。
   不处理子路径就解析不到包 → 脚本一度把它们报成 `UNKNOWN`。
   而 `web-startup` 实际经由 `koffi` **是 NATIVE**——也就是说，
   "解析不了就当待确认"不只是漏标，它会**把一个不可安装的插件放到"待确认"里**，
   而用户读到的"待确认"隐含"也许能装"。

**端侧实现路径（已落地，端到端）**：判据实现只有一份，在 `tools/lib/core-inventory.mjs`；
它被两处共用——`tools/scan-core-plugins.mjs`（离线出报告）与 `tools/pack-core.mjs`
（打包时把结果写进树内 `<top>/hdsh-core.json` 的 `plugins` / `pluginTotals` /
`nativePackages` 字段）。**端侧只读那一个小 JSON**，消费链路是：

```
tools/lib/core-inventory.mjs     判据（依赖闭包 + 子路径解析）
  └─ pack-core.mjs               写进树内 hdsh-core.json，并**读回来核对字段契约**
       └─ CoreStore.readTreeInfo()       hostruntime：强类型化，缺字段一律空值
            └─ DshHost.plugins()         取"会被启动的那一版"的清单，带 version 与 note
                 └─ Index.refreshCoreViewAsync()   清单取值 → PluginNative 枚举（显式映射）
                      └─ CorePane.pluginsCard()    真实行 + 可安装性徽标 + 禁用态
```

契约定在**唯一的产出口**上：`pack-core.mjs` 写完 `hdsh-core.json` 会读回来逐字段核对
（字段名/类型/`pluginRows` 与数组长度一致），不符就让打包失败。理由很具体：两端都是字符串键，
**任何一侧改名都不会报错**，只会静默退化成"读不到清单"，核心页永远显示"未读取到插件清单"，
而没有一处会告诉你是拼写问题。

> **为什么不在端侧现算**：在设备上算同一件事要在 27250 个文件 / 4000 个目录上递归
> （实测规模），是一秒级目录遍历加一堆错误分支；而这件事的答案在**打包那一刻就确定了**，
> 且能在一台能跑 Node 的机器上核对。构建期算一次、端侧读一个文件，是这里唯一合理的分工。
> 判据也**不允许**两边各写一套：一旦漂移，就会出现"报告说纯 JS、端侧标依赖原生"这种
> 谁都说不清的分歧。

**已知边界**：该清单是**打包时快照**。用户运行时自行增删插件后它会过期——
v1 的 profile 是固定的、插件变更走核心版本切换，所以可以接受；
真要支持热插拔插件时，这里必须补一条端侧增量扫描，而不是继续信任快照。

> 由此，插件页的三态契约（D7 / `appstate` 的 `PluginNative`）**可以从
> `UNKNOWN` 收敛到真实取值**：界面与契约都不用改，只是把真实取值传进去。
> 早期版本里"上游清单不含是否含原生模块的信息"这一说法**只对运行时端点成立**；
> 端侧自足运行后，我们**自己就是核心树的主人**，这个信息在磁盘上。

---

## 7. 目标指标（替换 D1 §5.1 中已失效的 G2/G3）

| # | 目标 | 指标（v1） | 与旧指标的差异 |
|---|---|---|---|
| G2′ | **自足** | 无任何外部 Host 时，应用可独立完成：起核心 → 建会话 → 对话 → 审批 → 文件投喂 | 新（旧 G 表没有这条，因为旧目标不需要） |
| G2″ | 体积 | 安装包 ≤ **400 MB**（Node + dsh + 资源）；首启解包完成 ≤ 60 s | 旧值 ≤ 50 MB **作废**（Node 运行时本身就几十 MB） |
| G3′ | 冷启动 | 冷启动到「核心 ready 且会话列表可见」≤ **5 s**（首次解包除外） | 旧值 1.5 s 是"连远程 Host"，不可比 |
| G4 | 全形态 | **四形态同技术前提**（不申请特殊权限、统一 jitless），手机 / 折叠屏 / 平板 / 2in1 均在目标内；某形态被 POC-13 判为不可用才如实降级 | 加了"同前提"的限定（2026-09-12） |
| G5 | 移动端一等交互 | 审批/提问 ≤ 2 步 | 不变 |
| G6′ | 不新增暴露面 | 端侧 Host 只监听 `127.0.0.1`；抓包确认无对外监听 | 从"不新增暴露面（指隧道）"改为**可验证的本地约束** |
| G7 | 可诊断 | 诊断页给出：运行时（libnode 是否加载）/ 核心（版本、health）/ 协议 / 端口 / 插件树失败行 | 诊断项按新架构重定义 |

---

## 8. 里程碑（在 D1 §9 之上重排）

| 里程碑 | 内容 | 出口判据 |
|---|---|---|
| **M0′ 运行时门（生死门）** | **A（阶段一）**：Electron 运行时里起 Node，`require` 三个移植件（koffi/node-pty/sharp）成功，打印 Node 版本、`zlib.createZstdDecompress` 是否存在、`process.platform`；**B（阶段二）**：`libnode.so` 可被 `dlopen` 并打印版本；**C（并行）**：POC-13 的 JIT 探测 | 真机日志能同时给出：Node 版本 + zstd 能力 + `process.platform` + 三个移植件加载结果 + JIT 状态；阶段二另需 `llvm-readelf -S libnode.so` 可见 `.codesign` |
| **M1′ 端侧 Host 起来** | 解包 dsh 核心 + 端侧 profile，`dsh web` 在设备上起 Host，`/api` 一元调用成功 | 设备上 `session/list` 返回真实结果 |
| **M2′ 客户端接上本地核心** | 用现有 `SessionHub` 接 `127.0.0.1`，全界面走真实数据；`$events`/`remote.mux` 在**同进程**下稳定（对照旧环境 5 秒断线的结论） | 长连接 ≥ 10 分钟不断；诊断页五项全绿 |
| **M3′ 端到端真实会话** | 设备上完成一次真实对话（含工具调用与审批） | 会话内容真实渲染，无桩数据 |
| **M4′ 核心版本激活事务** | 双版本并存、切换、健康检查失败回滚 | 注入一个坏版本，验证自动回滚且用户数据无损 |
| **M5′ 插件管理** | 安装/启停/卸载 + 失败回滚 | 装一个真实插件并生效；卸载后回到基线 |
| **M6′ 全形态与发布** | 四形态验收、体积/启动/内存基线、分发 | 指标表逐条达标 |

> **M0′ 未通过前不开工 M2′ 之后的一切**（沿用 D4 的顺序纪律）。

---

## 9. 风险

| # | 风险 | 概率 | 影响 | 应对 |
|---|---|---|---|---|
| R1 | `libnode.so` 构建失败（交叉编译/工具链/签名） | 中 | **致命** | 限时；失败转 F1/F2；每次尝试都要留下可复现脚本与错误原文 |
| R2 | 鸿蒙商用版拒绝加载未签名或签名不匹配的 `.so` | 中 | **致命** | E5 的签名流程前置到 M0′；把它当验收项而不是收尾项 |
| R3 | 沙箱禁止 `$DSH_HOME` 建符号链接 → `boot` 直接失败 | 中 | 高 | M1′ 首要验证项；必要时改走「ESM proxy 目录」路径（dsh 对 pkg 形态已有该分支）或预生成好链接结构 |
| R4 | 同进程内 loopback 仍被系统拦截 | 低 | 高 | M1′ 用最小 HTTP 打点验证；若失败转 F1 并重新评估 |
| R5 | 体积/内存超预算（Node + dsh ≈ 200 MB+ 解包） | 高 | 中 | 核心树按能力裁剪（§5.3）；默认不打包浏览器 UI 资源以外的冗余 |
| R6 | `@ohos-ports` 移植件版本落后于我们锁定的 dsh 版本 | 高 | 中 | 端侧 profile 以**能力探测**决定启用哪些行；移植件只作增强，不作前提 |
| R7 | dsh 上游升级引入新的硬原生依赖 | 中 | 中 | 沿用 D5 漂移门禁；新增包必须过「端侧可行性」检查（是否新增 `.node`） |
| **R8（最高）** | **不申请 JIT 权限的前提下，jitless 是否够用**（E13 + 上架约束） | **高** | **高（决定整个产品是否成立）** | POC-13 前置实测；若 jitless 不可用，退路是"缩小端侧职责"（例如端侧只做会话/审批，重负载留给可选的远程 Host），**不得**回头去申请特殊权限 |
| R11 | 阶段一 Electron-on-鸿蒙 运行时**无法以 jitless 启动**（其 V8 带 JIT，社区方案还依赖 JIT 做 fork） | 中高 | 中 | 明确把阶段一定位为"开发期验证通道"；上架形态以阶段二自建 `v8_jitless=true` 的 `libnode.so` 为准（§4.2/§4.4 约束 1） |
| R12 | jitless 下 `WebAssembly` 不可用，某些依赖直接失效 | 中 | 中 | 把"是否依赖 wasm"纳入端侧 profile 的能力探测项（§4.4 约束 2） |
| R9 | `libnode.so` 的 `--shared` 在 OpenHarmony 上未经验证（E12） | 中 | 中（阶段二） | 阶段一不被它阻塞；阶段二先用 `--shared` 构建最小验证（能 dlopen + 打印版本）再谈集成 |
| R10 | 原生插件无法热装（E14） | 高 | 中 | 插件页区分纯 JS / 原生两类（§6.2）；原生走随包或 HNP |

---

## 10. 需要同步修订的既有文档（本次已改 / 待改）

| 文档 | 条目 | 原内容 | 处置 |
|---|---|---|---|
| D1 §1.4 | 一句话任务 | 「写一个一等的 dsh 鸿蒙**客户端**」 | **已改**：改为「端侧自足运行 dsh + 客户端页面」 |
| D1 §3.3 | 明确不做 | 「端侧跑完整 dsh Host（Node 运行时进 HAP）」列为不做 | **已改**：从非目标中删除，移入关键路径 |
| D1 §5.1 | G2 体积 ≤ 50 MB | 已不可能 | **已改**：见本文 §7 的 G2″ |
| D1 §6.2 | 备选方案判定 | A（PC 侧 Host）为主方案，B（端侧 Host）为 P3 | **已改**：B 升为主方案，A 降为可选接入 |
| D1 §12.2 | Q4 端侧 Host 是否投入 | 「A 评估但限时」 | **已改**：改为「已决策：投入，M0′ 为门」 |
| D1 §8 | POC-11 | P3 限时评估 | **已改**：提升为生死门 |
| D4 §POC-11 | 同上 | 同上 | 待补执行细节（下一步） |
| README | 核心主张 §1 | 「无浏览器内核、**无端侧 Node 运行时**」 | **已改** |
| README | 进度段 | 「客户端」定位 | **已改**：加目标变更说明 |

---

## 11. 已决策记录

| # | 决策 | 取值 | 日期 | 影响 |
|---|---|---|---|---|
| D-1 | 运行时优先路线 | **阶段一复用 Electron-on-鸿蒙 运行时（只当 Node 用，不建 Chromium 窗口）打通端到端；阶段二替换为自建 `libnode.so`** | 2026-09-12 | 见 §4.0；阶段一产物不得泄漏到状态层/UI 层 |
| D-2 | 核心与运行时分发 | **随包内置，压缩为 `tar.gz`，首启流式解压** | 2026-09-12 | 离线可用、行为确定；安装包按 §7 G2″ 的 ≤400 MB |
| D-3 | 原生能力 | **引入 `@ohos-ports` 鸿蒙移植件，尽量保留完整能力** | 2026-09-12 | 见 §5.1；逐个验证，失败的按行降级 |
| D-4 | 远程 Host 接入 | 保留为可选能力，**不作为目标与验收口径** | 2026-09-12 | 见 §0 |
| **D-5** | **JIT 特殊权限** | **各端一律不申请、不依赖**；运行时统一 jitless，权限面只保留普通权限，以顺利上架 | 2026-09-12 | 见 §1.6 / §4.4；阶段一运行时若无法 jitless 启动，则降级为"开发期验证通道" |
| **D-6** | 核心分发包的容器 | **zip**（而非 tar.gz）——鸿蒙侧只有 `@ohos.zlib.decompressFile` | 2026-09-12 | 见 §4.2 R5；`tools/pack-core.mjs` 自带最小 zip 写入器 |
| **D-7** | 阶段一产物的定位 | 阶段一（Electron-on-鸿蒙 运行时）是**开发期验证通道**，上架形态以阶段二自建 jitless `libnode.so` 为准 | 2026-09-12 | 见 §4.4 约束 1 |

---

## 12. 设备复验清单（连机后照做，逐条留证）

> 这份清单是**唯一还没做完的事**：功能已实现并构建通过，但下面每一项都必须在真机上跑一次才算"验过"。
> 每条都给了命令与**预期读数**——读数不符就是缺陷，不是"环境问题"。

### 12.0 前置（一次）

```bash
devecocli build
HAp=$(ls -t entry/build/default/outputs/default/*.hap | head -1)
hdc install -r "$HAP"
hdc shell aa start -b com.hnmrxz.hdsh -a EntryAbility
# 取本机核心令牌（Host 启动时打印，形如 …?token=…）
hdc shell "hilog -x | grep 'HDSH-AUTO 启动 Host' | tail -1"
hdc fport tcp:3120 tcp:3120
```

### 12.1 核心版本切换 / 回滚（E119 修复的验证）

- **动作**：设置 → 核心 → 在 `0.1.5-rc.3` 一行点「切换」；随后点「回滚到上一版」。
- **预期读数**：`hilog -x | grep HDSH-CORE` 出现 `切换结果：ok=true`；`BOOT_10_ENV_READY core=…/cores/0.1.5-rc.3`（回滚时为 `rc.2`）；核心再次可连（`HDSH-AUTH connect 结果：ok=true`）。
- **失败形态（修前）**：`切换失败：运行时已经启动过；同一进程只允许一个 Node 实例`——那说明 `g_started` 复位修复（E119）没生效。

### 12.2 插件启停（E91 的真机验证）

- **动作**：设置 → 核心 → 插件「配置」子页签，禁用任一纯 JS 行，再点「恢复默认」。
- **预期读数**：`hilog -x | grep -E 'HDSH-CORE|plugin'` 出现写入成功的记录；插件行状态随之变化；**重启核心后**生效（`patchReload: 'startup'`）。

### 12.3 未加载原生件的名字（E121）

- **动作**：设置 → 核心 → 看「原生件加载」这一行。
- **预期读数**：不再是笼统的 `2/3 已加载`，而是 `未加载：<名字>：<原因>`。
- **据此决定**：若缺的是 `node-pty`（终端）——端侧本无进程创建能力，属预期；若缺 `sharp`（图片）——说明原生重定向那条路在真机上没生效，需按 E93 复核闭包。

### 12.4 技能清单的真实回复形状（E135）

- **动作**：设置 → 技能（确保已连接）。
- **预期读数**：要么列出技能卡片，要么显示「形状未识别（顶层键：…）」——**后者本身就是答案**：把那个键名写进 `dshcompat` 的 `skillsFromReply()` 并收紧解析。
- 旁路日志同时会记一行 `skills/list shape unknown keys=…`。

### 12.5 工作区删除 / 归档（E132–E134）

- **动作**：工作区组头点「删除」→ 再点「确认删除」；会话行点「归档」。
- **预期读数**：删除后该组从列表消失，**磁盘目录仍在**（用工作区文件视图或 `hdc` 复核）；归档后会话从常规分组消失、出现在「归档 · N」里。
- **注意**：删除按钮**只在宿主注册表里有这个目录时**才出现；如果某工作区没显示删除入口，那是设计如此（列表由会话 `cwd` 归并而来，注册表里没有它）。

### 12.6 形态适配（手机以外）

- **折叠屏**：展开/折叠切换，确认布局在 SINGLE ⇄ DOUBLE 之间切换时**不丢当前页**、不出现空面板。
- **平板 / 2in1**：确认三栏（导航 + 列表 + 详情）下工作区与设置页都可用；「选择文件夹」在 2in1 上走系统文件夹选择器（手机上是同一入口，可用性以真机为准）。
- **预期读数**：`hilog -x | grep -E 'breakpoint|LayoutMode'`（如无日志则以界面为准）。

### 12.7 凭据权限豁免（E127）

- **预期读数**：`hilog -x | grep 'HDSH-NODELIVE' | grep 平台标识` 出现 `HDSH_PLATFORM=ohos`；且**写入密钥后模型调用不再报** `no API key`（那条报错的根因是引用名而不是权限，两者都要确认）。

### 12.8 门禁（每次改动都应全绿）

```bash
node tools/check-store-readiness.mjs     # 上架红线（包名/权限等级/设备类型/jitless）
node tools/check-native-closure.mjs      # 原生依赖闭包（52 库）
node tools/check-dead-handlers.mjs       # 空实现扫描（用途是逐条判断，不是数字归零）
node tools/check-model-roundtrip.mjs --remote-url http://127.0.0.1:3120 --remote-token "$TOKEN" \
  --with-workspace --dir /data/storage/el2/base/haps/entry/files/workspace \
  --prompt "Reply with exactly: pong" --wait-ms 120000     # 真机真实模型往返
```

### 12.9 验收记录模板（跑一次填一张，别事后回忆）

| 项 | 动作 / 命令 | 实际读数（原样粘贴） | 结论 | 证据 |
|---|---|---|---|---|
| 12.1 切换 | 设置→核心→rc.3「切换」 | `HDSH-CORE 切换结果：ok=?` … | ☐PASS ☐FAIL | 截图 / hilog 片段 |
| 12.1 回滚 | 「回滚到上一版」 | `BOOT_10_ENV_READY core=…` | ☐PASS ☐FAIL | |
| 12.2 插件启停 | 禁用一行→重启核心 | 行状态变化 + 写入记录 | ☐PASS ☐FAIL | |
| 12.3 原生件 | 读「原生件加载」一行 | `未加载：<名字>：<原因>` | ☐PASS ☐FAIL | |
| 12.4 技能形状 | 设置→技能 | 列表 或 `形状未识别（顶层键：…）` | ☐PASS ☐FAIL | |
| 12.5 删除/归档 | 组头删除（两次）/ 会话归档 | 组消失但目录仍在 / 进入归档分组 | ☐PASS ☐FAIL | |
| 12.6 形态 | 折叠展开 / 平板三栏 | 布局切换不丢当前页 | ☐PASS ☐FAIL | |
| 12.7 凭据 | 写密钥后发消息 | `HDSH_PLATFORM=ohos` + 无 `no API key` | ☐PASS ☐FAIL | |
| 12.8 门禁 | 四条脚本 + 真机往返 | 全绿 / `RESULT: PASS` | ☐PASS ☐FAIL | |

> **FAIL 不等于"环境问题"**：每条都给了预期读数，读数不符就是缺陷。把实际读数**原样**粘进"实际读数"列——
> 改了措辞的转述会让下一个人无法判断。

## 13. 官方 web UI 逐包对照表（E180：以官方客户端包为权威，逐项核对）

> 官方 web 的界面不是一整块，而是 **41 个客户端插件包**（`@deepseek-ai/dsh-client-ui-*`）拼出来的。
> 每一包自带**中文文案**（内嵌 locale），所以"官网怎么说"可以直接从包里读出来，不必猜。
> 下表用一次全量抽取得到（`lib/client.js` 的标签键值），右侧是我们对应的实现与差距。

### 13.1 官方包 → 我们的对应关系

| 官方包 | 官方界面/按钮（抽取到的原文） | 我们的实现 | 差距 |
|---|---|---|---|
| `ui-workspace` | 工作区树（展开箭头、拖放前后） | ✅ 工作区分组 + `+ 会话 / 文件 / 删除` | 缺**拖放排序** |
| `ui-sidebar` / `sidebar-files` / `sidebar-documentpreview` / `sidebar-right` | 左栏、文件树、文档预览、右栏入口 | 🟡 工作区 + 文件视图（列表/预览） | 缺**右栏**与文档预览的正式形态 |
| `ui-layout` | 三栏框架（sidebar/center/right + 拖拽手柄） | ✅ SINGLE/DOUBLE/TRIPLE | 缺**手柄拖拽调宽** |
| `ui-conversation` / `ui-chat` / `ui-renderer` | 对话流、气泡、文件卡、渲染插槽 | ✅ 对话（气泡/工具卡/思考块） | — |
| `ui-trajectory` | **轨迹工具栏（E196 补全，比原先记的多得多）**：`duration`「时长」/ `useActualDuration`「使用实际时长」/ `useEqualWidth`「使用等宽操作」/ `actualTime`「实际时间」；**`turns`「轮次」+ `expandTurns`/`collapseTurns`「展开/收起所有轮次」**；**`calls`「调用」+ `expandCalls`/`collapseCalls`「展开/收起所有调用」**；**`search`「搜索轨迹」/ `searchPlaceholder`「搜索」** | 🟡 轨迹 chip（只有切换） | **缺工具栏全部项**；关键取证：时长偏好是**客户端持久化偏好**（官方 `createSnapshotStore(false, { persist: { name: 'dsh.trajectory…' } })`）⇒ **不依赖任何端点**，同一批会话事件即可绘制 ⇒ **可实现，不是能力缺口** |
| `ui-tool` / `ui-deliverables` / `ui-subagent` / `ui-workflow-run` | 工具卡、交付物、子代理、工作流（phase/members） | ✅ 工具卡、**交付物（代码命中 29 处）**、**子代理（33 处）** | **工作流成员视图未做**（`workflow` 命中 0） |
| `ui-approval` / `ui-user-questions` | 审批卡、提问卡（discuss/feedback） | ✅ 审批内联、**提问卡（73 处）**；**消息反馈原先的"14 处命中"是误报（E233），现已真正实现**（E252–E255：别名 → 中枢读写 → 契约修正 → 消息行 chip，待真机验证） | — |
| `ui-goal` / `ui-plan` / `ui-permission-presets` | 目标栏、计划模式 chip、权限预设 | 🟡 目标栏较薄（2 处）；权限显示宿主真实值 ✓ | **计划模式未做**（`plan` 命中 0） |

| E200 | **计划模式（`ui-plan`）取证：无专用端点，读 `plan` 投影 + 用 `plan` 命令切换** | 核对三处：① **兼容表里没有任何 plan 端点**（`plan` 命中 0）；② 但**投影里有**：`Wire.ets:130` 记录了形状 `"plan":{"active":false,"pending":false}`，第 186 行把 `'plan'` 列入投影键，中枢有通用的 `applySessionProjections()`（读 `projections.values`）；③ 官方原话：*"Plan-mode status over the **host-computed `plan` projection**. The chip renders only while the effective target is plan mode (`pending ? !active : active`)"*。<br>⇒ **两条可直接落地的结论**：<br>· **切换已经可用**：官方把"进入或退出计划模式"做成一条**命令**（`ui-commands` 的 `description.plan`）⇒ 用户现在就能通过**我们刚建好的命令面板**执行它（E190），不需要新端点；<br>· **缺的只是状态 chip**：按 `pending ? !active : active` 从 `plan` 投影推导显示，与官方同源。<br>**下一步**：确认 `applySessionProjections()` 是否已把 `plan` 落进快照（大概率已有，只是没人消费）⇒ 有就直接渲染 chip、没有就补一行解析；随后 chip 点击改为执行 `plan` 命令（复用 `executeCommand`）。 |> **E199 审计（按"先读代码再看官方"的顺序复核 §13 的差距列）**：一次性在 `entry` + `appstate` 里统计各能力的存在度，
> 又抓到四行**我猜错**的：子代理（33 处）、交付物（29 处）、提问卡（73 处）、消息反馈（14 处）**其实都已实现**；
> 而**确实没有**的是：工作流成员视图、计划模式、右栏、拖放排序、附件轮播（各 0 处）。
> ⇒ 对照表的差距列现已逐行核实；**"猜"出来的差距一律不算**。
| `ui-approval` / `ui-user-questions` | 审批卡（headline/body/command/actionRow）、提问卡（discuss/feedback） | ✅ 审批内联；提问卡与消息反馈**代码均有实现**（E199 审计：提问卡 73 处、消息反馈 14 处命中） | 按钮级细节待真机确认（原先记"未做"，E205 更正） |
| `ui-commands` | **命令面板**：压缩以上对话内容 / 导出为 ZIP / 发送反馈 / 目标 / **权限预设** / 计划模式 | ✅ **已完成**（E187 端点助手 / E188 中枢执行 / E190 面板 UI：快捷键打开、列出宿主命令、点选即执行） | — （原记"最大的按钮级缺口"，E205 清理该过时断言） |
| `ui-model-selection` | 会话输入区的**模型/强度选择**（cell/check/chevron） | ✅ **已完成**（E192–E195：别名 → Wire 助手 → 中枢 `selectSessionModel` → 面板内列表与点选执行） | 位置：做在会话操作面板内，官方在输入区（同一份状态） |
| `ui-permission-presets` | **权限**：选择新会话的默认权限模式；仅可查看 / 工作区内修改 … | ✅ 通用页显示宿主真实值 | 缺**新会话默认权限**的选择入口 |
| `ui-settings` / `settings-general` / `settings-models` / `settings-plugins` / `settings-plugin-inventory` | 设置总览、通用、模型、插件、插件清单 | ✅ 六个分区（通用/模型/插件/核心/预设/技能/设备） | 模型页进行中（E176–E179） |
| `ui-settings-models`（按钮名） | **`addButton` / `addCard` / `candidate` / `candidateId` / `addActions`** | ✅ **候选卡已落地**（E178 常量 → E179 中枢目录 → E181「可添加」列表：点选展开该提供方的自定义设置，未声明分区时如实标注不可点） | 模型页另有两处收编未做：「模型凭据」空壳已消除（E177），「当前模型/推理强度」只读区块仍待收编 |
| `ui-agent-preset` | 预设卡片（复制/位置/删除；只读查看器） | ✅ 名单+复制+删除+查看（E161/E170/E173） | — |
| `ui-skill` | 技能行（card/chevron/iconIdle） | ✅ 技能区（E174 修好会话范围） | — |
| `ui-theme` / `brand-official` | 外观（主题方块/分组/选中） | ✅ **外观已实现**（E260 核对）：`applyThemeMode` + `LocalPrefs.loadThemeMode()` 启动时应用；设置里按**官方译名**呈现「外观 / 浅色 / 深色 / 跟随系统」（`SettingsPane.ets:132/181/184`），写成偏好并可回读 | 缺的只是**图形化方块**这一呈现形式（可选升级，非能力缺失） |
| `ui-input-trigger` / `ui-reference` | 输入触发（@ 引用：文件与文件夹/对话/工作区 crumb） | ✅ **已实现**（E198 核对：中枢快照 `references: ReferenceCandidate[]` 由 `fileReferences/list` + `sessionReferenceResolver/candidates` 组成；`Index` 有 `@State references` 并在切会话时重拉；`Composer` 有 `@Prop references` + `filteredReferences()` 渲染候选） | — |
| `ui-attachment` | 附件（remove/arrowLeft/Right 轮播） | 🟡 附件发送（无轮播预览） | 预览形态待补 |
| `ui-jobs` / `ui-schedule` | 作业（count/kind/duration/menu）、定时（prompt/metadata/menu） | ❌ 未做 | 线协议无对应端点（E136 已取证：宿主侧无 Remote 面） |
| `ui-goal` / `ui-plan` / `ui-cordis` / `ui-open-in-app` / `ui-message-feedback` / `ui-directory-picker-*` | 目标栏、计划 chip、cordis 面板、外部打开、消息反馈、目录选择器 | 🟡 待决并入工作区、目录选择器用系统选择器、**消息反馈已实现**（E199：14 处命中） | **计划模式已完成**（E201 投影 + E202 chip）；**目标栏也已实现**（E242 `goals/get` 数据层 + E243 面板内目标栏，三种情况分开呈现，待真机验证）；cordis 面板与「外部打开」未做 |

> **方法教训（E198，重要）**：本表的"差距"列**必须以我们自己的代码为准**，不能只凭官方包名推断。
> E198 就抓到一个反例：`ui-input-trigger`/`ui-reference` 被我记成"未做"，核对后发现**早已完整实现**
> （中枢快照 `references` + `Index` 的 `@State references` + `Composer` 的候选渲染）。
> 同理 E197 发现"轨迹搜索"其实早有。**核对顺序应当是：先读我们的实现，再看官方有什么，最后写差距。**
### 13.2 由这张表得到的**按钮级优先级**（按官方权重）

> **进度（截至 E191）**：① ✅ **命令面板完成**（E187 端点助手 → E188 中枢执行 → E190 面板 UI：快捷键打开、列出宿主命令、点选即执行）；② ✅ 提供方候选卡已落地（E178/E179/E181）；③ 🔨 **输入区模型/强度**：通道已确认（`session/selectModel` 在兼容表 `Endpoints.ets:683`，**REQUEST 形态**；`params:['request']`），且已查明 `setDefaultModel` 写的是 `agent-default-model`（"新会话默认用什么"）而**按会话切换此前没有实现**——缺别名常量、Wire 助手、中枢方法与输入区 UI 四件；④⑤ 未开始。
> **实施纪律**：每项按"官方有这个按钮 → 它写哪条端点 → 我们有没有那条通道"三步确认；**同族"列能力"端点都是会话范围的**（`skills/list`、`commands/list` 都在此列——E174 的报错 `failed to observe session "undefined"` 就是没带会话所致），所以任何"列能力"的端点都必须带 `sessionId`，未选中会话时**不发请求并如实说明**。
> **环境注意（E191 实测）**：本轮新增别名时 `[System.IO.File]::WriteAllText` 连续报 *"The given path's format is not supported"*（路径合法、同样写法此前多次成功）⇒ 未能落盘并导致红树，已 `git checkout` 回退。**处置原则：出现无法解释的写入失败时先在红树上停手、回到可验证的绿状态，再换写入路径（改用 `edit` 工具）**——不在红树上继续叠加尝试。

1. **命令面板**（`ui-commands`）：官方把"压缩对话 / 导出 ZIP / 反馈 / 目标 / 权限预设 / 计划模式"都放在这里——**已完成**（E190）；
2. **添加提供方**（`ui-settings-models` 的 `addButton`/`candidate`）：数据层与候选卡已就绪；
3. **会话输入区的模型/强度选择**（`ui-model-selection`，写 `session/selectModel`）：现在只有设置页的只读展示与"新会话默认值"通道；
4. **轨迹工具栏**（时长 / 实际时长 / 等宽 / 实际时间）；
5. **@ 引用**（`ui-input-trigger` + `ui-reference`）。

> **方法说明**：这张表是"对齐"的**检查表**，不是待办清单的全部——每一项都要按"官方有这个按钮 → 它写什么端点 → 我们有没有那条通道"三步确认；没有通道的（如 jobs/schedule）已在 E136 记录为上游边界，不硬造。

### 13.3 轨迹时长偏好：适用与形态差异（E203）

- 我们的轨迹**已经逐项显示耗时**（`ConversationPane`：`item.elapsedMs` → `formatDuration`；思考块也标"字数/耗时"）✓
- 官方 `useActualDuration`「使用实际时长」/ `useEqualWidth`「使用等宽操作」控制的是**时间轴条宽的比例**（官方那份是**比例时间轴**，且偏好是**浏览器级持久化**的 `createTrajectoryDurationStore`）。**我们的轨迹是列表、没有时间轴** ⇒ 照搬这两项只会得到"**点了没效果的开关**"，属本项目一贯拒绝的东西 ⇒ **记为形态差异，不做**。
- 官方 `actualTime`「实际时间」= 显示**钟点**而非时长 ⇒ 这一项**对列表同样适用**，是真正可做的那个。
- **结论**：后续要动这块就做「**时长 ↔ 实际时间**」的显示切换（有实际效果），不做条宽偏好（无时间轴可调）。
### 13.4 「添加提供方」的官方细节与我们的差距（E210）

官方 `dsh-client-ui-settings-models` 的添加流程（从包内文案与代码取证）：

| 官方键 | 语义 | 我们 |
|---|---|---|
| `addButton` / `addCard` / `candidateList` | 添加入口 + 候选列表 | ✅ E181（候选列表内联在提供方区） |
| **`candidateSearch`** / `candidateToolbar` | 候选**可搜索**（还带工具栏） | ❌ 缺搜索 |
| `candidateId` / `candidateLabel` / `candidateEmpty` | 标识 / 名称 / 空态 | ✅ 标识与名称有；空态以"不显示该区块"处理 |
| **`addModelButton`** / `advancedHint` / `credentialDot` | **往提供方里加模型** / 高级提示 / 凭据状态点 | ❌ 未做 |
| 写入路径 | 读**该提供方设置命名空间的 schema**（`protocolChoices(namespace, schema)`：协议、baseURL…）再写设置 | ✅ 同类同源：我们展开的正是该提供方的「自定义设置」，且设置项编辑本就 schema 驱动 |


**进度（E211–E214）**：候选**搜索**已补（E211）；**模型目录已搬进提供方展开区**（E213："模型目录 · N" + 模型名/当前标记/档推理/描述 + 点选设为新会话默认）；E209 的遗留 `nothingToGroup()` 已删（E214）。

> **⚠️ 待"拆"（需真机确认后执行）**：搬进展开区的渲染**尚未真机验证**，故模型页底部那个独立区块**暂时保留**——
> 这是 E212 用"功能消失"换来的纪律：**先搬后拆，不先拆后搬**（替代渲染点未验证前绝不收起原处）。**结论**：行为**种类一致**（都是"写该提供方的设置命名空间"），差的是**搜索**与**加模型**两个便利入口。下一轮补：候选列表加搜索框（`candidateSearch`），并评估"加模型"入口（`addModelButton`）对应我们哪条通道。
### 13.5 「加模型」入口的结论：不是缺通道，缺的是便利表单（E215）

官方 `ui-settings-models` 的 `addModelButton` 在包内只是**CSS 类名**，其行为在逻辑层；而 E210 已取证的事实是：
**模型目录是该提供方"设置命名空间"里的一个设置项**（官方读命名空间 schema 渲染表单，见 `protocolChoices(namespace, schema)`）。

⇒ **结论**：官方所谓"加模型"就是**编辑该命名空间的模型目录设置**。而我们的提供方展开区**本来就渲染该命名空间的设置项**（schema 驱动，E213 起还并列列出已知模型）：

| 能力 | 我们 |
|---|---|
| 看到/编辑该提供方的命名空间设置（含模型目录这类设置项） | ✅ 展开区（`providerSettingGroup`，schema 驱动） |
| 看到该提供方已知的模型（名称/当前/档推理/描述，可点选设为默认） | ✅ E213 |
| 一个"**追加一条模型**"的专门表单 | ❌ 缺（属**便利性**，非通道缺失） |

**判定**：**不硬造**。要做得对，需要按该设置项的 schema 渲染一个列表编辑器（我们已能拿到 schema，但列表编辑 UI 是独立工作量）。
在真机上先确认"展开区里是否已有宿主声明的模型目录设置项"（§14.1 第 1 条会看到）——
**若已有**，则用户本就能在该项里增删模型 ⇒ 便利表单降为可选改进；**若没有**，再评估补一个追加表单。
### 13.5c 更正：目标栏**未实现**（E234，并把要件记入 §15.8）

E199/E206 我按命中数写"目标栏仍较薄（2 处命中）"。用**具体符号**复核：代码里**只有登记的常量**
（`GOALS_GET_ENDPOINT` 在 `Aliases.ets:225`）与兼容表条目（`Endpoints.ets:347–407`），
**没有任何调用或界面** ⇒ 准确说法是「**通道已登记、未实现**」。

**权威契约（可直接据以实施）**：

| 端点 | 参数 |
|---|---|
| `goals/get` | `agentId` |
| `goals/create` | `agentId`, `request` |
| `goals/edit` | `agentId`, `ref`, `request` |
| `goals/pause` / `goals/complete` / `goals/clear` | `agentId`, `ref` |

⇒ 官方 `ui-goal` 的目标栏（bar/dock/actions/iconBtn）**可以做**，且**要件齐全**（端点 + 参数 + agentId 的来源我们已有：`commandsFor`）。

**本轮刻意不实现**：按既定纪律——**没有设备判定时，不再新增"待验证改动"**。它现在作为**可接通道**登记在 §15.8，
等设备回接、§16.2 那 14 项判完，再决定接不接（这也是它的自然优先级：目标栏属"锦上添花"，而 §16.2 里那些是"已改但没验"）。
### 13.5b 更正：消息反馈**并未实现**（E233，并附方法教训）

E199/E206 我按"字符串命中数"判定「提问卡 73 处、消息反馈 14 处 ⇒ 已实现」。**逐条用具体符号复核后**：

| 声称 | 复核方式 | 结论 |
|---|---|---|
| 子代理 | `TrajectoryKind.SUBAGENT` + `subagentItem()` 渲染「子代理 <名>」 | ✅ **确实实现** |
| 交付物 | `TrajectoryKind.DELIVERABLE` + `deliverableItem()` + 工作区文件交付物标记 | ✅ **确实实现** |
| 提问卡 | `projectAskUserQuestionRequest` / `AskUserQuestionAnswerItem` / 答复逻辑（`Index.ets:3183`） | ✅ **确实实现** |
| **消息反馈** | `messageFeedback` **只在兼容表**（`Endpoints.ets:467/479/491`），代码里**没有任何调用或 UI**；那 14 处命中是注释里的「用户反馈①/②」 | ❌ **未实现**（原判为误报） |

**更正后的状态**：消息反馈 = **通道已登记、未实现** ⇒ 归入"确认缺/暂不做"一类（官方 `ui-message-feedback` 的 categories/chip/submit 那套 UI 我们没做；
但注意：官方把它也做成了命令（`/feedback`），而**我们的命令面板已能执行该命令**——所以"能不能反馈"其实可用，缺的是**消息级反馈的界面**）。

> **方法教训（第 8 条纪律）**：**"字符串命中数"不能证明实现** —— 中文注释里出现同一个词（"反馈"）就会把计数抬起来。
> 判定"某项是否已实现"必须搜**具体符号**（枚举成员、函数名、端点调用），而不是泛词。
> E198/E199 因此产生过一次误判与一次漏判（@ 引用漏判、消息反馈误判），两次都是同一原因。
### 13.5d §13 对照表的双向验证结论（E235）

到本轮为止，这张表的**三类结论都用"具体符号"验过一遍**（不靠命中数、不靠包名推断）：

| 方向 | 验证方式 | 结果 |
|---|---|---|
| **"已具备"** | 搜具体符号（枚举成员 / 函数名 / 端点调用） | 子代理、交付物、提问卡 ✅ 属实；发现并更正 **2 处误判**（消息反馈、目标栏——都只有常量、没有实现） |
| **"确认缺"** | 搜具体符号（`WorkflowRun` / `SidebarRight` / `onDrop` / `AttachmentRail` / `themeCube` / `openInApp` / `Cordis`） | **全部零命中** ✅ ⇒ 这些项**确实是缺的**，没有把"其实有"的误列为缺 |
| **"形态差异/上游边界"** | 契约与包内说明 | 轨迹条宽偏好（无时间轴）、官方四颗分组开关、jobs/schedule（宿主无 Remote 面）——结论有据 |

⇒ **§13 现在可以当作可信的对齐检查表使用**：每个"已有/没有"的结论都有具体符号或契约支撑，
且两个方向的误判都已在本会话内发现并更正（这本身说明验证方法是有效的——它既能证实，也能证伪）。
### 13.6 源码位置索引（E232：现场核对时直接跳过去）

> 下表是**该能力在代码里的首次出现位置**（多为导入或定义处，不是行级精确），用于现场核对时少搜一次。
> 行号会随后续改动漂移——**按符号名搜**比按行号更稳。

| 能力 | 位置（文件:行） |
|---|---|
| 命令面板（打开状态） | `entry/.../pages/Index.ets:444`（`@State paletteOpen`） |
| 命令面板（执行通道） | `appstate/.../store/SessionHub.ets:2880`（`executeCommand`） |
| 命令面板（载荷） | `appstate/.../model/Wire.ets`（`commandsExecutePayload`） |
| 本会话模型选择（中枢） | `appstate/.../store/SessionHub.ets:2922`（`selectSessionModel`） |
| 本会话模型选择（载荷，嵌套 `selected`） | `appstate/.../model/Wire.ets`（`sessionSelectModelPayload`） |
| 计划模式 chip | `entry/.../pages/Index.ets:441`（`planActive`） |
| 计划模式投影解析 | `appstate/.../store/SessionHub.ets`（`applySessionProjections` 内 `typed['plan']`） |
| 轨迹展开/收起全部 | `entry/.../view/ConversationPane.ets:126`（`expandAllItems`） |
| 轨迹搜索 | `entry/.../view/ConversationPane.ets`（`matchTrajectoryIndices`） |
| 轨迹钟点·时长 | `entry/.../view/ConversationPane.ets`（`clockOf`） |
| 工具卡 / 子代理 / 交付物 | `entry/.../view/ConversationPane.ets`（`toolItem` / 子代理与交付物渲染约 490 行附近） |
| 提问卡 | `entry/.../pages/Index.ets:3183` |
| 审批内联 | `entry/.../pages/Index.ets:133`（`PendingPane`） |
| 预设页 | `entry/.../view/SettingsPane.ets:378` |
| 技能页 | `entry/.../view/SettingsPane.ets:314` |
| 提供方卡片 | `entry/.../view/SettingsPane.ets:1083`（`providerCards()`） |
| 候选提供方搜索 | `entry/.../view/SettingsPane.ets:941`（`candidateQuery`） |
| 提供方展开区模型目录 | `entry/.../view/SettingsPane.ets`（搜 `模型目录 ·`） |
| 文件变更流 | `appstate/.../store/SessionHub.ets:2214`（`openFilesStream`） |
| 工作区删除 | `appstate/.../store/SessionHub.ets:2663`（`deleteWorkspace`） |
| 工作区归档 | `appstate/.../store/SessionHub.ets:2703`（`archiveSession`） |
| 旁路记账（进 hilog） | `appstate/.../store/SessionHub.ets`（`record`，搜 `HDSH-TRACE`） |
## 14. 接回设备后的一张核对单（UI 对齐 + 功能验收）

> **用途**：本轮之后所有改动**无真机可验**（以构建 + 门禁为限），下列项需要接回设备后**逐条确认**。
> 建议按顺序做；每条都给出"看哪里 / 期望什么"。结果填进 §12.9 的记录表。

> ### 先跑一条命令（E229）
>
> ```powershell
> powershell -NoProfile -File tools\device-acceptance.ps1
> ```
> 它自动完成**可脚本化**的部分：找设备 → 装最新 HAP → 启动 → 等就绪 → 抓五份日志（boot / connect / trace / features / errors）
> → 抓各页面布局 dump 与截图 → 在 `dist/acceptance/<时间戳>/report.md` 里**把能自动读到的读数填好**（核心路径、连接结果、平台标识、文件变更流是否开启）。
>
> **脚本不管的部分**（必须人眼看）就是下面 §14.1–14.5 的勾选项——它已经在 report.md 里列成清单，你照着勾即可。
> **任何 FAIL 请贴回执原文**（本项目排障靠原文，不靠转述），填进 §12.9 的记录表。
### 14.1 先做"拆"（依赖前面的"搬"）

| # | 动作 | 期望 | 若不符 |
|---|---|---|---|
| 1 | 设置 → 模型 → 展开任一提供方 | 展开区出现「**模型目录 · N**」+ 模型行（名称/「当前」标记/N 档推理/描述） | 若没出现，**不要拆**底部区块（先搬后拆） |
| 2 | 点展开区里的一个模型 | 提示"已写入 …"（设为**新会话默认**） | 记下提示原文 |
| 3 | 上一条无误后 | 把模型页底部「各提供方的模型…」区块收起（这一步才做"拆"） | — |

> **§14.1 第 2 条的出处核对（E248）**：判据写"点展开区里的模型 → 提示已写入（设为**新会话默认**）"，本轮按代码核实——
> `onSelectModel` → `Index.applyDefaultModel()` @3717：先置提示「正在写入默认模型 …」，
> 再把 `setDefaultModel()` 的返回（成功/失败原因）写进 `settingWriteNote`（设置页可见），并同时记 `HDSH-MODEL` 日志。
> ⇒ **行为与语义都对**：它写的是**新会话默认**（`agent-default-model`），**不是**"本会话切换"（那是面板里的「本会话的模型」）。
> 两者在同一页面相邻出现正是最容易混淆的地方，验收时请注意区分。
### 14.2 新 UI 项（本轮前后新增，全部未真机验证）

| # | 动作 | 期望 |
|---|---|---|
| 4 | 触发**命令面板**（快捷键，见 `ShortcutKeys` 的绑定） | 面板打开；列出宿主给的命令（名称 + 说明），如 `/compact`、`/export`、`/plan` |
| 5 | 点其中一条命令 | 提示"已执行 …"或宿主原文的失败原因；**不应崩溃** |
| 6 | 面板里「本会话的模型」 | 列出模型（当前项高亮）；点选后提示已切换（按会话） |
| 7 | 面板里「计划模式」 | 显示"已开启/已关闭"（判据 `pending ? !active : active`）；点「切换」后状态变化 |
| 8 | 轨迹模式：展开全部 / 收起全部 | 所有条目展开 / 全部收起 |
| 9 | 轨迹条目右侧 | 显示「**钟点 · 时长**」（如 `09:41 · 3.2s`） |
| 10 | 轨迹搜索框 | 输入关键词后只留命中项（原有能力，顺带确认未被我改动破坏） |
| 11 | 设置 → 模型 → 「可添加的提供方」 | 有候选行 + **搜索框**；输入关键字能过滤；未声明设置分区的候选显示"设置分区未声明"且不可点 |

### 14.3 既有待验项（与 §12 相同，此处汇总）

| # | 项 | 期望 |
|---|---|---|
| 12 | 插件启停（12.2） | 点某行启停 → 提示应含「**重启核心后生效**」（插件树在启动时挂载，见 `Index.ets:3734/3758`）→ 到「设置→核心→**重启核心**」→ 该行状态与设置一致 |
| 13 | 工作区删除 / 归档（12.5） | 删除后组消失**但磁盘目录仍在**；归档后进入「归档 · N」（默认折叠） |
| 14 | 折叠屏 / 平板 / 2in1（12.6） | 布局切换不丢当前页；三栏可用 |
| 15 | 核心版本切换 / 回滚（12.1） | 切换 → 提示"已安排…" → 完全退出重开 → 新版本运行（E152 已验证过一轮，回归确认） |

### 14.5b 回滚路径的**代码级复核**（E240：把最未被验证的一项验到逻辑层）

目标明确点名"核心版本的**安装 / 切换 / 回滚**"，而三者中**回滚**是唯一只有"与切换同机制"这一间接证据的。设备不在，
故本轮做**代码级复核**（能验的部分先验掉，剩下留给真机）：

| 复核点 | 结论 |
|---|---|
| `DshHost.rollbackTo()` 的"安排回滚"分支是否与 `switchTo()` 对称 | ✅ 对称：宿主在跑 ⇒ 只调 `store.rollback()` **写状态**、不起候选（避开同进程二次 `node::Start` 的 SIGABRT），提示"请完全退出应用后重新打开" |
| 提示语里的版本语义是否对 | ✅ `next.current` = 将生效的版本、`next.previous` = 被离开的版本，与**改造前**那句"已回滚到 X（退回的版本：Y）"语义一致 |
| **REFUSE 时会不会"已经改了状态却告诉用户没回滚"** | ✅ **不会**：`CoreStore.rollback()` 先 `decideRollback()` 判定，**REFUSE 直接返回、不写状态**；写状态失败时也明确"保持原版本"并把原因带出 ⇒ 我们的「未回滚：…」是**诚实**的 |
| 宿主未运行时 | ✅ 仍走原路径（那时是本次进程第一次启动 Node，不会崩） |

**仍未验证（必须真机）**：点「回滚到上一版」→ 提示"已安排回滚到 X"→ 完全退出重开 → `BOOT_10_ENV_READY` 显示 X。
这一步就是 §14.3 里的那一条，逻辑层已复核完毕，真机只需确认端到端。
### 14.6 多形态（折叠屏 / 平板 / 2in1）的可判定标准（E236）

**判据来自代码**（不是猜的）：`Breakpoints.ets` 的 `layoutModeOf(widthVp)` 按窗口宽度分档，
阈值在 `Tokens.ets`：**`MD = 600vp`、`LG = 840vp`**。

| 窗口宽度（vp） | 布局模式 | 应看到什么 |
|---|---|---|
| `< 600` | **SINGLE** | 单栏：工作区列表 ⇄ 详情（`stackPage`）**逐级前进/返回**；底部一级导航（工作区 / 设置） |
| `600 ≤ w < 840` | **DOUBLE** | 双栏：列表 + 详情并排；`detailPanelAvailable(DOUBLE)` 判定详情面板可用性 |
| `≥ 840` | **TRIPLE** | 三栏：侧栏 + 列表 + 详情（打开会话会置 `detailOpen = true`） |

**逐项判定（每条都有明确对错）**：

| # | 动作 | 通过判据 |
|---|---|---|
| 24 | 手机（< 600vp）打开一个会话 | 进入单栏详情；**返回**逐层退出且**不退出应用**——实际实现见 `Index.ets:879 onBackPress()`：先关凭据编辑浮层 → 再关设置编辑浮层 → …（每层返回 `true` 表示已处理） |
| 25 | 折叠屏**展开**（跨过 600vp） | 布局切到 DOUBLE；**当前选中的会话与工作区不丢**（`selectedSessionId` 不变） |
| 26 | 折叠屏**折叠**（回到 < 600vp） | 切回 SINGLE；若此前在详情，应仍在**同一个会话**上（不跳回列表至别的会话） |
| 27 | 平板（600–840vp） | 双栏并排；右侧详情内容与左侧选中项一致 |
| 28 | 2in1 / 桌面（≥ 840vp） | 三栏；**详情面板可关**（`detailOpen`）而列表保持可见 |
| 29 | 任一次形态切换 | 底部一级导航的**选中项不变**（`TABS` 两项：工作区 / 设置）；页面不出现空白面板 |

> **E247 更正（我把验收判据写错过一次）**：原文写"插件启停 → 状态转「待确认」→ 重启核心后生效"，
> 把两件不同的事混为一谈——
> · **「待确认」是关于"可安装性"的徽标**（三态：可安装 / 需发版 / **待确认=未知**）；
>   `Core.ets:103` 写得很清楚：*"把'不知道'硬塞进 `hasNative: false` 就会把'未知'渲染成'可安装'"* ⇒ 宁可显示待确认，也不给会失败的承诺；
> · **"重启后生效"是关于"启停是否已应用"**，由启停操作的提示直接说明（`Index.ets:3758`）。
> **安装/卸载仍是硬边界**：上游安装通道走 `spawnSync("pnpm")`，端侧无法创建进程（D6 E15/E86）——这不是待办，是物理约束。
> 这次的教训与 E233 同源：**验收判据也要按代码/契约核对**，不能凭印象写（印象里"待确认"像"待重启"，其实不是）。
> **E249 更正（一处我编造的交叉引用）**：原文写"系统返回键行为见 **E131** 系列"——查证后 **E131 讲的是"死按钮收尾 + 三项新需求的线协议事实"**，
> 与返回键**毫无关系** ✗。真实出处是代码本身：`Index.ets:879 onBackPress()`，实现为**分层退出链**
> （关凭据浮层 → 关设置编辑浮层 → …，逐层返回 `true` 表示"已处理"），因此**返回键不会退出应用**。
> **教训**：交叉引用（"见 Exxx"）也必须核对——**编造一个看起来合理的编号，比不写引用更糟**，
> 因为它会让读者以为"已经查过了"。这与 E233/E247 同源：**凡是"我记得"的内容都要落到可核对的位置**。
**已知的实现边界（如实记录，别当缺陷）**：
- 三栏比例**不可拖拽调整**（官方有拖拽手柄，我们没做，见 §13 的"确认缺"）；
- 从 SINGLE 切到 DOUBLE/TRIPLE 时**不新增**已打开会话——只是把已打开的内容摊开；
- `detailPanelAvailable(mode)` 决定某形态下详情面板是否存在，若某形态下没有详情，属**设计**而非缺失。
### 14.4 门禁（每次改动都应全绿）

```bash
node tools/check-store-readiness.mjs   # 上架红线
node tools/check-native-closure.mjs    # 原生依赖闭包
node tools/check-dead-handlers.mjs     # 空实现扫描（逐条判断，不追求归零）
```
### 14.5 本轮之后新增项的验收（并入 §12.9 记录表）

> 这些项在本会话后半段加入，**全部未真机验证**（以构建 + 门禁为限）。逐条给出**动作 → 判据 → 失败长什么样**，
> 便于一次跑完不返工。结果同样填进 §12.9 表。

| # | 动作 | 通过判据 | 失败形态（要记下来） |
|---|---|---|---|
| 16 | 打开**命令面板**（快捷键）→ 看列表 | 列出宿主给的多条命令（`/` 开头）+ 说明文字 | 空态原文见 `Index.ets:2952-2953`：「命令取决于具体会话：请先在工作区里选中一个会话。」⇒ 未选会话；「宿主没有报告该会话可用的命令（或尚未读取）。」⇒ 宿主确实没给 |
| 17 | 点一条无副作用命令（如 `/compact` 之外的只读类；先避开 `/plan`） | 回执"已执行 …"，**不崩溃** | 回执含宿主的失败原因 ⇒ 把它原样记下（可能是字段问题，见 §15.7 的纪律） |
| 18 | 面板里「**计划模式**」→ 点「切换」 | 状态文字在"已开启/已关闭"间变化；再点一次可切回 | 状态不变 ⇒ 记下回执；`/plan` 命令若不在列表里，说明该会话未提供 |
| 19 | 面板里「**本会话的模型**」→ 点另一个模型 | 回执"本会话已改用 …" | 若报字段类错误 ⇒ 对照 §15.7（`selected` 嵌套契约） |
| 20 | **轨迹**：展开全部 / 收起全部；看条目右侧 | 全部展开/收起；右侧显示「钟点 · 时长」 | 只见时长不见钟点 ⇒ `at` 为 0（事件没带时间戳） |
| 21 | 让 Agent **改动工作区文件**（或让它写一个交付物），停留在文件视图 | 文件列表**自动出现新文件/变化**（E226 的变更流） | 不刷新 ⇒ 查日志 `HDSH-TRACE files changes opened` 是否 `cancel=ok` |
| 22 | **模型页**：展开任一提供方 | 展开区出现「模型目录 · N」+ 模型行（名称/当前/N 档推理/描述） | **若看不到**：不要执行 14.1 第 3 条的"拆"（先搬后拆） |
| 23 | 模型页「可添加的提供方」→ 在搜索框输入关键字 | 候选**被过滤** | 无候选 ⇒ 宿主未声明可配置提供方（看 `providerCatalogNote`） |
| 24 | 打开命令面板 → 看「**长期目标**」一行 | 有目标则显示目标文本；没有则显示中枢的如实话术（该会话当前没有长期目标） | 显示「形状未识别（顶层键：…）」⇒ **那就是答案**：把该键写进 `goalTextFromReply` 收紧解析（同 E135 处理技能形状的方式） |
| 25 | 在会话里找到一条**助手消息** → 点「有帮助」或「没帮助」 | 该侧文字**高亮**（品牌色）；换点另一侧则高亮跟着换（显示的是宿主记录的状态） | 点击后无变化 ⇒ 看 `HDSH-TRACE` 里 `messageFeedback/put fail …`；若为 `arguments-invalid` ⇒ 是 `ifVersion` 的 null 编码问题（见 E254 备注），把它期望的写法记下来 |

**跑完这 8 条 + §14.1–14.4 的 15 条，即覆盖本会话全部未验证改动。** 任何 FAIL 请把**回执原文**贴进 §12.9 表——本项目排障靠原文，不靠转述。
## 15. `tools/` 工具清单（E216：把"没有出处的脚本"变成有说明的工具箱）

> **为什么补这一节**：核对发现 `tools/` 下 **10 个脚本在文档里 0 次出现**——不是它们没用，而是**没人写下来**。
> 这本身就是"项目文档过时/缺失"的一种：后来者看到一个陌生脚本，既不敢用也不敢删。

### 15.1 门禁（每次都该跑，失败即回归）

| 脚本 | 作用 |
|---|---|
| `check-store-readiness.mjs` | 上架红线（包名/权限等级/设备类型/jitless 等） |
| `check-native-closure.mjs` | 原生依赖闭包完整性 |
| `check-origin-fence.mjs` | 来源围栏（哪些代码允许碰上游细节） |
| `check-plugin-toggle.mjs` | 插件启停写入路径的回归 |
| `check-dead-handlers.mjs` | 空实现扫描（**逐条判断**用途，不追求归零） |
| `check-feature-wiring.mjs` | **功能接线回归**（E258）：对 **17 个**已实现功能检查"中枢实现 + **界面调用点**"是否都在。**构建查不出"调用点被删"**——功能留着、入口被重构删掉，编译照样通过（E135 的技能触发点缺失即此类）。判据：出现次数 **>1**（1 处往往只剩定义） |
| `arch-check.mjs` | **架构门禁**：上游接口细节不得泄漏出 `dshcompat`（本项目核心可维护性主张） |
| `compat-drift.mjs` | **上游漂移门禁**：对当前 checkout 的上游重新提取，与仓库里的端点表比对 |
| `check-parity.mjs` | **功能对等门禁**（P0，2026-09-14）：校验 `docs/parity-matrix.md`——覆盖官方 39 个能力面 id、状态 token 合法、**不许用整体 DONE 盖住某个形态的缺口**、非 DONE 必须在缺口登记里、DONE 不得留陈旧登记、§5 统计与实算逐项一致。**台账类文档的失效不会让构建失败**（它只是慢慢变得不可信），故须由门禁强制（与 arch-check 同理）；含注入式自检 12 个正/负样例 |
| `check-layout-fixtures.mjs` | **布局 fixture 门禁**（P1，2026-09-14）：把 `appstate/ui` 的纯逻辑 `.ets`（`Tokens`/`Breakpoints`/`LayoutController`）按 `.ts` 编译后**在本机直接执行**，断言四形态（PHONE/TABLET/DESKTOP/2-IN-1）+ 断点边界（599/600/839/840）+ 让步链三分支，共 28 条。**为什么需要它**：`entry`（UI 层）在本环境**零自动验证**（无编译、codelinter 也检不出语法错误，均已注入实测），布局决策的劣化不会有任何症状。被测的是同一源文件而非复制品；找不到 tsc 时**退出码 3（环境受阻）而不是假通过** |
| `check-web-fetch-jitless.mjs` | **web_fetch 端侧可用性门禁**（2026-09-14）：驱动**真实上游** `dsh-web-fetch-http` 的 `HttpFetchProvider.fetch()`（把解析器注入成"把 example.com 钉到 loopback"，于是能连本机服务而 URL 主机名不变——正是上游 DNS 钉住的语义），在 `--jitless --no-experimental-fetch` 下跑**两臂对照**：不注册 `undici` 解析钩子必须失败、且必须给出 WASM 因果证据（探针直接探真 undici 并打印 cause；只有"失败"却说不出原因 → 判不成立）；注册后必须 8/8 通过，其中两条是**安全语义**——同源跳转仍被上游跟到 200、**跨源跳转仍被拒为 `WEB_REDIRECT_BLOCKED`**。含 13 条判定器自检（含"全过时判不成立"这类反向样例）；缺核心树或上游包时退出码 3。**为什么必须有**：这个缺陷曾完全静默，结构检查与"Host 起来了"都掩盖不了它 |
| `check-arkts-entry.mjs` | **entry（UI 层）ArkTS 编译门禁**（2026-09-14）：跑 hvigor 的 `default@CompileArkTS`——它**只编 ArkTS、不碰原生**，故无需 `libnode.so` 等运行期产物即可验证 `Index.ets` 与全部 Pane。判定必须**解析输出**而不是看退出码：实测 hvigorw 在编译失败时**仍可能以 0 退出**；增量构建时任务被 `UP-TO-DATE` 跳过、**没有 `COMPILE RESULT` 行**，那种"通过"是复用旧结果，脚本会如实标注并提示 `--clean`。含 8 个样例的判定器自检；找不到 CLT/JDK 时退出码 3。**已用真实注入验证会失败**（往 `view/ShortcutKeys.ets` 注入语法错误 → 退出码 1 并点名 file:line） |
| `check-builder-recursion.mjs` | **`@Builder` 自递归门禁**（E343，2026-09-15）：任何一个 `@Builder` 体内都不许出现**自己的名字**。真机上表现为 `RangeError: Stack overflow!` 直接杀进程，而栈里成对出现的那两句（`this.mainContent(...)` 与它上面那行 `} else {`）**静态读起来像"交回上层继续分派"**——编译通过、代码评审也容易放过，只有冷启后点一下界面才炸。判据在**剥掉注释与字符串之后**的 Builder 体上做（不剥注释的门禁会把 E343 的始末说明逼没）；`build()` 调 Builder、不同组件同名 Builder 都不算违规。含 5 条注入式自检（正例 + 注释/字符串/合法兜底/跨组件同名四类反例）；**已用归真验证**：对修前的提交跑同一检测器 → 命中 `MainShell.ets:380` |
| `check-dead-code.mjs` | **死代码门禁**（E350，2026-09-15）：查「搬迁留下的壳」——零使用的 import / `@Builder` / 组件成员（`@Prop`/`@State`/回调 prop/`private` 方法）。**为什么必须自动化**：E345（三个零调用 `@Builder`）、E346（拆走两段后宿主那段唯一的读者还活着）、E346b（`settingsStates` 一条从头到尾没人读的死链）三次都是我手工扫出来的，手工会漏、会随轮次漂移。计数用**剥掉注释与字符串、但保留模板串 `${…}` 插值**的文本（显示串里的成员是真使用，而只被调试串读到的调用仍会被报出，由声明行 `// dead-exempt: 理由` 说明豁免）；`@Watch` 回调从原始文本取、框架回调与 `_` 前缀不判。含 9 条注入式自检（3 正例 + 6 类不误报），并**对修前的 `SettingsPane` 归真命中 5 处真实死代码** |

### 15.2 生成器（产物提交进仓库；漂移由上面的门禁检测）

| 脚本 | 作用 |
|---|---|
| `protocol-contract.mjs` | 从 dsh 的**生成调用描述符**提取权威 endpoint 契约（其它脚本的输入） |
| `gen-compat-endpoints.mjs` | 由契约生成兼容面端点表（`dshcompat/.../Endpoints.ets`）——**手写必然漂移**，故用生成 |
| `make-brand-assets.mjs` | 生成品牌图标资源（桌面/分层/启动/README 用图） |
| `patch-native-needed.mjs` | 原生依赖的 `DT_NEEDED` 修补 |
| `collect-libvips.mjs` / `fetch-koffi.mjs` | 原生件收集/获取（闭包的输入） |
| `pack-core.mjs` / `place-host-app.mjs` | 打包核心进 HAP、**放置入口脚本**（E148 的教训：改入口脚本必须重放，否则设备上跑的是旧脚本） |

### 15.3 开发与排障（不属门禁，按需使用）

| 脚本 | 作用 |
|---|---|
| `device-acceptance.ps1` | **接回设备后的一条命令**：装机 + 抓日志/布局/截图 + 生成 `dist/acceptance/<ts>/report.md`（自动读数已填、人工项列成勾选清单）。两个环境坑已记：① 本机无 `pwsh`，用 `powershell -NoProfile -File`；② Windows PowerShell 5.1 读 **UTF-8 无 BOM** 的 .ps1 会按 ANSI 解码 ⇒ 中文乱码+解析报错（像语法错其实是编码错），必须写 BOM，且 `GetBytes()` 不含 Preamble || `dev-host.mjs` | **用本项目自己启动 dsh 核心，并让设备上的应用自动连上**（免手填地址与令牌） |
| （同上，能力补记 E261/E262） | **按文本自动定位点击**：dump 布局 → 找 `attributes.text` 匹配节点 → 点其中心；找不到就跳过并提示（**不写死坐标**——坐标随分辨率/布局变化，点错会看起来像"功能坏了"）。**自动判定 6 项**：设备在线 / 核心已启动 / 客户端已接入（凭据豁免）/ 平台标识=ohos / 文件变更流已开 / 无崩溃；报告分"自动判定"与"需人眼看"两栏，并写明后者为何必须人眼（考的是行为与呈现，日志判不出来） |
| `protocol-probe.mjs` | 对真实 Host 做协议探测，产出「端点可用性 + 错误码语义」矩阵（证据来源） |
| `check-model-roundtrip.mjs` | 真实模型往返（本机或 `--remote-*` 真机），可带工作区/凭据/设置 |
| `audit-unused-exports.mjs` | 审计 `appstate`/`dshcompat` 里**从未被 UI 引用**的导出（找"实现了但没接"） |
| `scan-core-plugins.mjs` | 扫描核心树里的插件清单 |

### 15.3b `web_fetch` 端侧修复的完整证据链（E263/E264）

E33–E36 一路追到"jitless ⇒ 无 WASM ⇒ Node 自带 undici 起不来"，E36 还留下"下一步用 `Module._load` 钩子定位谁在加载期拉进 undici"这个待办。**本次把它查完并修好了**，机制与 E36 的猜测不同：不是"加载期"，而是**某一个工具自己在请求期 `await import("undici")`**。

| 编号 | 结论 | 证据强度 |
|---|---|---|
| E263 | **"只有 `web_fetch` 坏、`web_search` 好"的确切原因：同一个 App 里两条 HTTP 路线** | 上游 `dsh-web-fetch-http` **不用全局 fetch**，而是 `await import("undici")`（`lib/index.js:154` 与 `:193`）自建 `Agent({connect:{lookup:createPinnedLookup(addresses)}})` 再把 `dispatcher` 传进 fetch；undici 的 HTTP 解析器是 **WASM 版 llhttp**，故 `--jitless` 下每次 fetch 都抛 `fetch failed / cause: WebAssembly is not defined`。而 `web_search` 走本仓 `fetch-shim.js`（`node:http/https`，原生 llhttp）⇒ 只有 fetch 那一条挂。**对照实验**（同一份真实上游代码、同一个本地 HTTP 服务）：A 臂不注册钩子 → 真 undici 探针报 `WebAssembly is not defined`、真实 `HttpFetchProvider.fetch()` 4/4 全败；B 臂注册钩子 → **8/8 全过**。 | 高（真实上游代码 + 双臂对照 + 探针直接观测 cause） |
| E264 | **修复要两层，缺一层就会静默坏掉一个工具；且安全语义必须一起验** | ① `undici-shim.mjs`：把 `undici` 这个**模块名**接到已有 http 垫片，并把上游的 `dispatcher` 翻译成垫片的 `lookup`——**保住上游的 DNS 钉住/SSRF 防护**（上游的 pinned lookup 与 node 的 lookup 契约本就同构，直接透传即可，先前怀疑的签名不兼容是我自己坏 harness 造成的假象）。② `undici-loader.mjs` + `main.js` 的 `installUndiciNameHook()`：`module.register` 解析钩子，**只在 `WebAssembly` 不可用时注册**。**不改上游源码、不改核心树**。<br>B 臂里两条**安全语义**断言是这次修复最有价值的部分：同源跳转仍被上游自己跟到最终 200（`redirect:'manual'` 语义未被削弱）；**跨源跳转仍被拒为 `WEB_REDIRECT_BLOCKED`**。若当初为了"让它能通"而放开 redirect 或忽略 lookup，这两条会立刻炸。<br>已固化为 `tools/check-web-fetch-jitless.mjs`（自带双臂对照 + 13 条判定器自检）。**真机待验收**：`register()` 的钩子跑在独立线程里，端侧运行时是否允许起线程本环境无法证明 ⇒ 失败时只降级不阻断启动。 | 高（端到端 + 门禁固化） |
| E265 | **一个把两次验证带偏的环境陷阱：ESM `import 'node:http'` 在 `--jitless` 下会让进程崩溃退出** | 我最初两个 harness 都在第一行 `import http from 'node:http'`，于是**测试自己**崩掉、真正的结果被完全遮住，我还一度据此得出"垫片加载就炸""lookup 签名不兼容"两个**错误结论**。实测（均 `--jitless --no-experimental-fetch`）：`require('node:http')`（CJS）**干净**；`import http from 'node:http'`（ESM，静态或动态）**收尾时抛 internal undici 的 `WebAssembly is not defined`，退出码 1**；`import('node:https')`/`import('node:net')` 干净；不带 `--jitless` 也干净。这是 Node 自身对 `node:http` 的 ESM facade 行为，与我们代码无关（`dsh-host-webserver` 也是 `type: module` 且 `import {createServer} from "node:http"`，而 Host 是长活进程、被 kill 而非自然退出，故实践上不受影响）。**规矩：端侧/jitless 相关 harness 一律用 CJS `require` 取 `node:http`。** | 高（四组对照，可复跑） |

### 15.3c 改动对照（diff）呈现：规则从官方渲染器逐条抄，边界如实标注（E266）

§11 的 `ToolPresenter` 里，"编辑类工具要出改动前后对照"这一条本轮落地（`appstate/src/main/ets/model/ToolDiff.ets`，45 条断言）。

| 项 | 内容 |
|---|---|
| **判定基准** | 不是自己定的：逐条对齐官方 `dsh-client-ui-tool/lib/client.js` 的 `intendedDiff()` / `narrowDiffs()` / `validEscalationFields()`。`write` → `{path, oldText: null, newText: content}`（整文件都是新增）；`edit` → `{path, oldText: old_string \|\| null, newText}`；`str_replace_editor` 的 `create`/`str_replace` 各一支 |
| **抄下来的细节（都是会判错的点）** | ① `file_path` 必须非空且 `trim()` 后非空 ② `edit` 的**空 `old_string` 归一成 `null`**（表示纯新增），不是空串 ③ `replace_all` **出现则必须是布尔**（`null` 也算"出现但类型不对"⇒ 拒绝）④ `validEscalationFields`：出现 `sandbox_permissions`/`justification` 任一时，前者必须是 `workspace-write`/`danger-full-access`、后者必须非空字符串，否则**整卡不出** ⑤ **工具名按精确匹配**（我方 `toolKindOf` 是模糊判定，但出不出对照卡用精确名——否则 `create_file` 这类名字相近、参数不同的工具会被套上错误对照） |
| **"拒绝"是刻意的** | 上面每一条不成立时返回 null = **不显示对照卡**（官方语义），而不是尽力渲染一个可能误导的对照。这与本仓"宁可少分类，也不要凭想象塞进某类"一致 |
| **失败态不出对照卡** | 对齐官方 `isError → null`：失败时参数只是"意图"，真正的结果在输出里，画成对照会让人以为"改了"（其实没改成） |
| **半条边界（已登记，不假装做全）** | 官方有**两个来源**：运行中用参数推导（intended）、结束后优先用工具结果 `meta.diffs`（applied）。本仓**只做了 intended** —— 会话投影 `applyToolSlots` 目前只解析 `TOOL_NAME`/`TOOL_ARGS`/`TOOL_OUTPUT`/`CALL_ID` 四个槽位，**没有携带 `meta`**。另一处：官方对嵌套调用（`parentCallId !== undefined`）不出对照卡，而 `TrajectoryItem` 无父子关系字段（子代理是**独立的种类**），故无从区分。两项都在矩阵 `tool` 行缺口里 |
| **手机上的取舍** | LCS 是 O(n·m)，故设 DP 上限（250×250），超过就退回"全删 + 全增"——**宁可粗但真**，也不把 UI 线程算住；未变动段按 `context` 折叠（120 行文件改 1 行 → 121 行对照压到 12 行）；行数上限之外如实置 `truncated`。**折叠阈值属呈现层取舍**：官方那侧的具体阈值我没有证据，故不声称对齐 |
| **着色** | 加行 `sys.color.confirm`、减行 `sys.color.alert`、上下文次色、折叠提示三级色——**不写死红绿**（否则深色/高对比模式不对）。为此 `NativeCard` 增加一条可选的行数组通道（`diffLines`/`diffKinds`），单串 `bodyText` 表达不了逐行颜色 |
| **编译器又抓到两个错** | ① `Column` 上挂 `.copyOption(...)` —— 该属性不存在于 `ColumnAttribute`（逐行 `Text` 各自可复制才是对的）② `ToolState` 重复导入（我加的与文件上方已有的撞了）。两条都是"看代码看不出来、编译器一句话就指出"的典型 |

### 15.3d 输入模态：把"一个布尔当两个事实"改成三级事实链（E267）

P3 要求"触控 + 鼠标 + 键盘成为同一套交互语义"。策略层（`model/InputPolicy`）早就按
`hasKeyboard` / `hasPointer` **两个维度**写好了，缺的是**事实**——而事实此前是错的。

| 项 | 内容 |
|---|---|
| **原来的真实缺陷** | `Index.ets` 里 `hasPointerInput = readDeviceFacts().keyboardLikely`，并把它**同时**当作 `hasPointer` 与 `hasKeyboard` 传下去。后果：**手机插鼠标、平板配触控笔**画不出 hover 与右键（`keyboardLikely` 为假），而 **2in1 拆掉键盘（平板态）** 仍被当成有键盘。根因是"用一个综合事实代替两个各有消费点的事实" |
| **不再靠猜** | SDK 能**直接枚举输入设备**：`getDeviceList(): Promise<number[]>` + `getDeviceInfoSync(id)`，`InputDeviceData.sources: Array<SourceType>`，而 `SourceType` 是**字符串联合** `'keyboard' \| 'mouse' \| 'touchpad' \| 'touchscreen' \| 'joystick' \| 'trackball'`；两者**都没有 `@permission`**（已核 d.ts）。故"有哪些输入源"是读得出来的，没有理由继续从设备形态推 |
| **映射里最要紧的一条** | **`touchscreen` 刻意不算指针**：触控没有"悬停"、没有右键，它的次要操作入口是**长按**（`MenuGesture.LONG_PRESS`，触控下永远启用）。把它算成指针会让触控设备绑上一个永不触发的右键手势。`joystick` 同理；**未识别的取值一律算"都不是"**（SDK 以后新增输入源时不该悄悄打开 hover/右键） |
| **三级事实链与优先级** | ① **系统枚举**（权威，**可增可减**：拔掉鼠标就该降级）② **运行时事件证据**（悬停过⇒有指针、按过物理键⇒有键盘；**只增不减**，一次证明即长期成立）③ **设备形态猜测**（2in1/桌面模式，最弱兜底）。三者性质不同故分开处理：枚举的降级是对的，事件证据的降级没有意义。这样"枚举失败"不会比旧行为更差（退回猜测），而"手机插鼠标"能被一次悬停救回来 |
| **可解释性** | `modalityTrace()` 说出每个标志**来自哪里**，并作为一条**真实诊断项**接进诊断页（`passed` 判的是"枚举是否可用"）。这条链路上"采集失败"与"真的没有设备"现象一模一样，说不出理由就只能到设备上猜 |
| **热插拔** | `inputDevice.on('change', …)` → 重新枚举（`off` 传同一个 listener，避免误伤别的模块的回调）。"手机/平板当工作台用"时插上鼠标必须立刻生效 |
| **架构边界被工具挡住** | 我第一版在 `platform/system/InputDevices.ets` 里 `import { flattenSources } from 'appstate'`——编译器直接判 `Cannot find module 'appstate'`。**`platform` 在 `appstate` 之下，不许反向依赖**（摊平与判定规则都属纯模型）。已改为平台层按设备分组原样交回、由 `Index` 调纯模型摊平 |
| **又一个"看不见的错"** | 我用脚本往 `Index.ets` 加类型导入时，锚点其实是**多行 import 块**（不是 ConversationPane 里那种单行），替换静默无效；编译器在**使用处**报 `Cannot find name 'InputModality'`，一眼定位。**教训：替换脚本必须断言锚点命中**，否则"没报错"会被误当成"改好了" |
| **仍缺（如实登记）** | `LayoutDecision.pointerRich` 现在**算得对但仍无消费点**。建议的消费点是"有指针时消息操作条改为悬停显现"——那是会**藏起可点入口**的改动，手感与触控侧影响需真机判断，故不在此拍脑袋做 |
| **真机待验收（D5）** | 四件事只能上设备：① `getDeviceList()` 在真机是否可用（d.ts 无权限要求，但需实测）② 根 `Stack` 上的 `.onHover` 能否收到悬停（是否从子组件冒泡属平台行为）③ 根上的 `.onKeyEvent` 能否收到物理按键（按键走焦点链）④ 热插拔的即时生效与拔掉后的降级。已写入 `docs/device-validation.md` D5 |

### 15.3e 详情栏可拖宽 + 宽度记忆：把模型里那条路径接通（E268）

矩阵 `layout` 行的缺口之一是"**模型做好了、界面上不存在**"：`LayoutController.decideLayoutWithDetail(input, desiredVp)`
连让步链带 fixture 都齐了，但 `decideLayout()` 直接喂常量 `Sz.DETAIL_PANEL`，
**没有任何 UI 去产生这个宽度**。本轮把它接通。

| 项 | 内容 |
|---|---|
| **决策留在模型** | 拖拽策略全是判断，故全在纯模型里并有断言：`detailRoomOf`（可用空间 = 窗口宽 − 导航宽 − 主内容最小宽）、`clampDetailDesired`（夹进 `[DETAIL_MIN, room]`）、`dragDetailWidth`（**方向规则**）、`detailWidthOf`（记忆值收窄）。视图只把 pan 偏移量喂进去 |
| **最要紧的一行是方向** | 把手在详情栏**左边缘** ⇒ 往左拖（`offsetX < 0`）是**变宽**，故是 `start − deltaX` 而不是 `start + deltaX`。写反不会报错、只会让用户觉得"拖反了"，所以它必须被断言覆盖 |
| **夹取而不是跳变** | 拖拽时按可用空间夹住。否则会出现很难看的行为：往宽拖、超过可用空间 ⇒ 让步链判"想要的大于可用"⇒ **直接缩到最小**（往右拖一点，栏猛地变窄）。夹住之后让步链只在**窗口变小**时才介入 |
| **平台层不许依赖模型** | `LocalPrefs` 只回答"偏好里有没有一个数字"（`loadDetailWidth(): Promise<number | undefined>`），**范围校验与收窄留在模型**（`detailWidthOf`）。这与 `InputDevices` 那次是同一条边界：`platform` 在 `appstate` 之下，编译器会直接拒绝反向依赖 |
| **为什么记忆全局一份** | 四形态可用空间差别很大，但让步链已经会在放不下时收窄/关闭；按形态分记会让"桌面拖宽了、换平板发现没记住"更常见。先记一份、由模型按当前窗口夹住，是最少惊讶的做法。真机手感不对再改（已写进 D6） |
| **细把手怎么点得中** | 视觉宽度 12vp（两栏之间，视觉上应只是一条线），命中区域用 `responseRegion` 放宽到 **触控 44vp / 指针 24vp** —— 用的正是上一轮刚接通的输入模态事实（`Sz.TOUCH_MIN` / `Sz.POINTER_MIN` 这对取舍终于有了消费者）。"看起来能用、实际点不到"是移动端最常见的缺陷形态 |
| **落盘失败不静默** | 与主题不同（主题失效用户立刻看得见），但"静默不生效"正是本项目反复批评的形态 ⇒ 给一句轻提示（"详情栏宽度这次没能记住"），不阻断操作 |
| **一条已实测的边界事实** | 详情栏只在 TRIPLE（≥840vp）并排，而该档导航 240vp、主内容至少 280vp ⇒ **可用空间最小 320vp > `DETAIL_MIN`(260vp)** ⇒ 让步链的 `DETAIL_CLOSED` 分支**当前不可达**。它只在"可用空间 < 最小宽度"时触发，而那个条件在这套阈值下永远不会成立。**处置：保留（防御性）并钉成断言** —— 将来真有人调整阈值时，是**有意识地**让这条分支复活，而不是事后发现"历史遗留代码" |
| **实现里我自己又发现一处毛病** | 第一版 `beginDetailDrag()` 直接拿 `detailWidthDesired` 当拖拽起点。但窗口缩小后"意图"（900）会大于可用空间（480），而屏幕上显示的是被夹过的 480 ⇒ 往窄拖要**先把那 420 的差值拖掉**才动，表现为"把手推不动"。改为**起点先归一化**（`clampDetailDesired` 后再记起点），并把它钉成断言（未归一化 480→480 卡住、归一化后 480→430 跟随） |
| **写测试时我自己错了两次** | ① 以为 `detailRoomOf(700,240) < 0`（实际 180，`MAIN_MIN_VP` 是 280）② 以为 `ConcessionStep` 的值是 `'narrow'`（实际 `'detail-narrow'`）。两次都是**先打印真实值再写期望**才纠正的——断言的第一职责是描述真实行为，写错期望的门禁会反过来教坏实现 |
| **真机待验收（D6）** | ① 细把手在触控上是否点得中、会不会误触发 ② 与列表纵滑/长按菜单的手势冲突 ③ 宽度记忆跨重启、以及换到窄窗口时是否被让步链正确收窄 |

### 15.3f 双栏的详情改成侧边浅层面板：把一个死布尔换成被消费的枚举（E269）

计划 §12 要求四形态各有各的详情呈现，而此前**双栏（平板竖屏 / 手机横屏 / 2in1 拖窄）与手机一样
弹半模态 Sheet** —— 600–839vp 放不下"列表 + 真右栏"，但足够宽到让详情与列表**同时可见**，
用 Sheet 会把列表整个盖住，"边看列表边看详情"这件事就没了。

| 项 | 内容 |
|---|---|
| **根因是一个死字段** | `LayoutDecision.detailOverlay: boolean` 语义只覆盖"单栏下详情不是并排的栏"，**而且没有任何消费点**（视图用自己的 `sheetKind()` 判断）。一个布尔也表达不了三种呈现 ⇒ 换成三值枚举 `DetailPresentation`：三栏 `column` / 双栏 `side-panel` / 单栏 `overlay`，并**真的被视图消费**（`sheetKind()` 只在 `overlay` 档出 Sheet；`buildDouble` 在 `side-panel` 档渲染面板）。**死布尔已删除** |
| **为什么不写遮罩** | 本仓规矩是"遮罩由原生 Sheet 提供、不手写颜色"（手写 rgba 会绕过设计令牌与深色模式）。故侧边面板**没有蒙层**，用一层**透明**点击层承担"点面板外关闭"，把"用遮罩表达层级"留给真正需要模态的场合。这个取舍的代价（内容在面板打开时不响应）已写进真机项 D7 |
| **修掉两处相邻的真实缺陷** | ① **快捷键 `toggle-detail` 是死开关**：它只翻转 `detailOpen`（三栏右栏），而单栏/双栏渲染看的是另一个标志 ⇒ 手机与平板上按了没有任何反应。改为 `toggleDetail()` 按呈现方式翻转 ② **面板里的 × 会点了没反应**：`detailColumn()` 的 × 只清 `detailOpen`，而侧边面板由 `detailOverlayOpen` 控制。收口成 `closeDetail()` 一次清两个（任一时刻只有一个有效，故安全）——这正是本项目反复出现的"看起来能用、实际不生效"缺陷形态 |
| **名字不能骗人** | `detailSheetOpen` → `detailOverlayOpen`，`BackAction.CLOSE_DETAIL_SHEET` → `CLOSE_DETAIL_OVERLAY`（单栏是 Sheet、双栏是面板，旧名会说谎），同步改了 `OverlayState` 字段与门禁 fixture。**这是编译器逼出来的**：改名只改了一半时，`OverlayState` 的赋值直接报 `Type ... is not assignable` |
| **宽度沿用同一套规则** | 侧边面板宽度 = `clampDetailDesired(记住的宽度, 可用空间)` —— 复用上一轮的夹取逻辑，用户拖过的宽度在双栏下也被尊重，且不会吃掉大半屏 |
| **仍缺（如实登记）** | ① 侧边面板的**滑入动画**：`transition` 必须挂在面板自身的根上，而它与三栏右栏共用同一个 `detailColumn(widthVp)` builder ⇒ 值得专门做（要么拆 builder、要么把 transition 做成可选），不该顺手加 ② 侧边面板自身的拖拽调宽 ③ 把手的键盘调整 |
| **真机待验收（D7）** | ① 无蒙层时层级是否看得清（会不会被误认成"布局变成了三栏"）② 点面板外关闭是否顺手、透明层会不会让人觉得"内容点不动了" ③ 800vp 平板竖屏上列表露出是否足够 ④ 档位切换时详情是否连续（双栏↔三栏） |
| **验证** | fixture 是**整份决策深比较**：7 个形态 fixture 的期望值同步改成 `detailPresentation`，另加 5 条呈现方式断言（含"双栏不并排但仍要并可见"这条区分）。总断言 237 条 / 0 失败 |

### 15.3g 轨迹时间总览：把"投影出来却没人显示"的数字接上界面（E270）

矩阵 `trajectory` 行的缺口是"无交互式时间总览"。做这件事时先发现一个更基础的问题：
**`sessionStats`（turns/steps/llmMs/toolMs/ttftMs/ttftSteps/decodeMs/decodeTokens）早就投影出来、
也早就在快照里暴露，却没有任何界面显示它**——这些数字此前在界面上完全缺席。

| 项 | 内容 |
|---|---|
| **判定基准** | 官方 `dsh-client-ui-trajectory/lib/client.js`：7 个种类（`system/user/context/compacted/message/tool/subtool`）与其中文标签、`timeline.total/started/noTimingData` 文案、以及"**水平拖动可聚焦事件**"（`timeline.overviewAria` 原文）。全部逐字取用，不自创 |
| **一个容易做错的细节** | 比例段用**累计起点**摆放（`startRatio`），而不是让视图把宽度一个个加起来——后者在比例四舍五入后会出现缝隙或重叠 |
| **"哪一类不产生格子"也是判定** | 交付物/目标/任务/错误**不进时间线**：官方把错误记在格子自身的 `isError` 上、而不是一种 kind；交付物/目标/任务在官方是独立面板。`SUBAGENT` 一律算 `TOOL`——官方的 `tool`/`subtool` 之分需要父子调用关系，而我方 `TrajectoryItem` 没有该字段（与前面登记的边界一致）。**不假装能分** |
| **服务端没给的东西就不要建模** | 官方的逐步「首 token / 解码」来自 `assistantMetrics`，而 `timingRecorded` **只出现在官方自己的 bundle 里**（全树 grep 确认），我方投影没有 ⇒ 我最初按官方校验写好的 `assistantTimingOf`/`timelineTimingLabel` **没有消费者**，按"不预置空 API"删掉，只在文件头保留官方语义说明并把"接逐步 metrics"登记为缺口 |
| **同一个数字不要显示两遍** | `statsLinesOf` 最初包含「{turns} 轮 {steps} 步」，但会话头部那行（E125/E129）已经显示它 ⇒ 去掉该行，只保留**时间相关的四项**（模型用时/工具用时/TTFT/TPS），并在注释里写明理由 |
| **真机最可能返工的点** | 格子多且耗时分布悬殊时（一次 8 秒的助手回合 + 十几个几十毫秒的工具格），最窄的格可能只有一两像素。已写入 D8，并附"是否需要给最窄格最小宽度"这个待决问题 |
| **仍缺** | 条↔列表的双向联动（当前拖动聚焦只显示详情，不滚列表、不高亮）；`SYSTEM`/`CONTEXT`/`COMPACTED`/`SUBTOOL` 四种种类保留但**暂不可达**（我方投影没有对应事件类型） |
| **验证** | fixture 47 条断言（种类标签逐条、四类"不产生格子"、累计比例与和、拖动聚焦与越界夹取、统计四项与"为 0 即不显示"）；总断言 284 条 / 0 失败。另用编译产物跑了一次真实数据，确认比例与文案符合预期 |

### 15.3h 时间总览的双向联动（E271）

上一轮把时间总览落地时登记了缺口"条 ↔ 列表无联动"，并把它写进真机项 D8 第 4 条（怕出现"点了条却没反应"的观感）。本轮补上。

| 项 | 内容 |
|---|---|
| **条 → 列表** | 拖动松手或点选后滚到那一格对应的条目。**用 `ScrollAlign.START` 对齐**不是审美选择而是防抖手段：让该条落在列表顶部，"列表首行"与"聚焦格"就成了同一个东西，反过来那一路上来读到的 id 与当前聚焦一致 ⇒ 不会自己改自己 |
| **列表 → 条** | 滚动时让条上的聚焦跟着**首行**走（相当于"你在时间线的哪个位置"）。拖动条期间**关掉**这一路（`timelineDragging`），否则两边会互相覆盖——拖到哪都被列表当前行抢走 |
| **"找不到就不滚"是判断** | `listIndexForCell` 有三种"找不到"：还没聚焦（空 id）、该条目被过滤出列表（搜索/视图切换后两边来源可能不同）、列表为空。三者**都不该滚**：滚到越界行号会抛错，而"滚到第 0 行"比不滚更糟（用户没点任何东西，列表自己跳回顶部）。故返回 -1 且视图据此短路 |
| **高亮用哪个通道** | 列表项的左侧色条本来表示"搜索命中"（brand）。聚焦也用同一通道、但**换成 `font_emphasize`**，靠颜色而不是靠加粗/加宽区分两种含义；两者同时成立时以聚焦为准（那是用户刚刚做的动作） |
| **为什么松手才滚** | 拖动过程中每帧滚动会让列表剧烈抖动，且用户此刻看的是条上的详情行。松手再跳，符合"先扫、再过去看"的动作 |
| **为什么是立即跳转而不是平滑滚动** | 平滑滚动会在动画途中**反复**触发 `onScrollIndex`，于是"列表 → 条"那一路会把聚焦扫过中间的每一格——聚焦指示器在动画期间短暂地"说谎"，看起来像条在自己动。改成立即跳转直接消掉这条**我无法在本机验证**的观感风险（D8 里留了"若跳得太硬，再补程序化滚动期间挂起再换回平滑"的退路） |
| **真机要盯的仍是抖动** | 两路联动靠"拖条期间关一路 + 滚到行首使两侧一致"避免来回跳，这属**推断**，已写进 D8 第 4 条要求实机确认（并给出"若观感不好就只保留条→列表"的退路） |
| **仍缺** | 真正意义的 inspector：点格子**打开该条的详情/参数**（当前只滚过去 + 显示一行摘要：种类 · 第 n/共 m 格 · 开始于 · 耗时） |
| **验证** | fixture 新增 10 条断言（含三种"找不到就不滚"与越界行号不抛异常）；总断言 294 条 / 0 失败 |

### 15.3i Prompt 泄漏的两条路径都关掉（E272，P0-1）

工作令要求"Conversation 只允许 USER / ASSISTANT / ERROR；SYSTEM / developer / context / audit / unknown internal event
不得进入 Conversation；**不得靠 UI 文本判断**"。排查发现泄漏不是一条路径而是**两条**，第二条最容易被漏掉。

| 项 | 内容 |
|---|---|
| **路径一：直接显示** | `groupTurns()` 此前把**任何** `MESSAGE + 助手` 无条件塞进 `answers`，而"内部事件"戴着助手的角色：`command/run`（斜杠命令）、`compaction/summary`（内部 context）都被当成"助手的回答"；**未识别类型**更糟——`classifyEventType` 对未知类型有意降级为 MESSAGE，投影把**原始载荷 JSON** 当正文，于是对话里出现一条"助手说：{…}" |
| **根因是判据散且看错了对象** | 两处各自猜：`groupTurns` 无条件入桶，`chatVisibleItems` 另写一条"speaker 不是 SYSTEM 就显示"。两处判据不同，且**都看内容特征**（角色/文本），而"是不是用户可见的会话内容"是**来源事实** |
| **修法：结构字段 + 单一判据** | ① `dshcompat.isConversationVisibleEvent(name)`：**白名单**（两条真实会话消息 + 过程族 + 错误族）。用白名单而不是黑名单，因为事件类型由各插件 declaration merging 扩展、取值域只能实测得到——黑名单意味着"每出现一种新的内部事件就漏一次"，白名单的失败方向是保守的（不认识的进轨迹、不进对话）② 投影时算出 `TrajectoryItem.internal` ③ `conversationAudienceOf(item)` 成为**唯一判据**（HIDDEN/USER/ASSISTANT/PROCESS/NOTICE），`groupTurns` 与 `chatVisibleItems` 共用它 |
| **路径二：被搜到并计数** | `matchTrajectoryIndices()` 搜**全部条目**，而 `system/message` 的正文就是**完整的 agent 提示词**。于是"搜一个常见词"会在对话视图给出命中数与高亮，而那一行**根本不在对话里显示**——**泄漏不只是"显示出来"，"能被搜到、能被计数"同样算泄漏**。修法：新增 `matchConversationIndices()`（对话视图只搜可见条目，轨迹视图仍搜全部——内部事件在轨迹里本就该可查） |
| **顺带修掉一个滚错行的缺陷** | `jumpTo()` 把**条目下标**直接喂给 `scrollToIndex`，而对话视图的列表行是**回合**（两套下标根本不是一回事）⇒"搜到了却跳错地方"，且不报错。新增 `chatRowOfItemIndex()` 做映射，找不到所属回合时返回 -1（不滚，而不是滚到第 0 行） |
| **一条既有断言把 bug 固化了** | 既有 fixture 的注释写着"错误要留"，期望值却把错误条漏掉——因为旧判据按 `speaker !== SYSTEM` 把它静默丢掉。**断言写错会反过来教坏实现**：这条期望已按规格修正（ERROR 进 notices ⇒ 可见） |
| **fixture** | 新增 36 条（事件白名单 14 / 条目角色 9 / 分组与可见集合 11 / 不靠文本 2）+ 18 条（搜索作用域与行映射）。其中"不靠文本"是**反向断言**：正文里写着 `SYSTEM:` 的正常回答仍可见、长得像回答的内部事件仍被隐藏 |
| **验证** | fixture 388 条 0 失败；8 门禁 + web-fetch 门禁全绿；entry ArkTS 编译 0 error |

### 15.3j 页面框架的纯逻辑层：面板注册表 + 导航状态（E273，P0 第一步）

工作令要求 P0 做 `AppFrame + NavigationState + PanelRegistry`，并明确"**不是增加任何新功能**"。
按本仓纪律先做纯逻辑（可本机测），视图层随后。

| 项 | 内容 |
|---|---|
| **要修的结构问题** | "谁拥有哪个界面"没有被表达：页面 = `NavTab`（三者同级）+ `StackPage`，而右栏 = `DetailPane` 里一串 `if`。于是每加一个面板就要改 `Index.ets`（5185 行）这个大装配器 |
| **`PanelRegistry`** | 位置是**面板的属性**，不是页面的 if 分支：`PanelDescriptor{id, location(SIDEBAR/MAIN/RIGHTBAR), owner, label, icon, order, available}`。排序按 `order` + id 稳定（**不按注册顺序**：注册顺序取决于插件装配次序，不同设备会不一样）；**不可用的面板不进选择集**（比"选得中但打开是空的"诚实）；`select()` 失败时**保持原值**，不做静默回退（回退会让"我点了 A 却显示 B"成为常态且不报错） |
| **`NavigationState`** | 关键语义：**页面 ≠ 选中的面板**。`conversation / files / trajectory / settings.general / settings.models` 都是**面板**（各带 location），不再塞进一个 `NavTab`。转移函数（`navigateToMain`/`selectRightPanel`/`openSettings`/`enterSession`/`openOverlay`）全部**经注册表校验** |
| **迁移桥** | `mainPanelOfLegacyTab` / `legacyTabOfMainPanel`：本步目标是**改结构不改行为**，故不一次性重写几十处调用点，而是让旧 `NavTab` 取值落到新结构上；桥有**往返断言**（workspaces/core/settings 三个旧页签必须原样回来）——这是"迁移期间不会点错页面"的保证 |
| **`enterSession` 收在一处** | "切会话"要同时处理当前会话 id、主区回到会话面板、关掉手机抽屉、必要时给右栏一个可用面板。分散在各调用点写必然漏一个（历史上就有"切了会话但主区还停在工作区"） |
| **`ui/ShellTracks.ets`（几何层，不在导航模型里）** | 它回答"这条轨道怎么摆"，与 `LayoutController.detailPresentation` 同族，**不是**导航问题。分开的另一个好处：门禁把纯逻辑文件摊平到一个目录，跨目录相对路径解析不了——**这条约束反过来帮我们把分层边界划清了**（`NavigationState` 因此不必依赖 `LayoutMode`） |
| **最要紧的一条语义** | `shellTracksOf` **只**回答"轨道怎么摆"，**绝不**回答"这个形态要显示哪些栏目"：后者若随设备变，四形态就成了四套 UI，而官方是同一套 AppFrame + 面板选择跨设备 |
| **明确未做** | `view/shell/` 的四个 shell **尚未开始**。瓶颈是**状态归属**：主区六个面板现在直接读 `Index` 的约 100 个 `@State`。把 5000 行状态搬进 shell、或改用 `@Provide/@Consume`（本仓**无先例**），是一个要单独决策 + 先小范围验证的步骤（`@BuilderParam` 曾导致真机 JS 崩溃，D4）⇒ **不一次性重写 Index** |
| **验证** | fixture 46 条；总 440 条 0 失败；8 门禁全绿；entry ArkTS 编译 0 error |

### 15.3k 侧栏外壳抽出：AppFrame 的第一条轨道（E274）

P0 的目标是 `Index.ets → AppShell → SidebarShell/MainShell/RightbarShell`。三条轨道里**侧栏最小最自足**，
所以先抽它：用一个显式 props 面就能表达完（当前页签、待决徽标、宽度、两个回调），**不需要新机制**
（`@Provide/@Consume` 本仓无先例、`@BuilderParam` 曾致真机崩溃 D4）——它是 shell 模式的第一个真实证明。

| 项 | 内容 |
|---|---|
| **`SidebarShell` 拥有什么** | 品牌行 / 面板清单 / 待决徽标（只挂工作区那一项）/ 二级入口（本地调试，E96）/ 三种呈现（PANEL / RAIL / 底部标签）。`Index` 只保留**轨道的 surface**（宽度、底色）——"轨道怎么摆"与"内容是什么"就此分开 |
| **呈现与栏目分开** | 呈现由 `ui/ShellTracks.shellTracksOf(mode)` 给（几何层），栏目清单**不随形态变**。这是四形态共享一套信息架构的前提 |
| **顺带把 `navTabLabelRes` 移进 appstate** | 它与 `navTabSymbol` 同类（都是"页签 → 资源"的映射），原先散在 `Index` 里，导致 shell 无法只依赖 appstate 就把导航项画全 |
| **设计令牌棘轮抓到 6 处裸值** | 这些值原本在 `Index` 里属于**基线**（有祖父条款），搬进新文件就没了豁免。处置：新增 `HarmonyIconSize.NAV=20` / `NAV_RAIL=22`（导航图标比行内图标大一号，原先写死）、角标圆角与内边距改用 `Radius.S`/`Sp.S`、底部标签高度改用 `Sz.NAV_RAIL`、徽标前景色改用 `HarmonyColor.ON_BRAND_TEXT`（"状态色之上的前景"，原先写死 `Color.White`） |
| **代价：我自己制造了两次事故** | 用脚本按"锚点 → 锚点"切 span 改 `Index.ets`，第一次锚点没带起点命中到远处的同名分隔行，**删掉约 2000 行**；第二次 `bottomTabs` 的结束锚点落在 1988 行之外。两次都是编译器立刻报出来（"属性不存在"成片出现就是结构性损坏的信号），但**浪费了大量 token**。最终可用的做法：**每个锚点都带起点**（`index(tail, start)`）**+ 段长守卫**（`assert 20 <= 段长 <= 45`），一旦越界就拒绝执行而不是照删 |
| **验收状态** | `Index.ets` **仍直接负责页面级 Pane 选择** ⇒ 本步只完成 P0 的第一条轨道。下一步 `AppShell`/`MainShell`/`RightbarShell` 的瓶颈是**状态归属**（主区六个面板直接读 `Index` 的约 100 个 `@State`），要先小范围验证机制再全量迁移 |
| **验证** | 8 门禁 + web-fetch 门禁全绿；fixture 440 条 0 失败；entry ArkTS 编译 0 error；`Index.ets` 5185 → 5115 行 |

### 15.3l 右栏外壳抽出：第三条轨道（E275）

接着侧栏，把**右栏的面板本体**也抽成 `entry/src/main/ets/view/shell/RightbarShell.ets`。

| 项 | 内容 |
|---|---|
| **它拥有什么** | 标题行 + 关闭入口 + 内容（`DetailPane` 的 sections）+ 自身宽度与底色；三种呈现都在它内部：`COLUMN`（三栏真右栏）、`SIDE_PANEL`（双栏侧边浅层面板）、`OVERLAY`（单栏半模态 Sheet） |
| **为什么 Sheet 呈现里没有关闭按钮** | 原生 Sheet 自带关闭（拖拽条 / 点击外部）⇒ 再画一个是重复入口。这与"遮罩由原生 Sheet 提供、不手写"是同一条纪律 |
| **呈现由宿主给、不在这里判定** | 它属几何层（`LayoutController.detailPresentation`），与侧栏的处理一致：**信息架构不随设备变，只有几何与呈现变** |
| **踩到的坑（又一次）** | 我往 `Index` 的 appstate 导入里加了 `DetailPresentation`，而它**早已在该列表里** ⇒ 编译器报 `Duplicate identifier`。这类"导入重复"是机械编辑的常见副作用，编译器一眼定位 |
| **本步刻意不做（P3）** | `PanelRegistry` 的六个右栏候选面板（文件 / 轨迹 / 工具 / 子代理 / 交付物 / 预览）**还没有各自的视图**，右栏内容仍是"宿主投影好的一份 sections"。把 `selectedRightPanel` 真正接到不同面板上属 P3——届时这里按 descriptor 分派，而不是继续吃一份 sections。**现在做就成了假接线** |
| **仍缺（P0 验收未达成）** | `AppShell` / `MainShell` 未做，`Index.ets` 仍直接负责页面级 Pane 选择。瓶颈是**状态归属**（主区六个面板直接读 Index 的约 100 个 `@State`），需先小范围验证机制（`@Provide/@Consume` 无先例、`@BuilderParam` 曾致真机崩溃）再全量迁移 |
| **验证** | 8 门禁全绿；fixture 440 条 0 失败；entry ArkTS 编译 0 error；`Index.ets` 5185 → 5093 行 |

### 15.3m `@Provide/@Consume` 机制验证：侧栏改由面板注册表驱动（E276）

`AppShell`/`MainShell` 的瓶颈是**状态归属**：主区六个面板直接读 `Index` 的约 100 个 `@State`。
要把它们交给 shell，就得先确认"祖先提供、后代消费"在本仓 ArkTS 里可用
（`@Provide/@Consume` 本仓**无先例**；`@BuilderParam` 曾编译通过但真机 JS 崩溃，D4）。
本轮在最窄的一条轨道（侧栏）上把它用起来——**不是为验证而验证，而是让侧栏真的按注册表工作**。

| 项 | 内容 |
|---|---|
| **真实用途** | `Index` `@Provide('panelRegistry')`；`SidebarShell` `@Consume('panelRegistry')`，用 `visibleTabs()` 过滤：**不可用的面板不占位置**（"哪些入口可用"是面板的属性，不是页面的 if 分支） |
| **桥** | 侧栏当前仍按 `NavTab` 取值渲染（各面板自己的视图还没做，属 P1），故新增 `sidebarPanelIdOfTab()` 把两者接上：**注册表管"能不能用"，`NavTab` 暂时管"长什么样"**。3 条断言 |
| **编译通过 ≠ 真机可用** | 这是本仓的教训（D4）。故**新增 `docs/device-validation.md` D9**，并写明：**在 D9 通过之前，不要把主区的 100 个 `@State` 迁到 `@Provide/@Consume`**。判据包含"把某个面板的 `available()` 改成 false ⇒ 入口应消失"（若 `@Consume` 没连上，这里会抛错或静默全显） |
| **顺带删掉一个空 API** | `PanelDescriptor.icon` 是我上一轮加的字符串字段（图标名）。它**没有任何消费者**——图标是编译期资源（`$r` 只能字面量），模型不能持有 `Resource`，视图也无法把字符串变 `$r`。留着就是"预置空 API"，故删除，并在类型注释里写清原因与 P1 的替代做法（补一张"面板 id → 符号"的编译期表） |
| **验证** | 8 门禁 + web-fetch 门禁全绿；fixture 443 条 0 失败；entry ArkTS 编译 0 error |

### 15.3n 页面选择收成唯一真值；并查明 AppShell 不能走"注入 Builder"（E277）

**先查了一条旧账**：`@BuilderParam` 到底为什么在真机崩？答案是 **E118** ——
`@BuilderParam coreSlot`（把父组件的 `@Builder` 注入子组件**再调用**）真机点按即 `JsError` 杀进程，
当时的修复是**显式传 6 个属性**。这条对 P0 的影响是决定性的：
**"`AppShell` 接收三条轨道的内容（builder 注入）"这条路在本仓真机不可用**——编译能过，真机不认。
所以 AppShell/MainShell 只能走「显式 props」或「`@Provide/@Consume`」，
而后者按 D9 的闸门要等真机确认。

**然后做了一件便宜但语义核心的事**：把"当前是哪个页面"从 `@State tab: NavTab` 换成
`nav.selectedMainPanel`。

| 项 | 内容 |
|---|---|
| **为什么这是语义核心** | 此前页面（`NavTab`）与右栏选择、设置分区、抽屉、浮层各是一份状态——**页面与面板是两套机制**，`Index` 因此必须自己写 if 链装配 Pane。现在唯一真值是**选中的面板** |
| **迁移期的别名** | `NavTab` 没有消失，而是退化为别名：读经 `legacyTabOfMainPanel()`、写经 `mainPanelOfLegacyTab()`，**两个方向都有往返断言**（迁移期间不会点错页面） |
| **顺带得到一条保护** | 切页签改走 `navigateToMain()` ⇒ **经注册表校验**：不可用的面板切不过去（要么切过去，要么不动），而不是把界面切到一个空面板 |
| **改动面有多小** | `this.tab` 只有 **9 处读、1 处写**（先数后改，不猜）。把字段换成 `@State nav: NavigationState` + 一个 `currentTab()` 派生方法即可；唯一写点换成经桥的 `navigateToMain` |
| **编译器抓到的一处** | `legacyTabOfMainPanel` 返回 `string`，赋给 `NavTab` 报类型错 ⇒ 加显式收窄。**桥的类型故意用 `string`**（它同时服务尚未迁移的调用点），收窄点只有一处，是有意的 |
| **仍缺** | `AppShell`/`MainShell` 未做 ⇒ `Index` 仍把六个 Pane 逐个装在一起（只是"选哪个"已经状态化）。下一步按上面两条路之一搬主区 |
| **验证** | 8 门禁 + web-fetch 门禁全绿；fixture 443 条 0 失败；entry ArkTS 编译 0 error |

### 15.3o 主区"此刻显示哪个面板"收进纯模型（E278）

上一轮把**唯一真值**换成了 `nav.selectedMainPanel`，但主区实际显示什么仍由**视图自己组合**：
`stackPage`（下钻页）+ `showsConversation()` + 当前页签，三条线索在 `mainContent()` 的 if 链里拼。
这既是"栏目/框架看起来不对"的滋生处，也**不可测**（门禁跑不了 ArkUI）。

| 项 | 内容 |
|---|---|
| **收成什么** | `activeMainPanelOf(nav, stackPage, hasSession)` —— 三类线索进同一个函数，返回**一个面板 id**；视图只按它分派（`if (this.activePanel() === MAIN_DIAGNOSTICS)` …）。7 条断言 |
| **定下的优先级** | 下钻页（诊断 / 连接）> 会话页（**且必须已有会话**）> 选中的面板。其中"下钻到会话但没有会话 ⇒ 落回选中的面板"是一条**真实规则**（不该显示一个空会话），此前隐含在 `showsConversation()` 里，现在被显式写出来并测到 |
| **为什么这算 P0 的实质进展** | 它把"页面选择"从**视图的组合逻辑**变成**模型的判定**。`MainShell` 之所以贵，就是因为视图里到处是这种组合；把它逐条收干净，搬的时候才只是"搬代码"而不是"边搬边猜行为" |
| **仍缺** | `AppShell`/`MainShell` 未做；`Index` 仍把六个 Pane 逐个装在一起。下一步搬主区仍受 E118（不能用 builder 注入）与 D9（`@Provide` 运行时待真机）两个约束，只能走显式 props |
| **验证** | 8 门禁全绿；fixture 450 条 0 失败；entry ArkTS 编译 0 error |

### 15.3p 主区页头抽出为组件（E279）

AppFrame 三条轨道里，中间轨道的**页头**同样是"轨道自己的 chrome"，不该由装配器直接写。
抽出 `entry/src/main/ets/view/shell/MainHeaderShell.ets`。

| 项 | 内容 |
|---|---|
| **原来它知道什么** | `Index.header()` 直接读 `stackPage` / `mode` / `headerTitle()`，并直接写 `stackPage` 与 `detailOverlayOpen`——即**页头知道宿主的内部字段** |
| **现在它只认什么** | 三个 props（`title` / `showBack` / `showDetailEntry`）+ 两个回调。**导航事实与几何事实留在宿主**：组件不该知道"有没有下钻页"（导航层）或"是不是三栏"（几何层），只回答"两个入口现在要不要出现" |
| **语义** | 左=返回（只在有下钻页时出现——非下钻时不该有一个点了没反应的返回箭头）；中=标题（单行省略）；右=详情入口（只在会话页且非三栏时出现——三栏下详情是真右栏）。**这与 `activeMainPanelOf` 的分派是同一条纪律：组合逻辑归判定层** |
| **又踩了同一个坑** | 我用"从 `@Builder header()` 到下一个 `@Builder`"切段，段长 59 行而 `header()` 本体只有 43 行——多切的 16 行正好是紧随其后的 `headerTitle()` 方法，编译器立刻报 `headerTitle does not exist`。**教训：段长守卫给的是范围（30–60），范围太松就拦不住"多切一个方法"**；跨方法切段应当用**方法签名的精确锚点**，或者干脆先数方法个数 |
| **顺带一个 token** | 原实现把页头高度写死 52；抽成组件后没有祖父条款 ⇒ 新增 `Sz.HEADER`（值不变，视觉不变） |
| **验证** | 8 门禁 + web-fetch 门禁全绿；fixture 450 条 0 失败；entry ArkTS 编译 0 error |

### 15.3q 轨间把手抽出：AppFrame 的 chrome 全部各有其主（E280）

最后一块装配器里的轨道 chrome 是**轨间拖拽把手**（`detailResizer`）。它既不属于主区也不属于右栏——
它属于"两条轨道之间"，所以归一个新的小组件 `entry/src/main/ets/view/shell/TrackResizer.ets`。

| 项 | 内容 |
|---|---|
| **它带走的两条约束** | ① 视觉只有 12vp、**命中区域按输入模态放宽**（触控 44 / 指针 24，`responseRegion` 只扩大命中不改布局）② **拖动策略不在组件里**：起点归一化、可用空间夹取、"左拖变宽"的方向规则都在 `LayoutController.dragDetailWidth` 的纯函数里（有断言），组件只把 pan 偏移量交出去 |
| **宿主只给什么** | 命中区域宽度（取决于输入模态事实 + `Sz` 的取舍，是宿主的判定）+ 三个拖动回调 |
| **一条编译器才告诉你的规矩** | 回调**不能叫 `onDragStart` / `onDragEnd`**：它们是 ArkUI `CustomComponent` 基类的保留成员名，同名声明会被判"与基类不兼容"（`not assignable`）。统一改用 `onGrip*`（把手 grip 的语言），并在组件里写明原因 |
| **结果** | AppFrame 的 chrome 四位各有其主：`SidebarShell` / `MainHeaderShell` / `RightbarShell` / `TrackResizer`。装配器只剩"把谁放在哪" |
| **仍缺（P0 验收的核心，也是唯一剩下的大块）** | 主区**内容**（六个 Pane）的搬迁。受两个硬约束：**E118**（不能用 builder 注入，真机必崩）+ **D9**（`@Provide` 运行时待真机）⇒ 只能走**显式 props**；`mainContent()` 345 行、依赖宿主约 100 个 `@State`，需按面板分批搬且每批跑全门禁 |
| **验证** | 8 门禁 + web-fetch 门禁全绿；fixture 450 条 0 失败；entry ArkTS 编译 0 error；`Index.ets` 5106 行 |

### 15.3r 目标栏抽出；并勘明主区搬迁的门面方案（E281）

**先做了一件小但干净的事**：把命令面板里的**长期目标栏**抽成 `entry/src/main/ets/view/GoalBar.ets`。
它**不需要任何 props**——它直接读中枢快照（`SessionHub.shared().snapshot()`），
与"可添加提供方"同一先例：打开面板时重新渲染、读到最新值，少穿两层状态，也不会出现
"状态与中枢不一致"。抽出来之后这条性质不变，宿主只决定"放在哪"。

它保留了一条要紧的语义：三种情况分得很清——有目标 / 中枢说没有（`goalNote`）/ **未读取**。
**不把"没读到"说成"没有目标"**（那会让用户以为目标丢了）。

**再勘明了主区搬迁的可行方案**（下一轮可直接机械执行，不必重新发现）：
`mainContent()` 345 行、依赖宿主约 100 个 `@State`，逐 `this.x` → `props.x` 重写既慢又易错，
故采用**门面（facade）**：`MainShellFacade` 的成员名与宿主字段/方法**同名**，
`Index` 每次渲染重建它（值型是快照、方法型是 `() => this.xxx()` 闭包），
内容整段搬进 `MainShell` 后只做一次机械替换（块内 `this.` → `this.f.`）。

为什么可行：**不碰 E118 那条禁令**（没有 builder 注入），**也不用等 D9**（不依赖 `@Provide`），
只是把"父组件读自己的状态"换成"子组件读传来的门面"。风险与纪律：一次只搬一个面板分支，
每搬一块跑全门禁；**分派骨架留到最后搬**，否则中间态会出现两处分派。

⚠️ 一条必须遵守的细节：门面**必须每次渲染重建**——缓存它就等于放弃"中枢变了界面跟着变"。

| 项 | 内容 |
|---|---|
| **验证** | 8 门禁 + web-fetch 门禁全绿；fixture 450 条 0 失败；entry ArkTS 编译 0 error；`Index.ets` 5082 行 |
| **仍缺** | P0 验收（`Index` 不再负责页面级 Pane 选择）未达成：主区六个 Pane 仍在 `Index` 里装配 |

### 15.3s 会话模型选择抽出为组件；命令面板拆解进行中（E282）

命令面板（`paletteOpen` 那一块）此前是一个 120 行的内联块：长期目标栏 + 本会话模型 + 命令列表。
按"一个面板一个所有者"的做法逐块拆：上一轮拆出 `GoalBar`（**零 props**：自读中枢快照），
本轮拆出 `SessionModelPicker`（官方 `dsh-client-ui-model-selection`）。

| 项 | 内容 |
|---|---|
| **为什么它不是零 props** | 它需要会话 id、可选模型列表、当前选中模型——这些是**宿主投影好的数据**，不是中枢里现成可读的一整块（与目标栏不同）。故给 3 个 props + 1 个回调 |
| **动作刻意不进去** | "选中模型"会牵扯**面板开关**与**宿主级提示**（`settingWriteNote`）——那是导航与宿主状态。组件只把"选哪一个"交出去（`onPick`）。与 `MainHeaderShell` 交出 `showBack`/`showDetailEntry` 是同一条纪律：**呈现归组件，判定与副作用归宿主** |
| **保留的一条如实说明** | 未选会话时**不显示模型列表**而是说明原因：`session/selectModel` 需要会话作用域，列一张点了没反应的模型表比不列更糟 |
| **进度** | 命令面板拆掉两块（目标栏 / 模型选择），**命令列表仍是内联的**——它是下一块（官方 `ui-commands`）。`Index.ets` 5082 → 5061 行 |
| **仍缺** | P0 验收（`Index` 不再负责页面级 Pane 选择）未达成：主区六个 Pane 仍在 `Index` 里装配。**门面方案已勘明**（见 E281），但那是一次 345 行 + 约 100 个门面成员的搬迁，需要一整轮预算——**不做半途开工**（会留下破损的中间态） |
| **验证** | 8 门禁 + web-fetch 门禁全绿；fixture 450 条 0 失败；entry ArkTS 编译 0 error |

### 15.3t 命令列表抽出：命令面板三块拆完（E283）

命令面板（120 行内联块）至此拆成三块，各有其主：`GoalBar`（零 props，自读中枢快照）、
`SessionModelPicker`（3 props + 1 回调）、`CommandList`（2 props + 1 回调，官方 `ui-commands`）。
`Index.ets` 5082 → **5040** 行。

| 项 | 内容 |
|---|---|
| **统一的纪律** | **呈现归组件，判定与副作用归宿主**：三个组件都只把"选了哪个/要执行哪个"交出去；关面板、调中枢、写宿主级提示留在宿主。这与 `MainHeaderShell` 交出 `showBack`/`showDetailEntry` 是同一条 |
| **两处"如实说明"被保留** | ① 模型：未选会话时不列模型而是说明原因（`session/selectModel` 需要会话作用域）② 命令：未选会话 vs 已选会话但宿主未报告**分成两句**——**不把"还没读到"说成"这个会话没有命令"**，否则会让人去排查一个不存在的权限问题 |
| **一个观察** | 这三块都是"扁平列表 + 一行说明"，没有状态机、没有嵌套判定的组合逻辑——所以拆起来便宜。**主区那六个 Pane 不是这样**：它们读宿主约 100 个 `@State` 并互相牵连，因此必须走门面方案（E281），不能照这个节奏硬拆 |
| **仍缺** | P0 验收（`Index` 不再负责页面级 Pane 选择）未达成。剩下的就是主区内容搬迁，需要一整轮预算 |
| **验证** | 8 门禁 + web-fetch 门禁全绿；fixture 450 条 0 失败；entry ArkTS 编译 0 error |

### 15.3u 连接横幅抽出为 HubBanner；并勘定主区搬迁的真实规模（E284）

**先查了一件决定性的事**：`mainContent()` 体内是否调用了 `@Builder` 方法——因为 `@Builder`
**不能**通过门面对象调用（只能在组件里 `this.Xxx()`），若体内有很多 builder 调用，门面方案就得改。
结论：**只有一个**（`tabContent`），其余被调方（`activePanel` / `diagnostics` / `sendDraft` …）
都是普通方法，可以走门面。⇒ **门面方案成立**。

**但规模也勘清了**：`tabContent` 182 行 + 它牵连的 `workspaceHub` 158 行 + `workspaceGroup` 119 行
≈ 460 行，加上其余分支约 230 行 ⇒ **一次约 690 行 + 40 余个门面成员**。这是**一整轮的活**，
不做半途开工（会留下破损的中间态）。

**本轮完整做完的一块**：连接横幅 → `entry/src/main/ets/view/HubBanner.ets`（56 行 → 6 个 props）。
保留两条**不是装饰**的语义：

1. **"重连"只在 `phase === DEGRADED` 时出现**：`idle` 是"还没配置过 Host"，此时该做的是"添加 Host"
   而不是重连——否则按钮点了必然失败。D3 §4 把"手动重连"列为断连横幅的**必需**动作：
   自动重试有终点，没有手动入口时，用户在"Host 刚重启、网络刚恢复"这类可恢复场景下只能重启应用。
2. **保留协议旁路日志尾部**：把"连上了但列表为空"这类无法从界面区分的症状变成可读的原始轨迹。

| 项 | 内容 |
|---|---|
| **两次自我纠错（编译器/门禁逼出来的）** | ① 我用"下一个 `@Builder`"当结尾锚点，但 `hubBanner` 是那一段里最后一个 builder（下一个在 786 行外）⇒ 段长守卫拦住，改**精确结尾锚点**（`.backgroundColor(...)` + `}`）② 设计令牌棘轮抓到图标尺寸 16：我先随手编了个 `HarmonyIconSize.WARNING`（**并不存在**），改为新增真 token `HarmonyIconSize.STATUS = 16` |
| **分工** | "重连"直接调中枢（它本就是中枢自己的动作，无宿主状态牵连）；"诊断"走宿主回调（那是一次导航）。与其它面板同一条纪律 |
| **验证** | 8 门禁 + web-fetch 门禁全绿；fixture 450 条 0 失败；entry ArkTS 编译 0 error；`Index.ets` 5040 → **4999** 行（首次跌破 5000） |
| **仍缺** | P0 验收未达成：主区内容搬迁（约 690 行 + 门面）是唯一剩下的大块，规模已勘清 |

### 15.3v 主区搬迁量化；`openSettings` 接线；两次脚本事故（E285）

**量化（脚本实测，不再是估计）**：主区搬迁 = `mainContent` 276 + `tabContent` 182 + `workspaceHub` 158
+ `workspaceGroup` 119 = **735 行**，门面需要 **108 个成员**（其中 4 个的声明不是单行）。
⇒ **不靠一次性脚本**：改为"**搬一束、生成一束**"——先搬 `tabContent`+`workspaceHub`+`workspaceGroup`
（459 行，互相调用、自成一束），再搬 `mainContent` 的四个分支，最后搬分派骨架。每步跑全门禁，
每步的编译修复面都可控。

**顺手做了一件真实收益的事**：`NavigationState.openSettings()` 此前**只有 fixture 消费者**
（我自己建 API、自己测，但应用不用——正是本项目反复批评的"预置空 API"）。
现接到两处"进设置"的入口：行为等价（都是切到设置面板），但**多了注册表校验**。

| 项 | 内容 |
|---|---|
| **事故一（脚本切段）** | 我想删两个无消费者的导出（`setSidebarExpanded`/`defaultSidebarPanel`），用 `rfind('/**')` 找"函数上方的文档注释"——它匹配到了**更早**的段落注释，于是连同 `openSettings`/`openOverlay`/`closeOverlay`/`setDrawer` 一起删了。fixture 立刻报 `openSettings is not a function`，`git checkout` 恢复 |
| **事故二（同源）** | 上一轮用"下一个 `@Builder`"当结尾锚点是同一个病根：**向回/向前找锚点时没有限定在"当前成员"的范围内**。规矩：删函数时，起点用**函数签名行**、终点用**该函数自身的收尾行**（或用括号配平计数），不要用"上一个/下一个注释" |
| **结论** | 修剪"看起来没人用"的 API 风险高于收益（它只影响整洁度，却会误删有消费者的代码）⇒ **本轮放弃修剪**，改为如实登记：`NavigationState` 里仍有若干转移只有 fixture 消费者，等各自的使用场景落地时再接 |
| **仍缺** | P0 验收：主区 735 行搬迁未开始（量化与执行顺序已明确） |
| **验证** | 8 门禁 + web-fetch 门禁全绿；fixture 450 条 0 失败；entry ArkTS 编译 0 error |

### 15.3w 主区搬迁第一束的实操结论：门面必须带 setter（E286）

本轮**真去搬了**第一束（`tabContent` + `workspaceHub` + `workspaceGroup`，459 行），
一路修到只剩 4 类错误，然后发现了一个比编译错误严重得多的问题，遂**回退**（保住绿灯）。

| 项 | 内容 |
|---|---|
| **致命发现** | 被搬走的代码**会写宿主状态**：`this.treeEmptyTitle = '…'`、`this.pendingWorkspacePick = true`、`this.treeEmptyHint = '…'`。门面成员若是普通属性，这些赋值只写进**门面副本**⇒ 宿主收不到。**这不是编译错误**（接口属性可写，类型也对），而是**静默行为错误**：界面看着正常、状态没变，而且不会有任何报错 |
| **正确分型** | 只读值 → `x: T`（快照）；只读方法 → 闭包；**被写的值 → `setX(v: T): void`**，并把搬走代码里的 `this.f.x = v` 改写成 `this.f.setX(v)`；被调用的别的 builder → 若**已是组件**（`hubBanner` → `HubBanner`）就直接渲染组件，不走门面 |
| **本轮踩到并已解决的 7 个小坑（都记下来了）** | ① 段尾要用**括号配平**，不能用"第一个 `  }`"（上一轮我自己定的规矩，本轮又违反了一次，报错 100+ 条）② 提取要**连同 `@Builder` 装饰器一起搬**（否则组件里全是"UI 语法非法"）③ 值导入与**类型导入**要分别取差集（混在一起会 `Duplicate identifier`）④ 方法签名正则要允许 `async` ⑤ 搬走的代码渲染的组件（`WorkspacePane`/`SettingsPane`/`PendingPane`）与纯函数（`formatClock`/`relativeTime`/`sessionEmptyCopy`、`aboutInfo`）要一并导入 ⑥ 委托方法必须**保留原参数**（`tabContent(compact)` 被我写成无参，`mainContent` 的调用立刻报错）⑦ 组件必须有 `build()`，并由它调用内部 builder |
| **为什么回退而不是硬修** | setter 那一层要**先做写点分析**（列出所有 `this.X = …`）再改生成脚本；硬修等于边改边发现，容易把"静默行为错误"带进代码——那正是本项目最忌讳的一类缺陷 |
| **验证** | 回退后 8 门禁全绿、fixture 450 条 0 失败、entry ArkTS 编译 0 error |

### 15.3x 主区搬迁第一束：两次实操，收敛到一处 ArkUI 约束（E287）

**第一次**（上一轮）：发现"被搬走的代码会写宿主状态"⇒ 门面必须带 setter，否则是静默行为错误。
**第二次**（本轮）：按修正后的顺序真搬，编译错误从 100+ 收敛到 **4 个**，且同型：

| 错误 | 位置 |
|---|---|
| `'HubBanner({…})' does not meet UI component syntax` | 在 `@Builder workspaceHub` 内 |
| `'PendingPane({…})' does not meet UI component syntax` | 同上 |
| `'WorkspacePane({…})' does not meet UI component syntax` | 在 `@Builder tabContent` 内 |
| `'SettingsPane({…})' does not meet UI component syntax` | 同上 |

⇒ **"在被搬进组件的 `@Builder` 里实例化别的 `@Component`"这条写法未通过 ArkUI 检查。**

**已排除**：导入路径（`'../X'` → `'./X'`，错误不变）、缺 `build()`（已加）、`@Prop` 门面类型（编译器未就此报错）、
setter 缺失（已按写点分析生成 29 处 setter 改写）。

**本轮为通过这一关而修掉并验证的东西**（都对，保留在生成脚本里）：
① 段尾用**括号配平**（上一轮违反过一次，10+ 处语法错）② 连同 `@Builder` 装饰器一起搬
③ 值导入 / 类型导入**分别取差集**，且类型名要**收集 Index 的所有 `import type` 块**、并把**门面成员文本**也算作名字来源
④ 方法签名正则允许 `async` ⑤ 赋值改写要按**括号深度**扫表达式终点（多行三元 / 对象字面量），并用负向前查排除 `===`
⑥ 委托方法保留原参数 ⑦ 组件必须有 `build()` ⑧ 子组件与纯函数（`formatClock`/`relativeTime`/`sessionEmptyCopy`/`projectGeneralGroups`）一并导入

**下一轮的候选排查方向**（按可能性排序）：① **带参数的 `@Builder`** 内部是否允许实例化自定义组件
（试着挪到无参 `@Builder` 或直接放 `build()`）② 组件调用是否必须独占一行/以 `})` 结束语句
③ 其它。

**附带发现（环境层面）**：`/tmp` 下的脚本会被**别的进程**干扰（我的脚本 stdout 里混进过一段
"（dry-run）将替换 34 处…"，而 `grep -c dry-run 脚本` 为 0）⇒ 生成脚本已挪到 `dist/scratch/`，
输出回到干净。

**状态**：两次实操后仍**回退**（8 门禁全绿、fixture 450 条 0 失败、ArkTS 0 error）。
但可变因素已收敛到"一处 ArkUI 约束"，不是"整条路线不成立"。

### 15.3y 主区搬迁第一束**成功**：459 行搬出，Index 5040 → 4665 行（E288）

第三次实操通过。**"does not meet UI component syntax" 那 4 个错误是假象**：真因是**导入路径写错**
（`'../HubBanner'` 应为 `'./HubBanner'`），ArkTS 先用那句 UI 语法错误盖住了模块解析失败。
把它当"UI 语法约束"排查会完全走偏——而我是靠**把 4 处子组件换成 `Text` 桩**才让真错（`Cannot find module`）露出来的。

| 项 | 内容 |
|---|---|
| **成果** | `@Builder tabContent` + `workspaceHub` + `workspaceGroup` → `entry/src/main/ets/view/TabContentView.ets`（595 行，86 个门面成员，29 处赋值改写成 setter）。`Index.ets` **5040 → 4665 行** |
| **门面（facade）** | 成员名与宿主同名；宿主 `buildTabFacade()` **每次渲染重建**：只读值是快照、被写值成对给出 `setX`、方法是闭包。**被写的 13 个成员必须有 setter**——否则赋值只写进门面副本，是**静默行为错误**（第一轮实测出来的） |
| **顺带收益** | 裸值从 **53 处降到 41 处**（Index 的祖父条款值随代码搬走，新文件按 token 写） |
| **两次"编造 API"的自纠（同一个毛病犯了两遍）** | ① `HarmonyIconSize.WARNING`（不存在）→ 改为新增真 token `STATUS = 16` ② `Fs.EMPTY_STATE_ICON`（不存在）→ 改为门禁允许的 `// token-exempt: 空态大图标，仅此一处`。**教训：不确定的 token 名先 grep 再用** |
| **三个"看起来是 A、其实是 B"的教训（本轮最值钱的）** | ① **"does not meet UI component syntax" 可能是模块解析失败**——用替换成 `Text` 桩的办法让真错露出来 ② ArkTS 的 `arkts-no-untyped-obj-literals` 要求门面接口用**属性式函数类型**（`x: (a) => void`），**方法签名**式（`x(a): void`）会拒绝对象字面量 ③ 对象字面量需要**显式类型标注**（`const facade: TabContentFacade = {…}`），`return {…}` 这种位置不算 |
| **生成脚本** | `dist/scratch/mig.py`：括号配平切段、装饰器同搬、导入差集（含 platform 专名）、词法感知的赋值深度扫描、setter 生成、`build()`、子组件与纯函数导入、显式类型标注、属性式函数类型 |
| **仍缺** | P0 验收未达成：`mainContent`（276 行，含 4 个分支）与分派骨架仍在 `Index`。下一束按同一脚本搬 `mainContent` |
| **验证** | 8 门禁 + web-fetch 门禁全绿；fixture 450 条 0 失败；entry ArkTS 编译 0 error（`--clean` 强制重编） |

### 15.3z 主区第二束（`mainContent`）：只差"模块级名字"这一类接缝（E289）

用同一条已验证的生成器路子改造成 `mig2.py` 搬 `mainContent`（276 行、4 个分支）：

| 项 | 结果 |
|---|---|
| **已产出** | `view/shell/MainShell.ets` 397 行、**77 个门面成员**、**33 处 setter 改写**；`Index` 临时到 **4391 行**（起点 5040） |
| **未通过编译的原因（唯一）** | **`Index` 的模块级辅助函数/常量进不了门面**（门面只收类方法）。被搬走的代码以**裸标识符**引用它们：`storageBaseUrl`(2 次) / `composeHostUrl`(3) / `extractToken`(2) / `prettyJson`(3) / `reportConnectedHost`(3) / `TAG`(41)。appstate 里没有这些名字 |
| **为什么改名没覆盖** | 现有改写只把 `this.X` 加前缀；常量与模块函数是**裸引用** ⇒ 需要第二道改写（`\bTAG\b` → `this.f.TAG` 一类） |
| **过程中的其它接缝（本轮逐个修掉）** | ① 删除方法 span 时忘了插入委托（`Index` 直接丢了 `mainContent`）② 组件的 `build()` 调错方法名 ③ 6 个**组件导入**缺失（`GoalBar`/`SessionModelPicker`/`CommandList`/`ConnectPane`/`ConversationPane`/`DiagnosticsPane`——生成器**已经报告**了这 6 个"未归类的大写名"，是我没看）④ `tabFacade` 要用 `buildTabFacade()` 而不是 `this.tabFacade` ⑤ 两个纯函数（`extractToken`/`openSettings`）的导入 |
| **教训** | 生成器输出的 **"未归类的大写名（需人工确认）" 就是待办清单**——本轮我忽略了它一次，代价是一轮编译迭代 |
| **状态** | 已**回退**：8 门禁全绿、fixture 450 条 0 失败、entry ArkTS 0 error。下一轮按计划文档的修法 A（约 20 行改动）继续 |

### 15.4a 修法 A 已实施：生成器支持模块级名字；`mainContent` 剩三处机械接缝（E290）

上一轮定位的"模块级名字进不了门面"已修：生成器现在扫描 `Index` 的 `^function` / `^const`，
把被搬走代码引用的模块级名字收进门面（函数 → 方法 + 包装、常量 → 值），并对**裸标识符**加一道改写。

| 项 | 内容 |
|---|---|
| **本轮验证到的** | `Index` 临时到 **4520 行**、`MainShell.ets` 398 行 / 77 门面成员；编译错误从"缺模块级名字"收敛到**三处机械接缝** |
| **剩下三处（已可枚举）** | ① **逐个**模块级函数要在接口 + 包装里各出现一次（`storageBaseUrl`/`composeHostUrl`/`reportConnectedHost`/`prettyJson`）——我手工补了 `storageBaseUrl`，编译器立刻报 `reportConnectedHost`，**说明要一次性全补，不能一个一个来** ② **重复方法**：生成器的"插入委托"与手工插入各留一份 ⇒ 收尾先 `grep -c` 确认 ③ `connection` 包专名：`extractToken`/`parseJson` 来自 `'connection'` |
| **又一条"待办清单"教训** | 生成器输出的**"未归类的大写名（需人工确认）"就是待办清单**：本轮的六个组件导入（`GoalBar`/`CommandList`/`ConnectPane`/`ConversationPane`/`DiagnosticsPane`/`SessionModelPicker`）正是照它补的；上一次我忽略它，代价是一轮迭代 |
| **状态** | 已回退：8 门禁全绿、fixture 450 条 0 失败、ArkTS 0 error。**P0 验收仍未达成**，但剩下的是**机械收尾**而非机制未知 |

### 15.4b P0 验收达成：主区第二束搬出，Index 5040 → 4518 行（E291）

`mainContent`（276 行、四个分支）搬进 `entry/src/main/ets/view/shell/MainShell.ets`（409 行、77 个门面成员、
33 处 setter 改写）。**`Index.mainContent(compact)` 现在只是一句委托**：

```
@Builder
mainContent(compact: boolean) {
  MainShell({ f: this.buildMainFacade(), compact: compact })
}
```

⇒ **`Index.ets` 不再负责页面级 Pane 选择**（P0 的验收标准）——诊断 / 连接 / 会话 / 标签页内容
四个分支的选择与渲染都在 `MainShell` 里；`Index` 只提供门面（每次渲染重建）。

| 项 | 内容 |
|---|---|
| **收尾靠的是"后处理脚本"** | 生成器 `mig2.py` + 收尾器 `dist/scratch/fixup.py`：前者做搬运，后者做确定性修补（组件/包导入、模块级函数与**常量**的门面条目、去重复、委托与 `@Builder`、`tabFacade` 走构建器） |
| **最后一类编译错误很典型** | `tabContent: (compact: boolean) => this.tabContent(compact)` ⇒ **用箭头调用 `@Builder` 无法推断返回类型**（`arkts-no-implicit-return-types`）。而 MainShell 已直接渲染 `TabContentView`，这个成员本就多余 ⇒ **删掉**比补类型更对 |
| **又一个"启发式过宽"的坑** | 收尾器里"跳过声明行"的判断写成 `^\s*NAME\s*[:=(]`，把**调用行**（以 `(` 开头）也跳过了 ⇒ `reportConnectedHost(...)` 没被改写。改成只认 `:` / `=`（声明才长这样） |
| **"待办清单"第三次应验** | 生成器的"未归类的大写名"清单连着三轮都是真实待办：组件导入、模块级常量、`connection` 包专名 |
| **验证** | 8 门禁 + web-fetch 门禁全绿；fixture 450 条 0 失败；entry ArkTS 编译 0 error（`--clean`）；`Index` 4518 行 |

### 15.4c P0 完成：AppFrame 三形态轨道搬出（`AppShell`），顺带修掉"浮层够不着"（E292–E296）

`buildSingle` / `buildDouble` / `buildTriple` + `header` / `headerTitle` / `navPanel` / `navRail` / `bottomTabs` /
`detailColumn` / `detailResizer` / `sidebarPresentation`（225 行）搬进
`entry/src/main/ets/view/shell/AppShell.ets`（316 行、25 个门面成员、3 处 setter 改写）。
**`Index.ets` 4518 → 4290 行**；P0（页面框架）到此收口。

| # | 项 | 内容 |
|---|---|---|
| **E292** | **浮层门户原先挂在单栏根节点上 ⇒ 双栏/三栏够不着（真实缺陷，非遗漏）** | `bindSheet` 原来只出现在 `buildSingle` 的根 `Column` 上。而 `sheetKind()` 覆盖的六类浮层里，**四类的触发点都在主区内容里**（模型选择 `choosing` ← `SessionModelPicker` 回调、凭据 `credentialRef` ← `openCredentialSheet`、目录 `folderOpen`、文本/整值设置 ← `SettingsPane` 的三个回调），主区在三种形态下都可见 ⇒ **双栏/三栏下点了没反应**。修法：门户（`bindSheet` + `sheetContent()` 分派 + `closeSheet()` 复位）提到**页面根** `Index.build()`，与形态无关。这不是"顺手搬家"：官方 Web 的 portal 同样挂在 `App` 根、不属于任何 pane |
| **E293** | **脚本化搬迁的头号坑：替换点在被删区间之前 ⇒ 后面所有行号整体偏移** | `build()` 在 `buildSingle` **之前**，而生成器先替换 `build()`（更长）再按**旧行号**删 MOVE 跨度 ⇒ 删错位置，留下 12 段碎片、大括号净失衡 **+7**（编译器报 2285 error，首条是 `'return' outside of function` —— 一个位置完全不对的提示）。**规则**：一次搬迁里的所有改动必须按**原始行号降序**施加；改完先做"逐字符（跳字符串/注释）括号平衡"体检再编译 |
| **E294** | **`f"this.f.X =" not in code` 这种断言会误命中 `===`** | 残留检查写成 `this.f.stackPage =`（真源码里是 `this.f.stackPage === StackPage.MAIN`）⇒ 断言假红。**规则**：赋值判定一律用 `=(?!=)`（`rewrite_assignments` 里本就是对的，断言漏了） |
| **E295** | **`import \{(.*?)\} from 'appstate'` 会跨语句匹配** | 非贪婪也救不了：`import type { VersionCheck } from 'dshcompat';` 会被吞进"下一个 `} from 'appstate';`"之间，于是 `platform` 的 `copyText` 被算成 `appstate` 的类型导入，生成的组件里出现空的 `import type { } from 'appstate';` 与重复导入。**规则**：按语句解析（`^import( type)? \{([^}]*)\} from '([^']+)';`，`[^}]*` 保证不跨 `}`） |
| **E296** | **"未使用导入"检测器把 `a as b` 整串当成一个名字** | 于是 `fileIo as fs` / `selectTab as decideTabSelection` 被判为"未使用"而删掉，编译门禁逐条抓出（`Cannot find name 'fs'` / `'decideTabSelection'`）。**规则**：绑定名是 `as` 右边的那个；这类"清理"必须紧跟一次编译，且范围限定在本次动过的文件 |

**验证**：8 门禁 + web-fetch 门禁全绿；fixture 450 条 0 失败；entry ArkTS 编译 0 error（`--clean`）；
`AppShell.ets` 里**没有一处**裸 `this.X` 宿主状态（全部经门面）、没有赋值（全部走 setter）。
**环境限制（如实登记）**：`tools/check-origin-fence.mjs` 在本机跑不起来——它要启动真实 Host，
而本地 `dist/core/work/dsh-core-*` 是**软链到占位核心**（85 字节的 profile-boot），Host 起不来 ⇒
`host-ready.json missing`。与本轮改动无关（该门禁不读 `entry/` 源码），需在有真实核心的环境跑。

### 15.4d P1-1：工作区→会话树抽成 `WorkspaceBrowser`；删除死 Pane（E297–E299）

`TabContentView` 里的 `workspaceHub` + `workspaceGroup`（285 行）搬进
`entry/src/main/ets/view/WorkspaceBrowser.ets`（30 个门面成员）。`TabContentView` 595 → **341 行**
（`WorkspaceBrowser.ets` 本体 434 行：搬迁 285 行 + 窄版行与说明）。

| # | 项 | 内容 |
|---|---|---|
| **E297** | **门面做到位之后，搬迁成本会一路下降（本轮最重要的方法结论）** | 前三束（`TabContentView` / `MainShell` / `AppShell`）每次都要：批量改名 `this.X → this.f.X`、给被写的状态造 setter、把 `@Builder` 调用换成组件渲染。本束**一行都不用改**——它本来就只经 `this.f.<成员>` 访问宿主（`TabContentFacade` 是显式门面），新门面就是那 30 个成员的**子集**：声明从 `TabContentFacade` 逐行抄，包装行从 `Index.buildTabFacade()` 的同类行**筛出**（同源同形状，连 setter 都不用新造）。⇒ "先把门面做对"的收益在第四束上一次性兑现 |
| **E298** | **`SessionListPane`（221 行）是死代码，而文档一直说它在用** | 全仓检索只有 `Index` 里一条 `import`（外加 `entry/build` 缓存与设计令牌基线）——**没有任何渲染点**。它是 E108「把会话页签并进工作区视图」之前的产物：会话列表改由 `workspaceGroup` 渲染之后，这个 Pane 就再没人挂。矩阵 `sidebar` 行却写着"我方是 `WorkspacePane` + `SessionListPane` 两个并列 Pane" ⇒ **一条过期描述在位好几轮**。本轮删除文件 + 同步更正 `docs/parity-matrix.md` / `docs/ia-parity-plan.md` / `docs/30` / `docs/60` 四处引用。**纪律**：搬完一束就检索"谁还在渲染它"，比"下次再说"便宜得多 |
| **E299** | **副产物：设计令牌棘轮可以调紧了** | `--update-baseline`：**53 → 40 处裸值 / 11 → 10 个文件**；其中 `Index.ets` 从 **14 → 2** 处——那是上一轮 AppShell 搬迁把根容器 chrome 搬走的结果（裸值随代码一起离开 Index）。棘轮调紧是安全的：门禁本来就要求"以后不许变多" |

**没有顺手挂进侧栏的理由（写下来免得下轮重复讨论）**：侧栏 PANEL 轨道只有 `Sz.NAV_PANEL` 宽，
而现有工作区行把 `+ 会话` / `文件` / `删除` 三个文字入口摆在名字右侧 ⇒ 塞进去会把名字挤没。
**先做窄版行（动作进 `⋯` 菜单）、再挂载**；顺序反过来就是"为了看起来搬了而牺牲可用性"。

**验证**：8 门禁 + web-fetch 门禁全绿；entry ArkTS 编译 0 error；`WorkspaceBrowser` 里没有一处裸 `this.X` 宿主状态。

### 15.4e P1-2/P1-3：窄版行 + 树挂进侧栏 + 主区去重（E300–E302）

| # | 项 | 内容 |
|---|---|---|
| **E300** | **"只删首行"又栽了一次：多行门面条目必须整条删** | 裁掉 `WorkspaceBrowserFacade` 里 5 个（随逻辑上移到宿主而变死的）成员时，对 `setConfirmingDeletePath` / `setSettingWriteNote` 只删了它们的首行（`    setX: (value: T) => {`），留下函数体 ⇒ 编译器报 `arkts-no-standalone-this`（"在独立函数里用 this"）——**错误信息指向函数体，而病根在被删的首行**。规则：门面条目按 `^    NAME: ` 切分、整条（含多行块）删除；同类坑本仓已出现三次（E289 的"跳过声明行过宽"、E293 的行号偏移、本次）|
| **E301** | **把动作收进 `⋯` 菜单 ⇒ 在"无法真机验收"的条件下是拿功能可达性做赌注** | 第一版窄版把「新建会话 / 浏览文件 / 删除登记」全放进 `bindMenu`，**编译通过**（`MenuElement[]` 形态）。随后删掉：菜单打不开时这三件事在窄版里**没有任何别的入口**，而本仓当前做不了真机验证（D3）。改为"两行摆在明处"：第 1 行名字，第 2 行三个动作各给足触控高度。⇒ **可验证性不足时，选冗余可见而不是优雅隐藏**；等真机能测再收（届时是"删两行加一个菜单"，不是重构）|
| **E302** | **同一份事实只算一次：主区去重的判据取自 `ShellTracks`** | 三栏下树在侧栏、主区只留一句说明（`browserInSidebar`）。判据不是"mode === TRIPLE"而是 `shellTracksOf(mode).sidebar === PANEL` —— 与 `AppShell` 选 `navPanel`/`navRail` 用的是**同一个函数**，所以"侧栏到底能不能装树"两边不可能不一致。若哪天形态阈值调整，两处会一起变 |

**顺带的职责收敛**：`archiveSession` / `toggleDeleteConfirm` 从视图提到宿主（宽版行与窄版行共用同一份副作用与文案），
`WorkspaceBrowserFacade` 因此从 32 个成员裁到 **27 个、全部有用**。
**验证**：8 门禁 + fetch 门禁全绿；entry ArkTS 编译 0 error；三栏下主区不再渲染第二棵树（同一份门面，两处挂载）。

### 15.4f P1-4：一级导航改由面板注册表驱动（E303–E305）

| # | 项 | 内容 |
|---|---|---|
| **E303** | **把产品语义写进模型，而不是靠"视图恰好没遍历它"** | `sidebarPanels()` 里早就登记了 `sidebar.core` 席位（`available: () => true`），而 E110 已定的语义是"核心不是独立一级，内容在设置页的第一个分区里"。此前这条语义**只靠视图迭代 `navTabs()`（=[workspaces, settings]）恰好没碰到核心**而成立——谁改成遍历 `descriptors()`，核心入口就会冒出来。P1-4 把它改成 `available: () => false` 并写明理由 ⇒ 语义进了模型，且 fixture 直接测（`核心席位仍登记在清单里` / `主入口清单不含沉底项`）。**顺带修掉一个潜在缺陷**：`navTabLabelRes` 的映射**没有 `tab_core`**、会回落成"设置"（`tab_core` 资源已补：base 中文 + en_US）——虽然当前不可达，但映射完整才谈得上"按 id 取文案" |
| **E304** | **视图不再依赖 `NavTab`：入口的"存在/顺序"与"长什么样"分开归属** | `SidebarShell` 里现在一分三处：**存在与顺序**归 `sidebarEntries` / `sidebarPinnedEntries`（沉底写成清单属性 `SIDEBAR_PINNED_ORDER`，不是渲染时的 `if (id === 'settings')`）；**图标与文案**归 `sidebarPanelSymbol` / `sidebarPanelLabelRes`（按面板 id 的编译期映射——`$r` 只能字面量，模型持不了 `Resource`，所以这层永远在 `appstate/ui`）；**高亮**归宿主（`selectedPanelId` ← `sidebarPanelOfMainPanel(选中的主区面板)`；会话/诊断/连接没有席位 ⇒ 空串 ⇒ **不高亮任何一项**，而不是随便高亮）。同时删掉 `Index` 里一份**没人调用的**局部 `navTabLabelRes`（第二处同品类死码，见 E298）|
| **E305** | **`appstate` 的桶是显式再导出白名单：改了导出必须同步改桶** | 本轮删了 `Breakpoints` 的三个 `NavTab` 版映射、加了四个新导出，**漏了桶**⇒ 编译器逐条报 `Module './ui/Breakpoints' has no exported member 'navTabLabel'` / `'appstate' has no exported member 'sidebarEntries'`。另有一处脚本失误：从多行导出列表里删项时**把相邻行的分隔逗号也带走了**（`LayoutMode` 后面少了 `,`），报 `',' expected`。规则：删列表项后检查前后两行的分隔符；桶与源同步改（编译器会兜住，但要一次改全）|

**验证**：8 门禁 + fetch 门禁全绿；fixture **450 → 461 条**（新增：沉底清单 3 条 + 席位↔面板双向桥 7 条 + 清单一致性口径 1 条），0 失败；
entry ArkTS 编译 0 error；`SidebarShell` 中 `NavTab` 出现 0 次。

### 15.4g P1-5：单栏手机抽屉（E306–E308）

| # | 项 | 内容 |
|---|---|---|
| **E306** | **又一次"通道有、没接"：`mobileDrawer` 状态躺了几轮没人消费** | 模型里早就有 `DrawerState` / `mobileDrawer` / `setDrawer`，`enterSession` 甚至写了"进会话要收起抽屉"——但**全仓没有一处把它打开**，视图也没读它。本轮接上：页头根页给导航入口 → 抽屉叠在整页之上（`Stack` + 透明点击层，与双栏侧边面板同一款处理：**遮罩由原生 Sheet 提供、不手写颜色**）→ 返回键收抽屉 → 选入口/会话自动收起。**取舍**：底部标签栏**暂时保留**（抽屉是新增导航面，不夺走任何既有入口）——延续 E301 的口径：**真机能测之前，选"冗余可见"而不是"优雅替换"**；真机确认后再决定是否撤掉标签栏 |
| **E307** | **返回键阶梯新增"第 0 级"：抽屉最先收** | `decideBack` 原先 7 级，抽屉不在其中 ⇒ 手机上抽屉打开时按返回会**穿透到下面那层**（退栈/退页），而用户的本意是"收起盖着的东西"。现加 `BackAction.CLOSE_DRAWER` 作为第 0 级。**【字段放哪】** `drawerOpen` 加在 `NavState` 而不是 `OverlayState`：后者那一组的共性是"有一个待提交的编辑"（凭据/草稿/搜索/选择器/详情浮层），抽屉只是导航面的另一种呈现 |
| **E308** | **死函数的复现：`openSession` 手写字段同步，漏了抽屉与右栏回落** | 模型里的 `enterSession`（"进会话要同步的所有字段"）**全仓零调用**，视图在 `openSession` 里手写了一份（会话 id、标题、清轨迹、栈页、三栏开详情）——于是抽屉那条没人管（手机上从抽屉点会话 ⇒ 抽屉还挡着会话）。现改走 `enterSession`，并保留视图侧的两条**几何**事实（单栏进会话栈、三栏开右栏）。**观察**：模型里"零调用的完整函数"往往就是视图手写了一份它的职责——这是继 `mobileDrawer` 之后同一轮里第二例 |

**顺带**：`AppShell` 抽出 `sidePanelSurface(presentation, width)`——侧栏这条轨道的 surface 现在有**两个挂载点**（桌面固定轨道、手机抽屉），
内容与回调完全一样，写两遍必然走样。

**验证**：8 门禁 + fetch 门禁全绿；fixture **461 → 466 条**（返回键第 0 级 3 条 + 选面板收抽屉 2 条），0 失败；
entry ArkTS 编译 0 error。

### 15.4h P2-1：会话正文的 Markdown 渲染（E309–E311）

| # | 项 | 内容 |
|---|---|---|
| **E309** | **解析放模型、样式放视图：解析规则能被断言，样式调整碰不到规则** | 新增 `appstate/model/Markdown.ets`（纯字符串处理，**不许** import appstate 之外的东西、不许出现 `$r`）→ `tools/check-layout-fixtures.mjs` 本机直接跑：**31 条新断言**（标题只认"# 后带空格"、围栏含语言、**未闭合围栏**、列表 marker、引用合并、分隔线、段落合并、行内五标记、未配对标记原样、转义、CRLF、空串）。视图侧 `entry/view/MarkdownRenderer.ets` 只管字号/行高/底色/间距 |
| **E309b** | **流式未闭合的围栏必须当代码块，而不是当正文** | 模型一边生成一边被渲染，围栏经常"开了一半"。若未闭合时不认围栏，半截 ` ``` ` 会把它后面的所有内容吞进一个代码块（更糟）；若整段当正文，用户看到的是满屏标记。做法：未闭合时**剩余全部算代码块**并如实标 `closed: false`，视图据此在末尾加流式光标（**不改样式**，避免"闭合那一刻画面跳一下"）。这条是本模型最要紧的语义，已钉成断言 |
| **E310** | **第三次踩"凭记忆写令牌名"：`HarmonyColor.BG_SECONDARY` 不存在** | 语义色是 `SemanticColor`（`TEXT_*` / `BG_SECONDARY` / `DIVIDER` / `BRAND`…），`HarmonyColor` 是另一层（`ON_BRAND_TEXT` / `SURFACE_RAISED`…）。编译器当场报 `Property 'BG_SECONDARY' does not exist`——**规则照旧：用之前先 grep 一遍令牌名的定义处**（这是我第三次栽在同一个习惯上：前两次是 `HarmonyIconSize.WARNING`、`Fs.EMPTY_STATE_ICON`）|
| **E311** | **链接要么真能点开，要么别做成链接的样子** | 正文里的 `[文字](链接)` 若只染成品牌色却不能点，就是本仓最忌讳的**假入口**；而"点了复制链接"又是语义降级。故加了真能力：`platform/system/OpenLink.ets`（`openLink` + `appLinkingOnly: false`，只放行 http/https，失败回 false 不抛）。另外无障碍文案改为**去标记**文本（`plainTextOf`）——否则屏幕阅读器会把 `**`、反引号、链接目标一起念出来 |

**刻意不做**：HTML（安全问题）、表格、嵌套列表（ArkUI 的 `Text > Span` 只有一层，表达不了）——缺的部分原文照显，**不假装支持**。
**验证**：8 门禁 + fetch 门禁全绿；fixture **466 → 497 条**（+31），0 失败；entry ArkTS 编译 0 error。

### 15.4i P2-2：会话头的后台任务条（E312–E313）

| # | 项 | 内容 |
|---|---|---|
| **E312** | **第三例「通道有、没接」：模型 40 条断言 + 中枢字段，视图零消费者** | `appstate/model/Jobs.ets`（live 判定 / 状态点语义 / 五种文案 / 时长三档与小时封顶 / 排序 / 计数 / `jobTickerNeeded` / `jobListA11y`，40 条断言）与 `SessionHub.jobs`（`JOBS` 控制帧 + `BASELINE` 帧都在维护）**都到位**，但全仓没有任何视图读它——界面只在轨迹里显示 `JOB` 行。本轮接成：中枢 → `Index.@State jobs` → `MainShellFacade.jobs` → `ConversationPane.@Prop jobs` → 会话头任务条（`jobListVisible` 决定出现与否、`orderedJobs` 决定顺序、状态点用 `jobDotState` 映射语义色、耗时 `jobDurationText`、无障碍整段 `jobListA11y`）。**视图只画，判定一条都没重写** |
| **E312b** | **live 耗时走字要有「真依赖」** | 定时器每秒 `jobsTick++`，但若渲染根本不读它，ArkUI 的重绘与它无关（看着像"秒不动"）。做法：把刻度放进 `ForEach` 的 key（`${job.id}:${jobsTick}`）——既让刻度成为真实依赖，也让耗时每秒重算。**这类"状态写了但没人读"的坑，与门面副本赋值是同一族**（静默失效，编译不报）|
| **E313** | **把"接线还在不在"钉成门禁：新增第 16 条功能接线** | `tools/check-feature-wiring.mjs` 正是为 E135（数据层与界面都在、**触发点没了**）造的门禁。本轮把任务条登记进去（`orderedJobs` / `jobListVisible` / `jobs: this.f.jobs` 三段）——**这类缺口本来就该被它拦下**，现在是补上了这块网 |

**验证**：8 门禁 + fetch 门禁全绿（功能接线门禁 16 个功能）；fixture 497 条 0 失败（任务条复用既有 40 条 Jobs 断言，不重复造）；entry ArkTS 编译 0 error。

### 15.4j P2-3：消息反馈面板拆成独立组件（E314–E315）

| # | 项 | 内容 |
|---|---|---|
| **E314** | **表单态与列表态同处一个组件 ⇒ 每敲一个字重绘整个会话列表** | 反馈面板原先长在 1700 行的 `ConversationPane` 里，`fbFor/fbRating/fbCategory/fbNote/fbNote2` **五个** `@State` 与消息列表共享作用域：`TextInput.onChange` 每写一次 `fbNote` 就标脏整个组件——会话越长越明显，而用户此刻只是在给一条消息写备注。拆出 `view/MessageFeedback.ets`（121 行）后：**表单态（类别/说明）归组件**，会话列表不再因打字重绘；宿主只剩"哪条消息的面板开着"（`fbFor`）、"这次是好评还是差评"（`fbRating`）、回执（`fbNote2`）三个状态 |
| **E314b** | **不要为"少查一次"造一个语义不完整的对象** | 初值原本想在宿主拼一个 `FeedbackRow` 返回，但那一行还带 `version` 等并发令牌字段（"写入时必须回传 `ifVersion`"）——拼一个"假装没有版本"的行等于凭空造一个不完整的对象，将来谁拿它去写就会踩 CAS。改成两个专用取值方法（`storedCategoryOf` / `storedNoteOf`）只取要用的两段文字。**规则**：跨边界传数据时，宁可多写两个小取值函数，也不要把"半个领域对象"递出去 |
| **E315** | **拆组件时把"谁决定"一并搬清楚：视图只报"用户选了什么"** | 组件**不认识** `SessionHub`，也不知道"提交成功要不要关面板"（那是宿主的策略，见 `ConversationPane.submitFeedback` 里 `n.code === 'feedback.saved'` 才关）。初值由宿主在**打开那一刻**给（`initialCategory`/`initialNote`），之后编辑过程中的变化**不回灌**——否则用户打字打到一半会被宿主的新渲染覆盖。这条边界与 `WorkspaceBrowser` 完全一致：**呈现归组件、副作用与策略归宿主** |

**验证**：8 门禁 + fetch 门禁全绿（功能接线门禁的「消息反馈」一条同时保护了这次搬迁：
`putFeedback` / `refreshFeedback` 的调用点仍在）；entry ArkTS 编译 0 error。
**注**：`ConversationPane` 行数 1717 → 1714（净减很少——搬走 93 行、同时搬进接线与取值方法约 90 行）；
本轮的收益在**状态边界**（表单态不再拖着列表一起重绘），不在行数。

### 15.4k P3-1：右栏按面板分派（`selectedRightPanel` 第一次有了消费者）（E316–E317）

| # | 项 | 内容 |
|---|---|---|
| **E316** | **第四例「通道有、没接」，而且它掩盖了一个更重要的判断** | `selectedRightPanel` / `selectRightPanel` / `rightbarPanels()` 都在模型里（还有 fixture 断言"切右栏面板"），但视图**无条件渲染 `DetailPane`**——所以那条断言测的是模型自洽，与界面无关。接上之后才看清**缺口在哪**：不是"面板体系"，而是**六个候选面板的内容视图**。把这件事写进模型注释后，下一步的工作量变得可估（每个面板一次"搬家 + 接线"）|
| **E316b** | **不给面板起假名字** | 现有的 sections 清单是**已经投影好的摘要行**，官方那六个候选要的是**原始素材**（文件树 / 轨迹条目 / 工具卡…）。把 sections 挂到 `right.files` 下会让"文件"这个名字骗人 ⇒ 新增第七个 id `right.detail`（对应官方 `conversation.detail` 那一类），并在注释里写明区别。**规则**：id 与 label 是给人看的契约，宁可多一个诚实的 id，也不借一个听起来对的 |
| **E317** | **"改默认面板"是这类改动的必查项** | 一旦视图按 id 分派，**任何指向不可用 id 的初值/兜底都会表现为"右栏空白"**：`initialNavigationState()` 原先写死 `PANEL_RIGHT_FILES`（现在是不可用候选），`defaultRightPanel` 的兜底也是它。两处都改指「详情」，并钉成断言（`初值也指向详情`）。**规则**：把视图改成"按 id 分派"时，同时清点**所有产生该 id 的地方**（初值、兜底、迁移桥、持久化偏好），否则会留下一个只在特定路径下出现的空面板 |

**验证**：8 门禁 + fetch 门禁全绿；fixture **497 → 501 条**（右栏口径改写 + 初值/兜底/不可用候选断言），0 失败；entry ArkTS 编译 0 error。

### 15.4l P3-2：右栏「预览」面板 + 共用预览呈现（E318–E320）

| # | 项 | 内容 |
|---|---|---|
| **E318** | **又一次索引偏移：按原始行号删除多个跨度必须降序——这条规则我上一轮刚写过** | 从 `WorkspacePane` 搬预览代码时，我先删了靠前的 `previewView` 再删 `previewHeader`/`previewBody`（用的是**原始行号**）⇒ 后面的删除点全部前移，文件被切成一团碎片（编译当场报 `Object is possibly undefined` + UI 语法错位）。这正是 E293 记录过、上一轮还写进提交说明的坑。**做法**：`for name in sorted(WANT, key=lambda n: SPAN[n][0], reverse=True)`。**教训**：规则写进文档 ≠ 下次会自动遵守；**把"降序删除"做成搬迁函数的唯一入口**（本轮生成器里就是这么写的，出问题的是我临时写的那段补丁脚本） |
| **E319** | **`onAttach` 是 `CustomComponent` 的保留成员名** | 给预览组件起回调名时用了 `onAttach`，编译报 `Property 'onAttach' in type 'FilePreviewPane' is not assignable to the same property in base type 'CustomComponent'`。与 `onDragStart` / `onDragEnd`（E268）同一类坑 ⇒ 改 `onAttachFile`。**保留名清单继续变长**：凡是 `on*` 的短名都要先想一下是不是框架回调 |
| **E320** | **同一份呈现抽成组件，而不是"两边各画一份"** | 预览（空态 / 头部「投喂」/ 代码等宽正文 / 过大与二进制两档空态）原先只长在 `WorkspacePane` 的第三列里；右栏「预览」面板要的是**同一份**。抄一遍的代价很快会显形（改了一处忘另一处）⇒ 抽成 `view/FilePreviewPane.ets`（156 行，`preview` 为 undefined 时自带空态），工作区页签与右栏共用。**顺带**：切换器只在**可用面板 ≥2** 时才画——"一行只有一个选项的按钮组"是噪声（`panels.length > 1` 一个判据解决）|

**验证**：8 门禁 + fetch 门禁全绿；fixture **501 条**（右栏可用面板口径改为"详情 + 预览"），0 失败；
entry ArkTS 编译 0 error；设计令牌棘轮**调紧到 37 处 / 10 个文件**（预览搬走时 3 处插画尺寸显式 `token-exempt`）。

### 15.4m P3-3：右栏「文件」面板 + 共用文件树（E321–E322）

| # | 项 | 内容 |
|---|---|---|
| **E321** | **共用呈现的第二例（也验证了 E320 的做法可复制）** | 文件树（展开 / 选中高亮 / 交付物标记 / 空态）从 `WorkspacePane` 的第二列抽成 `view/FileTreePane.ets`（133 行），右栏「文件」面板与工作区页签共用。`WorkspacePane` 连续两轮被搬空：**464 → 349（预览） → 274 行（文件树）**——它从"三列大组件"退化成"列表 + 两个挂载点"的装配层。**规律**：一旦某个大组件里出现了"另一个挂载点也要同一份"的呈现，抽出来的收益立刻兑现，不必等重构计划排到它 |
| **E322** | **门面 vs 逐个 props：同一件事的几个面就打包** | 右栏「文件」面板需要 6 样东西（树 / 选中项 / 两个空态文案 / 两个回调）。逐个加 props 会让 `RightbarShell` 的 props 面继续变胖，而"少传一个"的表现是**点不动**（静默失效，编译器在 ArkUI 里不一定拦得住）。故打包成 `FileTreeFacade`（与 `AppShell`/`MainShell` 的门面同一口径）。**注**：抽出来之后 `WorkspacePane` 里 `fileSummary`/`formatBytes`/`NodeKind` 三个导入立刻变成死导入——**搬完就查一遍未使用导入**（本仓已有固定检查脚本） |

**验证**：8 门禁 + fetch 门禁全绿；fixture **501 条**（右栏可用面板改为"详情 + 文件 + 预览"），0 失败；
entry ArkTS 编译 0 error；令牌棘轮再调紧 **37 → 34 处**（三处树内几何尺寸显式 `token-exempt`，搬迁不改视觉）。

### 15.4n P3-4：右栏「交付物」面板 + 纯模型筛选（E323–E325）

| # | 项 | 内容 |
|---|---|---|
| **E323** | **"有几件事"不等于"更新了几次"** | 新增 `itemsOfKind` / `deliverablesOf`（`Trajectory.ets`）：按种类筛 + **同 id 只留最后一条**。过程流里同一条交付物会被**流式 merge 多次**，直接 `filter` 会得到一串重复卡（用户看到"产出了 5 个文件"而实际只有 1 个）。去重规则放进模型并钉成断言（`同 id 只留最后一条`）。**规律**：凡是"从过程流里挑出一类东西"，都要先问"同一条会不会出现多次" |
| **E324** | **`build()` 只能有一个根节点（`@Builder` 可以多个）** | 交付物卡原先是 `@Builder deliverableItem`，体内是**两个并列节点**（信息行 + 动作行）。搬进组件后编译报 `In an '@Entry' decorated component, the 'build' method can have only one root node` ⇒ 包一层 `Column`。**规则**：`@Builder` → 组件时，多根必须先收成一个容器（这是"搬迁不改视觉"里唯一必须动结构的地方） |
| **E325** | **`NativeAction` / `NativeActionBar` 在 `view/` 而不在 appstate** | 从 appstate 导入 `NativeAction` 报 `has no exported member`——它是 `view/NativePrimitives.ets` 里的**视图层类型**。搬组件时"这些名字从哪来"要按包核一遍（本仓有 arch-check 保护模块边界，但保护不了"名字在哪个包"）|

**验证**：8 门禁 + fetch 门禁全绿；fixture **501 → 505 条**（按种类筛选 4 条：保序 / 不改原数组 / 同 id 去重 / 空数组），0 失败；
entry ArkTS 编译 0 error；令牌棘轮再调紧 **34 → 33 处**。右栏可用面板：详情 / 文件 / 交付物 / 预览（四个）。

### 15.4o P3-5：右栏「工具」+「子代理」面板（E326–E328）

| # | 项 | 内容 |
|---|---|---|
| **E326** | **"卡片的判定与呈现一起搬"比"只搬呈现"更省事** | 工具卡（过程流里信息量最大的一张：图标 / 语气 / 参数摘要 / **改动对照** / 输出预览 / 截断提示）连同它那一整套判定（`diffOf` / `diffTextLines` / `diffKindLines` / `toolBadge` / `toolIcon`）一起搬进 `ToolCard`，宿主只剩"数据 + 展开态 + 回调"三个入参。若只搬呈现、判定留在宿主，就得再串一串 `diffLines`/`bodyTruncatedHint` 之类的派生值——**那些派生值本来就是卡的内部细节**。⇒ 抽组件时先问"这些计算只服务于这张卡吗"，是就一起搬。`ConversationPane` 1656 → **1534 行** |
| **E327** | **展开态必须是入参，即使"看起来组件自己管更自然"** | 主区有「展开全部 / 收起全部」两个按钮，它们要让**所有**卡一起开合 ⇒ 展开态的唯一真值在宿主（`ConversationPane.expanded`）。故 `ToolCard` 只吃 `expanded` + 报 `onToggle`。右栏「工具」面板同理，但它持**自己那一条列表**的展开集合（面板内交互归面板）——两处语义相同、归属不同，都是有意的 |
| **E328** | **脚本化搬迁的第三类坑：正则把标识符替换成了 `this.this.item`** | 本轮用正则把 `item.` 改成 `this.item.` 时，对已经是 `this.item` 的位置又加了一次前缀（`this.this.item`），另有一批裸 `item` 落在 build 体内没被替换。**编译器 5 分钟内全部抓出**（`Cannot find name 'item'` / `Expected 0 arguments`）。**规则**：改名用"负向环视 + 逐处断言"，并**在改名后立刻 grep 残留标识符**（本轮就是这么收尾的）；这类机械改名不要一次改多种模式 |

**验证**：8 门禁 + fetch 门禁全绿；fixture **505 条**（右栏可用面板口径改为六个），0 失败；
entry ArkTS 编译 0 error；搬完清掉 9 个死导入（`ToolKind`/`ToolState`/`presentDiff`/`previewOutput`/`toolSummaryOf` 等）。

### 15.4p P3-6：右栏「轨迹」面板 + P3 收口（E329–E330）

| # | 项 | 内容 |
|---|---|---|
| **E329** | **"共用呈现"到第六块时，先想清楚"两处的语义差"再动手** | 时间总览抽成 `TimelineOverview`（194 行）给主区与右栏共用。但右栏**只放总览、不放全量台账**——官方右栏的 trajectory 是"当前回合的过程"，而本仓 `TrajectoryItem` **没有回合字段**，"回合作用域的过程列表"在这个模型里不存在。若照抄主区那份台账，320vp 里既看不清也没必要，还会造出一个"看起来有、其实是另一个东西"的面板。⇒ **共用的是"呈现"，不是"语义"**；语义差写进模型注释（`PanelRegistry` 的 `right.trajectory` 那一条）与组件头注释 |
| **E329b** | **受控项要连"谁写、谁读"一起搬** | 总览有三个受控项，每个都有明确的第二个读写方：`focusedCellId`（主区列表滚动也会改它）、`dragging`（拖动期间宿主暂停"列表 → 条"，否则条被自己的滚动带着抖）、`onCommit`（松手才滚列表，要拿 `scroller`）。**受控项留在宿主 + 用回调写回**，组件只持"比例条宽度"这类纯自己的状态 |
| **E330** | **又一处"末尾无尾逗号"导致的清理失败** | 批量删死导入时 `toolSummaryOf` 是导入列表的**最后一项**（`toolSummaryOf }` 而非 `toolSummaryOf,`），我的 `name + ', '` 模式匹配不到；检查脚本仍报它未使用，一眼看出没清干净。**规则**：清理导入要同时覆盖"中间项（带逗号）"与"末项（不带逗号）"两种形态——本轮共清掉 16 个死导入 |

**验证**：8 门禁 + fetch 门禁全绿；fixture **506 条**（右栏七个面板全部可用 + "不存在的 id 切不过去"改用不存在的 id 断言），0 失败；
entry ArkTS 编译 0 error；`ConversationPane` 1714 → **1414 行**。

### 15.4q P4-1：设置分区进注册表，官方四段成为真分区（E331–E332）

| # | 项 | 内容 |
|---|---|---|
| **E331** | **"可选面板"这件事，第四处也复用了同一套机制** | 设置页原先自己持一个 `@State tab` + 7 个手写 chip（`general`/`model`/`plugins`/`core`/`preset`/`skills`/`device`）。P4-1 之后：新增 `PanelLocation.SETTINGS` + `settingsSections()`（8 项）并注册进同一个注册表 ⇒ 侧栏、右栏、设置页三处的"清单 + 排序 + 可用性 + 切换器"是**同一套代码路径**，设置分区也自动获得 `canSelect` 校验与 `firstAvailable`。**规律**：一套机制只要在第二个地方用顺手了，第四处就该直接复用，而不是"顺手写个 if 链" |
| **E331b** | **官方四段与本仓特有项要能一眼分开** | 官方 `ui-settings` 是 General / Models / Plugins / Plugin Inventory；本仓另有核心（E110）、预设（E129）、技能（E135）、设备四项（都已有内容、也都登记在矩阵里）。做法：官方四段 order 10–40 在前，本仓四项 50–80 在后，并把它们的 `owner` 标成 `'hdsh'`——**不给它们编一个"官方分区名"**（那是把端侧补充伪装成对齐项）|
| **E332** | **"子页签"往往是没被拆开的独立分区** | 「插件清单」此前是插件段里的子页签（`config` / `list` 两个 chip + `@State pluginView`）。官方把它作为**独立分区**（`settings-plugin-inventory`，只读的 Cordis Loader 清单）："能配置什么"与"装了什么"是两件事。拆开之后子页签状态与 `subChip` 一起消失（顺带清掉 `modelSelectionLabel` 死导入）。**规律**：看到"同一段里两个 chip 切两种内容"，先问它们是不是两个独立分区 |

**验证**：8 门禁 + fetch 门禁全绿；fixture **506 → 514 条**（分区顺序/数量/owner/初值/校验/清单独立 共 8 条），0 失败；
entry ArkTS 编译 0 error；`SettingsPane` 1890 → **1878 行**（本轮只动信息架构，内容原样搬）。

### 15.4r P4-2：设置页按域拆组件（第一刀：插件配置 / 插件清单）（E333–E335）

| # | 项 | 内容 |
|---|---|---|
| **E333** | **死代码第五例：`credentialsTab` 早就没人渲染** | `SettingsPane` 里有一个 52 行的 `credentialsTab()`，**全仓零调用**——因为凭据已经并入模型段（`modelTab` 里派生 `<ROUTE>_API_KEY` 引用，代码注释还写着"与官方 Models 页的做法一致"）。当"某段内容被另一处接管"时，旧的那份**不删就会一直躺着**。本轮删除；`SettingsPane` 1890 → **1698 行**（P4-1 + P4-2 两轮合计）|
| **E334** | **抽取组件会顺手暴露"重复渲染"** | 插件段原先自己画一行摘要、组件里又画一行（同一句话出现两次）。抽取时把"摘要归组件"写清楚之后，重复立刻显形 ⇒ 删除段内那行。**规律**：把内容归属写进注释的过程，本身就是一次查重 |
| **E335** | **第六次踩 `build()` 单根节点（E324 的同类）** | `pluginConfigView` / `pluginListView` 都是 `@Builder`（多根合法），搬进组件后必须包一个 `Column`。**这条已经写进文档两轮了**——说明它该进"搬迁检查清单"的第 1 条，而不是靠记性：**`@Builder` → 组件 = 先看根节点个数** |

**验证**：8 门禁 + fetch 门禁全绿；fixture 514 条 0 失败；entry ArkTS 编译 0 error；
令牌棘轮再调紧 **33 → 29 处**（新文件里的徽标药丸/状态点几何显式 `token-exempt`，搬迁不改视觉）；
本轮共清掉 4 个死导入（`classifyPlugin` / `PluginRow` / `pluginSummary` …）。

### 15.4s P4-3a：设置行拆成组件（顺便记一次失败的批量手术）（E336–E338）

| # | 项 | 内容 |
|---|---|---|
| **E336** | **脚本化批量手术的第三次"索引偏移"，这次我选择了回滚重做** | 本轮想一次搬两块（`settingRow` + 模型域 501 行）。第一版脚本按"原始行号"删除跨度、又用**删除前**的行号插入新成员 ⇒ 插入点落在 `diagnosticsEntry` 的函数体中间（`@Builder` 嵌在别人的闭包里），文件被切碎。**回滚到上一个提交、改成"一次只搬一块、每一步都编译"** 之后一次通过。**规则（已第三次记录，这次升级为硬约束）**：`删除 + 插入`必须**合成一趟按行号排序的操作列表**再施加；跨文件、多目标的手术不做一次性脚本 |
| **E337** | **被搬走的代码可能依赖宿主里的"模块级函数"，而导入扫描看不见它们** | `settingRow` 的三个取值助手用了 `choiceLabel` / `officialSettingLabel` / `officialSettingValue` / `officialSettingHint`——它们是 `SettingsPane.ets` 顶部的**模块级函数**（不是 import），所以"按导入块算依赖"的脚本看不见，编译才报 `Cannot find name`。⇒ 搬迁后的依赖清单要**同时扫两类**：导入的名字 + 宿主文件里 `^function` / `^const` 定义的名字。顺带说明这四个函数**本来就只服务于设置行**（官方中文措辞表），搬过来之后宿主的 1600 行里少了一块最杂的文案表 |
| **E338** | **删成员会留下"空的文档注释"** | 删掉 `labelOf`/`hintOf`/`valueLabelOf` 之后，它们上方的 `/** … */` 还留在原地（成了悬空注释）。已清理。**这类残留不会报错**，只会在下次读文件时浪费人的时间——搬迁收尾应固定包含"清理悬空注释" |

**验证**：8 门禁 + fetch 门禁全绿；fixture 514 条 0 失败；entry ArkTS 编译 0 error；
`SettingRow` **300 行**、`SettingsPane` 1694 → **1458 行**；清掉 3 个死导入 + 4 处悬空注释。
**本轮未做**（如实记录）：模型域（≈500 行）没搬——留到 P4-3b，且它的依赖已在本轮清干净。

### 15.4t P4-3b：模型域拆成组件（并补齐"搬迁手术"的操作规范）（E339–E341）

| # | 项 | 内容 |
|---|---|---|
| **E339** | **"删除 + 插入"必须一趟排序施加，且同一区间只能有一个动作** | 本轮第一次尝试把 `modelTab` 同时列进"删除"和"插入"，脚本按统一趟施加后 **`out[a:b] = CALL` 把 206 行的区间换成了 27 行**（本意是"删掉成员、在调用点插渲染"）⇒ 文件凭空少 179 行。**规则**：一个区间只允许一个动作（删除 或 替换）；要"删定义 + 在别处插入"就写两个不同区间的动作，且**插入点必须用删除后的坐标**重算 |
| **E340** | **代入装配的"零件"要按角色分开放，别让同一段文本进两个位置** | 组装组件时我先把 `providerCards` 等成员放进了 `build()` 的 Column，随后又把它们当"成员"追加在 struct 后面 ⇒ 同一份代码出现两次，编译器报一串 `Declaration expected`。**规则**：装配清单先写清楚"哪些是 build 体、哪些是成员"（各一份，不允许重叠），再拼 |
| **E341** | **编译器的报错位置会骗人，但报错内容不会** | 中途 `Cannot find name 'SessionHub'` / `Property 'settingRow' does not exist` 这类**符号级**报错是可靠的定位器（`providerCards` 里那处行渲染忘了替换、`SessionHub` 没进导入）；而 `Declaration expected` 这类**结构级**报错只会指向"碎片的第一个字符"。**做法**：先修符号级、再修结构级，且结构级错误优先用"人工看一眼那几行"而不是继续加脚本 |

**验证**：8 门禁 + fetch 门禁全绿；fixture 514 条 0 失败；entry ArkTS 编译 0 error；
`SettingsModels` **645 行**、`SettingsPane` 1458 → **932 行**；令牌棘轮再调紧 **29 → 26 处**（三处行内图标尺寸显式 `token-exempt`）。

### 15.4u P4-4：通用段与设备段拆成组件（搬迁规范首次一次通过）（E342）

| # | 项 | 内容 |
|---|---|---|
| **E342** | **把上一轮的三条规范写进流程之后，两块域组件"一次编译通过"** | 本轮拆 `SettingsGeneral`（183 行）与 `SettingsDevice`（129 行），照 P4-3b 记下的规范执行：① 一个区间只允许一个动作（`this.generalTab()` 调用点用**替换**，成员定义用**删除**，坐标都在原始文件上算）；② 装配清单先分行（`build` 体 = `inner('generalTab')`；成员 = `diagnosticsEntry`/`aboutRow`；模块级 = `permissionModeLabel`），互不重叠；③ 导入按"宿主导入块 ∩ 用到的名字 + 宿主模块级符号"两类一起算。结果：**两次搬迁都是 0 error 通过**（前两轮分别花了 6 次与 4 次编译修正）。⇒ **踩过的坑写成硬规范之后，同类手术的成本从"多轮试错"降到"一次通过"** |
| **E342b** | **回调签名不能靠猜** | `SettingsDevice` 的第一版把 `onPairHost`/`onRevokeHost` 都写成 `(h: HostRecord) => void`，而宿主实际是 `onPairHost: () => void` 与 `onRevokeHost: (id: string) => void` —— 编译逐条报出。**规则**：跨组件传回调时，签名从宿主声明处**照抄**（`grep` 出来的原文），不要凭接口"看起来该传什么" |

**验证**：8 门禁 + fetch 门禁全绿；fixture 514 条 0 失败；entry ArkTS 编译 0 error；
`SettingRow` 300 / `SettingsModels` 645 / `SettingsGeneral` 183 / `SettingsDevice` 129 行，
`SettingsPane` **1890 → 760 行**（P4-1…P4-4 五轮合计）；令牌棘轮 **26 → 25 处**。

### 15.4v P0 回归修复：真机一启动就崩（`@Builder` 自递归）（E343–E344）

真机报障（Mate 70 Pro+ / PLA-AL10 / API 24，debug 包）：

```
Reason:RangeError   Error message:Stack overflow!
    at mainContent entry (entry/src/main/ets/view/shell/MainShell.ets:407:4)
    at anonymous entry  (entry/src/main/ets/view/shell/MainShell.ets:405:7)
    at ifElseBranchUpdateFunction (…/stateMgmt.js:5356:1)
    at anonymous entry  (entry/src/main/ets/view/shell/MainShell.ets:404:12)
    …（同一组帧重复 250+ 次）→ AppKit: about to exit due to RuntimeError
```

| # | 项 | 内容 |
|---|---|---|
| **E343** | **`@Builder` 的兜底分支把自己叫了一遍** | 修前 `MainShell.mainContent` 的尾巴是 `} else { this.mainContent(this.compact) }`——上面已用 `if/else if` 分派了「命令面板 + 诊断 / 连接 / 会话」，`else` 本意是"剩下的面板交回上层继续分派"，实际是 **自递归**：每次调用都会重新走到这条 `else`（条件不变），于是栈一路涨到 `Stack overflow` 被系统杀进程。**为什么会走进 `else`**：主区还剩工作区 / 核心 / 设置三类面板，它们由**主区的第二束**（`TabContentView`）渲染——搬迁时这一束忘了接上，兜底才被触发。**正确形态**：兜底 = 渲染**真正拥有这些内容的那一束组件**，而不是回头问自己：`TabContentView({ f: this.f.tabFacade, compact: this.compact })`（`TabContentView` 自己有兜底且不再外调）。**规律**：`@Builder` 里的"交回上层"是一个不存在的语义——Builder 没有"上层"，`this.x()` 就是再调一遍自己 |
| **E343b** | **这个缺陷为什么此前的所有门禁都拦不住** | ① 语法 / 类型 / 编译全过（自递归是合法调用）；② `check-feature-wiring` 只管"调用点还在不在"，不管"调的是谁"；③ 静态评审也容易放过——**那两行读起来像控制流，实际是无限循环**；④ 它甚至**不影响 `build()` 的首次渲染**（首次只走一个分支），要**第二次更新**才炸，所以"起来看着好好的"。⇒ 故新增 `tools/check-builder-recursion.mjs`（剥注释后扫 `this.<自己>(`），并**用修前的提交跑同一检测器归真**（命中 `MainShell.ets:380`）——门禁必须先在已知坏版本上红过一次，才算证明它有用 |
| **E343c** | **`tabFacade` 是"通道有、没消费者"的又一例（第 7 例）** | `MainShellFacade.tabFacade` 由宿主 `buildMainFacade()` 一直造着，而修前的 `MainShell` **从未读过它**——正是这个"没人用的通道"掩盖了兜底分支的空洞（本来该由它把剩下的面板接出去）。修复同时把接线钉进 `check-feature-wiring`（新功能项「主区兜底」：`TabContentView({ f: this.f.tabFacade` 必须存在）。**规律**：门面里有成员却零消费者时，先问"是不是有一条分派路径本该由它兜底" |
| **E344** | **门禁自检要在"已知坏版本"上跑一次** | 新增的 `--self-test` 用的是**人工注入的样例**；样例能过只证明"检测器会匹配我写的样例"，不能证明"它会命中真实的那行代码"。故额外做了**归真验证**：`git show HEAD:…MainShell.ets` 存成临时文件 → 用 `scanText()` 扫 → 命中 1 处（`mainContent`，行 380）。**检测器导出 `scanText` 而不只在 `main()` 里跑**，就是为了让这种归真验证可复现 |
| **E345** | **搬迁搬走了消费者，却把三整套导入和三个 `@Builder` 留在原地** | 顺着 E343 查"通道有、没消费者"，查出 `Index.ets` 里还有**三个零调用的 `@Builder`**（`hubBanner` / `coreTabContent` / `tabContent`）——它们的**内容**都已搬进 AppShell 与两束组件，只剩宿主这份壳。`Index.ets` 是 `@Entry` 组件，**没有任何东西会渲染这些 Builder**，它们既不出现在界面、也不报错，只会让"这里到底谁画这个"越读越糊。同一次还清出 **22 个零使用 import**（`Index` 12 个、`MainShell` 9 个、其余 6 文件各 1 个），全部是搬迁留下的。⇒ **搬迁的收尾清单必须包含"删掉宿主那份壳与它的导入"**；判据：`this.<名字>(` 与"该名字在文件里出现次数"两个计数都为 0 |
| **E345b** | **写"零使用"检测器时，负向环视千万别把 `.` 也排除掉** | 第一版用 `(?<![\w.$])名字(?![\w])`，于是 `.borderRadius(Radius.M)` **不算使用**（`Radius` 前面是 `.`）⇒ 误判 `Radius` 为死导入、删掉后编译立刻报 `Cannot find name 'Radius'`（两处）。**规则**：被"点前缀"使用是 ArkTS 的常态（`.fontSize(Fs.BODY)` / `.borderRadius(Radius.M)` / `.padding(Sp.S)`），负向环视只能排除**标识符字符**；而**编译门禁是这条检测器的安全网**——它当场抓住了误删 |

**验证**：新增门禁 `tools/check-builder-recursion.mjs`（75 文件 / **99 个 `@Builder`** / 0 违规；5 条注入式自检 + 归真命中修前版本）；
`check-feature-wiring` 16 → **17 个功能**；8 门禁 + fetch 门禁全绿；entry ArkTS 编译 **0 error（`--clean`）**；
`MainShell.ets` 412 → **429 行**（净增的是解释这个坑为什么存在的注释，以及兜底那一句真正的接线），`Index.ets` 4460 → **4384 行**（三个死 Builder + 22 个死导入），全仓**零使用 import 归零**；
**真机复验仍待做**（本次修复的验收标准：冷启后点一下界面不再被杀进程）——按本仓规矩，无真机时不得宣称设备验收完成。

### 15.4w P4-5：技能段与预设段拆成域组件（+ 收掉 12 个成员的死链）（E346）

| # | 项 | 内容 |
|---|---|---|
| **E346** | **搬走渲染之后，调用方的 props / state 还会继续"活着"** | P4-5 把「技能」「预设」两段从 `SettingsPane.build()` 拆成 `SettingsSkills`（95 行）/ `SettingsPresets`（271 行）后，宿主那段代码是没了，但**它用的成员还在**：`skills`/`skillNote`/`presets`/`presetNote`/`onPresetCopy`/`onPresetDelete`（搬走那段唯一的读者）+ `confirmingPresetId`/`presetContent`/`presetViewedId`（三个临时态已经住在域组件里）+ `agentGroups()`（只服务于预设段）。**它们全是"零消费者"**：编译不报、界面无感，只是让下一个人以为"这些东西还在用"。**判据**：搬迁后对宿主跑一遍"成员名全文件出现次数 == 1"的扫描（== 1 即只有声明）——本轮靠它一次点出 15 个，逐条判断后删掉 12 个（另 3 个误报已核实：`allGroups` 还在给 `SettingsPresets`/`SettingsModels` 传值） |
| **E346b** | **搬迁连"上游的通道"一起清：`settingsStates` 这条链从头到尾没人读** | 顺着上一轮 E345 的自检往下查，发现 `HubSnapshot.settingsStates` → `Index.@State settingsStates` → 主区门面 → `TabContentView` 假门面成员 → `SettingsPane.states` 是**一条完整的死链**：末端那个 `states` prop 从前没人读过（本轮拆组件时又验证了一次），中间每一层都只是"忠实地转发一个没人要的字段"。已从 `entry` 侧全部摘掉（4 处 + 一个类型导入）。**保留**：中枢内部的 `namespaceStates` —— 它是 `settingStateOf(ns)` 的真值，与 `snapshot()` 那个字段是两件事（**删的是通道，不是事实**）。加上 `onPickEffort`（同样是零消费者回调），本轮共清掉 16 个死成员 |
| **E346c** | **脚本化搬迁的第三次同一类事故：一个区间的锚点用了"行数"，而块的真实长度不是那个数** | 删 `onPickEffort` 时写成"删 1+3 行"，而该块实为 4 行 ⇒ **多删了一个 `}`**，于是 `TabContentView` 的 struct 少了一层闭合、`build()` 被解析成普通成员，编译报的是**失真**的错（`Cannot find name 'build'` / `must have at most one 'build' method`）。**规则**：删除区间要用**文本锚点动态找结束**（例如"往下第一个独立的 `},`"），不要写行数；并且**报错内容比报错位置可靠**——"找不到 build"这种荒谬报错，第一反应应该是"我多删了一个花括号"。这已是同一类事故的第三次（E293/E339/E346c），三次都被真编译门禁当场抓住 |
| **E346d** | **行号锚点在同一个脚本里会随前面的删除失效——本轮连踩两次** | 同一份脚本里先删 468–502、再删 416–422、再删 386，后两段的**行号在第一次删除后已经变了**，于是断言失败（好在这一版会**在断言处停下且不写盘**，文件没被写坏）。最终采用的做法：**同一轮里对每个待删区间用"文本子串 + 唯一性断言"定位，并且按降序执行**；断言失败时**不写盘**（先攒 `src`/`lines`，最后一次性写）——这比"边删边写"安全得多 |

**验证**：8 门禁 + fetch 门禁全绿；fixture **514 条** 0 失败；entry ArkTS 编译 **0 error（`--clean`）**；
`SettingsPane` 758 → **497 行**（P4-1…P4-5 合计 **1890 → 497**），新增 `SettingsSkills` 95 行 / `SettingsPresets` 271 行；
全仓**零使用 import 归零**、**零使用 `@Builder` 归零**、宿主**零使用成员归零**（逐文件扫描）。
设置页现在的域组件：`SettingRow` / `SettingsGeneral` / `SettingsModels` / `SettingsPlugins` / `SettingsInventory` / `SettingsSkills` / `SettingsPresets` / `SettingsDevice`。

### 15.4x P4-6：核心段拆出组件 + 写入回执按域归属（E347）

| # | 项 | 内容 |
|---|---|---|
| **E347** | **一条全局回执字符串，被多个域写、只被一个域读** | 设置写入回执是**一个全局字符串**（`Index.settingWriteNote`），而它唯一的读取点是**设置页顶部那条回显**。这造成两类错误：① **串段落**——在「通用」改了外观，切到「模型」，那条回执还挂在模型页上方，看起来像模型页刚写出来的结果；② **看起来像本页失败**——失败回执（"被拒"）属于别的段落时同样挂在当前段落上方。两条都是"界面在撒谎"（D3 §4）。**修法**：回执带**归属域**（`settingsWriteDomain`），设置页只在"域 == 当前分区"时显示；域由 key 的命名空间判定（`settingsDomainOfNs`），**判不出来就不隐藏**（宁可多显示一条，也不要藏掉用户正需要的失败回执） |
| **E347b** | **同一根因的另一半：工作区/会话的回执写进了设置通道 ⇒ 用户从来没见过它们** | 顺着 E347 查"谁在写这条回执"，发现 `folderUsePath` / `folderChoose` / `forgetHost` / `createSessionIn` / `deleteWorkspaceByPath` / `toggleDeleteConfirm` / `archiveSessionById` 这 **7 个函数、19 处回执**都在写**设置回执通道**——而这条通道只在设置页顶部显示 ⇒ 用户在会话/工作区里**点「归档」等操作时看不到任何回执**（"点了没反应"）。已全部改到 `attachNotice`（会话输入区上方那条通道，本来就承担工作区/附件的回执）。**规律**：全局回执字段一旦只有一个读取点，任何"另一个域"往里写都是静默失败 |
| **E347c** | **域判定的纯逻辑必须住在零依赖文件里，否则断言一条都写不了** | 域判定最初写在 `model/Settings.ets` 里，而那个文件 `import { … } from 'connection'`；fixture 只编"纯逻辑清单"里的文件、**不编 `connection`** ⇒ 这段判定只能靠真机看。故把它（连同 `GENERAL_NAMESPACES` 白名单、`DEFAULT_MODEL_NS`）搬进新的 **`model/SettingsDomains.ets`（零依赖）**，在原文件里 `export { … } from './SettingsDomains'` **再导出**，于是所有既有导入点（含 barrel）一字未改。结果：**19 条新断言**（通用/模型/插件/预设/技能五类 + 三类"不猜"的边界 + key→域的三条边界） |
| **E347d** | **核心段收尾：`SettingsCore`（78 行）** | 最后一段内联内容搬成 `view/SettingsCore.ets`：它包住 `CorePane` 并承载两块提示（核心操作提示 + 插件区提示）。**它不是多余的转发层**——E118 的修复形态就长这样：这一段曾由宿主 `@BuilderParam` 注入 `coreSlot`，真机上点开「核心」分区直接 JS 崩溃；改成显式属性之后才稳。`SettingsPane` 497 → **500 行**（净增的是"为什么这一层必须存在"的说明） |

**验证**：8 门禁 + fetch 门禁全绿；fixture **514 → 533 条**（+19）0 失败；entry ArkTS 编译 **0 error（`--clean`）**；
`SettingsPane` **500 行**（P4-1…P4-6 合计 **1890 → 500**），设置域组件九个：
`SettingRow` / `SettingsGeneral` / `SettingsModels` / `SettingsPlugins` / `SettingsInventory` / `SettingsSkills` / `SettingsPresets` / `SettingsCore` / `SettingsDevice`。

### 15.4y P2-4：消息行整块抽成 `MessageRow`（E348）

| # | 项 | 内容 |
|---|---|---|
| **E348** | **"一条消息"是行级的事，不该长在 1400 行的正文组件里** | `ConversationPane` 原先自己带着这条消息的**五份状态**（悬停、上下文菜单开关与归属、反馈面板归属与回执）＋两个 `@Builder`（行内操作条、上下文菜单）＋三个输入策略包装方法。它与"过程流的分组与顺序"混在一起 ⇒ 任何一处改动都要通读全文。现整块搬进 **`view/MessageRow.ets`（221 行）**：卡片底色与悬停、长按/右键同一个菜单、行内动作条、反馈表单、以及 `InputPolicy` 的三条判定（`hoverEnabled` / `contextMenuGestures`）。`ConversationPane` 1414 → **1234 行** |
| **E348b** | **边界：什么**不**跟着搬** | ① 「展开全部 / 收起全部」是**跨行**操作 ⇒ 展开集合的唯一真值仍在父组件（与 `ToolCard.expanded` 同理）；② 菜单里给不给"重发 / 撤回评价"要读中枢的反馈记录 ⇒ 动作清单由父组件算好传进去（`messageActionsOf`）；③ 反馈的**持久化**（`feedback/put` 的 CAS 版本与失败原因）留在中枢，回执与"成功才关面板"的策略留在父组件。**规律**：抽组件时先把"哪些状态是跨行的、哪些判定要读中枢"列出来，剩下的才是这个组件的 |
| **E348c** | **顺带删掉一处重复实现：`clockOf` 与模型的 `formatClock` 逐字等价** | 父组件自己写了一个 `clockOf(ms)`（补零 + `HH:mm`），而 `appstate/model/Present.formatClock` 就是同一件事——**模型的那个还带 fixture**。搬组件时发现两处输出完全一致 ⇒ 删掉视图层那份（消息表头的"钟点 · 时长"仍按 E204 拼装，只是改吃模型的函数）。**规律**：搬一块代码时顺手比对"它调用的私有助手有没有模型层的等价物"，有就删一份 |
| **E348d** | **脚本化整块替换的第四次事故：结束标记用 `indexOf` 找了一个**在块内也出现**的字符串** | 本轮用「找 `private feedbackNoticeText(` 的位置当块的结束锚」，而这个名字**在一次失败的替换尝试里已经被写进了新块**，于是 `indexOf` 命中的是块**内部**那一次 ⇒ 结束位置取早了，**多删了 4 个成员**（`reasoningItem` / `inputModality` / `messageActionsOf` / `onMessageAction`），编译逐条报出（`Property 'reasoningItem' does not exist`）。**处置**：从 `git` 备份里按**花括号配平**取回这 4 个成员（含文档注释）并重新插入。**规则**：整块替换的边界锚必须是**块内不可能出现**的东西（例如"下一个 `@Builder` 行"或行号+配平），且**执行前先断言块长与块内特征**——本轮正是因为断言里只检查了"该有的东西在"，没检查"不该有的东西不在" |

**验证**：8 门禁 + fetch 门禁全绿；fixture 533 条 0 失败；entry ArkTS 编译 **0 error**；
`ConversationPane` **1234 行**（原 1414），新增 `MessageRow` 221 行；全仓零使用 import 归零、零使用 `@Builder` 归零。

### 15.4z P2-5：会话头的四块 chrome 抽成 `ConversationHeader`（E349）

| # | 项 | 内容 |
|---|---|---|
| **E349** | **"正文之外决定正文形态"的那 240 行，值得单独成组件** | 会话正文组件里有四块东西**不在列表里**：视图切换（对话 / 轨迹）+ 轨迹工具栏（展开全部 / 收起全部 / 事件计数）、后台任务条（官方 `conversation.header.jobs`）、时间总览（交互式 timeline）、会话内搜索条。它们的共同点是"**决定正文要以什么形态呈现**"，而不是正文本身。现整块搬进 **`view/ConversationHeader.ets`（284 行）**。`ConversationPane` 1234 → **1093 行** |
| **E349b** | **分工口径：跨行状态留父组件，块内状态留子组件** | **搜索**的命中判定要扫整条过程流、命中的高亮还要画在列表项上 ⇒ 状态与判定（`searchQuery` / `matchPos` / `hits()` / `step()`）留在父组件，头部只做"显示 + 转发"；**任务条**的定时器由父组件持有（"要不要起定时器"是模型 `jobTickerNeeded` 的判定），刻度 `jobsTick` 当 prop 传进去——**它同时是子组件的真实依赖**（刻度变了才重绘，live 任务的耗时才会走字）。这条口径与 P2-4 的 `MessageRow` 同源 |
| **E349c** | **`stats` 不造默认值** | 头部的时间总览要 `SessionStatsInput`。第一版给它写了一个"全 0 的对象字面量"当默认值 —— 编译直接报 `arkts-no-untyped-obj-literals`（ArkTS 不许无类型对象字面量），而**这个报错救了一次语义错误**：造"全 0 的默认统计"会让"没接到数据"看起来像"这场会话什么也没花"，那是**假读数**。改成与 `TimelineOverview` 一致的**必需 prop**（宿主一定给，它就是投影出来的事实） |
| **E349d** | **脚本化编辑的第五次事故：把"整块替换"和"再改一处"混在同一次 `str.replace` 里** | 我写了一个"先 `s.replace(旧块, 新块)` 再 `cut()` 三处"的脚本：`replace` 用的是**只匹配第一处**的语义，而后续 `cut()` 的整块文本与它**重叠**（都在同一个 `TimelineOverview({…})` 区域）⇒ 三处删除把彼此的内容与**收尾大括号**一起吃掉，编译报出 `No value exists in scope for the shorthand property 'wiring'` 这种"看着我自己的占位符"的荒谬错。**处置**：从内存里的原文重建被吃掉的 `searchBar`，并把 `stats` 那一处按块重写。**规则（第五次了，这次写死）**：① 同一次编辑里**不要混用"整块替换"与"局部再改"**；② 每次编辑后**先用 `grep` 看关键锚点在不在**再编译；③ 报错里出现"我的占位符/我写的词"时，第一反应是"上一个脚本把文件改坏了"，而不是"编译器在胡说" |

**验证**：8 门禁 + fetch 门禁全绿；fixture 533 条 0 失败；entry ArkTS 编译 **0 error**；
`ConversationPane` **1093 行**（原 1414，P2-4/P2-5 两刀合计 −321），新增 `MessageRow` 221 行 / `ConversationHeader` 284 行；
全仓零使用 import 归零、零使用 `@Builder` 归零、宿主零使用成员归零。

### 15.4aa P2-6（第一刀）：把「搬迁留下死代码」变成门禁（E350）

前两轮（P4-5 / P2-5）里，同一类缺陷**反复出现且全都无感**：编译通过、界面正常、既有八门禁全绿，
而宿主体内躺着一批「零使用」的壳 —— 它们是**读代码时的陷阱**（看起来那里还画着 / 还在用）。

| # | 项 | 内容 |
|---|---|---|
| **E350** | **手工扫了三次的东西，必须变成门禁** | `tools/check-dead-code.mjs` 判三条：① 零使用 import；② 零使用 `@Builder`；③ 零使用组件成员（`@Prop`/`@State`/回调 prop/`private` 方法，只出现 1 次 = 只有那行声明）。三条都是「零使用」级别的硬事实，不做跨文件数据流分析（那会误报） |
| **E350b** | **它在当前树上立刻查出 3 处真死代码** | ① **`MessageFeedback.itemId`**：随反馈面板从宿主搬进来，**搬来之后一处都没读**（判定与回执都在父组件算好传入），而它的文档还写着「只用来做无障碍文案」——那句话也已过期；② **`MessageRow.menuHint`**：父组件一直在传（`contextMenuHintLabel(...)`），组件里从未读；③ **`SettingsPane.settingRow`**：P4-3 留下的「薄包装」，P4-5 把通用段 / 预设段搬进域组件后**它一个调用点都没有了**，而文档还写着「多个调用点」（读者会以为这里还在用）。三处已删；**删②的绑定还连带暴露了第四处**：`ConversationPane.inputModality()` 的唯一调用点就是那个绑定 ⇒ 方法本体也成了死代码，顺带清掉 |
| **E350c** | **门禁的第一版误报了三处，值得记下来** | 第一版把模板串整段抹掉再计数，于是 `Text(\`Host ${this.authority} · 连接 ${this.phase} · 逻辑流 ${this.streams}\`)` 里的 `authority`/`streams`、以及 `ForEach` 键 `${job.id}:${this.jobsTick}` 里的 `jobsTick` 全被判成「零使用」。**修法**：只抹普通引号串，**模板串保留 `${…}` 插值**——显示串里的成员是**真正的使用**；而「只被调试串读到的调用」（`this.record(\`… ${this.countConfiguredCredentials()}\`)`）仍会被报出，由声明行 `// dead-exempt: 理由` 豁免（这正是我们要的那条边界：**可观测性不该为门禁删掉**） |
| **E350d** | **门禁的证明方式：对「已知坏版本」归真命中** | 与 `check-builder-recursion` 同一条纪律——自检样例只证明「匹配我写的样例」。本轮额外做归真：`git show 1b8728f:…SettingsPane.ets`（P4-4 之前那版）→ 同一检测器命中 **5 处**真实死代码（`PluginNative` 那条**注释被当成名字**的旧误判、`states`、`onPickEffort`、`currentModelDetail`、`currentEfforts`）——这 5 处正是后来几轮里逐个手工删掉的 |
| **E350e** | **还修掉了检测器自己的一处误报根源** | `@Watch('onSessionChanged')` 引用的方法：剥掉注释与字符串之后，**装饰器里的方法名也没了** ⇒ 被 `@Watch` 驱动的方法会被判成「零使用」（本轮误报 2 处）。修法：`@Watch` 的名字从**原始文本**里取；`constructor` 加入框架名白名单 |

**验证**：**9 门禁** + fetch 门禁全绿（新增 `check-dead-code`；81 文件 / 判定 1855 处声明 / 0 违规；9 条注入式自检 + 归真命中）；
fixture 533 条 0 失败；entry ArkTS 编译 **0 error**；`ConversationPane` 1093 → **1087 行**、`MessageRow` 221 → 218 行、`MessageFeedback` 121 → 119 行。

### 15.4ab P2-6（第二刀）：一个回合的渲染整块搬出 + 思考块两处共用（E351）

| # | 项 | 内容 |
|---|---|---|
| **E351** | **"一个回合怎么画"是一件事，就该住在一个组件里** | 从 `ConversationPane`（1087 行）整块搬出 **`view/TurnView.ets`（492 行）**：回合骨架、过程分组、条目分派、思考折叠块、目标/任务卡、错误块、展开集合与它的两个方法、消息动作清单与反馈辅助（共 20 个成员）。父组件只剩"列表 chrome、滚动跟随、搜索、输入区"——`ConversationPane` 1087 → **984 行**（1414 → 984，P2-4…P2-6 三刀合计 −430） |
| **E351b** | **展开集合留在子组件，按钮在父组件 ⇒ 控制器对象（本仓第一次）** | 「全部展开 / 收起全部」按钮长在**会话头**（官方轨迹工具栏的位置），而要改的是**回合内部的**展开集合。三条路权衡：① 把 `expanded` 提成父 prop —— 父组件就得维护"哪些条目 id"，而它不渲染条目；② 父组件持子组件回调 —— 渲染时机与注册时机不同，容易拿到过期闭包；③ **控制器对象**（父组件持空壳、子组件在 `aboutToAppear` 注册实现）—— 与 ArkUI 的 `Scroller` 同源。选③；未挂载时两个方法是安全空操作 |
| **E351c** | **思考块两处出现 ⇒ 抽 `ReasoningRow`（83 行）** | 思考在**轨迹视图**是一条独立条目、在**对话视图**挂在过程分组里。此前前者只能用宿主 `@Builder`、后者画在回合里 ⇒ 两份呈现必然走样（`ToolCard`/`DeliverableCard`/`FileTreePane` 都是同一个理由抽出来的）。现在两处**共用 `view/ReasoningRow.ets`**；折叠默认收起、流式标题"思考中…"、拿不到时长就退回"思考过程"（**不编读数**） |
| **E351d** | **轨迹视图的"条目级"分派与回合级分派**是**两个**东西** | 轨迹视图是条目级台账（一条思考、一次工具调用直接列在时间线上，没有回合外壳），而 `TurnView` 的分派长在回合语境里（要用回合的展开集合与反馈归属）。故父组件保留一个 `flatItem`：它**共用卡片**（`MessageRow` / `ReasoningRow` / `ToolCard` / `SubagentCard` / `DeliverableCard`）但**分派逻辑各写各的** —— **共享的粒度是卡片，不是分派**（与 P3 各面板和过程流共用卡片同一条规矩） |
| **E351e** | **门禁当轮就抓出两处** | ① 搬完 `TurnView` 里留了一个**再没人调用的 `reasoningItem`**（已被 `ReasoningRow` 取代）——死代码门禁点名；② `TurnView` 的 `hitIds` / `fbRating` 两个 prop **搬过来就没有读者**（命中色条由父组件画、取向只写进中枢）——同一门禁点名。两处已删。**这就是上一轮把手工审计变成门禁的直接回报** |

**验证**：9 门禁 + fetch 门禁全绿；fixture 533 条 0 失败；entry ArkTS 编译 **0 error（`--clean`）**；
`ConversationPane` **984 行** + `TurnView` 492 + `ReasoningRow` 83；令牌棘轮再调紧 **10 文件/24 处 → 9 文件/23 处**。

### 15.4ac P2-7：会话头的上下文行（工作区 · 模型 · 最近活动）（E352）

官方 `conversation.header` 带一串上下文事实：Session title / Workspace context / Agent preset /
Model / Jobs / Goal / Schedule / Open in App。本仓此前只有标题（在**页头**里）与任务条 ——
**用户得回工作区列表才知道这场会话在哪个目录里跑**。

| # | 项 | 内容 |
|---|---|---|
| **E352** | **只做能确证的三项，其余如实留在缺口台账** | 本轮补 **工作区 · 模型 · 最近活动**：三项的数据都已在手（选中会话的 `cwd` / `updatedAt` 在 `SessionList` 投影里，模型名由 `modelSelectionLabel` 从已有 prop 派生）。**没做的三项各有明确原因**：`Agent preset`——中枢有预设名单但没有"**当前会话用的哪个**"；`Schedule` 与 `Open in App`——需要协议面/系统窗口语义，本仓没有对应端点 ⇒ **不猜、不摆假入口**（登记在 §6） |
| **E352b** | **规则放纯模型、拼装留视图（`model/SessionContext.ets`，72 行）** | 两条规则要能被断言，故做成零依赖纯函数：① `workspaceNameOf` 取路径**末两段**（同名目录会撞，`~/a/app` 与 `~/b/app` 得区分开；只有一段时不加前缀——`app` 比 `/app` 干净）；② `sessionContextLine` 三段拼装，**每段拿不到就不出现**（不留空的分隔符），**三段全空返回空串** ⇒ 界面整行不画。0 依赖的另一个理由与 `SettingsDomains` 相同：fixture 可直接执行它 |
| **E352c** | **"现在"由调用点给，不引入第二个定时器** | 时间那一段用 `relativeTime(updatedAt, Date.now())`：`now` 由视图在渲染时给 ⇒ 这一行随时钟自然更新；而任务条已经有一个每秒刻度（`jobsTick`），**不再加第二个定时器**——两个定时器各刷各的，只会让"谁在刷新"更难读 |

**验证**：9 门禁 + fetch 门禁全绿；fixture **533 → 547 条**（+14：工作区名 8 条 + 上下文行 6 条）0 失败；
entry ArkTS 编译 **0 error（`--clean`）**；`ConversationHeader` 284 → **318 行**、`ConversationPane` 984 → 991 行、
新增 `model/SessionContext.ets` 72 行。

### 15.4ad P2-8：三个浮层共用一个"结果出口"（E353）

这一轮本来是打算拆 `Index.ets` 的浮层（六个 sheet builder ≈ 960 行）。**动手前先读了一遍**，
结果查出比"文件太长"更要紧的东西：三个浮层**共用同一对字段**。

| # | 项 | 内容 |
|---|---|---|
| **E353** | **一个全局回执 + 一个全局忙标志被三个浮层读写 ⇒ 串浮层** | 凭据浮层 / 文本设置浮层 / 结构设置浮层都用 `credentialNote`（结果/进度文案）与 `credentialBusy`。**两条实测后果**：① **串浮层**——在凭据浮层里写入失败 → 关掉 → 打开「编辑文本设置」，**那条凭据的失败文案会出现在文本编辑浮层里**（用户会以为文本设置写坏了）；② **跨浮层置忙**——文本/结构浮层的按钮读的正是凭据浮层的忙标志，凭据在写时另一边也会变灰。**这与设置页那条"回执没有归属域"（E347）是同一个缺陷类**：一个全局字符串被多个域写、被多个界面读 |
| **E353b** | **修法①：回执带归属（`sheetNoteOwner`），只让主人显示它** | 打开任一浮层时**认领并清空**上一条；关闭时（含返回键两条路径）**交还归属**。判定做成零依赖纯函数 `model/Sheets.ets`（50 行）：`sheetNoteVisible(owner, kind)` / `sheetNoteText(owner, kind, note)`，**判据只有一条**——归属必须等于当前浮层种类；**归属为空（没人认领）一律不显示**（显示一条"不知道谁写的"错误比不显示更糟：用户会照着它去改一个无关的输入） |
| **E353c** | **修法②：每个浮层拿自己的忙标志** | 新增 `textBusy` / `structBusy`，并把两个浮层按钮的 `.enabled(...)` 从"读凭据的忙标志"改成"读自己的输入 + 自己的忙标志"。**规律**：一个字段若被 N 处写、被 M 处读，先问"它到底属于谁"——`credentialBusy` 这个名字本身就在提醒它属于凭据浮层 |
| **E353d** | **为什么这一轮没有直接拆那 960 行** | 拆分本身是**纯搬家**（不改行为），而这个缺陷是**真缺陷**。先把缺陷修掉再搬有两条好处：① 搬家时不必把"谁在读这个错字段"的糊涂账一起搬到新组件里；② 修完之后的三个浮层**各自自足**（自己认领、自己的 busy），本来就是可拆的前置条件。⇒ 下一轮的拆分因此是"零风险搬家" |

**验证**：9 门禁 + fetch 门禁全绿；fixture **547 → 558 条**（+11：归属判定 9 条 + 取值稳定 2 条）0 失败；
entry ArkTS 编译 **0 error（`--clean`）**；新增 `appstate/model/Sheets.ets` 50 行。

### 15.4ae P2-9：`Index.ets` 第一刀 —— 沙箱文件夹选择器（E354）

`Index.ets`（4480 行）是现在最大的单点：门户 + 七个浮层 + 宿主的四十多个处理器挤在一起。
本轮拆出其中**最自足的一个浮层**。

| # | 项 | 内容 |
|---|---|---|
| **E354** | **谁的状态谁持有：一个浮层的全部状态与动作一起搬** | `view/FolderPicker.ets`（297 行）带走"挑一个目录当工作区"的**全部**：当前路径、这一层子目录、提示、开合，以及读目录 / 进子目录 / 上一级 / 新建文件夹 / 选定五个动作（共 4 个状态 + 6 个方法 + 1 段 UI）。`Index.ets` **4480 → 4297 行** |
| **E354b** | **打开它的**两个**入口 ⇒ 又是控制器对象** | ① 设置里「工作区 → 选择文件夹」（选择浮层的一个选项）；② 系统文件夹选择器**不可用时的降级**入口（`PickOutcome` 失败 → 用沙箱目录兜底）。两处都只是"叫它打开一次并带一句说明为什么"，故用与 `TurnViewController` 同一个形状的控制器：宿主持句柄、组件 `aboutToAppear` 注册实现。**真值（是否开着）在组件内** |
| **E354c** | **控制器上必须有"关"——原生 Sheet 的关闭路径不经过组件** | 下拉 / 点遮罩 / 关闭按钮**都不会**通知组件，而宿主的 `closeSheet()` 要"一处复位全部"⇒ 控制器上加了 `close()`；`escapePressed()` 与 `folderOpen` 那条返回键路径也都改走它。**少了它，原生关闭之后组件内的真值仍是"开着"，浮层再也打不开** —— 这与本仓在 `bindContextMenu.onDisappear` 上踩过的坑是同一个（"原生关闭要复位"） |
| **E354d** | **沙箱根留在宿主（平台能力）** | `filesDirOf()` 要 `getContext(this)` + `UIAbilityContext`，那是**平台能力**；组件只吃一个 `sandboxRoot: string`。拿不到时**不打开**并给一句原因——进去看一个空列表比"点了没反应"更糟 |

**验证**：9 门禁 + fetch 门禁全绿；fixture 558 条 0 失败；entry ArkTS 编译 **0 error（`--clean`）**；
`Index.ets` **4480 → 4297 行**，新增 `view/FolderPicker.ets` 297 行。

### 15.4af P2-10：两个设置编辑浮层抽组件 + 输入提示进纯模型（E355）

`Index.ets` 第二刀。这一轮把"两个编辑浮层"与它们**唯一的判定逻辑**一起搬走。

| # | 项 | 内容 |
|---|---|---|
| **E355** | **两个浮层各自成组件；判定另放纯模型** | `view/SettingTextSheet.ets`（131 行）与 `view/SettingStructSheet.ets`（142 行）分别收走文本/数字项与 JSON 项的编辑 UI；而它们的**输入提示**（类型、范围、步长、正则、必填、当前值占位）搬进零依赖的 `appstate/model/SettingEditors.ets`。`Index.ets` **4297 → 4133 行**（P2-9/P2-10 两刀合计 4480 → 4133） |
| **E355b** | **同一句文案两处各写一份的隐患，顺手消掉** | 文本项与结构项都要显示"当前：X / 当前未设置"。此前两处的占位串各写一遍（结构项那句还写在宿主的一个私有方法里）⇒ 抽成 `placeholderOf`，两处**共用一句**（"共用呈现"的最小形态：两句文案也算） |
| **E355c** | **可空 item 比"造一个 20 字段的占位项"诚实** | 两个浮层都要求 `item` 必给。第一版让宿主在"没在编辑"时造一个占位 `SettingItem`——那要写 **20 个字段**，编译当场报 `arkts-no-untyped-obj-literals` 与三个枚举成员名不符。**处置**：改成 `@Prop item: SettingItem \| undefined = undefined` + `if (this.item !== undefined)` 早返回，并删掉那个占位常量。**理由**：宿主只有在真在编辑时才渲染这两个浮层（`sheetKind()` 会先返回 'text'/'struct'），占位值永远不会被看到；而 20 个字段的占位对象一旦与 `SettingItem` 的真实形状漂移就是静默的坑（本仓已见过多次"形状靠猜"的代价） |
| **E355d** | **边界照旧：草稿 / 回执 / 写入留在宿主** | 草稿留在宿主——它用它判断"浮层是否开着"（`overlayState.settingDraftOpen`）与返回键优先级；回执归属由宿主统一认领（E353 的 `sheetNoteOwner`）；`settings/mutate` / `unset` / 客户端校验都是宿主的副作用。组件只负责"这一层长什么样 + 用户改了什么"（与 `MessageRow` / `TurnView` 一脉相承） |

**验证**：9 门禁 + fetch 门禁全绿；fixture **558 → 570 条**（+12：数字/文本项提示 9 条 + 占位 3 条）0 失败；
entry ArkTS 编译 **0 error（`--clean`）**；`Index.ets` **4297 → 4133 行**，新增两个浮层组件（131 + 142 行）与 `model/SettingEditors.ets`。

### 15.4ag P2-11：凭据浮层 + 选项浮层抽组件；`Index` 破 4100（E356）

`Index.ets` 第三、四刀，一次做两个浮层（都很自足）。

| # | 项 | 内容 |
|---|---|---|
| **E356** | **一次搬两个浮层：`CredentialSheet`（127 行）+ `ChoiceSheet`（99 行）** | ① **凭据浮层**：三条真机实测的语义原样保留——输入框**显式 44vp 高**（E95：不给高度时真机上出现过"凭据值铺满整屏"）、明文**只在这一个请求里存在**（宿主提交时先清草稿再发请求）、**成功时才关面板**（`HubSnapshot` 没有凭据写入结果字段，浮层盖住列表会让用户以为没生效）。② **选项浮层**：单选列表、选中即提交（**没有"确定"按钮**——单选里再确认是多余的一道门），并保留"原生 Sheet 自己就是表面"这条修正（此前自造居中卡片，模型目录长时会溢出，E191）。`Index.ets` **4133 → 4019 行** |
| **E356b** | **抽完这两个，`sheetContent` 分派才真正可读** | 六个分支现在各是一句组件调用：详情（待拆）/ 选项 / 凭据 / 文件夹 / 文本设置 / 结构设置。此前它们是六段 60–180 行的内联 UI 挤在同一个 `@Builder` 里。**"谁在显示就渲染谁"这句话，现在只要看六行** |
| **E356c** | **死代码门禁连续两轮抓出"搬迁留下的导入"** | 这一轮它先抓出 `sheetNoteText` / `Sp`，修完又抓出多行 import 里的 `HarmonyType` / `Radius` —— 都是搬走 UI 之后宿主不再用的名字。**这正是当初把它做成门禁的理由**：这类残留编译不报、界面无感，但会让下一个人以为"宿主还在直接用这些" |

**验证**：9 门禁 + fetch 门禁全绿；fixture 570 条 0 失败；entry ArkTS 编译 **0 error（`--clean`）**；
`Index.ets` **4133 → 4019 行**（P2-9…P2-11 四刀合计 **4480 → 4019**），新增 `CredentialSheet` 127 行 / `ChoiceSheet` 99 行。

### 15.4ah P2-12（第一步）：右侧内容的门面定义（E357，**接线待下一轮**）

拆 `detailSheet` 时发现它只有一个 `RightbarShell({...})` —— **真正的问题不是那 36 行，
而是 `RightbarShell` 的两个挂载点各自把 16 个 props 拼一遍。**

| # | 项 | 内容 |
|---|---|---|
| **E357** | **同一个 shell 两处挂载、各自拼 props ⇒ 迟早不同步** | 两个挂载点：① 三栏的**真右栏**（`AppShell.detailColumn`，props 经 `AppShellFacade` 的十来个 `right*` 成员上来）；② **详情浮层**（`Index.detailSheet`，在页面根**内联重算** `deliverablesOf(this.trajectoryItems)` / `itemsOfKind(...)` / `buildRightFilesFacade()`）。只要有一处漏改（新加一个面板的数据忘了给浮层），就会出现**同一个面板在两种呈现下不一样**。这正是本仓反复抽共用组件的那条理由（`ToolCard` / `DeliverableCard` / `FileTreePane` / `ReasoningRow`） |
| **E357b** | **本轮只做"定义"：`export interface RightbarFacade`（12 个成员）** | 把两处**共有的**那 12 项（`panelId` / `panelLabel` / `panels` / `preview` / `files` / `deliverables` / `subagents` / `tools` / `trajectoryItems` / `stats` / `onSelectPanel` / `onAttachFile` / `onLineTap`）收成一个接口，并在注释里写清"每个挂载点只提供**它自己那份**（`presentation` / `widthVp` / `sections` / `onClose`）"。**接线（把 `RightbarShell` 改成吃门面、两处各传一份）留到下一轮** —— 它要动 `AppShellFacade` 的十来个成员与两处调用点，与"定义"分开提交更安全，也让下一轮的改动面一眼可数 |
| **E357c** | **顺手记一条 ArkTS 硬约束（第三次遇到同类）** | 我把接口块插在了**已有的 `import` 之后**，于是文件里出现了"后面的 import" ⇒ 编译报 `arkts-no-misplaced-imports`（import 必须先于其它语句），而 `FileTreeFacade` 的类型导入我又多插了一份 ⇒ `Duplicate identifier`。**规则**：往已有文件里插"类型/接口块"时，锚点要选**第一个 `@Component` 之前**，并先 `grep` 一遍要用的类型是否已经导入 |

**验证**：9 门禁 + fetch 门禁全绿；fixture 570 条 0 失败；entry ArkTS 编译 **0 error（`--clean`）**。
**本轮没有减少 `Index.ets` 的行数**（4019 行不变）—— 这一轮的产出是**接口与契约**，不是搬家；
下一轮才是把两处调用点接到它上面。

### 15.4ai P2-12（第二步）：门面接线完成 —— 两处挂载点共用一份（E358）

上一轮只定义了 `RightbarFacade`，这一轮**接上**。

| # | 项 | 内容 |
|---|---|---|
| **E358** | **12 个 props → 1 个门面；`AppShellFacade` 的 11 个 `right*` 成员全删** | `RightbarShell` 的 12 个内容 props（`panelId` / `panelLabel` / `panels` / `preview` / `files` / `deliverables` / `subagents` / `tools` / `trajectoryItems` / `stats` + 三个回调）收成一个 `@Prop f: RightbarFacade`；**组件内 39 处用法**改成 `this.f.X`。`AppShellFacade` 里透传的 11 个 `right*` 成员与 `onSelectRightPanel` / `onAttachWorkspaceFile` **全部删除**，换成一句 `rightbar: RightbarFacade` |
| **E358b** | **两处挂载点现在都调同一个构造器** | `Index.buildRightbarFacade()` 是唯一投影点：三栏真右栏（`buildShellFacade` → `AppShell.detailColumn`）与「详情」浮层（`detailSheet`）都调它。**浮层那一份此前是内联重算**（`deliverablesOf(...)` / `itemsOfKind(...)` / `buildRightFilesFacade()` 各写一遍）—— 这正是"新加一个面板很容易只给一处"的来源，现已消除 |
| **E358c** | **删掉一处重复的复制实现** | 浮层里的 `onLineTap` 自己写了一遍"`text.length > 0` 才复制"的判断；真右栏那一份走的是同一个回调。收进门面后两处**只有一份**（与 `placeholderOf` / `ReasoningRow` 同一类收口） |
| **E358d** | **行数的诚实账**：`Index.ets` 4019 → **4026**（+7） | 本轮**没有变小**：新增了门面构造器（≈34 行），删掉的是 `buildShellFacade` 里的等价块（≈15 行，但原本是"透传"而非"投影"）。**收益不在行数上，而在"两处只有一个真值"** —— 若只看行数，会误判这一轮白做 |

**验证**：9 门禁 + fetch 门禁全绿；fixture 570 条 0 失败；entry ArkTS 编译 **0 error（`--clean`）**；
`RightbarShell` 451 → **444 行**、`AppShell`（shell 层）**389 行**、`Index.ets` **4026 行**。

### 15.4aj P2-13：右栏切换器进单栏 Sheet —— 补上 P3 登记的第三个缺口（E359）

P3 收口时登记过三个缺口：**侧边面板滑入动画**、**面板自身拖拽调宽**、**单栏 Sheet 里的切换器**。
这一轮补**第三个** —— 它是三者里唯一"功能不可达"级别的（另外两个是观感项，且必须真机验收）。

| # | 项 | 内容 |
|---|---|---|
| **E359** | **单栏用户此前根本切不到别的右栏面板** | `RightbarShell` 的面板切换器只画在 `framedBody`（真右栏 / 侧边浅层面板）里，而**单栏的详情 Sheet** 走的是 `sheetBody` —— 那一份只画了一行标题。于是手机用户打开详情后**无法切到文件 / 预览 / 工具 / 子代理 / 交付物 / 轨迹**：要么先关掉再想别的办法，要么根本不知道还有别的面板。修法：把切换器抽成**共用的 `@Builder panelSwitcher()`**，两处都调（与 `ToolCard` / `ReasoningRow` 同一条"两处呈现共用一份"的规矩） |
| **E359b** | **顺手拆掉一句会撒谎的提示** | Sheet 那一行原本是 `NativeSectionTitle({ text: 面板名, hint: '工具 / 子代理 / 交付物 / 目标 / 任务' })` —— 那句 hint 对**文件 / 预览 / 详情**三个面板是**错的措辞**（界面不撒谎是本仓 D3 §4 的纪律）。切换器的**当前项高亮**本来就承担"我在看哪个面板"，故整行换成切换器：信息不减，谎话去掉 |
| **E359c** | **为什么这一轮不做另外两个缺口** | **滑入动画**与**拖拽调宽**都属观感，且都要真机验收（动画时长/手感、拖拽命中区）。本仓纪律："无真机时允许开发完成，不允许宣称最终设备验收完成" ⇒ 在没有设备的情况下做这两项，只能产出"看起来实现了但没人验证过手感"的代码，反而把 D 系列缺口从"待做"变成"待验都在验什么"的糊涂账。它们继续留在缺口登记里，等设备通道打开再做 |
| **E359d** | **一个检测器的边界被顺手确认** | `NativeSectionTitle` 在这处改动后彻底没人用，但**死代码门禁没有报它** —— 因为门禁把"import 行里出现的名字"排除在计数之外（避免把导入自身当成使用），于是"最后一个使用点被删掉"这一类需要**导入名与使用名的差分**才能抓到。本轮是人工 `grep` 发现的。**规则**：删掉某组件的最后一个使用点时，顺手 `grep` 一遍它的导入并摘掉 |

**验证**：9 门禁 + fetch 门禁全绿；fixture 570 条 0 失败；entry ArkTS 编译 **0 error（`--clean`）**；
`RightbarShell` 444 → **461 行**（切换器抽成共用 builder + 两处调用 + 说明）。

### 15.4ak P2-14：`activeOverlay` 是一套"只有 fixture 在用"的状态机（E360）

这一轮从 `tools/audit-unused-exports.mjs`（"导出了但 entry 从未引用"清单）入手，查到一个
**比"没接线"更微妙**的情况。

| # | 项 | 内容 |
|---|---|---|
| **E360** | **`NavigationState.activeOverlay` / `Overlay` / `openOverlay` / `closeOverlay`：真实界面从不读它** | 探针读数：`activeOverlay` 只在 `NavigationState.ets` 内出现 6 次（**声明 1 + 初始值 1 + `copyOf` 拷贝 1 + 两个 setter + 字段**），`entry` 侧 **0 次**；`Overlay` 枚举同样只在同一文件与 barrel 里。**真相**：`Index.overlayState()` 是从**真实状态**（`credentialRef.length > 0` / `textSettingDraft` / `searchOpen` / `choosing !== undefined` / `detailOverlayOpen` / `preview !== undefined`）**派生**的，浮层优先级由 `decideBack` 吃那六个布尔。于是 `activeOverlay` 这套字段既没人写、也没人读 —— **永远停在 `Overlay.NONE`** |
| **E360b** | **它为什么"看起来还活着"：fixture 里有 4 条断言在测它自己** | `check-layout-fixtures.mjs` 里有 `初始没有浮层` / `浮层打开` / `浮层关闭` 三条（外加一条 import）**只**调 `openOverlay` / `closeOverlay` 并读 `activeOverlay`。**这是自证循环**：断言证明了"这两个函数按自己的定义工作"，而不是"界面的浮层行为正确"。⇒ **宁可少三条断言，也不要一套与真实界面平行的状态机** —— 平行的东西一旦漂移，读者会以为它是真的（本仓 E347/E353 都是"两个地方各说一套"的后果） |
| **E360c** | **处置：删掉，并把"少了三条断言"记在明面** | 删 `Overlay` 枚举、`activeOverlay` 字段、初始值、`copyOf` 拷贝、两个函数；barrel 去掉 `Overlay as NavOverlay` 与两个导出；fixture 删那 4 处（断言 570 → **567**）。**断言数变少是这一轮的预期结果，不是回归** —— 少掉的三条测的是一套不存在的机制 |
| **E360d** | **同一份清单里还有下一个同类候选（本轮不动，登记）** | `NavigationState.sidebarExpanded`：初始化为 `true`、`copyOf` 会拷、有 `setSidebarExpanded`，但**没有任何消费者**（`sidebarOccupiesLayout` 那个纯函数同样零引用）。它与 `activeOverlay` 是同一模式（字段 + setter + 无人读）⇒ 要么接成"侧栏收成 rail"的真功能，要么同样删掉；**需要先决定产品语义**，故不在本轮顺手删 |

**验证**：9 门禁 + fetch 门禁全绿；fixture **570 → 567 条**（删掉 3 条自证断言 + 1 处导入）0 失败；
entry ArkTS 编译 **0 error（`--clean`）**；`NavigationState.ets` 净减 ≈25 行。

### 15.4al P2-15：侧栏终于能收起 —— 给 `sidebarExpanded` 接上控制点（E361）

上一轮（P2-14）把"只有 fixture 在用"的 `activeOverlay` 删掉了，同一份清单纯里还留着一个候选：
`NavigationState.sidebarExpanded`。这一轮查清了它的性质 —— **它不是冗余字段，而是一个缺失的功能**。

| # | 项 | 内容 |
|---|---|---|
| **E361** | **同一个模式，两种结论：`activeOverlay` 该删，`sidebarExpanded` 该接** | 两者都是"字段 + setter + 无人读"，但**产品语义完全不同**：`activeOverlay` 描述的浮层优先级在 `Index.overlayState()` 里已经**有真值**（`decideBack` 吃那六个布尔）⇒ 它是平行副本，删；`sidebarExpanded` 描述的是"侧栏展开还是收成 rail"，而本仓的侧栏呈现**完全由形态决定**（`shellTracksOf(mode)`：单栏浮层 / 双栏 rail / 三栏 panel）⇒ **官方 AppFrame 那个"收起侧栏腾出宽度"的动作在本仓做不到**。这是功能缺口，不是字段冗余。**规律**：见到"字段 + setter + 无人读"，先问"它描述的东西别处有没有真值"——有就删，没有就是缺控制点 |
| **E361b** | **判定放纯函数：形态给默认、用户给偏好（`sidebarPresentationOf`）** | 两条规则，都很保守：① **单栏一律浮层**（那个档位没有"展开的侧栏"，用户要的是抽屉 ⇒ `expanded` 在单栏**不生效**，且按钮也不给——给了就是假入口）；② 其余形态跟随偏好（双栏默认 rail、展开即 panel；三栏默认 panel、收起即 rail）。`sidebarOccupiesLayout` 也随之改成吃 `(mode, expanded)`。**+7 条 fixture** |
| **E361c** | **"收起"必须是双向门** | 品牌行加「收起侧栏」（`chevron_left`），rail 顶部加「展开侧栏」（`chevron_right`）—— 少了后者，"收起"就是一道**单向门**（用户只能重启应用）。图标刻意用本仓**已在用的 chevron 对**而不是某个"side bar"符号：符号名不存在时不报错、只是画不出来，这个坑本仓踩过 |
| **E361d** | **顺带把这条功能钉进接线门禁（第 18 项）** | 它正是"接线断了不会有症状"的那一类（字段在、模型在、界面没有控制点）。新门禁项钉三段：纯函数判定（`sidebarPresentationOf`）+ 门面开关（`onToggleSidebarExpanded`）+ 视图控制点（`showExpandToggle`） |

**验证**：9 门禁 + fetch 门禁全绿；fixture **567 → 574 条**（+7：三栏/双栏/单栏的展开与收起 + 收起后仍占宽度）0 失败；
entry ArkTS 编译 **0 error（`--clean`）**；功能接线门禁 17 → **18 个功能**；`SidebarShell` 308 → **354 行**、`AppShell` 389 → **411 行**。

### 15.4am P2-16：「新建会话」在 rail 与底部标签里此前没有入口（E362）

上一轮让侧栏能收起之后，顺手查出**收起之后的一个直接后果**：

| # | 项 | 内容 |
|---|---|---|
| **E362** | **`＋ 新建会话` 只画在 `panelBody` 里 ⇒ 收起侧栏就开不了新会话** | `SidebarShell` 有三种呈现（PANEL / RAIL / OVERLAY），而「新建会话」这个一级入口**只在 PANEL 那一份**（品牌行下方）。于是：① 三栏收起侧栏 → rail 里没有它；② 单栏（手机）走 `tabsBody` → 底部标签栏里也没有它，用户只能靠**空态里那个按钮**或 Ctrl+N（还要设备有键盘）。而官方侧栏**不论宽窄**都把 New Session 当一级入口 ⇒ 这是"信息架构在窄形态下缺项"，不是观感问题 |
| **E362b** | **补的位置与理由** | rail：放在「展开侧栏」按钮**下面** —— 两者都是"轨道级"动作（不属于任何面板席位），排在一起读起来是一组；底部标签：**排在第一位**（官方在手机上把这个动作放在导航面首项）。两处都用 `sys.symbol.plus_circle`（本仓已在用的符号，不猜新名字） |
| **E362c** | **口径修正** | 组件头部的职责表原本写"New Session｜品牌行下方的 `＋ 新建会话`" —— 那句话在三种呈现下**只有一种成立**。已改成"**一级入口**：展开面板时在品牌行下方，rail 与底部标签栏里也各有图标入口"，并把 E362 的来由写在旁边 |

**验证**：9 门禁 + fetch 门禁全绿；fixture 574 条 0 失败；entry ArkTS 编译 **0 error（`--clean`）**；
`SidebarShell` 354 → **396 行**，`onNewSession()` 调用点 1 → **3 处**（三种呈现各一）。

### 15.4an P2-17：侧栏收起状态落盘（E363）

上一轮让侧栏能收起、这一轮补上"下次启动还记不记得"。做法照抄**详情栏宽度记忆**那一套
（`LocalPrefs` + 启动时读回 + 变更时落盘），但多了一个三态问题。

| # | 项 | 内容 |
|---|---|---|
| **E363** | **"没存过"与"存过 false"必须分得开** | 侧栏展开状态的**默认是展开**，而偏好里没有记录时如果直接读成 `false`，就会把"从没设置过"变成"收起" —— 用户第一次启动看到的是一条 rail。故 `KEY_SIDEBAR_EXPANDED` 存的是 `'true'` / `'false'` **字符串**，用 `has()` 判存在，**缺失即 `undefined`**。本仓已有两种同类做法（`loadThemeMode` 用字符串枚举、`loadDetailWidth` 用 `has()`）—— 这次明确写成"三态" |
| **E363b** | **默认值放纯模型，不放在偏好层**（**已被 P5-2 修订**：默认不再是常数 `true`，改为按形态给 ⇒ 见 §15.4ap）| `LocalPrefs` 只回答"偏好里有没有一个值"（`boolean \| undefined`）；"没存过时该展开"是**产品语义** ⇒ 新增 `sidebarExpandedOf(stored)`（零依赖纯函数，`undefined ⇒ true`）。好处与 `SettingsDomains` / `SessionContext` / `Sheets` 相同：可被 fixture 直接钉住，不依赖真机上的 preferences |
| **E363c** | **落盘失败不弹提示（与宽度记忆同类）** | 详情栏宽度落盘失败时本仓给了一句轻提示（"这次没能记住：下次打开会回到默认宽度"）。侧栏状态**不给**：它下次启动只是回到默认展开，**没有信息损失**，为它打断用户不划算。这条差别是**有意**的，写在代码旁边 |

**验证**：9 门禁 + fetch 门禁全绿；fixture **574 → 577 条**（+3：没存过 ⇒ 展开 / 存过 true / 存过 false）0 失败；
entry ArkTS 编译 **0 error（`--clean`）**。

### 15.4ao P5-1：核心页投影搬进 appstate（E364）

`Index.ets` 里 `pluginInventoryFact` / `corePluginRows` 这两个**纯投影**（宿主报告 → 核心页的事实与行）
长期住在页面里，而它们一行 UI 都不碰。本轮搬到 `appstate/model/CoreProjection.ets`，
顺手撞上一条 ArkTS 硬规则。

| # | 项 | 内容 |
|---|---|---|
| **E364** | **跨层契约不能靠"形状相同的接口"** | 第一版按结构声明了 `PluginInventoryReport`，把宿主的 `HostPluginReport` 直接传进去 —— 编译**红**：`Structural typing is not supported (arkts-no-structural-typing)`（两处调用点各一条）。ArkTS 里"字段一样"**不等于**可赋值：接口之间只认 `extends`。出路只有两条 —— 让 `appstate` 反向依赖宿主实现（**方向是反的**，`hostruntime` 连 `oh-package.json5` 的 `dependencies` 都是空的），或者让调用点把用到的字段**逐个取出来**。选了后者 |
| **E364b** | **接口降级为"参数分组"，两侧谁也不必认识谁** | 改后的签名是 `pluginInventoryFact(note, version, totals, nativePackageCount)` + `pluginRowOf(id, name, nativeKind, disabled)` + `rankPluginRows(rows)`。`PluginInventoryTotals` 仍留着 —— 但不是"共享契约"，而是**避免 7 个位置参数写错顺序**。调用点用显式类型的对象字面量填它（ArkTS 允许字面量赋给接口），逐字段取值，**没有任何结构化赋值** |
| **E364c** | **mapper 也一并搬走，页面只剩搬运循环** | `pluginRowOf` 把"清单行 → 界面行"的映射（含 `pluginNativeOf` 那条**故意的**字符串换算）收进纯函数；`rankPluginRows` 只排序不隐藏，且**返回新数组**（`slice()` 后 `sort`）—— 调用点那份仍保持清单原顺序，避免"排序顺手改了别人的数组"这类下次才会咬人的副作用 |
| **E364d** | **顺带把棘轮调紧** | `Index.ets` 少了一批裸值，设计令牌门禁从 23 处 / 9 文件降到 **21 处 / 8 文件**，跑 `--update-baseline` 把棘轮拧到位（棘轮只许变少，不主动调紧就等于白降） |

**验证**：9 门禁 + fetch 门禁全绿；fixture **577 → 595 条**（+18：清单文案三态 / 行映射四项 / 排序含"不改原数组"）0 失败；
entry ArkTS 编译 **0 error**（第一版红过一次，见 E364）；`Index.ets` 4061 → **4027 行**。
`rankPluginRows` / `pluginRowOf` 这类"页面里长出来的纯投影"是本仓最值得继续搬的一类 —— 判据是
**它是否 import 了 ArkUI**，而不是"它有多长"。

### 15.4ap P5-2：侧栏轨道的几何跟随实际呈现（E365）

上一轮把"侧栏能收起"接通了（P2-15）、也把偏好记住了（P2-17）。这一轮去查"收起之后到底发生了什么" ——
结论是**只有呈现变了，几何没变**：三栏收起侧栏后轨道照样占 240vp，所谓"腾出宽度"一次都没发生。

| # | 项 | 内容 |
|---|---|---|
| **E365** | **"改呈现"不等于"改几何"** | `AppShell.navPanel()` 传的是 `Sz.NAV_PANEL` 常量，而 `SidebarShell` 的 rail 宽度取的是**布局决策**里的 `navWidthVp`（按形态算：三栏 240 / 双栏 56 / 单栏 0）。两者都不看"用户收起了没有" ⇒ 三栏收起 = 240vp 轨道里画一条 rail；双栏收起 = 240vp 轨道里放 56vp 内容，右边空出 184vp 底色。**凡"某状态改变了外观"，必须同时问"它该改变几何吗"** |
| **E365b** | **两个宽度是两个问题，不许合并** | 决策里的 nav 宽度回答"按形态该留多少"，让步链（`decideLayoutWithDetail`）与拖拽夹取（`detailRoomOf`）**必须**用同一个值，否则夹出来的宽度会被决策再收窄一次、拖动时跳变；侧栏轨道实际多宽是另一件事。故新增 `sidebarTrackWidthOf(mode, stored)` 专管后者，页面只读结论；详情栏可用空间仍按前者算（偏保守只会让栏窄一点，不会重叠） |
| **E365c** | **"没存过"的默认必须按形态给，不能是一个常数** | P2-17 定的规则是 `undefined ⇒ true`（展开）。可 `shellTracksOf(DOUBLE).sidebar` 是 **RAIL**、四形态 fixture 登记的也是 `nav: 'rail'` ⇒ 双栏首启被读成"展开"，画出一个 240vp 的面板，把 600vp 窗口的主区挤到 360vp。`sidebarExpandedOf(stored)` 只有一个参数，**表达不了"默认随形态变"** ⇒ 换成 `sidebarExpandedForMode(mode, stored)`。**规律**：一个"默认值"如果需要上下文，它的签名里就必须有那个上下文 |
| **E365d** | **偏好搬出 `NavigationState`** | 生效值既然随形态变，就不能把解析后的 `boolean` 存进导航状态（窗口一变它就过期）。故删掉 `NavigationState.sidebarExpanded` / `setSidebarExpanded`，真值只剩页面里那份**原始**偏好（`boolean \| undefined`），生效值每次渲染派生。顺带清掉文档表里两行早就不存在的字段（`activeOverlay` 是 E360 删的，文档没跟上） |
| **E365e** | **`sidebarOccupiesLayout` 删掉** | 它和 `sidebarTrackWidthOf(...) > 0` 回答同一个问题，而它自己**只有 fixture 在用**（P2-15 留下的自证循环苗头）。合并成一个函数后，断言从"是否占布局"改成"占多宽"，信息量更大 |
| **E365f** | **`SidebarShell` 的 rail 宽度归它自己** | railBody 里四处 `.width(navWidthVp)` 用的其实是"rail 有多宽"，而它一直由**调用方**按形态猜一个数传进来。改成直接用 `Sz.NAV_RAIL`（同一个常量已经在 tabsBody 的高度上用着），`navWidthVp` 这个 prop 与门面字段一起删掉 —— **组件自己的几何不该由外部按形态猜** |

**验证**：9 门禁 + fetch 门禁全绿；fixture **595 → 601 条** 0 失败（新增：三态取值按形态四条 / 轨道宽度六条，
删掉 3 条"占不占布局"+1 条重复断言）；entry ArkTS 编译 **0 error**（第一版红过一次：门面新增的箭头函数
没写返回类型，`arkts-no-implicit-return-types`）。
**真机验收**：D19 补一条判据（轨道宽度必须跟着呈现变），本地证明不了"看得见"。

### 15.4aq P5-3：双栏下的「展开侧栏」是个死按钮（E366）

上一轮把"呈现"与"几何"接上之后，这一轮去数"**呈现判定到底被几个调用点问了**" ——
答案是一半：`buildTriple` 问了，`buildDouble` 没问。

| # | 项 | 内容 |
|---|---|---|
| **E366** | **"判定写对了"不等于"每个挂载点都问了判定"** | `buildDouble` 调的是 `navRail()`，那份 surface 把呈现**写死**成 `TrackPresentation.RAIL`。于是双栏下点「展开侧栏」：偏好变了、纯函数判定也变了、`sidebarTrackWidthVp()` 也跟着变 —— **可唯一那个绘制侧栏的调用点根本没问判定**，rail 照画不误。用户看到的是一个**没有反应的按钮**，而 D19 第 2 条（"双栏展开 ⇒ 变完整面板"）在真机上必然失败 |
| **E366b** | **同一个表面有两份实现，走样只是时间问题** | `navRail()` 与 `sidePanelSurface(...)` 逐字段相同，只差 `presentation` 与宽度来源 —— 也就是说它**是** `sidePanelSurface(sidebarPresentation(), sidebarTrackWidthVp())` 的手抄版。删掉 `navRail()`（−31 行），双栏改调 `navPanel()`：一份 surface，一个真相 |
| **E366c** | **这类缺陷正面计数拦不住，要加"反面规则"** | `check-feature-wiring` 只能表达"某特征至少出现 N 次"，而这里的问题是**多了一个不该有的东西**（硬编码的呈现常量）：该在的特征（`sidebarPresentationOf` 的定义与调用）全都在。故新增 `FORBIDDEN` 段：`AppShell.ets` 里不许出现 `TrackPresentation.RAIL`。**为什么只拦 RAIL** —— 它在本仓**永远是判定的结果**（形态默认或用户收起），写常量就等于绕过判定；而 `PANEL`（手机抽屉）与 `OVERLAY`（底部标签）在 `AppShell` 里是**结构上固定**的表面，写常量是对的。门禁**先剥注释**再匹配：本轮留下的说明文字里就写着那个常量名 |
| **E366d** | **门禁必须先在已知坏版本上红过一次** | 新规则写完立刻用 `git show HEAD:…AppShell.ets`（修前那份）回灌跑一遍：**命中 `AppShell.ets:228`**；换回修好的版本即绿。这是本仓从 E343 起的老规矩——没在坏版本上红过的门禁不算门禁 |

**验证**：9 门禁 + fetch 门禁全绿；fixture **601 条** 0 失败（本轮不改纯函数，改的是调用点）；
entry ArkTS 编译 **0 error**；`AppShell` 420 → **397 行**。
**真机验收**：D19 第 2 条补一句"这一条本地曾查出一处硬编码导致按钮完全无效"。

### 15.4 已知的迭代产物（唯一待清理项）

- `protocol-enum.mjs` 与 `protocol-enum2.mjs`：**后者是前者的修正版**（v2 修了 v1 的两个盲区：命名空间不在同一文件、以及另一处漏枚举）。
  ⇒ `protocol-enum.mjs` 属**被取代的迭代产物**，标记为待确认无引用后删除——**本轮不动**（先确认引用，避免"先拆后搬"的老毛病）。
### 15.5 本轮实跑发现（E217）

**① 架构门禁 `arch-check.mjs`：✅ 无违规** —— 扫描 64 个文件（connection / appstate / platform / entry），
除 `dshcompat` 外**没有任何模块持有上游端点名/事件名字面量**。这说明本项目"上游细节只允许出现在 dshcompat"的架构主张**当前是成立的**（也说明近期新增的端点助手都走对了位置）。

**② 上游漂移门禁 `compat-drift.mjs`：❌ 当前环境跑不起来**（两个原因）：
- `protocol-contract.mjs` 第 24–25 行的兜底路径**硬编码了别的用户名**（`C:\Users\aotian\…`）；虽有 `DSH_NODE_MODULES` 可覆盖，但
- 设了 `DSH_NODE_MODULES` 指向本仓库上游后，生成器**仍未产出** `.research/protocol/contracts.json`（无输出、无文件）。

⇒ **结论**：漂移检测这条门禁**目前是坏的**，意味着"端点表与上游一致"**没有自动保障**（现有证据是历史上生成过一次 + 逐条人工取证，如 E159/E182/E210）。
**待办**：修 `protocol-contract.mjs`（去掉硬编码用户名、给出清晰的失败信息），再把 `contracts.json` 纳入生成流程——**它是"方便适配今后 DSH 上游升级"这条需求的核心机制**。

**③ 导出审计 `audit-unused-exports.mjs` 给出 128 个"近零引用"符号**（这是它的正常产出，不是错误），其中一批 `(0)` 引用值得逐个看，因为它们往往是**"通道有、没接"**的信号：
`SESSION_FEEDBACK_RECORD_ENDPOINT`（消息反馈的写入）、`WORKSPACE_FILES_CHANGES_ENDPOINT` / `WORKSPACE_FILES_STAT_ENDPOINT`（文件变更/状态）、`PROJECTION_INBOX`（收件箱投影）、`evaluateCapability` / `allCapabilityIds`（能力评估）、`upstreamIdentity` / `UPSTREAM_IDENTITY`（上游身份）等。
**注意**：审计统计的是 `entry`（UI）的引用 ⇒ `(0)` 表示**界面没用到**，不代表中枢没实现；下一步应逐个核对"中枢是否已实现、只差接线"，与 E198/E199 的教训一致（先读代码再下结论）。
### 15.5b `.research/` 的构成与清理结论（E237）

`.research/` 是**工作区**（已在 `.gitignore`，**0 个跟踪文件**），但它一度累计到 **26,030 文件 / 311.9 MB**。本轮逐块核对：

| 子目录/文件 | 规模 | 判定 |
|---|---|---|
| **`corebuild/`** | **25,743 文件 / 293.1 MB** | ❌ **纯过程产物**：内容是 `node_modules` + `package*.json`（为组装核心做的一次 npm 依赖树）。核对结果：**没有任何工具/文档引用它**、最后改动在两天前 ⇒ **已删除**（可重建：需要时重新 npm install） |
| `protocol/` | 2 文件 | ✅ **保留**：`contracts.json`（43 KB）是**漂移门禁的输入**——删了 `compat-drift` 就跑不了 |
| `ref-harmony/` | 266 文件 / 18.7 MB | ✅ 保留：鸿蒙参考材料（开发查证用，体积不大） |
| `probe-node/`、`wsl/`、若干 `*.ps1`、`commit-msg.txt` | < 0.1 MB | ⏸ 保留（体积极小；`*.ps1` 是早期一次性排查脚本，删与不删都无影响） |

**清理后**：`.research/` 从 311.9 MB 降到 **18.8 MB**（287 文件），且：
- `compat-drift` 复跑仍 **✅ 无漂移（84/84）**（证明没误删门禁输入）；
- `git status` 仍然干净（该目录本就不入库）。

> **这次的判断顺序值得记住**：删之前先做三件事——**谁会用它**（全仓搜引用）、**它是什么**（看内容：npm 树 ≠ 源码）、**多久没动**（两天前）。
> 三条都指向"过程产物"才删；而 `protocol/` 只因为**是门禁输入**就保留——**体积大不是删的理由，没人依赖且可重建才是**。
### 15.6 漂移门禁已修复并跑通（E221）

`compat-drift.mjs` 此前报"缺少契约数据"、`protocol-contract.mjs --json` 又**静默不写文件**。两个根因都已定位并修掉：

| 根因 | 修法 |
|---|---|
| 生成器的兜底路径**硬编码了别的用户名**（`C:\Users\aotian\…`）⇒ 换机器必然找不到上游、门禁**静默失效** | 改为按**当前用户**推导，并在找不到上游时**明确失败**（附如何用 `DSH_NODE_MODULES` 指定） |
| `--json` 的目标目录 `.research/protocol/` **不存在** ⇒ 写入失败但**不报错**（"看起来跑了其实没数据"） | 先建目录再生成；生成后 43 KB 契约文件落盘 |

**跑通后的结论**：

```
当前环境核心包   : @deepseek-ai/dsh 0.1.5-rc.2
已提交基线核心包 : 0.1.5-rc.2      采集时间 2026-09-11T13:10:38Z
端点数量         : 期望 84 / 基线 84
✅ 无漂移：当前上游与已提交的兼容面上表一致。
```

⇒ **"端点表与上游一致"现在有自动保障**（此前只有历史一次生成 + 人工取证）。这也正是"方便适配今后 DSH 上游升级"这条需求的核心机制：**升级后先跑这条门禁，漂移点会直接列出来**。

**顺带得到的能力**：`protocol-contract.mjs` 现在能随时输出 84 个端点的**权威参数表**——本轮就是靠它发现并修掉了 `commands/execute` 的字段错误（E219）。**今后新增端点前应先跑它**，而不是按"同族"推断。
### 15.7 已实现端点的契约审计结果（E223）

用 `protocol-contract.mjs`（84 端点权威参数表）+ 上游 `types.d.ts` 的 REQUEST 请求体定义，把**本会话新增的每个端点载荷**逐个核对：

| 端点 | 我发的载荷 | 权威契约 | 结论 |
|---|---|---|---|
| `commands/list` | `{agentId}` | `agentId` | ✅ |
| `commands/execute` | 原 `{sessionId,name}` | `{agentId, line, submittedAttachments}` | ❌→✅ **已修（E219）** |
| `session/selectModel` | 原扁平四字段 | `{sessionId, selected: ModelSelection}` | ❌→✅ **已修（E222）** |
| `skills/list` | `{sessionId}` | `SkillListRequest = { sessionId }` | ✅（E174 的修复正确） |
| `agentPresets/list` | 无参数 | 无参数 | ✅（真机返回真实名单） |
| `agentPresets/copy` | `{from, id, name}` | `from, id, name` | ✅ |
| `agentPresets/deletePreset` | `{id}` | `id` | ✅（真机验证过宿主拒绝随附预设） |
| `agentPresets/read` | `{agentPreset}` | `agentPreset` | ✅ |
| `llm/listProviders` / `listConfigurableProviders` | 无参数 | 无参数 | ✅ |
| `workspace/delete` | `{workspaceId}` | `WorkspaceDeleteRequest = { workspaceId }` | ✅ |
| `workspace/archiveSession` | `{sessionId}` | `WorkspaceArchiveSessionRequest = { sessionId }` | ✅（真机验证可用） |
| `workspace/create` | `{path}` | `WorkspaceCreateRequest = { path }` | ✅ |

**结论**：本会话新增的端点载荷**全部经过权威契约核对**，两处不符已修正。

> **纪律（并入第 7 条）**：**新增任何端点之前，先跑 `node tools/protocol-contract.mjs`** 取该端点的参数表；
> REQUEST 形态的还要查上游 `types.d.ts` 里的请求体字段。**不要按"同族端点"推断字段名，也不要照抄自己别处的命名**——
> 本会话就因此写错过两次（`commands/execute` 的字段名、`selectModel` 的嵌套层级），而两次都是被这套契约工具抓出来的。
> 设备不在时，这是**唯一**能提前发现"字段不符"的手段。
### 15.8 已登记但尚未接线的通道（E225）

`audit-unused-exports.mjs` 报 128 个"近零引用"符号。逐个核对**中枢是否真的在用**（用则只差接线，不用则属"已登记未接"）：

**核对方法**：统计符号在 `appstate`（中枢）与其他模块的出现次数。**其他模块的 2 次 = 定义 + 导出** ⇒ 那种情况说明**除登记外无人使用**；若 `appstate` 有引用则属"已接线"（例如 `eventShapeOf` 中枢用到 ✓）。

**已登记、尚未接线的通道（按可能的用户价值排）**：

| 符号 | 上游能力 | 潜在价值 |
|---|---|---|
| `SESSION_FEEDBACK_RECORD_ENDPOINT` | `sessionFeedback/*`（会话反馈记录） | 与官方"发送关于当前会话的反馈"对应（命令面板里那条命令已可执行，但**记录**这条通道仍未接） |
| `WORKSPACE_FILES_CHANGES_ENDPOINT` / `WORKSPACE_FILES_STAT_ENDPOINT` | `workspaceFiles/*`（文件变更 / 文件状态） | 文件预览随工作区变化刷新、拿到文件大小/类型等元信息 |
| `PROJECTION_INBOX` | `inbox` 投影 | 待处理项/收件箱（我们目前用 `todos` 与 `pendingBadge` 覆盖了部分语义） |
| `evaluateCapability` / `allCapabilityIds` | 能力评估 | 插件能力矩阵的展示与判定 |

**更新（E227，逐条核对后的结论）**：

| 符号 | 结论 |
|---|---|
| `WORKSPACE_FILES_CHANGES_ENDPOINT` | ✅ **已接线**（E226：文件视图随变更自动刷新，任何变更帧即重列） |
| `WORKSPACE_FILES_STAT_ENDPOINT` | ❌ **不需要接**：文件元信息**已随 `workspaceFiles/list` 到达**——我们的 `FileNode` 本就带 `size` / `modifiedAt` / `kind` / `loaded` / 是否交付物。审计里那个 `(0)` 只说明"这个常量没人用"，**不代表能力缺失**（又一次"以为缺、其实有"） |
| `SESSION_FEEDBACK_RECORD_ENDPOINT` | ⏸ **暂不接**：界面用的是**消息级**反馈（`messageFeedback/list·put·delete`），而"会话级反馈"在官方是一条**命令**（`/feedback`）——我们的命令面板已能执行它 ⇒ 没有非接不可的理由 |
| `PROJECTION_INBOX` / `evaluateCapability` / `allCapabilityIds` | ⏸ 保留登记：`inbox` 的语义当前由 `todos` + 待决角标覆盖；能力评估尚无明确界面入口 |

**这条更新本身的意义**：审计清单**不等于待办清单**。逐条核对后，「已接线」「不需要接（能力已在别处）」「暂不接（无明确价值）」三种结论要分开写——
否则清单会长期挂着一批"看起来欠着、其实不必做"的条目，下一轮又会有人去"补"。
| `goals/*`（get/create/edit/pause/complete/clear） | 长期任务目标（官方 `ui-goal` 目标栏） | **要件齐全**：端点 + 参数（`agentId` + `ref`/`request`）+ 我们已有 agentId 来源（`commandsFor`）⇒ **✅ 已接线**（E242 数据层 `goals/get` + E243 面板内目标栏；`create/edit/pause/complete/clear` 仍为可接通道——先做「看」，写操作待真机确认读通路后再开） |
| `messageFeedback/*`（list / put / delete）+ `sessionFeedback/record` | **消息级反馈**（官方 `ui-message-feedback`：categories / chip / submit）与会话级备注 | **要件齐全（E251）**：`messageFeedback/list` = `{sessionId}` → `{items}`；`messageFeedback/put` = `{sessionId, messageId, rating, note?, category?}`（语义："创建或替换**某一条助手消息**的反馈"）；`sessionFeedback/record` = `{sessionId, text?, category?}`（自由文本备注）。⇒ 可实现（消息行上加评分/备注 UI）；****✅ 已接线（评分部分，E252–E255）**：别名 → 中枢 `refreshFeedback/feedbackOf/putFeedback`（含 `ifVersion` 乐观并发）→ 会话切换触发 → **消息行上的「有帮助 / 没帮助」chip**；`categories` / `note` 的提交面板待排期 |
**处置约定（写下来，避免"死代码 vs 登记"反复争论）**：

1. `dshcompat` 是**上游表面的登记处** —— 已登记但未接线的端点/投影常量**保留**（`SKILLS_LIST_ENDPOINT` 就是先登记、后接线的先例）；
2. 但**必须登记在案**（即本节）——不允许出现"没人知道为什么在那儿"的符号；
3. 接线时机：有明确用户价值时接（并在此表划掉），否则长期保留登记状态，**不硬造用途**。
## 16. 交接总清单：哪些改动已验证、哪些待验证（E231）

> **为什么要这张表**：本会话跨 200+ 轮。无设备之后仍持续改代码（构建、门禁、契约、文档都能推进），
> 于是"**改了但没在真机上看过**"的清单越来越长。不列出来，接手的人只能翻提交历史猜。
> 下表以 **git 历史为事实来源**（不是凭记忆），逐条对应到验收条目。

### 16.1 ✅ 已在真机验证（不必重测，除非回归怀疑）

| 改动 | 证据 |
|---|---|
| 端侧自足运行（HAP 内嵌自建 Node + 本机 Host） | 真机日志 `BOOT_10_ENV_READY core=…/0.1.5-rc.2 profile=ondevice` + 真实模型对话 |
| **核心版本切换**（安排激活 + 重启生效，E152） | `HDSH-CORE 切换结果：ok=true` → 重启 → `core=…/0.1.5-rc.3` → `connect ok=true` |
| 工作区**归档**（E132–E134） | 点归档后顶部显示本地化回执「已归档（可在「归档」里查看）。」 |
| 凭据权限豁免（E127/E148） | 真机 `平台标识：HDSH_PLATFORM=ohos`（并发现"入口脚本没进包"这一发布件缺陷） |
| 未加载原生件点名（E121/E148） | 真机 `原生件加载 2/3 已加载 · 缺 koffi` |
| 预设页名单（E161/E162） | 真机显示预设自带中文名与描述：端侧模式（无 Shell）/标准模式/PTC 模式/极简模式/创造模式 + 「默认」「随附」标记 |
| 工作区新建会话入口（E163） | 真机组头显示 `+ 会话 / 文件 / 删除` |
| 模型页密钥去重（E157/E158） | 真机 dump 显示密钥入口只剩提供方卡片一处 |
| 预设页中文标签（E155） | 真机显示「提供方 / 模型 / 推理强度 / 最大并行工具调用数」 |
| 核心页去括号（E166） | 真机显示「核心已在运行」「JIT 状态｜未启用」 |
| 切换门控与导出（E150/E151） | 真机验证不再崩溃；门控已按 E152 重新开放 |

### 16.2 ⏳ 已实现但**未真机验证**（接设备后按表中条目逐条看）

| 改动（提交） | 改了什么 | 验收条目 |
|---|---|---|
| E174 技能会话范围（SessionHub） | `skills/list` 带 `sessionId`（原先空请求致宿主 `failed to observe session "undefined"`） | §14.2 + 设置→技能页有内容 |
| E175/E177 去凭据区（SettingsPane） | 模型页不再单列凭据，空壳灰条消除 | 视觉：模型页只有提供方与密钥 |
| E178/E179/E181 提供方目录与候选（SessionHub/SettingsPane） | `llm/listProviders ∪ listConfigurableProviders` + 候选卡 | **§14.5 #23** |
| E187/E188/E190 命令面板（Aliases/Wire/SessionHub/Index） | 名单 + 执行通道 + 面板 UI | **§14.5 #16/#17** |
| E192–E195 本会话模型选择 | 别名 → Wire 助手 → 中枢 `selectSessionModel` → 面板内列表 | **§14.5 #19** |
| E201/E202 计划模式 | `plan` 投影进快照 + chip（判据 `pending ? !active : active`，经 `/plan` 命令切换） | **§14.5 #18** |
| E204 轨迹钟点+时长（ConversationPane） | 条目右侧显示「钟点 · 时长」 | **§14.5 #20** |
| E207/E208 移除推理强度与当前模型区块 | 设置页只管提供方与密钥 | 视觉 + §14.1 附近 |
| E209/E212/E213 模型目录归位 | 收编 → 恢复可见 → **搬进提供方展开区** | **§14.1 + §14.5 #22**（看不到就别做"拆"） |
| E211 候选搜索（SettingsPane） | 候选区搜索框（对齐官方 `candidateSearch`） | **§14.5 #23** |
| E219 命令执行载荷修正（Wire/SessionHub） | `{agentId, line, submittedAttachments}`（原先错为 `{sessionId,name}`） | **§14.5 #17** |
| E222 模型选择载荷修正（Wire） | 嵌套 `{sessionId, selected}`（原先扁平 ⇒ 必被拒） | **§14.5 #19** |
| E226 文件变更流（SessionHub `openFilesStream`） | `workspaceFiles/changes` 订阅，任何变更帧即重列 | **§14.5 #21**（失败看 `HDSH-TRACE files changes opened`） |
| E164 旁路记账进 hilog（SessionHub `record`） | 原先被开关挡住且不写 hilog ⇒ 排障看不见 | 间接：`trace` 日志有内容即生效 |

### 16.2b 载荷字段已按契约核对（E241：把"真机失败的可能原因"收窄）

§16.2 里那批新端点，**载荷字段**已逐个与权威契约对照过（代码级，非推断）：

| 端点 | 代码里的实际字段 | 权威契约 | 结论 |
|---|---|---|---|
| `skills/list` | `inner['sessionId'] = skillScope`（`SessionHub` 3075） | `SkillListRequest { sessionId }` | ✅ |
| `commands/execute` | `{ agentId, line, submittedAttachments }`（`Wire` 2173–2175） | `agentId, line, submittedAttachments` | ✅ |
| `workspaceFiles/changes` | `workspaceFileScopeId`（`openFilesStream`） | `workspaceFileScopeId`（stream） | ✅ |
| `session/selectModel` | `{ sessionId, selected: {provider, modelId, effort} }`（`Wire` 2216） | `SessionSelectModelRequest { sessionId, selected }` | ✅ |

**这个结论的实际用处**：真机上这几项若失败，**原因基本不会是"字段被宿主拒绝"**（那类错误已经被排除），
而更可能是**行为/呈现**问题（例：命令列表为空 ⇒ 该会话确实没提供命令；模型目录不出现 ⇒ 宿主未声明该设置项）。
排查时按这条去缩小范围，比从头猜要快得多。
### 16.2c 接线成环审计（E245：第四层验证，专抓"实现了没触发"）

E135 出过一次**沉默的缺陷**：技能模块的数据层与界面都写了，**但触发点没接**（那一步的编辑失败被忽略），
于是技能页永远显示"未读"——不报错、不崩溃，只有跑一遍界面才会发现。为防同类问题，本轮把**每个新功能的三个环**
逐项核对：**状态（中枢有方法/字段）→ 触发（界面真的调了）→ 呈现（界面真的渲染）**。

| 功能 | 状态环（中枢定义） | 触发环（界面调用） | 结论 |
|---|---|---|---|
| 技能清单 | `refreshSkills` @3119 | `Index` 1341 / 1347 | ✅ 成环 |
| Agent 预设 | `refreshAgentPresets` @3074 | `Index` 1348 | ✅ 成环 |
| 提供方目录 | `refreshProviderCatalog` @2859 | `Index` 1350 | ✅ 成环 |
| 命令面板 | `refreshCommands` @2112 | `Index` 1583（打开面板时） | ✅ 成环 |
| 长期目标 | `refreshGoal` @2971 | `Index` 1585（打开面板时） | ✅ 成环 |
| 文件变更流 | `openFilesStream` @2228 | `openWorkspace` 内 | ✅ 成环 |

**第三环（呈现）的具体位置补全（E246）**——上一轮只断言"呈现 ✓"而没给证据，不符合本项目"每个结论有出处"的标准：

| 功能 | 呈现点 |
|---|---|
| 长期目标 | `Index.ets:2859`（`Text('长期目标')` 一行） |
| 计划模式 | `Index.ets:2884` |
| 本会话模型 | `Index.ets:2912` |
| 命令列表 | `Index.ets:2958`（`ForEach(this.commands, …)`） |
| 提供方候选 | `SettingsPane.ets:1111`（`ForEach(this.filteredCandidates(), …)`） |
| 展开区模型目录 | `SettingsPane.ets:1201`（「模型目录 · N」） |
| 文件树 | `Index.ets:678`（`fileNodes` 渲染） |

> **附带教训（E246）**：第一次查这些锚点时，我用 `Text(.长期目标.)` 做匹配，结果**三项全报"未找到"**——
> 因为 `(` `)` 在正则里是**分组符**，不是字面括号。**验证脚本自身也会骗人**：报"未找到"时，
> 先怀疑模式写错，再怀疑代码缺失（与 E164 那次"没有输出被误读成没有发生"是同一类问题）。
**这层验证的价值**：构建、门禁、契约核对能证明"代码是通的"，但**证明不了"界面真的会去调它"**——
E135 就是在这条缝里溜过去的。把"状态→触发→呈现"三环当成固定检查项之后，这类缺陷可在无设备条件下被排掉。
### 16.2d 命令面板的一处正确性确认（E250）

核对既有实现时确认了一件容易出错的事：**"列命令"与"执行命令"用的是同一个 agentId**。

| 环节 | agentId 来源 |
|---|---|
| 列（`refreshCommands`） | `const agentId = this.selectedSessionId`（`SessionHub.ets:2113`），成功后记进 `commandsFor` |
| 执行（`executeCommand`） | 用 `commandsFor`（E219 按契约改为 `{agentId, line, submittedAttachments}`） |

⇒ **不会出现"列表来自 A、执行落到 B"的错位**（这类错位在真机上表现为"点了命令但跑在别的会话上"，极难排查）。
同时确认既有 `refreshCommands()` **没有提示字段**（未选会话/未连接时只清空列表，不写文案）——
**空态解释由面板 UI 提供**（`Index.ets:2952-2953`，E190 写的）⇒ §14.5 第 16 条的判据已按该原文对齐。
### 16.4 全部改动后的复跑与死代码核对（E257）

自上次跑全套门禁（E224）之后又积累了十几处改动（目标栏、消息反馈、文档校准），本轮**复跑**：

| 门禁 | 结果 |
|---|---|
| ① 上架红线 | ✅ PASS |
| ② 原生依赖闭包 | ✅ PASS |
| ③ 架构门禁 | ✅ 无违规（上游字面量只在 dshcompat） |
| ④ 上游漂移 | ✅ 无漂移（84/84） |
| ⑤ 来源围栏 | ✅ PASS |
| ⑥ 插件启停 | ✅ PASS |
| ⑦ 空实现扫描 | 69 处（逐条判断；与 E150 时的 67 处相当，多为组件默认空回调） |
| ⑧ **接线回归（新增）** | ✅ 15 个功能的接线全部在位（扫描 66 文件）——同时证明本会话十几处改动**没有剪断任何已验功能的接线** |
| 构建 | ✅ SUCCESSFUL |

并核对了本轮新增符号的引用数（**1 处 = 只有定义**判死代码），结论：**无意外死代码**。其中 **2 处**的两个常量属**有意未接**，在此登记以免下次被误判：

| 常量 | 状态 |
|---|---|
| `MESSAGE_FEEDBACK_DELETE_ENDPOINT` | 已登记、**未接**（删除反馈的 UI 没做；评分已可用） |
| `SESSION_FEEDBACK_RECORD_ENDPOINT` | 已登记、**未接**（会话级备注；官方把它也做成命令 `/feedback`，我们命令面板可执行） |

> **纪律补充**：引用数 **2 处 = 定义 + 导出/表项**，往往就是"登记了没接"——这与"死代码（1 处）"要分开看：
> **1 处该删，2 处该登记**。这个判据把"死代码"和"有意登记"区分开，避免把台账当成垃圾清掉。
### 16.3 不需要设备验证（构建 / 门禁 / 契约 / 工具）

| 改动 | 验证方式与结果 |
|---|---|
| E218/E221 契约生成器与漂移门禁修复 | `compat-drift` **84/84 无漂移** |
| E219/E222/E223 契约审计 | 12 项逐条核对；两处不符已修 |
| E216/E217/E224/E225 工具清单、三条门禁实跑、通道治理 | `arch-check` 无违规、七项门禁全绿 |
| E228 死代码清理 | 自检 16 个符号，1 处死代码已删 |
| E229/E230 一键验收脚本与文档闭环 | 无设备时退出码 3 且不建目录 |
## 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.2 | 2026-09-12 | 补入运行时深挖结论并据此收紧目标：新增证据 E9~E16（**本项目实测**：端侧核心树可在 Windows 上按 `--os=openharmony --cpu=arm64` 物化，三个移植件均带 `.note.ohos.ident` + `.codesign`；**JIT 权限仅平板/PC-2in1 可申请**；`.node` 必须随 HAP、热更新 so 被拦；原生子进程仅平板/PC；loopback 只需普通权限）；新增 §4.4 JIT 闸门与 POC-13（手机 jitless 是否可用用真实会话判，不预设死）；§6.2 增加"原生插件不能热装"的设计约束；§7 G4 与 §8 M0′ 据此收紧；新增风险 R8~R10 |
| v1.1 | 2026-09-12 | 落定四项决策（§11）：运行时分两阶段（阶段一复用 Electron-on-鸿蒙 运行时，阶段二换自建 `libnode.so`）、核心随包内置 + 首启解包、引入 `@ohos-ports` 移植件并逐个验证、远程 Host 降为可选。新增 §4.0 两阶段说明与 §4.1 阶段一实施步骤（含沙箱禁 symlink 的实测项） |
| v1.0 | 2026-09-12 | 首版：更正项目目标（端侧自足运行 dsh）、给出目标架构与可行性证据、运行时主/回退路线、端侧 profile 策略、核心与插件版本管理设计、新指标与新里程碑 |
