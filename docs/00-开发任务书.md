# HDSH —— DeepSeek Harness 鸿蒙客户端 开发任务书

| 项 | 内容 |
|---|---|
| 文档编号 | `docs/00-开发任务书.md` |
| 版本 | v1.0（首版，待评审） |
| 日期 | 2026-09-10 |
| 状态 | **待评审**（评审通过后冻结为基线，变更走 §12.3） |
| 工程名 | `deepseek-harness-harmony-client`（本仓库 `deepseek-harness-desktop-HarmonyOS`） |
| 产品名 | **HDSH**（HarmonyOS + DeepSeek Harness；即 DeepSeek Harness 鸿蒙客户端） |
| 目标平台 | HarmonyOS NEXT —— 手机 / 折叠屏 / 平板 / 2in1 PC |
| 技术选型 | **ArkTS + ArkUI 原生**（Stage 模型，Hvigor 构建，API 26 起） |
| 参考项目 | ① [`deepseek-harness/apps/desktop`](https://github.com/deepseek-ai/deepseek-harness/blob/master/apps/desktop/README.zh.md)（官方 Electron 桌面壳，架构与工程标准）<br>② [`fellow99/deepseek-harness-harmony`](https://github.com/fellow99/deepseek-harness-harmony)（社区 Electron-on-鸿蒙移植，**反面对标**） |

---

## 1. 任务背景与依据

### 1.1 上游事实：dsh 已经是「Host / Client 分层」的可远程化架构

DeepSeek Harness（`dsh`）是 DeepSeek AI 开源的 agent harness，「一切皆插件」架构（Cordis 驱动）。它的既有事实决定了本次开发**不需要、也不应该**去 fork 它的业务代码：

1. **Host 与 Client 分离**：Host（Node.js）挂 `apiProxy` + `webserver`；Client（浏览器）经 `WebApiClient` 走 **HTTP 上行 + WebSocket 下行**。
2. **协议是稳定且独立的**：`@deepseek-ai/dsh-host-webserver` 只提供一张路由表（`exact` / `prefix` / `upgrade` / 单一 fallback），`@deepseek-ai/dsh-client-connection` 与 `@deepseek-ai/dsh-api-gateway` 在 `POST /api/<endpoint>` 上跑 Remote RPC，在 `/api/remote.mux` 上跑 WebSocket 逻辑流复用。**任何能说这两种载体的客户端都是合法的 dsh 客户端**，与浏览器、Electron 无关。
3. **客户端 UI 是插件而不是产品**：`dsh-client-ui-*` 是被 Client 侧 Cordis 组合出来的插件 bundle（preact），`dsh-client-ui-layout` 提供三栏 `AppFrame` + 让步链 + 主题呈现器。也就是说：**UI 层是可替换的**，官方前端不是唯一形态。
4. **安全围栏是显式设计**：`dsh web --host 0.0.0.0` **被上游明确拒绝**（"出于安全考虑"）；`/api` 前有 Host/Origin 信任栅栏 + 启动 token 换签名 cookie 的认证；特权方法（`settings.*`、`credentials.*`、`host.pickDirectory/openPath`、`agentPreset.*`、`llm.discoverModels`）锁定 loopback-only。

> 这四条事实合起来给出结论：**做鸿蒙客户端 = 实现一个协议兼容的原生 Client +（可选的）一个不触碰上游的 Host 接入方案。**

### 1.2 官方桌面端给了什么工程标准

官方 `apps/desktop`（Electron 壳）不是我们要复制的形态，但它的**决策表达方式**是本任务书的写作模板。其中可迁移的 6 条：

| 官方决策 | 可迁移到鸿蒙端的形式 |
|---|---|
| 发布身份：壳 API / Web 客户端 / 后端 / 插件依赖图作为**一个组合**验证，版本不许分裂 | 鸿蒙端与所对接的 dsh 版本组成**受支持的版本矩阵**，矩阵外不承诺 |
| 运行时隔离：绝不使用系统 Node/pnpm，一切走内置运行时 | 鸿蒙端绝不依赖设备上的 Node/终端/包管理器；Host 能力缺失时**降级而非崩溃** |
| 状态归属：Electron 独占 `$DSH_HOME/profiles/desktop`，与 CLI 共享数据但**绝不共享可执行包/锁/插件激活** | 鸿蒙端只做 Client：独占自己的本地缓存与设备绑定，**不写 `$DSH_HOME` 的包状态** |
| 激活事务：staging → 健康检查 → 切换 → rollback 日志 | 连接切换 / Host 版本升级 / 缓存迁移同样走「先验证后激活」 |
| 无共享执行图 / 单实例锁 / 事务锁 | 鸿蒙端多窗口共享单一连接池与单一会话状态机（§7.7.5） |
| 发布验证必须真机 + 上一版本升级路径 | 验收必须在**真机矩阵 + 模拟器**双轨，含跨版本升级（§11） |

### 1.3 社区移植给了什么教训

`fellow99/deepseek-harness-harmony`（Electron-on-鸿蒙，已在 HarmonyOS 6.1.0.135 / API 24 真机跑通 dsh Web UI）证明了两件事，也暴露了一条必须换掉的路线：

**它证明可行的**：dsh Host 能在鸿蒙沙箱里被跑起来（Node 22.17.0），Web UI 同源加载可用，会话持久化（better-sqlite3 aarch64）可用。

**它必须被替换的**（详见 §3 对标表）：内置 `libelectron.so` ≈ **172.7 MB**，dsh 产物打成 **143 MB** `dsh-dist.tar.gz` 启动时流式解压；为绕过鸿蒙 loopback 进程隔离而**绑 `0.0.0.0` 并 patch 掉上游的拒绝检查**，再用 Host 头改写去骗过 loopback-only 围栏；`node-pty` / `koffi` / `sharp` 三个原生模块无 aarch64 产物，导致**终端工具、内容搜索、进程沙箱、图片校验缩略图全部缺失**，agent preset 里 7 个工具行被禁用；`deviceTypes: ["2in1","tablet"]`，**手机不在目标内**。

### 1.4 一句话任务

> **用 ArkTS/ArkUI 写一个一等的 dsh 鸿蒙客户端：协议完全兼容上游、零上游 patch、覆盖手机到 2in1 全设备形态、端上体积与内存比 Electron 方案低一个数量级，并把「审批 / 提问 / 文件 / 通知」做成移动端真正好用的第一交互。**

---

## 2. 本任务书的交付物定义

| # | 交付物 | 路径 | 说明 |
|---|---|---|---|
| D1 | 开发任务书（本文） | `docs/00-开发任务书.md` | 本阶段唯一硬交付 |
| D2 | 协议兼容事实基线 | `docs/10-协议兼容事实基线.md` | §附录A，写代码时逐条对照 |
| D3 | 产品需求与体验规范 | `docs/20-产品需求与体验规范.md` | §附录B，UI 实现的验收依据 |
| D4 | 技术验证（POC）清单 | `docs/30-技术验证清单.md` | §附录C，带判定条件与止损点 |
| D5 | 里程碑与任务分解 | `docs/40-里程碑与任务分解.md` | 由 D1 §9 展开为可派工粒度 |
| D6 | 验收与测试计划 | `docs/50-验收与测试计划.md` | 由 D1 §11 展开 |

**本文（D1）以「开工前必须达成一致的决策」为主体**：不写代码细节，只写清范围、架构、接口契约、验证门、里程碑与验收。评审不通过不开工。

---

## 3. 对标分析：我们要比谁好在哪

### 3.1 三方能力矩阵

| 维度 | 官方 dsh Web（浏览器） | 官方 Desktop（Electron） | 社区鸿蒙版（Electron-on-鸿蒙） | **本项目（ArkTS 原生）** |
|---|---|---|---|---|
| 运行时 | 系统浏览器 | 内置 Node + Electron | 内置 Node（Electron 37）+ `libelectron.so` | **无额外运行时** |
| 端上体积 | — | 数百 MB 级安装包 + seed | SO ≈172.7 MB + `dsh-dist.tar.gz` ≈143 MB | **目标 ≤ 50 MB 安装包** |
| 冷启动 | 服务器已就绪 | 解压/校准 seed 后建窗 | **流式解压 143 MB** 后建窗 | **目标 ≤ 1.5 s 到可交互（含连接）** |
| 设备形态 | 任意浏览器 | Win/macOS x64/arm64 | 2in1 / tablet | **手机 / 折叠屏 / 平板 / 2in1 PC + 多窗** |
| 手机适配 | 网页降级 | 无 | **不支持** | **一等目标**（单栏 + 底部操作区 + 手势） |
| 终端（bash/pwsh 工具） | 有 | 有 | **无**（node-pty 无 aarch64） | **有**（Host 在 PC 侧，天然保留） |
| 内容搜索（ripgrep） | 有 | 有 | **无**（subprocess 被禁） | **有**（同上） |
| 进程沙箱 | 有 | 有 | **无**（koffi 被禁） | **有**（同上） |
| 图片校验/缩略图 | 有 | 有 | **stub，no-op** | **端侧原生解码 + 缩略图**（ArkTS image） |
| 审批 / 提问交互 | 网页卡片 | 桌面通知 | 桌面通知（弱） | **端侧推送 + 锁屏直达 + 语音答复** |
| 网络暴露面 | loopback | 无监听端口（`dsh-app://` + IPC） | **绑 `0.0.0.0`，且 patch 掉上游拒绝检查** | **不新增暴露面**（loopback 直连 / 显式授权的加密隧道） |
| 上游改动 | 无 | Host 侧私有 overlay | **4 个上游 patch** | **目标 0 个上游 patch** |
| 分发 | — | 签名安装包 + electron-updater | 本地签名 HAP 自用 | 签名 HAP/APP + AGC 内测分发（分阶段） |

### 3.2 差异化定位（对外可宣称的三条）

1. **原生而非套壳**：ArkUI 原生渲染 + 原生协议客户端。没有 172 MB 的浏览器内核，没有 143 MB 的启动解压，没有「先起一个 Web 服务器再套一个 Web 视图」的中间层。
2. **全形态而非只做 PC/平板**：手机竖屏单手可用、折叠屏展开态秒变双栏、平板/2in1 多窗口 + 键鼠 + 拖拽 + 快捷键。手机不是「缩小的桌面」，而是「审批与随行的主控台」。
3. **不打洞而是接入**：不绑 `0.0.0.0`、不 patch 上游安全围栏。跨设备访问走**显式配对 + 加密隧道**，Host 侧仍是纯 loopback，安全语义与上游一致。

### 3.3 明确不做（Non-Goals，v1 范围）

| 不做 | 理由 |
|---|---|
| fork 或魔改 dsh 业务代码 / agent 逻辑 | 上游演进快，维护成本不可控；协议已是稳定接入面 |
| 端侧跑完整 dsh Host（Node 运行时进 HAP） | 见 §6.2，作为 P3 独立评估（POC-11），不进 v1 关键路径 |
| 自建模型调用链路（绕过 Host 直连 DeepSeek API） | 会话、工具、审批、权限语义全在 Host；绕过即失去产品价值 |
| 端侧执行 agent 工具（bash/文件/sandbox） | Host 侧执行是正确边界，端侧只需呈现与审批 |
| 复刻官方 Web 前端的全部内部界面 | 我们只保证**协议兼容**，不保证**像素兼容**；必要界面按移动端重设计 |
| 插件市场的完整安装/卸载编排 | dsh-market 的注册表操作归 Host；端侧只做「列出 / 触发 / 展示状态」 |
| 网页版 dsh 的浏览器扩展、桌面 TUI 等其它表层 | 超出范围 |

---

## 4. 目标用户与产品定位

### 4.1 用户与场景

| 用户 | 设备 | 核心场景 |
|---|---|---|
| **开发者（主）** | 2in1 PC / 平板 + 手机 | PC 上跑 Host 与 agent；离开工位时用手机看进度、批审批、答提问、发补充需求 |
| **移动办公者** | 折叠屏 / 平板 | 会议间隙查看会话、批注 deliverable、把手机里的文件/照片投喂进工作区 |
| **长任务监督者** | 任一形态 | 长任务（goal round / 工作流）挂机运行，只在关键节点被打断 |

### 4.2 三形态的产品定位（不是「一套 UI 缩三档」，而是三种分工）

| 形态 | 定位 | 主导航 | 标志性能力 |
|---|---|---|---|
| **手机（单栏）** | **审批与随行控制台** | 会话列表 → 会话 → 详情（推送式下钻） | 锁屏通知批审批、语音追加需求、相机/相册直投工作区 |
| **折叠屏** | **可变形的会话台** | 折叠态同手机；展开态自动切双栏 | 悬停态（半折）上半预览下半输入；展开/折叠零卡顿重排 |
| **平板 / 2in1 PC** | **完整工作台** | 三栏（导航 / 会话 / 详情） | 多窗口、键鼠快捷键、拖拽投喂、子系统状态与插件管理全功能 |

---

## 5. 设计目标与核心原则

### 5.1 目标（可度量，验收即按此表）

| # | 目标 | 指标（v1 门槛） |
|---|---|---|
| G1 | 协议兼容 | 通过 §11.2 协议一致性套件；对目标 dsh 版本**零上游补丁** |
| G2 | 轻量 | 安装包 ≤ **50 MB**；安装后占用 ≤ 80 MB；常驻内存 ≤ **150 MB**（典型会话） |
| G3 | 快 | 冷启动到可交互 ≤ **1.5 s**（P50，设备已配对）；会话切换 ≤ 200 ms；流式首 token 呈现延迟 ≤ 150 ms（相对网络） |
| G4 | 全形态 | 手机 / 折叠屏 / 平板 / 2in1 四形态在真机与模拟器均通过 §11.3 布局矩阵 |
| G5 | 移动端一等交互 | 审批/提问 从「收到通知」到「完成答复」≤ **2 步**；离线可读最近 50 条会话 |
| G6 | 不新增暴露面 | 默认部署下不监听任何对外端口；跨设备访问必须走显式配对，未配对设备一律拒绝 |
| G7 | 可诊断 | 任何连接/渲染失败都能在端内「连接诊断」页给出：端点、TLS、认证、协议、Host 版本五项判定 |
| G8 | 可持续演进 | 通过 `Connection` 薄接口替换载体（LAN / 隧道 / 未来端侧 Host），不触碰上层 UI 与状态层 |

### 5.2 核心原则

1. **协议优先于实现**：先把 wire 事实固化成 D2，再写代码；任何协议层猜测必须先用 §附录A 的实测方法证伪。
2. **零上游改动**：所有鸿蒙侧差异都在我们这一端消化；一旦发现必须有上游改动，走 §6.6 的接口申请流程，而不是 fork。
3. **安全围栏不绕过**：不做 Host 头改写、不做 `0.0.0.0` 绑定、不做「关掉校验」的补丁。要跨设备就**加一层我们自己的、显式授权的加密通道**。
4. **能力降级而非崩溃**：Host 侧缺什么（终端/图片/沙箱），端侧只是无法呈现对应入口，绝不让会话不可用。
5. **先桩后真**：UI 与状态层面向 `Connection` 与 `RemoteApi` 接口编程，用「协议录制回放桩」开发，不阻塞在 Host 可用性上。
6. **一条数据流**：所有形态共用同一套状态机与同一套协议层，差异只体现在视图与输入层。

---

## 6. 总体架构

### 6.1 逻辑分层（A 方案，v1 关键路径）

```
┌─ HarmonyOS 应用（HAP/APP）──────────────────────────────────────────────┐
│  UI 层（ArkUI，一多自适应）                                              │
│    ├ 手机单栏栈式导航        ├ 折叠屏可变栏    ├ 平板/2in1 三栏 + 多窗    │
│    └ 组件：会话列表 / 轨迹 / 工具卡片 / 审批卡 / 输入区 / 设置 / 诊断      │
├────────────────────────────────────────────────────────────────────────┤
│  状态层（ArkTS，无 UI 依赖）                                             │
│    ├ SessionStore（会话投影与增量）  ├ ApprovalStore（待决审批/提问）     │
│    ├ WorkspaceStore / SettingsStore / JobStore                          │
│    └ OfflineCache（最近会话 + 待发队列 + 已读水位）                       │
├────────────────────────────────────────────────────────────────────────┤
│  协议兼容层（本项目核心资产，可单测）                                     │
│    ├ RemoteRpc：POST /api/<endpoint> 信封编解码 + rpcId 关联 + 取消       │
│    ├ RemoteMux：WS /api/remote.mux 逻辑流（open/cancel/item/error）       │
│    ├ EventStream：$events 逻辑流 + opening ready 项 + 重连代次            │
│    ├ AuthSession：token 交换 → 签名 cookie → 自动续期/失效重绑            │
│    ├ TrustHeaders：Host / Origin 的正确构造（不伪造，见 §6.4）            │
│    └ Reconnect：500ms→1s→2s→4s→8s→10s + 50%~100% 抖动的等阶重试          │
├────────────────────────────────────────────────────────────────────────┤
│  设备接入层（Connection 薄接口，可替换）                                  │
│    ├ LoopbackConnection（Host 与端同机：直连 127.0.0.1）                  │
│    ├ LanPairingConnection（显式配对 + 一次性密钥，见 §6.4）               │
│    └ TunnelConnection（PC 侧搭桥服务，TLS + 设备密钥，见 §6.5）           │
├────────────────────────────────────────────────────────────────────────┤
│  平台适配层                                                              │
│    ├ @ohos.net.http（含 enableAutoCookie）/ @ohos.net.webSocket          │
│    ├ 通知 / 后台任务 / 剪贴板 / 分享 / 相机 / 相册 / 语音识别 / 无障碍     │
│    └ 窗口与形态（断点、折叠态、多窗口、悬停态、悬浮窗）                    │
└────────────────────────────────────────────────────────────────────────┘
                                  │  HTTP + WebSocket（dsh 既有载体，不改）
                                  ▼
┌─ dsh Host（PC / 开发机 / 服务器，上游原样）──────────────────────────────┐
│  webserver（127.0.0.1 或经搭桥服务受控暴露）                             │
│  ├ /api            → Remote RPC（一元）                                 │
│  ├ /api/remote.mux → WebSocket 逻辑流复用（心跳 2s Ping/Pong）            │
│  ├ /api/events.*   → 转发事件流（$events → ready + 增量）                 │
│  └ /               → SPA dist（端侧不使用）                              │
│  Host 侧完整的 agent / 工具 / 沙箱 / bash / ripgrep / 插件市场 —— 全部保留 │
└────────────────────────────────────────────────────────────────────────┘
```

### 6.2 为什么是「端侧原生 Client + Host 在 PC 侧」（A 方案的论证）

| 备选 | 描述 | 判定 |
|---|---|---|
| **A：原生 Client + PC 侧 Host** | 端侧只说协议；Host 由用户的 PC/开发机/服务器提供 | ✅ **v1 主方案**。零上游改动、零端侧运行时、全工具能力保留、体积与性能最优；手机/平板/2in1 全部自然覆盖 |
| B：端侧完整 Node Host（`libnode.so` 进 HAP） | 复刻社区的可行部分但换掉 Electron | ⚠️ **P3 评估**。需要为 aarch64-ohos 交叉编译 Node 22+ 与全部原生模块，且要重新面对 loopback 隔离与沙箱 HOME 问题。收益（离线自足）真实但成本巨大，不进关键路径 |
| C：端侧 ArkWeb 复用官方 Web UI + 端侧 Host | 社区路线，用 ArkWeb 替 Electron | ❌ **不采用为主方案**。端侧体积/内存仍是 Web 级；且必须解决 loopback 隔离 → 又一次面对「绑 0.0.0.0」的诱惑。**保留为 D 级回退**：若 POC-1 证明原生客户端存在不可逾越的协议障碍，则退到「ArkWeb + 官方 dist + 合法暴露方案」 |
| D：端侧原生 Client + 端侧只读缓存（无 Host） | 纯浏览器式离线阅读器 | ❌ 无 agent 能力，不构成产品 |

> **POC-1 是本方案的门**：原生 ArkTS 的 `POST /api` + `/api/remote.mux` 能否在真机上完成一次完整会话往返（含审批）。POC-1 失败 → 立即评估 C，并回写本任务书。

### 6.3 端侧模块划分

| 模块 | 类型 | 职责 |
|---|---|---|
| `entry` | HAP entry | 应用入口、Ability、窗口与形态、路由壳 |
| `feature/connection` | HAR | 协议兼容层（RemoteRpc / RemoteMux / EventStream / AuthSession / Reconnect） |
| `feature/state` | HAR | 状态层与缓存（会话投影、待决、离线队列） |
| `feature/ui-kit` | HAR | 一多自适应组件与主题（断点、栅格、三栏、卡片族） |
| `feature/chat` / `feature/trajectory` / `feature/toolcards` | HAR | 会话主界面与轨迹/工具/子代理/交付物呈现 |
| `feature/approval` | HAR | 审批与用户提问的收、推、答全链路 |
| `feature/workspace` | HAR | 工作区、文件树、文件投喂、引用 |
| `feature/settings` | HAR | 设置、模型、凭据状态、插件与插件市场（薄壳） |
| `feature/device` | HAR | 配对、发现、设备密钥、诊断 |
| `feature/platform` | HAR | 通知/后台/剪贴板/分享/相机/相册/语音/无障碍适配 |
| `hostkit`（PC 侧） | Node 包 | 搭桥服务（可选，P3 前的 P2 交付）：配对、发现、隧道、会话代持 |

### 6.4 设备接入与安全设计（本项目最需要评审的一节）

**问题**：上游对 `--host 0.0.0.0` 明确拒绝，`/api` 前有 Host/Origin 信任栅栏 + token/cookie 认证，特权方法 loopback-only。手机要连 PC 上的 Host，必然跨过 loopback。

**我们不做什么**（这些是社区方案的做法，我们明确禁止）：

- ❌ 不 patch 掉上游的 `0.0.0.0` 拒绝检查；
- ❌ 不伪造/改写 `Host`/`Origin` 去骗过 loopback 围栏；
- ❌ 不把「局域网内其他设备也能访问」当作可接受副作用。

**我们做什么**（三条渐进路径，按审批权限分级）：

| 级别 | 场景 | 方案 | 安全属性 |
|---|---|---|---|
| **L0** | Host 与端同机（PC/2in1 上同时跑 Host 与客户端） | 直连 `127.0.0.1:<port>`，Host/Origin 天然 loopback | 与浏览器同源姿态一致，无新增暴露面 |
| **L1** | 局域网内 PC ↔ 手机 | **显式配对 + 一次性密钥**：PC 侧生成短时配对码（二维码/数字码），手机扫码换取**设备密钥**；跨设备访问经我们自己的**加密隧道**（TLS + 设备密钥双向认证），隧道客户端在 PC 侧，隧道出口仍在 Host 的 loopback 上 | Host 不暴露；未配对设备一律拒绝；密钥可单设备吊销 |
| **L2** | 跨网络（外出、多机） | 同上隧道 + 中继（可选自建/自托管）；凭证仍为设备密钥 | 端到端加密；中继不持有明文会话 |

> **关键点**：L1/L2 的隧道**出口在 Host 的 loopback**，所以上游信任栅栏与 loopback-only 特权方法**天然满足**，不需要任何伪造。这是与社区方案的本质区别。

**凭据存储**：设备密钥、签名 cookie、token 一律进 `@ohos.security.huks` 加密存储（或 `preferences` + 加密），不落明文文件；日志脱敏（见 §11.5）。

### 6.5 Host 侧搭桥服务（`hostkit`，P2 交付，不影响 P1 开工）

| 职责 | 说明 |
|---|---|
| 保活与代持 | 以受管方式拉起/守护本机 `dsh` Host（沿用官方 `runProfile` 语义，不改上游） |
| 发现 | 局域网 mDNS/广播播发「可配对的 DSH Host」，供端侧一键发现 |
| 配对 | 生成短时配对码，收端侧公钥，写入设备白名单 |
| 隧道 | TLS 出口 → Host loopback；WebSocket 透传（含 2s 心跳） |
| 审计 | 记录设备接入/吊销事件，端侧可查 |

**约束**：`hostkit` 必须是**可选组件**——没有它，L0 场景必须完全可用。

### 6.6 上游接口申请流程（发现必须改上游时）

1. 在 D2 中登记缺口（现象、复现、最小复现脚本）；
2. 评估三种规避：客户端侧兼容 / `hostkit` 侧补齐 / 配置层覆盖（`cordis.patch.yml` 属于组合配置，不算改代码）；
3. 三者都不可行时，才向 `deepseek-ai/deepseek-harness` 提 issue / PR，并同步本项目 `patches/` 目录**仅作为临时手段**，且必须在任务书中登记为技术债与退出计划。

---

## 7. 功能需求规格

> 优先级：**P0** = v1 必须；**P1** = v1 目标（可延后一个小版本）；**P2** = v1.x；**P3** = 待评估。
> 「协议依赖」列指向 §附录A 的端点/事件条目——**未在附录A 确认的依赖，实施前必须先做协议探测**。

### 7.1 设备接入与连接管理

| # | 需求 | 优先级 | 协议依赖 | 验收要点 |
|---|---|---|---|---|
| 7.1.1 | 添加 Host：手输地址 / 扫码 / 局域网自动发现 | P0 / 发现 P1 | — | 三种方式都能落到同一 `Connection` 记录 |
| 7.1.2 | 认证：token 交换 → 签名 cookie；cookie 失效自动重绑 | P0 | `/` + `?token=` → 303 + cookie | 失效后可无人工干预恢复；无法恢复时给出明确引导 |
| 7.1.3 | 会话与端点管理：多 Host 并存、快速切换、健康检查 | P0 | — | 切换 ≤ 500 ms，切换不丢未发内容 |
| 7.1.4 | 重连：等阶退避 500ms→10s + 抖动；前台/后台行为区分 | P0 | `$events` 断流 → 新一代 | 断网 1 分钟后恢复，会话状态一致（不重复、不缺失） |
| 7.1.5 | 连接状态可观察：`connecting/connected/disconnected` + 手动重连 | P0 | — | 状态与 Host 真实一致（对照 Host 侧日志） |
| 7.1.6 | 连接诊断页（G7） | P0 | — | 五项判定：端点 / TLS / 认证（cookie 有效性）/ 协议（版本与端点可用性）/ Host 版本 |
| 7.1.7 | 设备配对与吊销（L1/L2） | P1 | `hostkit` | 未配对设备 100% 被拒；吊销即时生效 |

### 7.2 会话与项目

| # | 需求 | 优先级 | 协议依赖 | 验收要点 |
|---|---|---|---|---|
| 7.2.1 | 会话列表：最近/搜索/按工作区分组/未读标记 | P0 | 会话查询端点（附录A-A3） | 1000 会话下列表滑动 ≥ 55 fps |
| 7.2.2 | 会话创建 / 关联工作区 / 标题自动生成展示 | P0 | `session.create`（含 `workspaceId`） | 与 Web 端创建结果一致 |
| 7.2.3 | 会话轨迹：消息、工具调用、子代理、交付物、计划、目标、任务（jobs） | P0 | 事件流 + 会话投影 | 长会话（>500 事件）增量渲染不掉帧、不错序 |
| 7.2.4 | 离线可读：最近 50 条会话本地缓存 + 已读水位 | P0（G5） | — | 飞行模式下可读、可搜索、可写草稿 |
| 7.2.5 | 待发队列：离线时的 prompt/审批答复入队，恢复后按序提交 | P1 | — | 恢复后无重复提交、无乱序 |
| 7.2.6 | 会话操作：重命名、导出、删除、分支/分叉（如上游支持） | P1 | 附录A-A3 | 操作结果与 Web 端一致 |
| 7.2.7 | 全文搜索 | P2 | dsh-session-query | 结果与 Web 端一致 |

### 7.3 对话与输入

| # | 需求 | 优先级 | 验收要点 |
|---|---|---|---|
| 7.3.1 | 流式呈现：assistant 文本增量、思考（reasoning）、工具调用时间线 | P0 | 增量更新可见延迟 ≤ 150 ms；中断/恢复后内容一致 |
| 7.3.2 | 富文本渲染：Markdown、代码块高亮、表格、KaTeX 公式 | P0 | 与 Web 端同源内容渲染正确率 ≥ 99%（抽样 100 条） |
| 7.3.3 | 代码块：横向滚动、行号、复制、分享 | P0 | 10k 行文件不卡死 |
| 7.3.4 | 输入区：多行、@引用（工作区/文件/技能）、斜杠命令、附件、模型与推理强度选择 | P0 | 与 Web 端同一套命令语义 |
| 7.3.5 | 语音输入（端侧语音识别）+ 语音追加需求 | P1 | 识别结果可编辑后发送；无网络时降级为不可用而非报错 |
| 7.3.6 | 中断/停止当前轮次；补发与重发 | P0 | 与 Host 真实状态一致 |
| 7.3.7 | 输入历史与草稿持久化（按会话） | P1 | 杀进程后草稿仍在 |
| 7.3.8 | 键盘/外设：外接键盘快捷键、Shift+Enter 换行、Tab 补全、触控板手势（2in1） | P1 | §附录B 快捷键表逐条可用 |

### 7.4 审批与用户提问（移动端第一交互，G5）

| # | 需求 | 优先级 | 验收要点 |
|---|---|---|---|
| 7.4.1 | 审批卡片：动作、影响范围、可展开的原始载荷、允许/拒绝/按策略放行 | P0 | 与 Web 端同一审批语义；拒绝路径同样可达 |
| 7.4.2 | 用户提问（`ask_user`）：单选/多选/自由文本；问题可带选项与描述 | P0 | 选项与自由输入都可用；提交后 Host 侧正确继续 |
| 7.4.3 | 待决聚合视图：所有会话的待决事项一处处理 | P0 | 从通知到完成答复 ≤ 2 步（G5） |
| 7.4.4 | 端侧通知：审批/提问/轮次完成/长任务结束；点通知直达待决 | P0（P1 推送） | 后台/锁屏可见；点击直达正确会话与卡片 |
| 7.4.5 | 锁屏/快捷卡片答复（不打开 App 完成简单审批） | P2 | — |
| 7.4.6 | 审批策略预设切换（对应上游 permission presets） | P1 | 与上游语义一致，风险提示明确 |

### 7.5 工作区与文件

| # | 需求 | 优先级 | 验收要点 |
|---|---|---|---|
| 7.5.1 | 工作区管理：列出/添加/切换，默认起始目录交由 Host 决定（**不假设设备 `homedir`**） | P0 | 添加工作区成功并可关联会话 |
| 7.5.2 | 目录浏览：`host.listDirectory` 浏览式选择器（端侧原生 UI 替代 Web 目录浏览器） | P0 | 大目录（>5k 项）可用；路径越权被拒 |
| 7.5.3 | 文件预览：文本/代码/图片/Markdown；大文件截断提示 | P1 | 10 MB 文本预览不 OOM |
| 7.5.4 | 投喂：从相册/相机/文件管理/分享目标导入 → 进工作区或作为附件 | P0 | 图片走端侧原生解码与压缩（G 的替代项，见 7.5.6） |
| 7.5.5 | 交付物（deliverables）：列出、预览、保存到设备、分享 | P1 | 保存后设备文件可用 |
| 7.5.6 | 图片管线：端侧解码/EXIF 校正/压缩/缩略图生成（替代上游 `sharp` 能力缺口） | P1 | 20 MB 原图 → 压缩上传，Host 侧收到有效图片；缩略图列表流畅 |
| 7.5.7 | 拖拽投喂（2in1/平板）：文件/文本拖入输入区或工作区 | P1 | 拖拽目标高亮与预期一致 |

### 7.6 设置、模型与凭据

| # | 需求 | 优先级 | 验收要点 |
|---|---|---|---|
| 7.6.1 | 设置读改：通用/模型/插件（对应 `settings.*`） | P0 | 特权方法的可达性由 §6.4 路径保证 |
| 7.6.2 | 模型选择与推理强度；模型发现（`llm.discoverModels`） | P0 | 与 Web 端可选集一致 |
| 7.6.3 | 凭据状态展示与设置入口（`credentials.*`）；**交互式授权 flow**（粘贴码/登录） | P1 | 敏感值绝不明文回显；授权完成后无重启即可用 |
| 7.6.4 | 插件清单与插件市场（薄壳：列出/触发/展示状态） | P2 | 安装编排仍由 Host 侧完成 |
| 7.6.5 | 主题：跟随系统深浅色 + 品牌色；字号缩放（无障碍） | P0 | 系统切换主题 1 s 内生效；字号 200% 不破版 |

### 7.7 平台与系统集成

| # | 需求 | 优先级 | 验收要点 |
|---|---|---|---|
| 7.7.1 | 通知渠道与分级（审批/提问 = 高优先级；轮次完成 = 普通） | P0 | 渠道可单独配置；静默时段可用 |
| 7.7.2 | 后台保活：前台服务型长连接 + 断连补偿（不做无限保活，遵守功耗约束） | P1 | 后台 10 分钟内事件不丢（重连后补偿一致） |
| 7.7.3 | 剪贴板：文本/图片双向；复制代码块带语言标记 | P0 | 图片粘贴进输入区可用 |
| 7.7.4 | 分享：接收（其他 App 分享文本/文件到 DSH）；发出（会话/交付物） | P1 | 分享目标在系统分享面板可见 |
| 7.7.5 | 多窗口（平板/2in1）：主窗 + 会话子窗；窗口位置/大小记忆 | P1 | 两窗同时操作不串状态 |
| 7.7.5b | **多窗口状态归属**：全应用**单一**连接池 + 单一会话状态机 + 单一待发队列；窗口只是视图，不得各自开连接或各自维护会话状态 | P0 | 开 3 个窗口后抓包确认只有 1 条 mux 连接、1 条 `$events`；关任一窗口不影响其他窗口与连接 |
| 7.7.6 | 无障碍：TalkBack/屏幕朗读标签、焦点顺序、对比度 | P1 | 核心路径（读消息、批审批、发消息）可纯朗读完成 |
| 7.7.7 | 应用生命周期（2in1）：关闭按钮/快捷栏/多实例/窗口三键区行为正确 | P1 | 符合系统预期，不留孤儿连接 |
| 7.7.8 | 折叠屏：折叠态/展开态/悬停态（半折）差异化布局；重排不卡顿、不白屏 | P0 | 折叠 10 次无破版、无可见白屏 |

### 7.8 诊断与可观测

| # | 需求 | 优先级 | 验收要点 |
|---|---|---|---|
| 7.8.1 | 连接诊断页（同 7.1.6） | P0 | 五项判定给出可执行建议 |
| 7.8.2 | 端内日志（分级、可导出、脱敏） | P0 | 导出日志中无 token/cookie/密钥/API Key |
| 7.8.3 | 协议旁路日志：可开关记录 RPC 方法名与耗时、WS 帧类型与大小（**不含内容**） | P1 | 开关默认关闭 |
| 7.8.4 | 崩溃与 ANR 上报（可关闭，默认仅本地） | P2 | — |

### 7.9 版本兼容与升级

| # | 需求 | 优先级 | 验收要点 |
|---|---|---|---|
| 7.9.1 | 受支持 dsh 版本矩阵（见 §11.4）；超出矩阵给明确提示而非静默异常 | P0 | 矩阵外版本可用性明确（可用/降级/拒连 + 原因） |
| 7.9.2 | 协议能力探测：启动时探测端点可用性，缺失能力对应入口置灰 | P0 | 老 Host 上不出现「点了没反应」 |
| 7.9.3 | 端侧自升级（HAP/APP 分发策略见 §10.4） | P2 | — |

---

## 8. 技术验证清单（POC，全部带止损点）

> 完整可执行版本见 **D4 `docs/30-技术验证清单.md`**；本节只列门与判定。

| # | 验证项 | 判定（通过条件） | 失败处置 |
|---|---|---|---|
| **POC-1** | **原生协议往返（本方案的生死门）**：ArkTS `@ohos.net.http` 完成 token 交换 + cookie 落盘 + `POST /api` 一元调用；`@ohos.net.webSocket` 打开 `/api/remote.mux` 并维持 2s 心跳 | 真机上完成：认证通过 + 一次 `session.create` 成功 + 订阅 `$events` 收到 `ready` 与至少一条增量事件 | **立即评估架构 C（ArkWeb + 官方 dist）**，并回写 §6.2 |
| POC-2 | loopback 直连可行性（Host 与端同机） | 端侧 HTTP/WS 均可直连 `127.0.0.1:<port>` | 若被系统隔离：走 L1 隧道（§6.4），但**不配方 0.0.0.0** |
| POC-3 | 特权方法可达性 | `settings.describe` 等在 L0 路径下返回成功（非 403） | 分析围栏构成（Host/Origin/cookie 绑定），只在**合法构造**范围内解决 |
| POC-4 | 长连接稳定性与恢复 | 弱网/切网/后台各 10 分钟，重连后事件不重不漏（对照 Host 侧序列） | 修正重连策略与代次判定 |
| POC-5 | 大轨迹渲染性能 | 500+ 事件会话：滚动 ≥ 55 fps，内存 ≤ 150 MB | 引入虚拟列表 + 增量补丁 |
| POC-6 | 一多形态骨架 | 手机/折叠屏/平板/2in1 四形态在模拟器 + 真机通过 §11.3 矩阵 | 调整断点策略（GridRow/断点监听/ContainerReader） |
| POC-7 | 端侧图片管线 | 20 MB JPEG：端侧解码+压缩+缩略图，端到端可用 | 降级为直传原图 + 体积告警 |
| POC-8 | 通知直达与后台补偿 | 后台收到审批并点通知直达卡片；重连后待决不漏 | — |
| POC-9 | 凭据安全存储 | 密钥/cookie 进 HUKS；导出日志脱敏通过审查 | — |
| POC-10 | 0 上游 patch 验证 | 从干净上游 checkout 起 Host，全流程无需任何 patch | 按 §6.6 流程登记缺口 |
| POC-11（P3） | 端侧 Host（`libnode.so`）可行性 | 能跑通 `dsh` 的 `sdk`/`headless` 表层最小往返 | 不做则正式关闭该路线 |

**POC 纪律**：每项必须产出（a）可复现步骤，（b）失败时的止损结论，（c）回写 D1/D2 的差异修订。POC-1 未通过前，不投入 P1 之后的功能开发。

---

## 9. 里程碑与任务分解

### 9.1 总体节奏

| 里程碑 | 名称 | 内容 | 出口判据（Exit Criteria） |
|---|---|---|---|
| **M0** | 文档冻结与基线 | D1~D6 评审通过；D2 协议基线首版；工程骨架可构建 | 评审记录归档；`devecocli build` 出 HAP |
| **M1** | 协议层打通（← POC-1/2/3/10） | `feature/connection` 全量实现 + 协议一致性套件首版 | 真机上完成认证 + 会话创建 + 事件流 + 一元调用；0 上游 patch |
| **M2** | 单形态可用（手机） | 会话列表 / 会话轨迹 / 输入发送 / 流式呈现 / 中断 | 手机上可完成一次端到端真实对话（含一次工具调用呈现） |
| **M3** | 审批闭环 + 通知 | 审批卡 / 提问 / 待决聚合 / 通知直达 / 后台补偿（← POC-8） | G5 达标：通知→答复 ≤ 2 步；后台场景不漏待决 |
| **M4** | 一多形态完成 | 折叠屏 / 平板 / 2in1 三栏 + 多窗口 + 键鼠 + 折叠态（← POC-6） | §11.3 布局矩阵全绿 |
| **M5** | 文件与投喂闭环 | 工作区 / 目录选择器 / 文件预览 / 相册相机投喂 / 分享 / 图片管线（← POC-7） | 从手机相册投喂一张 20 MB 图片并让 agent 使用成功 |
| **M6** | 设置与运维完备 | 设置 / 模型 / 凭据授权 flow / 诊断页 / 日志脱敏 / 版本矩阵提示（← POC-5/9） | 无 Host 侧介入即可自助排查连接问题 |
| **M7** | 跨设备接入（L1） | 配对 / 设备密钥 / 隧道 / 吊销（`hostkit` 同步交付） | 手机连 PC：未配对拒绝、配对即用、吊销即时 |
| **M8** | 发布候选 | 性能达标（G2/G3）/ 无障碍（7.7.6）/ 崩溃收敛 / 分发流水线 | §11 验收全绿 + 上一版本升级路径验证 |

### 9.2 阶段依赖与并行

```
M0 ──► M1 ──┬─► M2 ──► M3 ──┐
            │                ├─► M8
            ├─► M4（骨架可并行：M0 后即可搭 UI，用协议桩驱动）
            ├─► M5（依赖 M1 的 host.* 端点）
            └─► M6（依赖 M1 + M2）
M7 独立轨道（hostkit + 端侧配对），不与 M2~M6 争抢关键路径
```

### 9.3 任务分解粒度约定

- 每个任务 = **一个可独立验收的行为**（例：「`RemoteRpc.call` 在 401 时触发一次 token 重绑并重放原请求，最多 1 次」）；
- 每个任务必须声明：所属模块、优先级、协议依赖（附录A 条目号）、验收方法（单测/真机/模拟器）；
- **不做「实现某某页面」这种任务**——页面任务必须拆到行为级。

---

## 10. 工程规范与协作约定

### 10.1 仓库与目录

```
deepseek-harness-desktop-HarmonyOS/
├── AppScope/                     # 应用 scope（图标/名称/权限）
├── entry/                        # HAP 入口
├── feature/                      # HAR 模块（§6.3）
├── hostkit/                      # PC 侧搭桥服务（Node，独立可发布）
├── docs/                         # 本任务书及配套文档 D1~D6
├── specs/                        # 实施期 spec（每个里程碑一份 as-built 记录）
├── tools/                        # 协议探测、录制回放、一致性套件
└── scripts/                      # 构建/打包/发布脚本
```

**依赖方向铁律**：`entry` → `feature/*` → `feature/connection`；`feature/platform` 可被上层引用，但不反向依赖 UI；`feature/connection` **零 UI 依赖、可纯单测**。任何跨层反向依赖在评审阶段即拒。

### 10.2 分支与提交

- 主分支 `main` 保护；功能走 `feat/*`，修复走 `fix/*`；
- 提交信息遵循 Conventional Commits（`feat(connection): ...`），scope 用模块名；
- 每个 PR 必须包含：变更说明、验收方法、影响面（协议/形态/无障碍）；
- **协议层变更必须同时更新 D2 并附实测证据**。

### 10.3 文档纪律

- D1 冻结后，**范围/目标/验收的变更**必须走变更评审并升版本号；
- 实施期与设计的偏差写进 `specs/` 的 as-built 记录（社区项目的工程规划 §18 是个好范例：它如实记录了规划与最终实现的全部差异）；
- 每个 POC 产出独立结论条目，失败也要留档。

### 10.4 构建、签名与分发

| 项 | 约定 |
|---|---|
| 环境 | DevEco Studio DS-261.23567.138.36.2600821，SDK API 26（`C:\Program Files\Huawei\DevEco Studio\sdk\default`，apiVersion 26 / 26.0.0.105） |
| 构建 | `devecocli build` / `devecocli run`；增量调试用 `--hotreload` / `--apply`（见 skill 说明） |
| 签名 | `devecocli signature generate`（AGC 自动签名）→ 发布用正式证书 |
| 调试设备 | 真机优先；模拟器 `Huawei_Phone`（HarmonyOS 5.1.0(18)）用于形态与布局回归 |
| 分发 | v1：签名 HAP/APP + AGC 内测；是否上架应用市场在 M8 前决策（含鸿蒙 PC 分发规范预审） |
| 端侧体积门 | 每次 RC 记录安装包体积与内存基线，超门槛即阻断发布 |

---

## 11. 质量、测试与验收

### 11.1 测试分层

| 层 | 范围 | 工具/方式 |
|---|---|---|
| 单元测试 | `feature/connection` 信封编解码、重连等阶、cookie 生命周期、状态归并 | 本机 Node/ArkTS 单测（`devecocli` 单测运行器） |
| 协议一致性套件 | 对**真实 Host** 逐端点验证请求/响应/错误码 | `tools/protocol-suite`（本项目自建） |
| 桩回放测试 | 用录制的协议流量驱动全部 UI 与状态路径 | `tools/recorder` + 回放桩（不依赖 Host） |
| 界面与形态测试 | 四形态 × 深/浅色 × 字号档位 | `devecocli ui`（layout/screenshot）+ 模拟器折叠态 |
| 端到端 | 真机真实会话、工具调用、审批、投喂 | 手测清单 + 关键路径自动化 |
| 性能与功耗 | 冷启动、内存、帧率、后台功耗 | 真机 profiling + 基线对比 |

### 11.2 协议一致性验收（G1）

- 覆盖：认证（token→cookie→失效重绑）、一元 RPC（成功/业务错误/传输错误/rpcId 不匹配）、WS 逻辑流（open/item/error/cancel/心跳）、事件流（ready + 增量 + 重连代次）、特权方法可达性、404/401/403 语义；
- **结论必须是「与官方 Web 客户端行为一致」**，不一致项需在 D2 写明原因与影响；
- 通过条件：套件全绿；0 上游 patch（POC-10）。

### 11.3 形态与布局验收矩阵（G4）

| 形态 | 代表尺寸/状态 | 必测项 |
|---|---|---|
| 手机竖屏 | 折叠态 / 直板 | 单栏导航、输入区安全区、手势返回、软键盘避让 |
| 手机横屏 | — | 输入区不遮挡、轨迹可读 |
| 折叠屏 | 折叠 / 展开 / 悬停（半折） | 断点切换、重排无卡顿、悬停态上预览下输入 |
| 平板 | 竖 / 横 / 分屏 | 双栏/三栏取舍、分屏自适应 |
| 2in1 PC | 窗口缩放 320→全屏 / 多窗口 | 三栏、快捷键、拖拽、窗口记忆、生命周期 |
| 通用 | 深/浅色、字号 100%/150%/200%、TalkBack | 不破版、对比度达标、朗读完整 |

### 11.4 版本兼容矩阵（对应 7.9.1）

| 版本维度 | 受支持 | 降级 | 拒连 |
|---|---|---|---|
| dsh Host（核心包 `@deepseek-ai/dsh`） | **M1 冻结**（以 POC-10 记录的上游 commit SHA 为准） | 端点探测缺失 → 入口置灰 | 无 `/api` 或协议握手不兼容 |
| HarmonyOS | API 26（targetSdk） | — | — |
| 设备形态 | 手机 / 折叠屏 / 平板 / 2in1 | — | 其他（wearable/tv）不在范围 |

> **版本判定只读核心包版本，不读外层打包包版本**（本机外层 `deepseek-harness-pkg` 为 `0.1.2-alpha.5`，核心 `@deepseek-ai/dsh` 为 `0.1.2-rc.1`；两者不可混用，详见 D2 §0）。
> 矩阵必须随上游 rc 迭代更新，**每次更新都要重跑协议一致性套件**。

### 11.5 安全与隐私验收

| # | 项 | 判定 |
|---|---|---|
| S1 | 默认无对外监听端口 | 抓包/端口扫描确认 |
| S2 | 未配对设备被拒 | 未配对访问 100% 失败并有审计记录 |
| S3 | 凭据加密存储 | HUKS/加密存储；进程内存转储不含明文密钥（抽检） |
| S4 | 日志脱敏 | 导出日志不含 token/cookie/密钥/API Key/会话明文内容 |
| S5 | 不伪造信任头 | 代码审查确认无 Host/Origin 改写逻辑 |
| S6 | 0 上游 patch | `git diff` 空（对目标版本） |
| S7 | 权限最小化 | `module.json5` 权限逐条有使用场景说明 |
| S8 | 隐私说明 | 数据流向（本机 Host / 局域网 / 中继）在应用内明示 |

### 11.6 性能基线（每次 RC 记录）

| 指标 | 门槛 |
|---|---|
| 安装包体积 | ≤ 50 MB |
| 冷启动到可交互 | ≤ 1.5 s（P50） |
| 会话切换 | ≤ 200 ms |
| 长会话滚动 | ≥ 55 fps（500+ 事件） |
| 常驻内存 | ≤ 150 MB |
| 后台 10 分钟功耗 | 增量 ≤ 2% 电量（典型设备） |

---

## 12. 风险、待决事项与变更管理

### 12.1 风险表

| # | 风险 | 概率 | 影响 | 应对 / 止损 |
|---|---|---|---|---|
| R1 | 原生 ArkTS 无法满足协议层（cookie/WS 心跳/取消语义） | 中 | **致命**（架构切换） | POC-1 前置；失败退架构 C 并回写任务书 |
| R2 | loopback 在端侧/同机场景被系统隔离 | 中 | 高（须走隧道） | POC-2；L1 隧道兜底，**绝不配方 0.0.0.0** |
| R3 | 特权方法围栏在非浏览器客户端下不可达 | 中 | 高（设置不可用） | POC-3；仅在合法构造（真实 loopback + 真实 cookie）内解决；否则走隧道出口回 loopback |
| R4 | 上游 rc 版本快速演进导致协议漂移 | 高 | 中 | 版本矩阵 + 能力探测 + 一致性套件纳入 CI；协议层与 UI 层解耦 |
| R5 | 事件流重连后出现缺口/重复，导致轨迹错乱 | 中 | 高 | POC-4；以 Host 侧序列为准做对账；状态层幂等归并 |
| R6 | 长会话渲染性能不达标 | 中 | 中 | POC-5；虚拟列表 + 增量补丁 + 折叠策略 |
| R7 | 一多形态适配成本被低估（尤其三折叠/悬停/多窗） | 中 | 中 | POC-6 + 形态矩阵纳入每里程碑回归；形态专项任务单列 |
| R8 | 端侧图片管线与上游 `sharp` 语义不一致 | 中 | 低 | POC-7；以「Host 侧可用」为验收，不以「完全等价」为目标 |
| R9 | 后台保活受系统功耗策略限制 | 高 | 中 | 不做无限保活；以「重连补偿不丢事件」为验收口径 |
| R10 | `hostkit` 跨平台维护成本（Win/mac/Linux） | 中 | 中 | 先只支持开发主力平台；接口层与平台层分离 |
| R11 | 分发与合规（应用市场、鸿蒙 PC 规范） | 中 | 中 | M8 前完成预审；保留内测分发路径 |
| R12 | 团队对 dsh 协议理解偏差导致返工 | 中 | 高 | D2 事实基线 + 每条协议断言附实测证据 + 桩回放测试 |

### 12.2 待决事项（需产品/技术负责人拍板）

| # | 待决 | 备选 | 建议 |
|---|---|---|---|
| Q1 | v1 是否必须支持手机（相对只做 PC/平板） | A 全形态 / B PC+平板优先 | **A**——手机是本项目最大的差异化来源 |
| Q2 | L1 隧道是否进 v1 | A 进（M7）/ B 延后 | **A**——没有它手机场景不完整 |
| Q3 | 是否允许任何形式的上游 patch | A 绝对禁止 / B 允许且登记技术债 | **A（v1）** |
| Q4 | 端侧 Host（POC-11）是否投入 | A 评估 / B 直接关闭 | **A 评估但限时**（避免团队被拖入交叉编译泥潭） |
| Q5 | 分发方式 | A 仅内测 / B 上架 | M8 前决策 |
| Q6 | 端侧 UI 是否需要与 Web 端像素一致 | A 仅协议一致 / B 尽量一致 | **A**（移动端重设计优先） |
| Q7 | 是否支持多 Host 同时在线 | A 支持（串行连接）/ B 单连接 | **A（串行）** |

### 12.3 变更管理

- **范围变更**（新增/移除功能、调整优先级）：产品 + 技术负责人双签，升 D1 版本；
- **架构变更**（6.1/6.2/6.4 任一层）：必须附 POC 结论 + 回退方案；
- **里程碑出口变更**：只可因 POC 结论触发，且必须在文档中记录原因；
- 所有变更在同一份 D1 中以「变更记录」小节追加，不覆盖历史。

---

## 附录 A：协议兼容事实基线（摘要）

> 完整版见 **D2 `docs/10-协议兼容事实基线.md`**，包含每个端点的实测方法、错误码表与探测脚本。

### A.1 载体与路径（已在上游代码中核实）

| 项 | 事实 | 来源 |
|---|---|---|
| 一元 RPC 路径 | `POST /api/<endpoint>`（`endpoint` 由 descriptor 的 namespace/method 拼成，段字符集 `[A-Za-z0-9_$.-]`） | `dsh-client-connection` 的 `createWebConnectionRpc` |
| 一元请求体 | `{ type: "client-request", rpcId, method: endpoint, payload }`，`content-type: application/json` | 同上 |
| 一元响应体 | `{ type: "server-response", rpcId, result }`；`result` = `{ ok: true, value }` 或 `{ ok: false, error: { code, message, details } }` | 同上（`parseConnectionResponse`） |
| rpcId 校验 | 客户端必须校验响应 `rpcId` 与请求一致，不一致即视为传输错误 | 同上 |
| 传输错误语义 | HTTP 非 2xx → `transport failure for <channel>/<endpoint>: HTTP <status>` | 同上 |
| WS 逻辑流路径 | `/api/remote.mux`（精确 upgrade 路由） | `dsh-api-gateway` 的 `REMOTE_STREAM_MUX_PATH` |
| WS 客户端帧 | `{ type: "open", streamId, endpoint, payload }`、`{ type: "cancel", streamId }` | `dsh-api-gateway/lib/client.js` |
| WS 服务端帧 | `item`（逐项 yield）、`error`（终止并抛 `RemoteError`）；正常终止即结束 | 同上 |
| 事件流逻辑端点 | `$events`（payload `{ args: {} }`），另有 `$events/result` 一元端点用于 waterfall 回送 | `dsh-api-gateway/lib/types/stream-protocol.js` |
| 事件流首项 | 唯一 `{ type: 'ready', clientId, host: { home } }`；此后才是增量事件 | `dsh-client-connection` README、`dsh-api-gateway` README |
| 心跳 | Host 每 `websocketHeartbeatIntervalMs`（默认 **2 s**）发 Ping；未在下一周期前回 Pong 则 Host 主动断开 | `dsh-api-gateway` README |
| 重连策略 | 500ms、1s、2s、4s、8s、10s 上限，50%–100% 抖动；10s 档失败后进入 `disconnected` 终态；`reconnect()` 立即从 retry 1 开始 | `dsh-client-connection` README |
| 认证 | 进程级随机启动 token → `GET /?token=...` 换**绑定 authority 的签名 cookie**（host-only、`Path=/`、`HttpOnly`、`SameSite=Strict`、默认 30 天）；静态资源公开；`/api` 全部方法与 WS 均要求该 cookie | `dsh-client-connection` README、`dsh-host-frontend-static` README |
| 信任栅栏 | `Host` 必须 loopback 或匹配 `trustedHosts`；带 `Origin` 时必须等于该 `Host`；`sec-fetch-site: cross-site` 一律拒绝；失败 **403**；Host 可信但未认证 **401** | `dsh-client-connection` README |
| 上游姿态 | `dsh web --host 0.0.0.0` **被启动期拒绝**（安全考虑）；默认 `127.0.0.1`；`--trusted-host` 可追加权威 | `dsh-web-app` README |
| 特权方法 | `settings.*`、`credentials.*`、`host.pickDirectory/openPath`、`agentPreset.*`、`llm.discoverModels` 等锁定 loopback-only | 社区工程的实测记录（§18.7 问题 2） |
| Host 路由表 | `exact` > 最长 `prefix` > 唯一 fallback；upgrade 仅精确匹配，未命中直接关闭连接 | `dsh-host-webserver` README |

### A.2 另一条可用协议（备选，非 v1 载体）

`@deepseek-ai/dsh-sdk-protocol`：**按换行分帧的 JSON-RPC 2.0**，方法 `initialize` / `session/prompt` / `shutdown`，通知 `session.event` / `session.status` / `subagent.started` / `subagent.finished`。它是 stdio 载体，服务端插件为 `dsh-sdk-jsonrpc-server`（`dsh --profile sdk`）。

**用途**：① 作为 §附录C POC-11（端侧 Host）的最小验证面；② 作为「端侧无 webserver」场景的备选接入方式。**它不是 Web 客户端的载体，不能替代 `/api`**。注意其已知限制：无取消方法、无会话关闭方法、无协议版本协商。

### A.3 本地参考版本与环境

| 项 | 值 |
|---|---|
| 本机 dsh 打包版本 | `deepseek-harness-pkg` 0.1.2-alpha.5（`@deepseek-ai/dsh` **0.1.2-rc.1**） |
| DSH 安装位置 | `C:\Users\aotian\AppData\Roaming\io.github.hairyf.deepseek-harness-desktop\dependencies\dsh\` |
| 客户端插件 bundle | `node_modules/@deepseek-ai/dsh-client-ui-*/client.js`（含 `dsh-client-ui-layout/trajectory/tool/approval/user-questions/workspace/settings` 等 50+ 个） |
| Web 前端 dist | `node_modules/@deepseek-ai/dsh-web-frontend/dist/`（含 `langs/*` 语法高亮） |
| DevEco Studio | DS-261.23567.138.36.2600821 |
| HarmonyOS SDK | API 26（`platformVersion 26.0.0`，`version 26.0.0.105`） |
| 模拟器 | `Huawei_Phone`（phone，HarmonyOS 5.1.0(18)），当前 stopped |
| 本机工具链 | Node v22.20.0；pnpm 11.21.0；`devecocli` 已装；`hdc.exe` 位于 SDK `toolchains/` |

---

## 附录 B：产品需求与体验规范（纲要）

> 完整版见 **D3 `docs/20-产品需求与体验规范.md`**。

1. **信息架构**：会话（列表/轨迹/输入）· 待决（审批/提问）· 工作区（文件/交付物）· 设置（通用/模型/凭据/插件）· 诊断；
2. **三形态导航模型**：手机栈式（列表→会话→详情下钻，系统级手势返回）；折叠屏可变栏（展开自动双栏，折叠记忆上次栏位）；平板/2in1 三栏（导航/会话/详情）+ 详情栏可收；
3. **一多断点策略**：以窗口宽度为主断点 + 折叠态/悬停态为特征断点；采用 `GridRow/GridCol`、`SideBarContainer`、断点监听；组件级细化用 API 26 的 `ContainerReader`；
4. **关键组件规格**：会话列表项（标题/时间/状态/未读）、轨迹消息（角色/时间/耗时/折叠）、工具调用卡（名称/参数摘要/状态/结果折叠/耗时）、审批卡（动作/影响/原始载荷/三选）、提问卡（选项/自由文本/多选）、输入区（附件/@引用/命令/发送/停止）、待决徽标与聚合页；
5. **交互细则**：长按复制/引用/分享；侧滑操作；下拉刷新与「连接中」态；错误态与空态文案；弱网提示；
6. **快捷键与手势表（2in1）**：新建会话、搜索、切栏、发送、换行、停止、上下条导航、批量复制代码；
7. **通知文案规范**：审批/提问/完成/失败四类，含会话标题与动作摘要，不含敏感内容；
8. **无障碍规范**：可朗读标签（消息、卡片状态、按钮）、焦点顺序、动态字号、对比度 4.5:1；
9. **视觉与主题**：跟随系统、品牌色映射、深浅色 token 表、代码高亮主题与 Web 端一致性抽样验收。

---

## 附录 C：技术验证（POC）清单（纲要）

> 完整版见 **D4 `docs/30-技术验证清单.md`**。每项含：目标、前置、步骤、**判定条件**、失败处置、产出物、回写项。

| POC | 一句话目标 | 关键判定 | 失败处置 |
|---|---|---|---|
| POC-1 | 原生端完成一次真实会话往返 | 认证 + `session.create` + `$events` ready + 增量事件 | 切架构 C |
| POC-2 | 同机 loopback 直连可用 | HTTP/WS 均通 | 走 L1 隧道 |
| POC-3 | 特权方法可达 | `settings.describe` 非 403 | 分析围栏，合法构造解决 |
| POC-4 | 弱网/后台恢复不重不漏 | 与 Host 侧序列对账一致 | 修正重连与代次 |
| POC-5 | 大轨迹性能 | ≥55 fps / ≤150 MB | 虚拟列表 |
| POC-6 | 四形态骨架 | §11.3 矩阵全绿 | 调整断点策略 |
| POC-7 | 端侧图片管线 | 20 MB 图端到端可用 | 直传 + 告警 |
| POC-8 | 通知直达 + 后台补偿 | 2 步完成答复；不漏待决 | — |
| POC-9 | 凭据安全 | HUKS + 日志脱敏审查 | — |
| POC-10 | 0 上游 patch | 干净上游全流程通过 | 走 §6.6 |
| POC-11 | 端侧 Host 可行性（P3，限时） | sdk/headless 最小往返 | 关闭该路线 |

---

## 附录 D：参考资料

| 资料 | 位置 |
|---|---|
| 官方桌面端 README（中文） | [deepseek-harness/apps/desktop/README.zh.md](https://github.com/deepseek-ai/deepseek-harness/blob/master/apps/desktop/README.zh.md) |
| 社区鸿蒙移植工程 | [fellow99/deepseek-harness-harmony](https://github.com/fellow99/deepseek-harness-harmony) |
| 社区工程规划与最终实现记录 | `…/docs/工程规划.md`（本仓库调研副本：`.research/ref-harmony/docs/工程规划.md`） |
| 上游协议包文档（中文） | `@deepseek-ai/dsh-client-connection`、`dsh-api-gateway`、`dsh-host-webserver`、`dsh-host-frontend-static`、`dsh-web-app` 的 `README.zh.md` |
| 上游 SDK 协议 | `@deepseek-ai/dsh-sdk-protocol` README（中文） |
| 本仓库调研副本 | `.research/dsh-docs/*.README.zh.md`、`.research/dsh-desktop-README.zh.md`、`.research/OHSidian-README.md` |
| 鸿蒙一多适配 | 本地文档：`devecocli docs read 最佳实践/多设备界面开发/界面布局响应式变化/页面布局场景/bpta-multi-device-page-layout`、`最佳实践/多设备界面开发/布局概述/bpta-multi-device-layout-overview`、`API参考/ArkUI_方舟UI框架/ArkTS组件/行列与堆叠/ContainerReader/ts-container-containerreader` |
| 鸿蒙 PC 生命周期 | `devecocli docs read 开发指南/Ability_Kit_程序框架服务/应用生命周期/应用退出_PC_2in1/app-stop-for-2in1` |
| 折叠屏适配 | `devecocli docs read FAQ/一次开发多端部署/三折叠设备未适配_页面出现错乱_重叠_拉伸_截断等异常情况/faqs-app-framework-multi-device-1` |

---

## 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-09-10 | 首版：背景与对标、目标与原则、A 方案架构（含安全分级 L0/L1/L2）、功能需求 R7.1–7.9、POC-1~11、里程碑 M0–M8、验收体系、风险与待决事项 |
