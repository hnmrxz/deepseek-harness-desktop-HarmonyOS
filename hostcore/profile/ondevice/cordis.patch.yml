# HDSH 端侧 profile —— 用户 patch 层
#
# dsh 的 patch 应用顺序（见 D6 §5.3）：
#   bundle 层 patch（按 dsh.profile.bundles 顺序）
#     → 本文件（profile 的 cordis.patch.yml）
#     → $DSH_HOME/cordis.patch.yml
#     → --patch 覆盖层
# 本文件里的覆盖**整块替换**目标行的 config；`{insert: […]}` 是追加。
#
# ── 纪律 ────────────────────────────────────────────────────────────────
# 只放**有证据支持**的覆盖，每条都写清证据与恢复条件。
#
# ── 判别口径（哪些该禁、哪些必须留）──────────────────────────────────────
# 判据不是"用了原生模块就禁"，而是**这件事在鸿蒙上能否直接实现**：
#   · koffi（FFI）**能用**：我们已用应用自身的 OHOS 工具链自建 3.2.1（D6 E51），
#     DT_NEEDED 结构性满足。所以 `web-startup` / `web-runtime` / `session-persistence-jsonl`
#     这些"经由 koffi"的行**必须保留**——`web-startup` 正是起 webserver 的那一行，
#     禁掉它 Host 就永远不会监听（这就是早先"runProfile 返回了但没有 webServer"的成因链）。
#   · node-pty（PTY + `spawn-helper`）**不能用**：要 `posix_openpt`/`forkpty` 并
#     **创建进程**执行同目录下的 `spawn-helper`，而鸿蒙对应用创建进程有平台级限制（D6 E15）。
#     依赖它的整条链都得禁（见下）。
#   · sharp/libvips **不能用**：缺 `libvips-cpp.so.42`、且其加载器要建软链（沙箱禁止），
#     WASM 回退在 `--jitless` 下必失败（D6 E46）。
# 另外：koffi 3.x 是**刚自建**的（E51）。在只有 2.16.2 移植版的那段时间里，
# “经由 koffi”的行会因 2.x↔3.x 代差而加载失败（E47），那份禁用是为了先跑通主流程的
# **临时**措施；koffi 3.x 到位后，与 koffi 有关的临时禁用全部撤销（见下）。

# ── 永久禁用（鸿蒙上无法直接实现）────────────────────────────────────────

# ⓪ web-runtime 的三个默认值里有两个与本应用不符（E85）
#
# 依据（`dsh-web-app/lib/index.js`）：`openBrowser` / `printUrl` / `surfaceContext` 的
# 默认值都是 **true**。
#   · `surfaceContext: true` 会**注入一段系统提示**，内容是"你正在通过 DeepSeek Harness
#     Web GUI 与用户交互……浏览器不提供隐式 DOM/路由/截图上下文"（`lib/index.js:177`）。
#     我们的端侧页面是**原生 ArkUI**，不是 Web GUI——留着它等于对模型说了一句假话，
#     而且它会塑造模型对"界面能力"的判断（E85 引用实现的同一处）。
#   · `printUrl: true` **必须保留**：我们的 token 正是从这行 stdout 抓的
#     （`hostcore/app/main.js` 的 `watchdogAuthUrl`）。关掉它等于关掉自己的凭据来源。
#   · `openBrowser: false` 显式写死：端侧没有可打开的浏览器；虽然启动参数里已有
#     `--no-open`，但那是**命令行**层的约定，写在 profile 里的是**部署**层的事实，
#     两者都写，任何一侧被改动时另一侧仍然成立。
- id: web-runtime
  config:
    surfaceContext: false
    printUrl: true
    openBrowser: false

# ① PTY / 进程创建链：node-pty + spawn-helper 需要创建进程（D6 E15）
#    证据：`dsh-subprocess-local` 与 `dsh-sandbox-local` 在真机上因
#    `koffi.struct(...).size === undefined`（E47）先失败；即使 koffi 换成 3.x，
#    这两个插件在本平台仍需要 fork/exec 才能真正工作。
- id: subprocess
  disabled: true
- id: sandbox
  disabled: true
# bash/pwsh 执行器（`dsh-bash-sandbox` → `dsh-bash-local`、`dsh-pwsh-sandbox` → `dsh-pwsh-local`，
# 两者的 `static inject = ["subprocess"]`）。provider 禁用后它们必然缺服务。
- id: bash-sandbox
  disabled: true
- id: pwsh-sandbox
  disabled: true
# 两个 shell 工具行：其后端就是上面那两个执行器，留着只会让模型拿到一个必然失败的工具。
- id: tool-bash
  disabled: true
- id: tool-pwsh
  disabled: true

# ② sharp / libvips（D6 E46）
#    三个独立阻塞点：缺 libvips-cpp.so.42、加载器要建软链（沙箱禁止）、WASM 回退在 jitless 下必失败。
#    恢复线索：`@ohos-ports/img-sharp-libvips-openharmony-arm64/lib/libvips-cpp.so.42.20.3`
#    确实在核心树里，将来可连同依赖搬进 HAP libs 并处理带版本号的 SONAME。
- id: attachment-local
  disabled: false

# ③ "在应用中打开"依赖 subprocess 服务（`@deepseek-ai/dsh-host-open-in-app` 的
#    `inject = ['webServer','connection','subprocess']`）。端侧就是本应用自身，
#    这个"把外部 Host 的产物丢给本机程序打开"的能力没有对应物。
- id: open-in-app
  disabled: true

# ── 依赖级联（0.1.6-alpha.2 复核，W2 2026-09-20）────────────────────────────
#
# 【boot 语义变了】rc.2 时代 dsh 的 boot 要求**所有行都 activate**，禁掉一个 provider
# 会把下游消费者拖成 pending 并让整棵树 fail-loud（所以当时除 `permission` 外还临时禁过
# file-upload/session-controller/ui-deliverables）。0.1.6 起 host 侧 boot 对 pending 改为
# **警告不失败**，下面的临时禁用早已全部撤销，只保留 `permission` 一条（见 ④）。
#
# 【0.1.6 端侧 boot 的真实 pending 集（本机实测，boot 探针 2026-09-20，逐字）】
#     dsh: warning: 4 entries did not activate
#     ptc-runtime (@deepseek-ai/dsh-ptc-runtime-node): pending (waiting for services: subprocess, sandbox)
#     terminal-controller (@deepseek-ai/dsh-api-terminal-controller): pending (waiting for service: subprocess)
#     ui-deliverables (@deepseek-ai/dsh-client-ui-deliverables): pending (waiting for service: workspaceChanges)
#     workspace-changes (@deepseek-ai/dsh-workspace-changes): pending (waiting for service: subprocess)
# 四条全部根在 subprocess/sandbox 被禁（E15），属**上游硬约束的如实呈现**，不再加禁：
#   · ptc-runtime / workspace-changes / terminal-controller：PTC 工作流运行时、git 变更
#     摘要、终端面板——端侧没有进程创建，这些能力本来就没有对应物；
#   · ui-deliverables：Web UI 的"交付物/变更"面板（桌面特性）。0.1.6 它新增依赖
#     workspaceChanges（rc.2 时代它 pending 的原因还是 sessionController），端侧它
#     **从未激活过**；US2 的工作区文件面板不受影响——走的是 workspaceFiles/*
#     （dsh-api-workspace-files，仅依赖 fs，已激活；check-core-loop 实测 list/changes 通）。
#
# 【与 preset 的分界】host 侧 boot 只警告，但 **preset mount（session/create）仍是全有或
# 全无**（dsh-agent-presets/lib/index.js:893-894）：0.1.6 的 standard preset 新增
# workflow-ptc / tool-workflow 两行，端侧必挂（waiting for ptcRuntime / workflowEngine）。
# 已由 tools/pack-core.mjs 的 addOnDevicePreset 随包禁用（2026-09-20 实测：不禁则
# session/create 回 agent-preset/invalid、US1 整条断；禁后 check-core-loop 22/22 全通）。
# ④ 权限预设（E116）：**端侧无法启用**——官方该插件需要 `shell` 服务，而 shell 只能由 `bash-sandbox`/`pwsh-sandbox` 提供，
#    两者在鸿蒙上没有对应物（无法创建进程，E15）⇒ 这条链是**上游硬约束**，不是配置问题。
#    三档本身的取值仍然有效（`dsh-sandbox-policy` 的 `SANDBOX_MODES` =
#    `read-only` / `workspace-write` / `danger-full-access`，真机系统提示里也确实写着
#    `Current DSH file policy: workspace-write`），但把它们**捆绑成用户可选预设**的这一层
#    在端侧缺席。客户端因此按官方规则"没有权限组合就不显示选择器"处理，
#    并在设置里如实说明原因（而不是给一个点了没反应的档位）。
- id: permission
  disabled: true
- id: file-upload
  disabled: false
- id: session-controller
  disabled: false
- id: ui-deliverables
  disabled: false

# 端侧 preset：不带 Shell/子进程工具（由 tools/pack-core.mjs 的 addOnDevicePreset 在打包期
# 从 standard 复制生成到核心树 dsh-agent-presets/presets/ondevice/，禁用 tool-bash /
# tool-pwsh / tool-fs-search / workflow-ptc / tool-workflow 五行；见上面【与 preset 的分界】）
- id: agent-presets
  config:
    default: ondevice
