{
  "module": {
    "name": "web_engine",
    "type": "har",
    "deviceTypes": [
      "phone",
      "tablet",
      "2in1"
    ],
    /*
     * ── 权限：只保留真正需要的两条（两者都是 normal，无需 ACL 审批）──
     *
     * 【为什么把 vendored 模块的权限表砍到这里】原文件带了 25 条权限 + 一条
     * `definePermissions: ohos.permission.kernel.ALLOW_WRITABLE_CODE_MEMORY`。
     * HAR 的权限会被**合并进最终 HAP**（实测：`entry/build/.../merge_profile/default/module.json`
     * 里有 30 条），于是本应用会被判定为需要大量受限权限审批 —— 而
     * `ALLOW_WRITABLE_CODE_MEMORY` 更是本项目的**硬禁令**（D6 §4.4：不申请任何特殊权限，
     * 各形态统一 jitless，以保证顺利上架）。
     *
     * 那批权限来自上游 Web 引擎的通用清单（定位、相机、麦克风、蓝牙、截屏、分布式同步、
     * 文件目录、剪贴板、打印、生物识别、悬浮窗、置顶窗口…），本项目**一个都不使用**：
     *   - 阶段一的 Electron/浏览器宿主已按设计默认关闭（D6 E27，`MOUNT_ELECTRON_RUNTIME_HOST=false`）；
     *   - 端侧 Host 只监听 127.0.0.1，ArkUI 客户端只走 HTTP/WS 与本地存储。
     * 因此这里只保留 INTERNET 与 GET_NETWORK_INFO。
     *
     * 【若要恢复浏览器能力】把需要的权限一条条加回来，并逐条确认它是否属于需要 ACL 审批的
     * 受限权限；**不要**整段恢复上游清单。
     */
    "requestPermissions": [
      {
        "name": "ohos.permission.INTERNET"
      },
      {
        "name": "ohos.permission.GET_NETWORK_INFO"
      }
    ]
  }
}
